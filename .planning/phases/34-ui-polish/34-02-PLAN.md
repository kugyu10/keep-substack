---
phase: 34-ui-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(auth)/layout.tsx
  - src/app/(auth)/login/page.tsx
  - src/app/(auth)/login/LoginForm.tsx
  - src/app/(auth)/login/actions.ts
  - src/app/(auth)/login/__tests__/sendMagicLinkAction.test.ts
  - src/app/(auth)/signin-51cf21389c56/page.tsx
  - src/app/(auth)/signin-51cf21389c56/LoginForm.tsx
  - src/app/(auth)/signin-51cf21389c56/actions.ts
  - src/app/(auth)/signin-51cf21389c56/__tests__/sendMagicLinkAction.test.ts
autonomous: true
requirements:
  - UI-01
  - UI-02

must_haves:
  truths:
    - "/login URL resolves without 404 after directory move"
    - "/signin-51cf21389c56 URL resolves without 404 after directory move"
    - "Keep Substack logo (text-2xl font-black Georgia serif) appears on login page above the h1"
    - "Keep Substack logo appears on signin page above the h1"
    - "Invitation note appears on /login page below the form"
    - "No invitation note on /signin-51cf21389c56"
    - "(auth)/layout.tsx has no <html> or <body> tags"
    - "All tests pass (npm test exits 0)"
  artifacts:
    - path: src/app/(auth)/layout.tsx
      provides: Auth-only layout with no Header or Footer
      contains: "min-h-screen flex flex-col items-center justify-center bg-background"
    - path: src/app/(auth)/login/page.tsx
      provides: Login page with logo and invitation note
      contains: "Keep Substack"
    - path: src/app/(auth)/signin-51cf21389c56/page.tsx
      provides: Signin page with logo only
      contains: "Keep Substack"
  key_links:
    - from: src/app/(auth)/layout.tsx
      to: src/app/(auth)/login/page.tsx
      via: "Next.js Route Group — layout wraps login page's <main>"
      pattern: "children"
    - from: src/app/(auth)/login/page.tsx
      to: src/app/(auth)/login/LoginForm.tsx
      via: "import LoginForm from './LoginForm'"
      pattern: "LoginForm"
---

<objective>
Create the (auth) Route Group, move login and signin directories into it, create the auth-only layout, and add the Keep Substack logo + invitation note to each page. URL paths remain unchanged.

Purpose: Authentication pages must not show the site Header or Footer (per D-01, D-02). Route Groups achieve this without changing URLs. The logo (per D-04 to D-06) and invitation note (per D-07 to D-09) improve the login experience.

Output: src/app/(auth)/ directory with layout.tsx, login/, and signin-51cf21389c56/. Both page URLs still work. npm test passes.
</objective>

<execution_context>
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/kugyu10/work/keep-substack/.planning/ROADMAP.md
@/Users/kugyu10/work/keep-substack/.planning/phases/34-ui-polish/34-CONTEXT.md
@/Users/kugyu10/work/keep-substack/.planning/phases/34-ui-polish/34-RESEARCH.md
@/Users/kugyu10/work/keep-substack/.planning/phases/34-ui-polish/34-PATTERNS.md
@/Users/kugyu10/work/keep-substack/.planning/phases/34-ui-polish/34-UI-SPEC.md

<interfaces>
<!-- Current login/page.tsx — src/app/login/page.tsx lines 1-24 -->
From src/app/login/page.tsx (current, before move):
```tsx
import { redirect } from 'next/navigation'
import { createSupabaseServerClient } from '@/lib/supabase/server'
import LoginForm from './LoginForm'
import { safeRedirectPath } from '@/lib/safe-redirect'

export default async function LoginPage({
  searchParams,
}: {
  searchParams: Promise<{ next?: string }>
}) {
  const supabase = await createSupabaseServerClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (user) redirect('/my')

  const { next } = await searchParams
  const safeNext = safeRedirectPath(next) ?? undefined

  return (
    <main className="max-w-sm mx-auto px-4 py-16">
      <h1 className="text-2xl font-semibold mb-8 text-center">ログイン</h1>
      <LoginForm next={safeNext} />
    </main>
  )
}
```

<!-- Current signin/page.tsx — src/app/signin-51cf21389c56/page.tsx lines 1-22 -->
From src/app/signin-51cf21389c56/page.tsx (current, before move):
```tsx
export default async function LoginPage({
  searchParams,
}: {
  searchParams: Promise<{ pid?: string; handle?: string }>
}) {
  ...
  return (
    <main className="max-w-sm mx-auto px-4 py-16">
      <h1 className="text-2xl font-semibold mb-8 text-center">サインイン</h1>
      <LoginForm pid={pid} handle={handle} />
    </main>
  )
}
```

<!-- Header logo style for reference — src/components/Header.tsx lines 11-17 -->
Logo style from Header.tsx (text-lg on Link — auth pages use text-2xl on p, non-clickable):
```tsx
className="text-lg font-black hover:opacity-70 transition-opacity"
style={{ fontFamily: 'Georgia, serif' }}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move login and signin directories into (auth) Route Group</name>
  <files>
    src/app/(auth)/login/page.tsx,
    src/app/(auth)/login/LoginForm.tsx,
    src/app/(auth)/login/actions.ts,
    src/app/(auth)/login/__tests__/sendMagicLinkAction.test.ts,
    src/app/(auth)/signin-51cf21389c56/page.tsx,
    src/app/(auth)/signin-51cf21389c56/LoginForm.tsx,
    src/app/(auth)/signin-51cf21389c56/actions.ts,
    src/app/(auth)/signin-51cf21389c56/__tests__/sendMagicLinkAction.test.ts
  </files>
  <read_first>
    - src/app/login/page.tsx — full current content (to understand before move)
    - src/app/login/LoginForm.tsx — check imports (no path changes needed — same relative imports)
    - src/app/login/actions.ts — check imports
    - src/app/login/__tests__/sendMagicLinkAction.test.ts — check imports
    - src/app/signin-51cf21389c56/page.tsx — full current content
    - src/app/signin-51cf21389c56/__tests__/sendMagicLinkAction.test.ts — check imports
    - .planning/phases/34-ui-polish/34-RESEARCH.md — Pitfall 1 (move whole directories, not just page.tsx), Pattern 1 (Route Groups)
  </read_first>
  <action>
    Move the entire login directory and the entire signin-51cf21389c56 directory into a new (auth) Route Group directory. Use shell mv commands to move both directories atomically:

    mkdir -p src/app/\(auth\)
    mv src/app/login src/app/\(auth\)/login
    mv src/app/signin-51cf21389c56 src/app/\(auth\)/signin-51cf21389c56

    After moving, verify that all imports within the moved files still resolve correctly. All imports within login/ (e.g., LoginForm, actions) use relative paths (./LoginForm, ./actions) and the @/ alias paths — these remain valid after the directory move because relative paths are relative to the file location, and @/ always resolves from src/.

    Do NOT change any import paths inside the moved files. The mv operation preserves relative relationships.

    Confirm the move succeeded by listing src/app/(auth)/ contents and verifying src/app/login/ no longer exists.
  </action>
  <verify>
    <automated>ls /Users/kugyu10/work/keep-substack/src/app/\(auth\)/login/page.tsx /Users/kugyu10/work/keep-substack/src/app/\(auth\)/signin-51cf21389c56/page.tsx 2>&1</automated>
  </verify>
  <done>
    - src/app/(auth)/login/page.tsx exists
    - src/app/(auth)/signin-51cf21389c56/page.tsx exists
    - src/app/login/ no longer exists (moved)
    - src/app/signin-51cf21389c56/ no longer exists (moved)
    - Both __tests__ directories exist under (auth)/login/ and (auth)/signin-*/
  </done>
</task>

<task type="auto">
  <name>Task 2: Create (auth)/layout.tsx and update login/signin pages with logo and note</name>
  <files>
    src/app/(auth)/layout.tsx,
    src/app/(auth)/login/page.tsx,
    src/app/(auth)/signin-51cf21389c56/page.tsx
  </files>
  <read_first>
    - src/app/(auth)/login/page.tsx — current content (after Task 1 move)
    - src/app/(auth)/signin-51cf21389c56/page.tsx — current content (after Task 1 move)
    - src/app/layout.tsx — reference for how RootLayout uses html/body (to confirm (auth)/layout.tsx must NOT repeat those tags)
    - .planning/phases/34-ui-polish/34-RESEARCH.md — Pitfall 2 (no html/body in auth layout), Pattern 1 code example
    - .planning/phases/34-ui-polish/34-PATTERNS.md — (auth)/layout.tsx section, login/page.tsx section, signin page section
    - .planning/phases/34-ui-polish/34-UI-SPEC.md — UI-01+UI-02 Auth Layout Contract, Typography section (font-black), Copywriting Contract
  </read_first>
  <action>
    Create src/app/(auth)/layout.tsx as a Server Component (no 'use client'). It must NOT contain html, body, Header, or Footer elements — those are in RootLayout. Content: a single div wrapper with className "min-h-screen flex flex-col items-center justify-center bg-background" that renders {children}.

    Update src/app/(auth)/login/page.tsx: (1) Change py-16 to py-12 in the main element's className (per UI-SPEC spacing note — auth layout provides outer centering, so main padding reduces). (2) Change font-semibold to font-black on the h1. (3) Add a logo p element directly above the h1: className "text-2xl font-black text-center mb-6" with inline style fontFamily "Georgia, serif" and text "Keep Substack". (4) Add an invitation note p element directly after LoginForm: className "text-xs text-gray-400 text-center mt-6", text exactly "サブスタ継続可視化ツールKeep Substackは現在完全招待制です。招待されている方のみログインできます" (per D-07, D-08).

    Update src/app/(auth)/signin-51cf21389c56/page.tsx: (1) Change py-16 to py-12. (2) Change font-semibold to font-black on h1. (3) Add the same logo p element (text-2xl font-black text-center mb-6, Georgia serif, "Keep Substack") directly above the h1. (4) Do NOT add any invitation note (per D-09).

    The logo element is a p tag, not a Link — it is non-clickable (per D-06).
  </action>
  <verify>
    <automated>cd /Users/kugyu10/work/keep-substack && npm test 2>&1 | tail -20</automated>
  </verify>
  <done>
    - src/app/(auth)/layout.tsx exists, contains "min-h-screen flex flex-col items-center justify-center bg-background", contains no html or body tags
    - src/app/(auth)/login/page.tsx contains 'Keep Substack' in a p element with font-black and Georgia serif style
    - src/app/(auth)/login/page.tsx contains 'py-12' (not py-16)
    - src/app/(auth)/login/page.tsx contains the invitation note text "サブスタ継続可視化ツールKeep Substackは現在完全招待制です"
    - src/app/(auth)/signin-51cf21389c56/page.tsx contains 'Keep Substack' in a p element
    - src/app/(auth)/signin-51cf21389c56/page.tsx does NOT contain "招待制"
    - npm test passes (all tests green)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → login/signin page | URL path resolution via Next.js App Router routing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-02-01 | Spoofing | (auth) Route Group bypass | accept | The Route Group change is layout-only; authentication logic in login/page.tsx and middleware.ts is unchanged. /my is still protected by middleware. Route Group only controls which layout renders. |
| T-34-02-02 | Tampering | (auth)/layout.tsx missing html/body double-nest | mitigate | Layout must not contain html or body tags (per Pitfall 2); acceptance criteria verify this via grep. |
| T-34-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan. |
</threat_model>

<verification>
- ls src/app/(auth)/login/page.tsx exits 0
- ls src/app/(auth)/signin-51cf21389c56/page.tsx exits 0
- grep -c "min-h-screen flex flex-col items-center justify-center bg-background" src/app/(auth)/layout.tsx returns 1
- grep -v "^//" src/app/(auth)/layout.tsx | grep -c "html" returns 0
- grep -c "招待" src/app/(auth)/login/page.tsx returns 1
- grep -c "招待" src/app/(auth)/signin-51cf21389c56/page.tsx returns 0
- npm test exits 0
</verification>

<success_criteria>
- /login and /signin-51cf21389c56 URLs function (no 404) after directory move
- Auth pages show no Header or Footer (controlled by (auth)/layout.tsx replacing RootLayout for those routes)
- Keep Substack logo (text-2xl font-black, Georgia serif, non-clickable p element) appears above h1 on both pages
- Invitation note appears only on /login page
- All tests pass
</success_criteria>

<output>
Create .planning/phases/34-ui-polish/34-02-SUMMARY.md when done
</output>
