---
phase: 34-ui-polish
plan: 04
type: execute
wave: 2
depends_on: []
files_modified:
  - src/components/Footer.tsx
  - src/components/HeaderNav.tsx
  - src/components/__tests__/Footer.test.tsx
  - src/components/__tests__/HeaderNav.test.tsx
autonomous: true
requirements:
  - UI-03
  - UI-05

must_haves:
  truths:
    - "Footer.tsx renders unauthenticated copy when user is null"
    - "Footer.tsx renders individual view link when user has publicationId"
    - "Footer.tsx renders /my link when user exists but has no publicationId"
    - "HeaderNav.tsx renders マイページ link when user is authenticated and path is not /my"
    - "HeaderNav.tsx renders nothing when user is authenticated and path is /my"
    - "HeaderNav.tsx renders ログイン link when user is null"
    - "All tests pass (npm test exits 0)"
  artifacts:
    - path: src/components/Footer.tsx
      provides: Server Component with 3-state footer copy
      contains: "createSupabaseServerClient"
    - path: src/components/HeaderNav.tsx
      provides: Client Component with usePathname-based nav logic
      contains: "'use client'"
    - path: src/components/__tests__/Footer.test.tsx
      provides: Tests for all 3 footer states
      contains: "unauthenticated"
    - path: src/components/__tests__/HeaderNav.test.tsx
      provides: Tests for /my hide behavior and other routes
      contains: "/my"
  key_links:
    - from: src/components/Footer.tsx
      to: src/lib/supabase/server.ts
      via: "await createSupabaseServerClient() — async, must await"
      pattern: "await createSupabaseServerClient"
    - from: src/components/Footer.tsx
      to: src/lib/supabase/admin.ts
      via: "createSupabaseAdminClient() — sync, no await on client itself"
      pattern: "createSupabaseAdminClient\\(\\)"
    - from: src/components/Header.tsx
      to: src/components/HeaderNav.tsx
      via: "Server Component passes user prop to Client Component"
      pattern: "HeaderNav"
---

<objective>
Create Footer.tsx (Server Component, 3-state copy based on auth + member DB query) and HeaderNav.tsx (Client Component, hides マイページ when on /my). Create tests for both. These components will be wired into layout.tsx and Header.tsx in Plan 05.

Purpose: UI-03 requires footer copy to adapt to auth state (per D-10 to D-12). UI-05 requires hiding the マイページ nav button on /my (per D-19 to D-21). Both are standalone components that can be built independently of Plans 01-03.

Output: src/components/Footer.tsx, src/components/HeaderNav.tsx, and test files for both. npm test passes.
</objective>

<execution_context>
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/kugyu10/work/keep-substack/.planning/ROADMAP.md
@/Users/kugyu10/work/keep-substack/.planning/phases/34-ui-polish/34-CONTEXT.md
@/Users/kugyu10/work/keep-substack/.planning/phases/34-ui-polish/34-RESEARCH.md
@/Users/kugyu10/work/keep-substack/.planning/phases/34-ui-polish/34-PATTERNS.md
@/Users/kugyu10/work/keep-substack/.planning/phases/34-ui-polish/34-UI-SPEC.md

<interfaces>
<!-- createSupabaseServerClient — async (must await) -->
From src/lib/supabase/server.ts (pattern):
```typescript
// ASYNC — always await
const supabase = await createSupabaseServerClient()
const { data: { user } } = await supabase.auth.getUser()
```

<!-- createSupabaseAdminClient — sync -->
From src/lib/supabase/admin.ts (pattern):
```typescript
// SYNC — no await on client creation; only await the query
const admin = createSupabaseAdminClient()
const { data: member } = await admin.from('members')...
```

<!-- Existing inline footer in layout.tsx — what Footer.tsx replaces -->
From src/app/layout.tsx (existing inline footer, lines 26-38):
```tsx
<footer className="py-4 text-center">
  <span className="text-xs text-gray-400">
    このSubstack継続可視化ツールに参加したい方は{' '}
    <a href="https://uojun.substack.com/p/8cd" target="_blank" rel="noopener noreferrer"
       className="hover:text-[#FF6719] underline">コチラ</a>
  </span>
</footer>
```

<!-- Current header nav links — what HeaderNav.tsx replaces -->
From src/components/Header.tsx (lines 18-32), the nav block:
```tsx
{user ? (
  <Link href="/my" className="text-sm border border-gray-300 rounded px-3 py-1 hover:bg-gray-50 transition-colors">
    マイページ
  </Link>
) : (
  <Link href="/login" className="text-sm border border-gray-300 rounded px-3 py-1 hover:bg-gray-50 transition-colors">
    ログイン
  </Link>
)}
```

<!-- LogoutButton.tsx — closest 'use client' analog -->
From src/components/LogoutButton.tsx (pattern):
```tsx
'use client'
import { ... } from '...'
export default function ComponentName(...) { ... }
```

<!-- Existing page.test.tsx element-tree test pattern (analog for Footer/HeaderNav tests) -->
From src/app/__tests__/page.test.tsx (element tree inspection without jsdom):
```typescript
import { describe, it, expect, vi } from 'vitest'
import type { ReactElement } from 'react'
// No jsdom — inspect element tree via React.createElement return value
const el = await ComponentName(props)
// Walk el.props.children to find expected text or element types
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Footer.tsx Server Component with 3-state copy</name>
  <files>src/components/Footer.tsx, src/components/__tests__/Footer.test.tsx</files>
  <read_first>
    - src/app/layout.tsx — existing inline footer markup (lines 26-38) — this is what Footer.tsx replicates and extends
    - src/components/Header.tsx — auth fetch pattern (lines 1-6) — same pattern for Footer.tsx
    - src/app/my/page.tsx — admin query pattern with .eq('user_id', ...).maybeSingle() — same pattern for member query in Footer.tsx
    - src/app/__tests__/page.test.tsx — element tree test pattern (no jsdom, inspect React element tree)
    - .planning/phases/34-ui-polish/34-CONTEXT.md — D-10 (Server Component), D-11 (3 states), D-12 (admin query)
    - .planning/phases/34-ui-polish/34-RESEARCH.md — Pitfall 5 (must await createSupabaseServerClient), Pattern 3 (Footer.tsx full pattern)
    - .planning/phases/34-ui-polish/34-PATTERNS.md — Footer.tsx section (full pattern with conditional JSX)
    - .planning/phases/34-ui-polish/34-UI-SPEC.md — UI-03 Footer Copy Contract (exact link text and URLs)
  </read_first>
  <behavior>
    - Footer() is an async function (Server Component, no 'use client')
    - When user === null: renders "このSubstack継続可視化ツールに参加したい方は" with external anchor to https://uojun.substack.com/p/8cd
    - When user !== null and publicationId is not null: renders "あなたの個人ビューは" with Link to /member/{publicationId}
    - When user !== null and publicationId is null: renders "参加登録は" with Link to /my
    - Footer wrapper: footer className "py-4 text-center"
    - Text wrapper: span className "text-xs text-gray-400"
    - All link text (inline): "コチラ" with className "hover:text-[#FF6719] underline"
    - External link (unauthenticated state): a tag with target="_blank" rel="noopener noreferrer"
    - Internal links: next/link Link component
  </behavior>
  <action>
    Create src/components/Footer.tsx as an async Server Component. Imports: Link from next/link, createSupabaseServerClient from @/lib/supabase/server, createSupabaseAdminClient from @/lib/supabase/admin. No 'use client' directive.

    The function body: (1) await createSupabaseServerClient() — must use await (async function). (2) await supabase.auth.getUser() to get user. (3) If user exists, createSupabaseAdminClient() (sync — no await on creation), then await admin.from('members').select('publication_id').eq('user_id', user.id).maybeSingle() to get publicationId. (4) Return JSX with 3 conditional branches per D-11 and UI-SPEC.md UI-03 Footer Copy Contract.

    Exact copy per D-11 and UI-SPEC:
    - Unauthenticated: "このSubstack継続可視化ツールに参加したい方は" + space + a href="https://uojun.substack.com/p/8cd" target="_blank" rel="noopener noreferrer" className="hover:text-[#FF6719] underline" > "コチラ"
    - Authenticated + member: "あなたの個人ビューは" + space + Link href={`/member/${publicationId}`} className="hover:text-[#FF6719] underline" > "コチラ"
    - Authenticated + no member: "参加登録は" + space + Link href="/my" className="hover:text-[#FF6719] underline" > "コチラ"

    Create src/components/__tests__/Footer.test.tsx. Mock createSupabaseServerClient and createSupabaseAdminClient. Three test cases: unauthenticated (user=null), authenticated+member (publicationId set), authenticated+no-member (publicationId null). Use element-tree inspection (no jsdom) — call Footer() as an async function, walk the returned element tree to verify the rendered copy text and link hrefs. Reference src/app/__tests__/page.test.tsx for the element-tree inspection pattern (isElement, flattenChildren, findByType helpers).
  </action>
  <verify>
    <automated>cd /Users/kugyu10/work/keep-substack && npm test -- --reporter=verbose 2>&1 | grep -E "(Footer|PASS|FAIL)" | head -20</automated>
  </verify>
  <done>
    - src/components/Footer.tsx exists, is NOT a 'use client' component
    - grep -c "await createSupabaseServerClient" src/components/Footer.tsx returns 1
    - grep -c "あなたの個人ビューは" src/components/Footer.tsx returns 1
    - grep -c "参加登録は" src/components/Footer.tsx returns 1
    - src/components/__tests__/Footer.test.tsx exists with at least 3 test cases
    - npm test passes
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create HeaderNav.tsx Client Component with pathname-based button control</name>
  <files>src/components/HeaderNav.tsx, src/components/__tests__/HeaderNav.test.tsx</files>
  <read_first>
    - src/components/Header.tsx — current nav block (lines 18-32) — HeaderNav.tsx replaces this block; copy link styles exactly
    - src/components/LogoutButton.tsx — 'use client' component pattern (full file)
    - .planning/phases/34-ui-polish/34-CONTEXT.md — D-19 (extract nav to HeaderNav), D-20 (usePathname, /my hides マイページ), D-21 (Header passes user prop)
    - .planning/phases/34-ui-polish/34-RESEARCH.md — Pitfall 6 (must have 'use client'), Pattern 2 (HeaderNav full code)
    - .planning/phases/34-ui-polish/34-PATTERNS.md — HeaderNav.tsx section (full pattern with usePathname)
    - .planning/phases/34-ui-polish/34-UI-SPEC.md — UI-05 HeaderNav Contract (props interface, behaviour table, button style)
  </read_first>
  <behavior>
    - HeaderNav.tsx has 'use client' as first line
    - Props: { user: User | null } where User is from @supabase/supabase-js
    - When user !== null and pathname !== '/my': renders マイページ Link to /my
    - When user !== null and pathname === '/my': returns null (renders nothing)
    - When user === null: renders ログイン Link to /login
    - Both links use className: "text-sm border border-gray-300 rounded px-3 py-1 hover:bg-gray-50 transition-colors"
  </behavior>
  <action>
    Create src/components/HeaderNav.tsx. First line must be 'use client'. Imports: usePathname from next/navigation, Link from next/link, type User from @supabase/supabase-js. Define type HeaderNavProps = { user: User | null }. Export default function HeaderNav({ user }: HeaderNavProps).

    Logic inside the function: const pathname = usePathname(). Then: if user is truthy and pathname === '/my', return null. If user is truthy and pathname !== '/my', return the マイページ Link. If user is falsy, return the ログイン Link.

    Both Link elements use className "text-sm border border-gray-300 rounded px-3 py-1 hover:bg-gray-50 transition-colors" — copied exactly from the current nav links in Header.tsx.

    Create src/components/__tests__/HeaderNav.test.tsx. Mock next/navigation to control usePathname() return value. Mock next/link. Three test cases:
    1. user + pathname '/my' → component returns null (no マイページ link rendered)
    2. user + pathname '/' → マイページ link rendered with href="/my"
    3. user=null + any pathname → ログイン link rendered with href="/login"

    For testing: use vitest with vi.mock for next/navigation and next/link. Render HeaderNav using React createElement or directly call it as a function and inspect the returned element (no jsdom needed — element-tree inspection matches the project's existing test pattern in page.test.tsx and CommitGoalRow.test.tsx).
  </action>
  <verify>
    <automated>cd /Users/kugyu10/work/keep-substack && npm test 2>&1 | tail -20</automated>
  </verify>
  <done>
    - src/components/HeaderNav.tsx exists
    - grep -c "'use client'" src/components/HeaderNav.tsx returns 1 (first line)
    - grep -c "usePathname" src/components/HeaderNav.tsx returns at least 1
    - grep -c "pathname === '/my'" src/components/HeaderNav.tsx returns 1
    - src/components/__tests__/HeaderNav.test.tsx exists with at least 3 test cases
    - npm test passes with all tests green
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Supabase Auth → Footer.tsx | user object from getUser() is trusted (server-side, cookie-based session) |
| Supabase Admin → Footer.tsx | member.publication_id from admin query is trusted (service role key, no user input in query) |
| Browser URL → HeaderNav.tsx | pathname from usePathname() is the actual current path; no user-controlled injection possible |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-04-01 | Information Disclosure | Footer renders /member/{publicationId} for authenticated member | accept | publicationId is already public (used in /member/{pid} URL which is accessible without auth). No new information disclosed. |
| T-34-04-02 | Tampering | Footer admin query uses user.id from auth session | mitigate | user.id comes from Supabase session (server-side, not user-editable). The admin query uses .eq('user_id', user.id) with the verified session user id — no injection possible. |
| T-34-04-03 | Spoofing | HeaderNav hiding マイページ button does not enforce access control | accept | Hiding the button is UI-only. /my page access control is enforced by src/middleware.ts (already implemented in Phase 33). HeaderNav is a display-only component. |
| T-34-04-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan. |
</threat_model>

<verification>
- grep -c "export default async function Footer" src/components/Footer.tsx returns 1
- grep -c "await createSupabaseServerClient" src/components/Footer.tsx returns 1
- grep -c "createSupabaseAdminClient()" src/components/Footer.tsx returns 1
- grep -c "'use client'" src/components/HeaderNav.tsx returns 1
- grep -c "usePathname" src/components/HeaderNav.tsx returns at least 1
- npm test exits 0
</verification>

<success_criteria>
- Footer.tsx is a Server Component that renders 3 different copy variants based on auth state and member membership
- HeaderNav.tsx is a Client Component that hides the マイページ link on /my
- Tests cover all 3 footer states and all 3 HeaderNav states
- npm test passes with no regressions
</success_criteria>

<output>
Create .planning/phases/34-ui-polish/34-04-SUMMARY.md when done
</output>
