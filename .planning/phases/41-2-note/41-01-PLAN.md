---
phase: 41-2-note
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/notes.ts
  - src/lib/__tests__/notes.test.ts
  - src/lib/__tests__/fixtures/profile_feed.json
  - src/app/(main)/admin/notes/page.tsx
  - .env.local
  - .env.example
autonomous: false
requirements: [NOTE-01, NOTE-02, NOTE-03]
user_setup:
  - service: vercel
    why: "SUBSTACK_ADMIN_USER_ID を本番/preview で供給（D-01）。値は公開 user_id だが env で管理"
    env_vars:
      - name: SUBSTACK_ADMIN_USER_ID
        source: "値=110584954（@uojun 公開 user_id）。Vercel Dashboard -> keep-substack -> Settings -> Environment Variables（dev/preview/production）"

must_haves:
  truths:
    - "admin は /admin/notes で自分が投稿した Note の一覧を見られる（都度サーバー fetch・永続化なし）"   # NOTE-01 / D-05
    - "各 Note に本文（全文・改行保持）と投稿日時 JST が表示される"                                      # NOTE-02 / D-02 / D-04
    - "取得失敗時と 0件時で別文言が表示される"                                                          # NOTE-03 / D-07
    - "他人の restack（comment_restack / post_restack）は一覧に出ない"                                  # D-04 判別
    - "コメント数・リアクション数・外部リンクは表示されない"                                            # D-04 / D-06
  artifacts:
    - path: "src/lib/notes.ts"
      provides: "取得層 fetchAdminNotes() + 純関数 parseNoteFeed() + 判別ユニオン型（Phase 42 再利用）"
      exports: ["fetchAdminNotes", "parseNoteFeed", "NoteItem", "NoteListResult"]
      contains: "context.type"
    - path: "src/lib/__tests__/notes.test.ts"
      provides: "parseNoteFeed のフィルタ/正規化/判別ユニオン unit テスト"
      contains: "parseNoteFeed"
    - path: "src/app/(main)/admin/notes/page.tsx"
      provides: "RSC ページ。status 分岐表示・force-dynamic で永続化なし担保"
      contains: "force-dynamic"
  key_links:
    - from: "src/app/(main)/admin/notes/page.tsx"
      to: "src/lib/notes.ts"
      via: "import { fetchAdminNotes }"
      pattern: "fetchAdminNotes"
    - from: "src/lib/notes.ts"
      to: "substack.com/api/v1/reader/feed/profile"
      via: "fetch with cache:'no-store'"
      pattern: "no-store"
---

<objective>
admin（開発者本人）が投稿した Substack Note の一覧を `/admin/notes` に都度サーバー fetch して読み取り専用で表示する最小 PoC を実装する。DB・キャッシュ・永続化なし。

Purpose: 機能2（Note 一覧）の MVP。取得層 `lib/notes.ts` は Phase 42（コメント可視化）でも再利用される前提で安定した型/シグネチャに切り出す。
Output: 取得層 `src/lib/notes.ts`、その unit テスト、`/admin/notes` RSC ページ、env 設定。
</objective>

<execution_context>
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# 確定済み技術調査（実装の根拠・サンプルコード・型・落とし穴）
@.planning/phases/41-2-note/41-RESEARCH.md
@.planning/phases/41-2-note/41-CONTEXT.md

# 再利用パターン（リトライ思想・JST 整形・admin ガード）
@src/lib/fetchFeed.ts
@src/middleware.ts

# フィクスチャ流用元（実レスポンス 11 items / note 8件）— Task 1 でコピーする元データ
# .planning/spikes/001-substack-notes-list/raw/profile_feed.json

<interfaces>
<!-- 取得層が公開する契約。RESEARCH Pattern 1 で確定済み。実装はこれに従う。 -->
<!-- src/lib/notes.ts が export する型（Phase 42 で再利用される安定 API） -->

export type NoteItem = {
  id: number       // item.comment.id（一意キー）
  body: string     // item.comment.body（plain text, \n 含む。pre-wrap で描画）
  date: string     // item.comment.date（ISO UTC, Z 付き。描画側で JST 整形）
}

export type NoteListResult =
  | { status: 'ok'; notes: NoteItem[] }   // notes.length===0 が「0件」
  | { status: 'error' }                   // 取得失敗 / env 未設定

export function parseNoteFeed(json: unknown): NoteItem[]   // 純関数（fetch 非依存・テスト対象）
export async function fetchAdminNotes(): Promise<NoteListResult>

<!-- 既存 JST 整形規約（RESEARCH Standard Stack 確定。AdminMemberList と一貫）: -->
new Date(isoZ).toLocaleString('ja-JP', { timeZone: 'Asia/Tokyo' })

<!-- 既存リトライ規約（fetchFeed.ts）: 1回目失敗 → 1秒待ち → 1回リトライ → なお失敗で error -->
<!-- タイムアウト規約: AbortSignal.timeout(5000) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 取得層 src/lib/notes.ts と parseNoteFeed の RED→GREEN テスト</name>
  <files>src/lib/notes.ts, src/lib/__tests__/notes.test.ts, src/lib/__tests__/fixtures/profile_feed.json</files>
  <behavior>
    parseNoteFeed（純関数・fetch 非依存）の検証：
    - フィクスチャ（spike の profile_feed.json をコピー、11 items / context.type=note 8件・comment_restack 2件・post_restack 1件）を渡すと、note だけ 8件抽出される（restack は除外）。返り値は NoteItem[]（id/body/date のみ。children_count/reaction_count は含めない＝D-04）。
    - 各 NoteItem の id は number、body は string、date は string であること。body に \n を含む item（例 id=278196925）で \n が保持されること。
    - items が空配列の json → 空配列を返す（0件分岐の素地）。
    - items キーが無い / null / 配列でない不正 json → 空配列を返す（throw しない。RESEARCH 型ガード思想）。
    - comment.id/body/date のいずれかが欠落/型不正な item は除外される（型ガード）。
    fetchAdminNotes の判別ユニオン検証（fetch を mock）：
    - HTTP エラー（res.ok=false）が2回続く → { status:'error' }（空配列フォールバックしない＝NOTE-03 / Pitfall 1）。
    - SUBSTACK_ADMIN_USER_ID 未設定 → fetch せず { status:'error' }（Pitfall 3）。
    - 正常レスポンス（フィクスチャ json）→ { status:'ok', notes: 8件 }。
    - items 空の正常レスポンス → { status:'ok', notes:[] }（0件＝error と区別）。
  </behavior>
  <action>
    まず `.planning/spikes/001-substack-notes-list/raw/profile_feed.json` を `src/lib/__tests__/fixtures/profile_feed.json` にコピー（テストフィクスチャ化。RESEARCH Validation Architecture 推奨）。
    `src/lib/__tests__/notes.test.ts` を Vitest（既存 `src/lib/__tests__/*.test.ts` と同形式・`vitest run` で実行）で先に書き、上記 behavior を満たす失敗テストを作る（RED）。fetch のモックは Vitest の `vi.stubGlobal('fetch', ...)` または `vi.spyOn(globalThis,'fetch')` を使う。env は `vi.stubEnv('SUBSTACK_ADMIN_USER_ID', ...)` で制御。
    次に `src/lib/notes.ts` を RESEARCH Pattern 1 に従って実装し GREEN にする：
    - `parseNoteFeed(json: unknown): NoteItem[]` を純関数として切り出す。`items` を `item.context?.type === 'note'` でフィルタし（D-04 判別必須・restack 除外）、`comment.id`(number)/`comment.body`(string)/`comment.date`(string) を型ガードで検証して `{ id, body, date }` に正規化。post_restack は comment=null のため型ガードで自然に除外される。children_count/reaction_count は型に含めない（D-04）。
    - `fetchAdminNotes(): Promise<NoteListResult>` は env `SUBSTACK_ADMIN_USER_ID` を読み（未設定→`{status:'error'}`・console.error）、`fetch(feedUrl, { cache:'no-store', headers:{Accept:'application/json'}, signal: AbortSignal.timeout(5000) })`（cache:'no-store' で永続化なし＝NOTE-01 / Pitfall 4）。`res.ok` 不成立は throw。1回目失敗→1秒待ち→1回リトライ（fetchFeed.ts 踏襲）→なお失敗で `{status:'error'}`（空配列フォールバックしない＝NOTE-03 / Pitfall 1）。成功時 `res.json()` を `parseNoteFeed` に通し `{status:'ok', notes}`。
    - URL は `https://substack.com/api/v1/reader/feed/profile/${userId}?types=note`（MANIFEST 確定エンドポイント）。
    NOTE: action 内にコード断片を貼らない。型/シグネチャは <interfaces> と RESEARCH Pattern 1 を直接参照すること。
  </action>
  <verify>
    <automated>npx vitest run src/lib/__tests__/notes.test.ts</automated>
  </verify>
  <done>notes.test.ts が green。parseNoteFeed が context.type==="note" で 8件抽出（restack 除外）・不正/空入力で空配列・型ガードで欠落 item 除外。fetchAdminNotes が成功/0件/失敗/env未設定を判別ユニオンで返し分ける。NoteItem/NoteListResult/parseNoteFeed/fetchAdminNotes が export 済み。</done>
</task>

<task type="auto">
  <name>Task 2: /admin/notes RSC ページ（status 分岐・本文 pre-wrap・JST 日時）</name>
  <files>src/app/(main)/admin/notes/page.tsx</files>
  <action>
    `src/app/(main)/admin/notes/page.tsx` を RSC（async server component）として新規作成。RESEARCH Pattern 2 に従う：
    - `export const dynamic = 'force-dynamic'` を宣言（fetch の no-store と二重で永続化なしを担保＝NOTE-01 / Pitfall 4）。
    - `import { fetchAdminNotes } from '@/lib/notes'` し `await fetchAdminNotes()` を呼ぶ。
    - status 分岐（D-07 / NOTE-03 — 2状態を必ず別文言で）：
      (a) `status === 'error'` → 「取得に失敗しました」系の文言（例「Note の取得に失敗しました。時間をおいて再読み込みしてください。」）
      (b) `status === 'ok'` かつ `notes.length === 0` → 「まだ Note がありません」系の別文言
      (c) `status === 'ok'` かつ 1件以上 → Note 一覧（リスト/カード）
    - 各 Note の描画（NOTE-02）：本文 `body` をテキストノードで描画し `whitespace-pre-wrap break-words`（改行保持＝D-02/D-03。`dangerouslySetInnerHTML` 不使用＝XSS 安全・Anti-Pattern）。投稿日時は `new Date(n.date).toLocaleString('ja-JP', { timeZone: 'Asia/Tokyo' })`（JST 整形・既存 AdminMemberList 規約踏襲＝Pitfall 6）。
    - 出さないもの（厳守）：コメント数/リアクション数（D-04）、実 Note への外部リンク（D-06）、ページング/追加取得（D-05）。
    - admin ガードは middleware の `/admin/:path*` matcher で済んでいるため page 側で再ガードしない（RESEARCH Architectural Responsibility Map / 既存 admin ページと一貫）。
    レイアウト/文言の細部は Claude's Discretion。既存 `src/app/(main)/admin/page.tsx` のスタイル傾向に合わせる。
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx next build 2>&1 | grep -E '/admin/notes' | grep -E 'ƒ|Dynamic|server-rendered'</automated>
  </verify>
  <done>tsc 通過。build 出力で `/admin/notes` が Dynamic（ƒ、Static ○ ではない）として現れる（force-dynamic 効いている）。page は fetchAdminNotes を import し、error/0件/一覧の3分岐を別文言で描画。本文は whitespace-pre-wrap、日時は JST。children_count/reaction_count/外部リンクなし。</done>
</task>

<task type="auto">
  <name>Task 3: env 設定（.env.local / .env.example）</name>
  <files>.env.local, .env.example</files>
  <action>
    `.env.local` に `SUBSTACK_ADMIN_USER_ID=110584954`（@uojun 公開 user_id・D-01）を追記する（既存行は保持・重複追記しない）。`.env.example` に `SUBSTACK_ADMIN_USER_ID=` のプレースホルダ行＋用途コメント（admin の Substack user_id。Note 一覧取得に使用）を追記してリポジトリに env 契約を記録する。Vercel への登録は Claude では行えない（Task 4 の開発者作業）。
  </action>
  <verify>
    <automated>grep -q '^SUBSTACK_ADMIN_USER_ID=110584954' .env.local && grep -q '^SUBSTACK_ADMIN_USER_ID=' .env.example && echo OK</automated>
  </verify>
  <done>.env.local に SUBSTACK_ADMIN_USER_ID=110584954、.env.example にプレースホルダ行が存在する。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: 表示 UAT + Vercel env 反映</name>
  <action>checkpoint — 開発者による表示確認と Vercel 環境変数登録。Claude の自動作業は Task 1-3 で完了済み。</action>
  <verify><human-check>下記 how-to-verify 手順</human-check></verify>
  <done>開発者が表示・JST・失敗/0件の別文言を確認し "approved"。Vercel に env 登録済み。</done>
  <what-built>
    取得層 `src/lib/notes.ts`（fetchAdminNotes + parseNoteFeed・判別ユニオン）と `/admin/notes` RSC ページ。env `SUBSTACK_ADMIN_USER_ID` を `.env.local` と `.env.example` に反映済み。Vercel 環境変数（dev/preview/production）への登録のみ Claude では設定できないため開発者作業。
  </what-built>
  <how-to-verify>
    1. ローカルで env 反映後に dev サーバ再起動（`npm run dev`）。
    2. admin アカウントで `http://localhost:3000/admin/notes` を開く → 自分の Note 一覧が表示され、各 Note に本文（全文・改行保持）と投稿日時 JST が出ること。他人の名前/restack が出ていないこと（D-04 判別）。コメント数・リアクション数・外部リンクが出ていないこと（D-04/D-06）。
    3. 0件確認（任意）：env を一時的に Note 0件 user_id に変えて「まだ Note がありません」が出ること。
    4. 失敗確認：`.env.local` の `SUBSTACK_ADMIN_USER_ID` を空にして再起動 → 「取得に失敗しました」系文言（0件文言とは別＝NOTE-03）が出ること。確認後 110584954 に戻す。
    5. Vercel（開発者作業）：Vercel Dashboard -> keep-substack -> Settings -> Environment Variables に `SUBSTACK_ADMIN_USER_ID=110584954` を dev/preview/production すべてに追加。preview デプロイで `/admin/notes` が同様に表示されることを確認。
  </how-to-verify>
  <resume-signal>表示・JST・状態文言（失敗/0件の別文言）を確認したら "approved"、問題があれば内容を記載</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Substack reader feed API → notes.ts | 非公式・無認証の外部 JSON。信頼しない入力 |
| browser → /admin/notes | admin のみ。middleware `/admin/:path*` でガード済み |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-01 | Tampering | 外部 API 由来の body（XSS） | mitigate | body をテキストノードで描画（React デフォルトエスケープ）。`dangerouslySetInnerHTML` 不使用。Task 2 |
| T-41-02 | Tampering | 非公式 API の形変更/汚染レスポンス | mitigate | parseNoteFeed の厳格な型ガード（id:number/body:string/date:string 検証）+ 失敗時 `status:'error'` フォールバック。Task 1 |
| T-41-03 | Information Disclosure | 非 admin の `/admin/notes` 閲覧 | mitigate | middleware `/admin/:path*` admin ガード（既存 isAdmin）。表示内容は公開 Note のため残存リスク低 |
| T-41-04 | Information Disclosure | env 未設定で `.../profile/undefined` を叩く | mitigate | fetchAdminNotes 冒頭で env 未設定→`status:'error'`（fetch しない）。Task 1 / Pitfall 3 |
| T-41-SC | Tampering | npm/pip/cargo installs | accept | 新規パッケージ install なし（Node 組込 fetch + Intl + 既存 Vitest のみ）。RESEARCH Package Legitimacy Audit = N/A |
</threat_model>

<verification>
- `npx vitest run src/lib/__tests__/notes.test.ts` green（NOTE-01 判別/正規化・NOTE-03 状態分岐）
- `npm test`（フルスイート）green（既存テストにリグレッションなし）
- `npx next build` で `/admin/notes` が Dynamic（ƒ）として出力される（永続化なし＝NOTE-01）
- 人手 UAT：本文全文+改行保持・JST 日時表示・失敗/0件の別文言（NOTE-02/03/D-07）
</verification>

<success_criteria>
1. admin が `/admin/notes` で自分の Note 一覧を取得・表示できる（都度 fetch・永続化なし・restack 非表示）— NOTE-01 / D-01,D-04,D-05
2. 各 Note に本文（全文・改行保持・リンクなし）と投稿日時 JST が表示される — NOTE-02 / D-02,D-03,D-06
3. 取得失敗と 0件が別文言で画面に明示される — NOTE-03 / D-07
4. `lib/notes.ts` が安定した型/シグネチャ（NoteItem/NoteListResult/fetchAdminNotes/parseNoteFeed）を export し Phase 42 で再利用可能
</success_criteria>

<output>
Create `.planning/phases/41-2-note/41-01-SUMMARY.md` when done
</output>
