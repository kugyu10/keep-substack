---
phase: 37-url-url
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/shareUrl.ts
  - src/lib/__tests__/shareUrl.test.ts
  - src/app/(main)/member/[publicationId]/page.tsx
  - src/components/CalendarGrid.tsx
autonomous: true
requirements: [URL-01]

must_haves:
  truths:
    - "未ログイン訪問者が /member/[publicationId]?ym=YYYY-MM を開くと、そのYYYY-MMの月のカレンダーが復元表示される"
    - "個人カレンダーで月送りボタンを押すとURLの ym パラメータが更新され、リロード/別タブでも同じ月が表示される"
    - "ym 未指定 / 不正値のときは現在月（JST）にフォールバックする"
    - "トップ Commit&Goal でチームタブ切替が ?team= に反映され、リロード/別タブで同じチームで開く（既存挙動の確認）"
    - "/daily でチームフィルタが ?team= に反映され、未ログインで同じ表示にアクセスできる（既存挙動の確認）"
    - "対象4ビューの正規公開URLを buildShareUrl 1関数で取得でき、Phase 38 が参照できる"
  artifacts:
    - path: "src/lib/shareUrl.ts"
      provides: "対象4ビューの正規公開URLを返す buildShareUrl と、ym searchParam の parse/format ヘルパ"
      exports: ["buildShareUrl", "parseYmParam", "formatYmParam"]
    - path: "src/lib/__tests__/shareUrl.test.ts"
      provides: "buildShareUrl / parseYmParam / formatYmParam の単体テスト"
      contains: "buildShareUrl"
    - path: "src/app/(main)/member/[publicationId]/page.tsx"
      provides: "ym searchParam を読み取り CalendarGrid に表示月を渡すServer Component"
      contains: "searchParams"
    - path: "src/components/CalendarGrid.tsx"
      provides: "URL駆動の月ナビ（client useState を廃し、表示月を props 受け取り＋月送りは next/link）"
      contains: "ym"
  key_links:
    - from: "src/app/(main)/member/[publicationId]/page.tsx"
      to: "src/lib/shareUrl.ts"
      via: "parseYmParam で ym searchParam を {year, month} に解釈"
      pattern: "parseYmParam"
    - from: "src/components/CalendarGrid.tsx"
      to: "URL (?ym=)"
      via: "月送りリンクが formatYmParam で生成した href を持つ next/link"
      pattern: "ym="
---

<objective>
URL-01 を満たす。対象4ビュー（トップ Commit&Goal `/`、`/daily`、個人カレンダー `/member/[publicationId]`、チーム選択中 `/?team=X`・`/daily?team=X`）が、状態を含む公開URLで未ログインでも復元できるようにする。

唯一の実ギャップは個人カレンダーの表示月が client `useState` に閉じている点。これを `?ym=YYYY-MM` searchParam 化し、URL駆動ナビへ移行する。あわせて Phase 38 の共有ボタンが参照する正規URL取得規約 `buildShareUrl` を `src/lib/shareUrl.ts` に薄く実装する。

Purpose: 共有・リロード・別タブで「見ていた状態」が壊れない公開URLを成立させ、Phase 38/39 の基盤を提供する。
Output: `src/lib/shareUrl.ts`（+テスト）、URL駆動化した個人カレンダー。
</objective>

<execution_context>
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/kugyu10/work/keep-substack/CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/37-url-url/37-CONTEXT.md

<interfaces>
<!-- 既存の関連シグネチャ。executor はこれをそのまま使うこと。コードベース探索は不要。 -->

From src/lib/calendarUtils.ts:
- export function buildDayGrid(year: number, month: number): DayInfo[]
- export function parseIsoDate(isoDate: string): { year: number; month: number; day: number } | null
- type DayInfo = { date: number; colStart?: number }

CalendarGrid 現状の Props（src/components/CalendarGrid.tsx）:
- memberName: string
- articleMap: [string, HeatmapArticle[]][]
- imageUrl?: string
- substackHandle?: string
- 現状: const [year,setYear]=useState(now.getFullYear()); const [month,setMonth]=useState(now.getMonth()+1)
- prevMonth/nextMonth が setYear/setMonth でローカル状態を更新（←これを廃止する）

member page 現状（src/app/(main)/member/[publicationId]/page.tsx）:
- params: Promise<{ publicationId: string }> のみ受け取り、searchParams は未受領
- <CalendarGrid memberName=... articleMap=... imageUrl=... substackHandle=... /> を描画

既存 searchParam 規約（踏襲対象）:
- 既に page.tsx / daily/page.tsx は searchParams: Promise<{ team?: string }> を await し、a href で `?team=${encodeURIComponent(t)}` を生成している（Server Component, useState 不使用）
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: shareUrl ヘルパと ym parse/format を実装（TDD）</name>
  <files>src/lib/shareUrl.ts, src/lib/__tests__/shareUrl.test.ts</files>
  <read_first>
    - src/lib/calendarUtils.ts（parseIsoDate / buildDayGrid の規約。new Date() でのTZ依存を避ける既存方針）
    - src/lib/__tests__/validation.test.ts（Vitest の記法・import スタイルの参考）
    - .planning/phases/37-url-url/37-CONTEXT.md（searchParam 規約 / 成功基準3）
  </read_first>
  <behavior>
    parseYmParam(ym: string | undefined | null): { year: number; month: number }
      - "2026-06" → { year: 2026, month: 6 }
      - undefined / null / "" → 現在のJST年月にフォールバック（テストは「12以下の月・正の年が返る」で検証し、現在日付に依存しない形にする）
      - "2026-13"（月域外）/ "abc" / "2026-6"（ゼロ詰めなし） → 現在JST年月へフォールバック
    formatYmParam(year: number, month: number): string
      - (2026, 6) → "2026-06"（月は2桁ゼロ詰め）
      - (2026, 12) → "2026-12"
    buildShareUrl(view): string  // 対象4ビューの正規公開URL（相対パス）を返す
      - { type: 'goal', team?: string } → team未指定で "/"、team="A" で "/?team=A"（encodeURIComponent 適用）
      - { type: 'daily', team?: string } → "/daily" / "/daily?team=A"
      - { type: 'member', publicationId: string, ym?: string } → "/member/uojun"、ym="2026-06" で "/member/uojun?ym=2026-06"
      - team / publicationId は encodeURIComponent でエンコードされる（テスト: team="a b" → "?team=a%20b"）
  </behavior>
  <action>
    src/lib/shareUrl.ts を新規作成し、parseYmParam / formatYmParam / buildShareUrl を export する。buildShareUrl の引数はビュー種別の discriminated union（type: 'goal' | 'daily' | 'member'）とし、それぞれ team?（goal/daily）, publicationId + ym?（member）を受ける。返り値は相対公開URL文字列（先頭 "/"）。クエリ値は必ず encodeURIComponent でエンコード。月のゼロ詰めは String(month).padStart(2,'0') を使う（既存 calendarUtils と同一規約）。フォールバックの「現在JST年月」は new Date(Date.now() + 9*60*60*1000) の getUTCFullYear / getUTCMonth+1 で算出（calendarUtils の isoToJSTDateKey と同じ JST 換算規約に合わせる）。ym の妥当性判定は /^(\d{4})-(\d{2})$/ にマッチし、かつ month が 1〜12 のときのみ採用、それ以外は現在JST年月へ。先に src/lib/__tests__/shareUrl.test.ts を書き（RED）、実装後に GREEN にする。Vitest の import { describe, it, expect } from 'vitest' 形式を使う（既存テストに合わせる）。フェンスコードは書かない＝この action 記載のシグネチャ／識別子に従って実装すること。
  </action>
  <acceptance_criteria>
    - src/lib/shareUrl.ts が parseYmParam, formatYmParam, buildShareUrl を export している（grep で確認）
    - src/lib/__tests__/shareUrl.test.ts が上記 behavior の各ケースを網羅している
    - `npm test -- shareUrl` が全テスト pass する
    - formatYmParam(2026,6) === "2026-06"、parseYmParam("2026-06") の month === 6 を検証するアサーションが存在する
    - buildShareUrl({type:'member',publicationId:'uojun',ym:'2026-06'}) === "/member/uojun?ym=2026-06" を検証するアサーションが存在する
  </acceptance_criteria>
  <verify>
    <automated>npm test -- shareUrl</automated>
  </verify>
  <done>shareUrl.ts の3関数が behavior どおり動作し、shareUrl.test.ts が全 pass する。</done>
</task>

<task type="auto">
  <name>Task 2: 個人カレンダーを ym searchParam 駆動へ移行し、top/daily を検証</name>
  <files>src/app/(main)/member/[publicationId]/page.tsx, src/components/CalendarGrid.tsx</files>
  <read_first>
    - src/app/(main)/member/[publicationId]/page.tsx（現状: searchParams 未受領、CalendarGrid を描画）
    - src/components/CalendarGrid.tsx（現状: 月を useState で保持、prevMonth/nextMonth ボタン）
    - src/lib/shareUrl.ts（Task 1 で作成: parseYmParam / formatYmParam / buildShareUrl）
    - src/app/(main)/page.tsx（既存の searchParams: Promise<{team?:string}> await パターンと a href 規約）
    - src/lib/calendarUtils.ts（buildDayGrid のシグネチャ）
  </read_first>
  <action>
    (1) member/[publicationId]/page.tsx を Props に searchParams: Promise<{ ym?: string }> を追加し await する（既存の page.tsx と同じ Server Component パターン）。parseYmParam(ym) で { year, month } を求め、CalendarGrid に year / month を props で渡す。フォールバックは Task 1 の parseYmParam に委譲（page 側で現在月計算をしない）。
    (2) CalendarGrid.tsx から 'use client' と useState による year/month 状態・prevMonth/nextMonth の setState を撤去。Props に year: number, month: number を追加して受け取り表示に使う（buildDayGrid(year, month) はそのまま）。月送りボタン（＜ ＞）を next/link の Link に置き換え、href は formatYmParam で算出した前月/翌月の ym を持つ `/member/${publicationId}?ym=...`。そのため Props に publicationId: string を追加し、page.tsx 側で渡す。前月/翌月の桁上げ（month===1→前年12月 / month===12→翌年1月）は href 計算側で行う。HeatmapTooltip を含む内部要素はクライアント側のままで良いが、CalendarGrid 自体に状態がなくなれば Server Component 化できる場合はしてよい（裁量。Tooltip が client なら CalendarGrid は client のままでも可。最小変更を優先し、状態だけ撤去すれば足りる）。
    (3) top `/` と `/daily` は既に ?team= searchParam で URL 反映済みのため変更不要。動作確認のみ行う（下記 verify）。Server Component で team を await し a href で `?team=` を出している既存実装を壊さないこと。
    必要なら buildShareUrl を import 可能だが、本タスクの月ナビ href は formatYmParam ベースで十分（KISS）。フェンスコードは書かない。
  </action>
  <acceptance_criteria>
    - member/[publicationId]/page.tsx が searchParams: Promise<{ ym?: string }> を await し、parseYmParam を呼んでいる（grep: parseYmParam）
    - CalendarGrid.tsx に year/month を更新する useState が存在しない（grep -v '^\s*//' で setMonth / setYear が0件）
    - CalendarGrid.tsx の月送りが next/link の Link で href に "ym=" を含む（grep: ym=）
    - `npm run build` が成功する（型エラーなし。CalendarGrid Props 変更と呼び出し側が整合）
    - `npm test` が全 pass する（既存テストを壊していない）
    - 手動確認: 未ログインで `/member/<実在publicationId>?ym=2026-05` を開くと2026年5月が表示され、リロードでも維持される。月送り後の URL に ym= が付与される。
    - 手動確認: `/` でチームタブ切替 → URL に ?team= が付き、別タブで同URLが同じチーム表示。`/daily?team=X` も同様。
  </acceptance_criteria>
  <verify>
    <automated>npm run build && npm test</automated>
    <human-check>未ログインブラウザで /member/<publicationId>?ym=2026-05 を開き5月表示を確認、月送りでURLのymが変わることを確認、/ と /daily の ?team= 切替がリロード/別タブで復元することを確認</human-check>
  </verify>
  <done>個人カレンダーの表示月が URL(?ym=) で復元・共有可能になり、top/daily の team URL 復元も確認済み。build と test が pass。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 未ログイン訪問者 → 公開ページ(searchParams) | URL の team / ym / publicationId は信頼できない外部入力 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-01 | Tampering | ym searchParam の不正値（例 "2026-13", "abc", "../"） | mitigate | parseYmParam が正規表現 `^(\d{4})-(\d{2})$` ＋ month 1〜12 検証を通過しない値は現在JST月へフォールバック。数値以外は計算に使われない |
| T-37-02 | Information Disclosure | publicationId による任意メンバー閲覧 | accept | 個人カレンダーは元々公開ページ。存在しない publicationId は既存の notFound() で404。新たな情報露出はない |
| T-37-03 | Tampering | team searchParam の不正値 | accept | 既存実装の挙動を踏襲（一致しないteam名は単に空フィルタ）。本フェーズで新規導入しない既存挙動 |
| T-37-SC | Tampering | npm/pip/cargo installs | mitigate | 本プランは新規パッケージ導入なし（next/link は既存）。install タスクが無いため slopcheck 対象外 |
</threat_model>

<verification>
- `npm test` 全 pass（shareUrl 新規テスト含む、既存テスト無破壊）
- `npm run build` 成功（型整合）
- CalendarGrid から月状態 useState が消えている（grep）
- 個人カレンダーが ?ym=YYYY-MM で復元される（手動）
- top/daily の ?team= 復元（手動、既存挙動の回帰確認）
</verification>

<success_criteria>
1. トップ Commit&Goal のチームタブ切替が ?team= に反映され、リロード/別タブで同じチームで開く（既存・確認済み）。
2. /daily と個人カレンダーが、状態（team / ym）を含む公開URLで未ログインでも同じ表示にアクセスできる。
3. 対象4ビューの正規公開URLを buildShareUrl 1関数で取得でき、Phase 38 が参照できる。
</success_criteria>

<output>
Create `.planning/phases/37-url-url/37-01-SUMMARY.md` when done
</output>
