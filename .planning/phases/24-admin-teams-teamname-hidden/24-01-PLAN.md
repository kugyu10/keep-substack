---
phase: 24-admin-teams-teamname-hidden
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/admin/teams/[teamName]/page.tsx
  - src/app/admin/teams/__tests__/teamPage.test.tsx
autonomous: true
requirements: [VIEW-01]
must_haves:
  truths:
    - "管理者が /admin/teams/{teamName} を開くと、そのチームのメンバーの週次ヒートマップが表示される"
    - "hidden ステータスのチーム名でもメンバーが表示される（status は無視され teamName 完全一致のみで絞る）"
    - "日本語/エンコードされた teamName でも decode されて正しくフィルタされる"
    - "DB に存在しない teamName でも 200 でメッセージが表示される（404 にしない）"
    - "存在するがメンバー0人のチームでも 200 でメッセージが表示される"
  artifacts:
    - path: "src/app/admin/teams/[teamName]/page.tsx"
      provides: "Next.js 16 動的ルート RSC。teamName 完全一致フィルタ → WeeklyHeatmapGrid 描画 / 空時メッセージ"
      contains: "decodeURIComponent"
    - path: "src/app/admin/teams/__tests__/teamPage.test.tsx"
      provides: "RSC element-tree 検査による VIEW-01 / D-01 / D-06 / D-07 / decode のテスト"
      contains: "findByType"
  key_links:
    - from: "src/app/admin/teams/[teamName]/page.tsx"
      to: "@/lib/members getMembers"
      via: "await getMembers() 全件取得"
      pattern: "getMembers\\(\\)"
    - from: "src/app/admin/teams/[teamName]/page.tsx"
      to: "@/components/WeeklyHeatmapGrid"
      via: "results props でフィルタ後フィードを渡す"
      pattern: "WeeklyHeatmapGrid"
    - from: "src/app/admin/teams/[teamName]/page.tsx"
      to: "@/lib/fetchFeed fetchAllFeedsCached"
      via: "空チェック後にフィルタ済みメンバーで呼ぶ"
      pattern: "fetchAllFeedsCached"
---

<objective>
管理者専用の動的ルート `/admin/teams/{teamName}` を新設する。指定 teamName（public/private/hidden を問わない）に所属するメンバーの週次ヒートマップを表示する単一チームビュー。

Purpose: 管理者が hidden を含む任意のチームを URL 直接アクセスで確認できるようにする（VIEW-01）。トップページ（`src/app/page.tsx`）の取得パイプラインを流用し、単一チーム用に簡素化する。

Output:
- `src/app/admin/teams/__tests__/teamPage.test.tsx`（Wave 0 テスト — 先に作成）
- `src/app/admin/teams/[teamName]/page.tsx`（新規 RSC ページ）
</objective>

<execution_context>
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-admin-teams-teamname-hidden/24-CONTEXT.md
@.planning/phases/24-admin-teams-teamname-hidden/24-RESEARCH.md
@.planning/phases/24-admin-teams-teamname-hidden/24-PATTERNS.md
@.planning/phases/24-admin-teams-teamname-hidden/24-VALIDATION.md

# 流用元コード（最重要）
@src/app/page.tsx
@src/app/admin/teams/page.tsx
@src/app/my/__tests__/page.test.tsx

<interfaces>
<!-- Executor が直接使う契約。コードベース探索は不要。 -->

From src/lib/types.ts:
```typescript
export type Member = {
  name: string
  publicationId: string
  teams: { name: string; status: string }[]   // status は plain string（union ではない）
  addedAt: string
}
export type MemberFeedResult = {
  member: Member
  items: FeedItem[]
  imageUrl?: string
}
```

From src/lib/members.ts:
```typescript
export function getMembers(): Promise<Member[]>   // 全メンバーを teams(name,status) 付きで返す。内部で status フィルタはしない
```

From src/lib/fetchFeed.ts:
```typescript
export function fetchAllFeedsCached(members: Member[]): Promise<MemberFeedResult[]>
```

From src/components/WeeklyHeatmapGrid.tsx:
```typescript
// default export, 'use client'
export default function WeeklyHeatmapGrid({ results }: { results: MemberFeedResult[] }): JSX.Element
```

Next.js 16 動的ルート規約:
```typescript
type Props = { params: Promise<{ teamName: string }> }   // params は Promise。await 必須
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — teamPage.test.tsx を先に作成（5ケース、全 RED）</name>
  <files>src/app/admin/teams/__tests__/teamPage.test.tsx</files>
  <read_first>
    - src/app/my/__tests__/page.test.tsx（element-tree ハーネス・vi.mock パターンの流用元 — `isElement`/`findByType` を verbatim でコピー、lines 45-69）
    - src/lib/types.ts（Member / MemberFeedResult のフィクスチャ形状）
    - src/components/WeeklyHeatmapGrid.tsx（findByType の target として default export を import）
    - .planning/phases/24-admin-teams-teamname-hidden/24-VALIDATION.md（テスト名と per-task 検証マップ）
  </read_first>
  <behavior>
    - Test "renders heatmap": 有効 teamName → 返り要素ツリーに `WeeklyHeatmapGrid` が存在し、`.props.results` がフィルタ後メンバー数と一致する（VIEW-01）
    - Test "filters by exact teamName": 複数チームのフィクスチャで、別チームのメンバーは除外され、status='hidden' のメンバーは INCLUDE される（D-06/D-07）
    - Test "decodes encoded teamName": `encodeURIComponent('営業部')` を params に渡すと decode され「営業部」のメンバーがフィルタ一致する（Pitfall 2）
    - Test "unknown team shows message": DB に無い teamName → メッセージ `<p>` が存在し、`WeeklyHeatmapGrid` は不在（D-01）
    - Test "empty team shows message": 存在するがメンバー0人 → メッセージ表示、`WeeklyHeatmapGrid` 不在（D-01）
  </behavior>
  <action>
    `src/app/my/__tests__/page.test.tsx` の no-jsdom element-tree ハーネスをクローンする。`isElement` と `findByType` ヘルパーは verbatim でコピー。モック対象は `@/lib/*` 高レベルヘルパー: `vi.mock('@/lib/members', () => ({ getMembers: mockGetMembers }))` と `vi.mock('@/lib/fetchFeed', () => ({ fetchAllFeedsCached: mockFetchAllFeedsCached }))`。`WeeklyHeatmapGrid` は実 default export を import し findByType の target に使う（レンダリングしないのでスタブ不要。alias 解決が問題なら `vi.mock('@/components/WeeklyHeatmapGrid', () => ({ default: () => null }))` でスタブ）。ページは `import AdminTeamPage from '../[teamName]/page'` で取り込む。呼び出しは `await AdminTeamPage({ params: Promise.resolve({ teamName: encodeURIComponent('営業部') }) })`。フィクスチャは Member[] を `{ name, publicationId, teams: [{name,status}], addedAt }` 形状で用意。`mockFetchAllFeedsCached` は受け取った members を `results` 形に写像して返す（例: `members.map(m => ({ member: m, items: [] }))`）ことで results 長 = フィルタ後メンバー数を検証可能にする。beforeEach で `vi.clearAllMocks()` + デフォルト戻り値設定。空/不在ケースのメッセージは `findByType` ではなくツリー内の `<p>` テキストを走査して assert（または `WeeklyHeatmapGrid` が null であることで代替判定）。この時点でページが未実装のため 5 ケースは全て RED。コミット: `test(24-01): add failing tests for admin team view`。
  </action>
  <verify>
    <automated>npx vitest run src/app/admin/teams/__tests__/teamPage.test.tsx 2>&1 | grep -qE "fail|cannot find|Cannot find module" && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/admin/teams/__tests__/teamPage.test.tsx` が存在し `findByType` を含む
    - テストファイルが 5 つの `it(...)` を含む（テスト名: "renders heatmap", "filters by exact teamName", "decodes encoded teamName", "unknown team shows message", "empty team shows message"）
    - `npx vitest run src/app/admin/teams/__tests__/teamPage.test.tsx` がこの時点で FAIL する（ページ未実装のため RED）
    - テストは `@/lib/members` と `@/lib/fetchFeed` を `vi.mock` し、実 RSS/DB に依存しない
  </acceptance_criteria>
  <done>5 ケースを持つテストファイルが存在し、ページ未実装のため全て RED で落ちる。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: page.tsx を実装してテストを GREEN にする</name>
  <files>src/app/admin/teams/[teamName]/page.tsx</files>
  <read_first>
    - src/app/page.tsx（取得パイプライン流用元。filter ロジック lines 23-25 は丸ごとコピーしない）
    - src/app/admin/teams/page.tsx（admin back-link `← 管理画面へ` と `<main>` chrome の流用元）
    - src/app/admin/teams/__tests__/teamPage.test.tsx（Task 1 で作成したテスト — これを GREEN にする契約）
    - .planning/phases/24-admin-teams-teamname-hidden/24-RESEARCH.md（§Code Examples の完成形・§Common Pitfalls 1-5）
    - .planning/phases/24-admin-teams-teamname-hidden/24-PATTERNS.md（§Pattern Assignments）
  </read_first>
  <action>
    `src/app/admin/teams/[teamName]/page.tsx` を新規作成。import は `getMembers`（@/lib/members）, `fetchAllFeedsCached`（@/lib/fetchFeed）, `WeeklyHeatmapGrid`（@/components/WeeklyHeatmapGrid, default）の3つのみ — `PrBanner` は import しない（D-04）。`export const revalidate = 300`（D-05, Claude裁量でトップに合わせる）。シグネチャは `type Props = { params: Promise<{ teamName: string }> }`、`export default async function AdminTeamPage({ params }: Props)`。本体: `const { teamName: raw } = await params`（Next 16 で await 必須 — Pitfall 1）→ `const teamName = decodeURIComponent(raw)`（日本語対応必須 — Pitfall 2）。フィルタは D-07 の status 非依存・完全一致のみ: `const filtered = allMembers.filter((m) => m.teams.some((t) => t.name === teamName))`。トップの `.every((t) => t.status !== 'hidden')` 除外分岐は絶対にコピーしない（Pitfall 3 / D-06）。空判定は単一分岐 `filtered.length === 0`（DB未存在もメンバー0人も同一扱い — Pitfall 5）で、`fetchAllFeedsCached` を呼ぶ前に判定する。空なら 200 でメッセージ `<p>`（例「該当するチームのメンバーがいません」）を返し、`notFound()` は使わない（D-01/D-02）。非空なら `const results = await fetchAllFeedsCached(filtered)` を取り `<WeeklyHeatmapGrid results={results} />` を描画。chrome は `<main className="max-w-[600px] mx-auto px-3 py-4">`（D-05）内に `<a href="/admin" ...>← 管理画面へ</a>` 戻りリンク（admin sibling から流用）+ teamName 見出し `<h1>{teamName}</h1>`（D-03）。チームタブ・PrBanner は置かない（D-04）。ページ内に admin 認可チェックを書かない（proxy.ts が担保 — Pitfall 4 / VIEW-02 は 24-02 で確認）。実装後コミット: `feat(24-01): implement admin single-team heatmap view`。
  </action>
  <verify>
    <automated>npx vitest run src/app/admin/teams/__tests__/teamPage.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/app/admin/teams/__tests__/teamPage.test.tsx` が exit 0（5 ケース全 GREEN）
    - `src/app/admin/teams/[teamName]/page.tsx` が `await params` を含む
    - `src/app/admin/teams/[teamName]/page.tsx` が `decodeURIComponent` を含む
    - filter が `.some((t) => t.name === teamName)` を使い、`.status !== 'hidden'` を含まない: `grep -c "status !== 'hidden'" src/app/admin/teams/[teamName]/page.tsx` が 0
    - `notFound` を含まない: `grep -c "notFound" "src/app/admin/teams/[teamName]/page.tsx"` が 0
    - `PrBanner` を import していない: `grep -c "PrBanner" "src/app/admin/teams/[teamName]/page.tsx"` が 0
    - `filtered.length === 0` の単一空判定が存在する
    - `npm run build` が成功する（Next 16 型チェック含む）
  </acceptance_criteria>
  <done>page.tsx が存在し 5 テストが全 GREEN、ビルドが通り、D-01/D-04/D-06/D-07 の禁止パターンを含まない。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → proxy.ts | 未認証/非admin の untrusted リクエストがここで遮断される（既存ゲート） |
| proxy.ts → RSC page | admin 検証済みリクエストのみ到達。teamName(encoded) が untrusted 入力として渡る |
| RSC page → Supabase/RSS | サーバー側のみ。`getMembers()`/`fetchAllFeedsCached()` 経由。生 Supabase 行はクライアントに渡らない |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01 | Information Disclosure | `[teamName]/page.tsx`（hidden チームデータ） | mitigate | RSC はサーバー側でのみデータ取得し、クライアントには `MemberFeedResult[]` のみ渡す（生 Supabase 行・id を渡さない）。VIEW-02 の認可は既存 proxy.ts が担保（24-02 で確認） |
| T-24-02 | Tampering | teamName 入力 | mitigate | teamName は `decodeURIComponent` 後に in-memory `===` 比較のみ。DB クエリ文字列に連結しない（`getMembers` 内 select はパラメータ化済み）。インジェクション面なし（ASVS V5） |
| T-24-03 | Elevation of Privilege | `/admin/teams/{teamName}` ルート | accept (this plan) | ページ内に認可コードを置かない。`/admin/:path*` matcher が自動カバー。確認は 24-02 の proxy 確認テストで担保 |
| T-24-04 | Denial of Service | `fetchAllFeedsCached` 大量フェッチ | accept | admin 限定で攻撃面小。既存の5秒タイムアウト+リトライ1回、`revalidate=300` キャッシュで十分 |
| T-24-SC | Tampering | npm/pip/cargo installs | N/A | 新規パッケージインストールなし（RESEARCH §Package Legitimacy Audit: 適用対象外） |
</threat_model>

<verification>
- `npx vitest run src/app/admin/teams/__tests__/teamPage.test.tsx` が exit 0（5 ケース GREEN）
- `npm test`（全スイート）が green
- `npm run build` 成功
- `grep` ゲート: page.tsx に `status !== 'hidden'` / `notFound` / `PrBanner` が出現しない（各 0）
- `src/proxy.ts` に diff が出ていない（このプランは proxy.ts を一切変更しない）
</verification>

<success_criteria>
- VIEW-01: `/admin/teams/{teamName}` で指定チームの週次ヒートマップが表示される（テスト "renders heatmap" GREEN）
- D-06/D-07: status 非依存・完全一致でフィルタし hidden チームも表示（テスト "filters by exact teamName" GREEN）
- decode: 日本語チーム名が一致する（テスト "decodes encoded teamName" GREEN）
- D-01/D-02: 未存在/0人チームは 200 でメッセージ（テスト "unknown team shows message" / "empty team shows message" GREEN）
- ビルドが通り、proxy.ts は無変更
</success_criteria>

<output>
Create `.planning/phases/24-admin-teams-teamname-hidden/24-01-SUMMARY.md` when done
</output>
