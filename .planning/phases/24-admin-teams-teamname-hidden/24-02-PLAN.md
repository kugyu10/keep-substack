---
phase: 24-admin-teams-teamname-hidden
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/__tests__/proxy.test.ts
autonomous: true
requirements: [VIEW-02]
must_haves:
  truths:
    - "非admin/未認証ユーザーが /admin/teams/{teamName} にアクセスすると / にリダイレクトされる"
    - "admin ロールユーザーは /admin/teams/{teamName} を通過できる（リダイレクトされない）"
    - "src/proxy.ts は無変更のまま新ルートを既存ゲートでカバーする"
  artifacts:
    - path: "src/__tests__/proxy.test.ts"
      provides: "既存 proxy.ts が /admin/teams/{teamName} を自動カバーすることの確認テスト（VIEW-02）"
      contains: "/admin/teams/"
  key_links:
    - from: "src/__tests__/proxy.test.ts"
      to: "src/proxy.ts proxy()"
      via: "NextRequest をモックして proxy() を直接呼び戻り値を assert"
      pattern: "proxy\\("
---

<objective>
VIEW-02 を確認するテストを追加する。既存の `src/proxy.ts` の `/admin/:path*` matcher と admin ロールゲートが、新ルート `/admin/teams/{teamName}` を **新規コードなしで** カバーすることを自動テストで証明する。

Purpose: VIEW-02（adminロールのみアクセス可、proxy.tsで制御）の充足を回帰テストとして固定する。**proxy.ts 自体は一切変更しない（confirm-only）。**

Output:
- `src/__tests__/proxy.test.ts`（proxy 関数を直接呼ぶ確認テスト）
</objective>

<execution_context>
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-admin-teams-teamname-hidden/24-CONTEXT.md
@.planning/phases/24-admin-teams-teamname-hidden/24-RESEARCH.md
@.planning/phases/24-admin-teams-teamname-hidden/24-VALIDATION.md

@src/proxy.ts
@src/app/my/__tests__/page.test.tsx

<interfaces>
<!-- 確認のみ。proxy.ts は無変更。 -->

From src/proxy.ts:
```typescript
export async function proxy(request: NextRequest): Promise<NextResponse>
// /admin 配下: user==null OR user.app_metadata?.role !== 'admin' → NextResponse.redirect(new URL('/', request.url))
// pathname は request.nextUrl.pathname を参照（startsWith('/admin')）
export const config = { matcher: ['/admin', '/admin/:path*', '/my', '/my/:path*'] }
```

proxy は `createServerClient(@supabase/ssr)` を呼び `supabase.auth.getUser()` で user を取得する。
テストでは `@supabase/ssr` の `createServerClient` を `vi.mock` し、`auth.getUser` の戻り値（user/null・role）を差し替える。
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: proxy.test.ts — /admin/teams/{teamName} の認可ゲートを確認</name>
  <files>src/__tests__/proxy.test.ts</files>
  <read_first>
    - src/proxy.ts（テスト対象。`startsWith('/admin')` ゲート lines 34-38、matcher line 50-52。**読むだけ・変更禁止**）
    - src/app/my/__tests__/page.test.tsx（`vi.mock` / `vi.hoisted` モックパターンの流用元）
    - .planning/phases/24-admin-teams-teamname-hidden/24-RESEARCH.md（§VIEW-02 / §Code Examples の proxy 抜粋）
  </read_first>
  <behavior>
    - Test "non-admin redirect": `request.nextUrl.pathname = '/admin/teams/営業部'`、`getUser` が role!=='admin' の user を返す → `proxy()` の戻り値が `/` への redirect（status 307/308、location が `/`）
    - Test "unauthenticated redirect": 同パスで `getUser` が `{ user: null }` → `/` への redirect
    - Test "admin passes through": 同パスで `getUser` が `app_metadata.role==='admin'` の user → redirect ではなく通常の `NextResponse.next()` 相当（location ヘッダなし）
  </behavior>
  <action>
    `src/__tests__/proxy.test.ts` を新規作成。`@supabase/ssr` の `createServerClient` を `vi.mock` し、`{ auth: { getUser: mockGetUser } }` を返すスタブにする（`mockGetUser` は `vi.hoisted` で巻き上げ）。`NextRequest` は `next/server` の実物を使い、`new NextRequest(new URL('https://example.com/admin/teams/' + encodeURIComponent('営業部')))` で生成（`request.nextUrl.pathname` が `/admin/teams/...` になる）。proxy は `request.cookies.getAll()` を呼ぶため、NextRequest 実物を使えばそのまま動く。`process.env.NEXT_PUBLIC_SUPABASE_URL` / `NEXT_PUBLIC_SUPABASE_ANON_KEY` をテスト内でダミー値設定（`'https://x.supabase.co'` / `'anon'`）。各テストで `mockGetUser.mockResolvedValue(...)` を設定し `const res = await proxy(req)` を呼ぶ。redirect 判定は `res.status`（307/308）と `res.headers.get('location')` が `/`（= `https://example.com/`）で終わることを assert。通過判定は location ヘッダが null（`NextResponse.next()` は location を持たない）で assert。beforeEach で `vi.clearAllMocks()`。**proxy.ts には一切手を加えない** — このテストは「既存ゲートが新ルートをカバーする」ことの確認専用。コミット: `test(24-02): confirm proxy gates /admin/teams/{teamName} (VIEW-02)`。
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/proxy.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/__tests__/proxy.test.ts` が存在し `/admin/teams/` を含む
    - `npx vitest run src/__tests__/proxy.test.ts` が exit 0（3 ケース GREEN: non-admin redirect / unauthenticated redirect / admin passes through）
    - `src/proxy.ts` が無変更: `git diff --quiet src/proxy.ts` が exit 0
    - テストは `@supabase/ssr` を `vi.mock` し実 Supabase に接続しない
  </acceptance_criteria>
  <done>proxy.test.ts が存在し 3 ケース GREEN、proxy.ts に diff が出ていない。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → proxy.ts | 全 `/admin/*` リクエストはここで `supabase.auth.getUser()`（サーバー側検証）を通過し、非admin は遮断される |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-03 | Elevation of Privilege | `/admin/teams/{teamName}` ルートの認可 | mitigate | 既存 proxy.ts の `pathname.startsWith('/admin')` + `user.app_metadata?.role !== 'admin'` ゲートが新ルートを自動カバー。本プランはその挙動をテストで固定する（ASVS V4 Access Control） |
| T-24-05 | Spoofing / Info Disclosure | cookie セッション改ざんによる admin なりすまし | mitigate | proxy は `supabase.auth.getUser()`（サーバー側検証）を使用しており cookie の自己申告 role に依存しない（既存・無変更、ASVS V2/V3） |
| T-24-SC | Tampering | npm/pip/cargo installs | N/A | 新規パッケージインストールなし（RESEARCH §Package Legitimacy Audit: 適用対象外） |
</threat_model>

<verification>
- `npx vitest run src/__tests__/proxy.test.ts` が exit 0（3 ケース GREEN）
- `git diff --quiet src/proxy.ts` が exit 0（proxy.ts 無変更）
- `npm test`（全スイート）が green
</verification>

<success_criteria>
- VIEW-02: 非admin/未認証が `/admin/teams/{teamName}` で `/` にリダイレクトされる（テスト GREEN）
- admin ロールは通過できる
- proxy.ts は新規コードゼロ・無変更（confirm-only）
</success_criteria>

<output>
Create `.planning/phases/24-admin-teams-teamname-hidden/24-02-SUMMARY.md` when done
</output>
