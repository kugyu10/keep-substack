---
phase: 25-publication-id
plan: 02
type: execute
wave: 2
depends_on: ["25-01"]
files_modified: []
autonomous: false
requirements: [SCHEMA-01, SCHEMA-02]

must_haves:
  truths:
    - "member_publications table + partial unique index + RLS public-select policy are live in the Supabase database"
    - "Every existing members row has exactly one matching member_publications row with is_primary=true (row-count parity)"
    - "The sync trigger fires on members INSERT (creates a primary row) and on members UPDATE of publication_id (follows the change); member DELETE cascades to purge child rows"
    - "App code under src/ is unchanged and the app builds, tests, and lints green (Success Criteria #3)"
  artifacts:
    - path: "supabase/migrations/20260530_add_member_publications.sql"
      provides: "The migration applied to the live DB via Supabase SQL Editor"
      contains: "CREATE TABLE IF NOT EXISTS member_publications"
  key_links:
    - from: "Supabase SQL Editor"
      to: "live Postgres (project xolhjcngrwwwqtklmoyk)"
      via: "human pastes migration SQL and clicks Run"
      pattern: "member_publications"
    - from: "members table write paths (addMember/updateMember in src/lib/members.ts)"
      to: "member_publications"
      via: "trg_sync_member_publications AFTER INSERT OR UPDATE trigger"
      pattern: "trg_sync_member_publications"
---

<objective>
Apply the Phase 25 migration to the live Supabase database (the repo convention is the Supabase SQL Editor, NOT `supabase db push` — RESEARCH Pitfall 5), then run post-migration SQL verification queries to confirm the table, partial unique index, RLS policy, backfill parity, and trigger behavior are all live and correct. Finally confirm the app is unchanged and green.

Purpose: This is the [BLOCKING] schema-apply step. Build/type checks pass WITHOUT the migration being applied (this app does not generate types from the live DB), so the phase would be in a false-positive verification state unless the schema is actually applied. This plan closes that gap and proves SCHEMA-01 (table live) and SCHEMA-02 (data migrated) against the real database.

Output: A live, self-maintaining member_publications table; recorded verification-query results in the SUMMARY.
</objective>

<execution_context>
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/25-publication-id/25-CONTEXT.md
@.planning/phases/25-publication-id/25-RESEARCH.md
@.planning/phases/25-publication-id/25-VALIDATION.md
@supabase/migrations/20260530_add_member_publications.sql
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: [BLOCKING] Apply migration via Supabase SQL Editor</name>
  <files>supabase/migrations/20260530_add_member_publications.sql</files>
  <read_first>
    - supabase/migrations/20260530_add_member_publications.sql (the full migration text the human will paste)
    - .planning/phases/25-publication-id/25-RESEARCH.md Pitfall 5 (why SQL Editor, not db push) and §Environment Availability (project ref xolhjcngrwwwqtklmoyk)
  </read_first>
  <what-built>
    Plan 01 authored `supabase/migrations/20260530_add_member_publications.sql` — an idempotent, BEGIN/COMMIT-wrapped migration that creates member_publications (table + partial unique index + RLS public-select policy), backfills it from existing members (is_primary=true, ON CONFLICT DO NOTHING), and installs the AFTER INSERT OR UPDATE sync trigger on members. Claude cannot apply it automatically: the repo convention is the Supabase SQL Editor, and `supabase db push` has a known non-TTY prompt bug (supabase/cli #2238). This step must be done by the human.
  </what-built>
  <action>
    This is a blocking human-action checkpoint — Claude cannot apply the migration (repo convention = Supabase SQL Editor; `supabase db push` has a non-TTY prompt bug, RESEARCH Pitfall 5). Present the <how-to-verify> steps to the human and pause for the <resume-signal>. Do NOT attempt `supabase db push` or any automated apply. The text to paste is the full contents of supabase/migrations/20260530_add_member_publications.sql (authored in Plan 01, BEGIN/COMMIT-wrapped, idempotent — safe to paste and re-run).
  </action>
  <how-to-verify>
    1. Open the Supabase dashboard for project `xolhjcngrwwwqtklmoyk` → SQL Editor → New query.
    2. Copy the ENTIRE contents of `supabase/migrations/20260530_add_member_publications.sql` and paste it into the editor.
    3. Click "Run". Expect "Success. No rows returned" (or similar) with no errors.
    4. Because the migration is idempotent, re-running it is safe — if you ran it once already, a second Run must NOT error (no "duplicate key" / "policy already exists").
    5. Report back: success (and any row counts shown), or paste any error message.
  </how-to-verify>
  <verify>Human confirms the migration ran in the Supabase SQL Editor with no error (resume-signal "applied").</verify>
  <acceptance_criteria>
    - The migration runs in the SQL Editor with no error.
    - Re-running the migration produces no error (idempotency holds).
    - Human confirms completion (reports "applied" or pastes the success output).
  </acceptance_criteria>
  <done>member_publications migration is applied to the live Supabase database (project xolhjcngrwwwqtklmoyk) with no error; idempotent re-run confirmed safe.</done>
  <resume-signal>Type "applied" (or paste any error encountered)</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: [BLOCKING] Post-migration SQL verification (SCHEMA-01 + SCHEMA-02 + trigger behavior)</name>
  <files>supabase/migrations/20260530_add_member_publications.sql</files>
  <read_first>
    - .planning/phases/25-publication-id/25-RESEARCH.md §Validation Architecture (Phase Requirements → Test Map — the exact verification queries) and §4 (edge cases)
    - .planning/phases/25-publication-id/25-VALIDATION.md (Manual-Only Verifications table)
  </read_first>
  <what-built>
    Verification of the now-live schema. These SQL queries prove each requirement and the trigger invariants. Run each in the Supabase SQL Editor and confirm the expected result.
  </what-built>
  <action>
    This is a blocking human-verify checkpoint — present the verification queries A–H in <how-to-verify> to the human, have them run each in the Supabase SQL Editor, and pause for the <resume-signal>. These queries assert SCHEMA-01 (table/columns/partial-index/RLS), SCHEMA-02 (backfill parity), and the D-08 trigger invariants (INSERT/UPDATE sync, DELETE cascade) plus the D-05 single-primary constraint. Record the A–H results in the SUMMARY. Do not modify any files.
  </action>
  <how-to-verify>
    Run these queries in the SQL Editor and confirm each expected outcome:

    A. TABLE + COLUMNS exist (SCHEMA-01):
       `SELECT column_name, data_type, is_nullable FROM information_schema.columns WHERE table_name = 'member_publications' ORDER BY ordinal_position;`
       Expect rows: id (uuid, NO), member_id (uuid, NO), publication_id (text, NO), is_primary (boolean, NO).

    B. PARTIAL UNIQUE INDEX exists (SCHEMA-01 / D-05):
       `SELECT indexname, indexdef FROM pg_indexes WHERE tablename = 'member_publications';`
       Expect a row `uq_member_publications_one_primary` whose indexdef contains `WHERE (is_primary ...)`.

    C. RLS enabled + policy present (SCHEMA-01 / threat T-25-01):
       `SELECT relrowsecurity FROM pg_class WHERE relname = 'member_publications';` → expect `t` (true).
       `SELECT policyname, cmd FROM pg_policies WHERE tablename = 'member_publications';` → expect `public select member_publications`, cmd `SELECT`. There must be NO INSERT/UPDATE/DELETE policy.

    D. BACKFILL PARITY (SCHEMA-02 / D-07):
       `SELECT count(*) FROM members m LEFT JOIN member_publications mp ON mp.member_id = m.id AND mp.is_primary WHERE mp.id IS NULL;` → expect `0` (every member has a primary publication row).
       Also: `SELECT (SELECT count(*) FROM members) AS members, (SELECT count(*) FROM member_publications WHERE is_primary) AS primaries;` → the two counts should be equal.

    E. INSERT-SYNC trigger (D-08): create a throwaway member and confirm a primary row appears, then clean up:
       `INSERT INTO members (name, publication_id) VALUES ('zz_phase25_test', 'zz_phase25_test_pub');`
       `SELECT is_primary FROM member_publications WHERE publication_id = 'zz_phase25_test_pub';` → expect one row, is_primary = true.

    F. UPDATE-SYNC trigger (D-08, defensive branch):
       `UPDATE members SET publication_id = 'zz_phase25_test_pub2' WHERE publication_id = 'zz_phase25_test_pub';`
       `SELECT publication_id FROM member_publications WHERE member_id = (SELECT id FROM members WHERE publication_id = 'zz_phase25_test_pub2') AND is_primary;` → expect `zz_phase25_test_pub2`.

    G. DELETE CASCADE (D-08 verified-redundant — CASCADE, no trigger):
       `DELETE FROM members WHERE publication_id = 'zz_phase25_test_pub2';`
       `SELECT count(*) FROM member_publications WHERE publication_id IN ('zz_phase25_test_pub','zz_phase25_test_pub2');` → expect `0`.

    H. SINGLE-PRIMARY enforcement (D-05): attempt a second primary for an existing member and confirm it is rejected:
       `INSERT INTO member_publications (member_id, publication_id, is_primary) SELECT member_id, 'zz_phase25_dup_primary', true FROM member_publications WHERE is_primary LIMIT 1;` → expect a unique-violation error on `uq_member_publications_one_primary`. (If it errors, the invariant holds — this is the success case. Nothing to clean up since the insert failed.)

    Report the result of each query (A–H).
  </how-to-verify>
  <verify>Human runs queries A–H in the SQL Editor and confirms each matches the expected result (resume-signal "verified").</verify>
  <acceptance_criteria>
    - A: four expected columns present with correct types/nullability.
    - B: index `uq_member_publications_one_primary` exists with a `WHERE (is_primary` predicate.
    - C: relrowsecurity = true; exactly one policy `public select member_publications` for SELECT; no write policies.
    - D: orphan count = 0 AND members count == primaries count.
    - E: throwaway member produced exactly one member_publications row with is_primary = true.
    - F: the primary row's publication_id followed the members UPDATE.
    - G: deleting the member cascaded — zero leftover member_publications rows for the test publication_ids.
    - H: the second-primary INSERT was rejected with a unique-constraint violation (invariant enforced).
    - All test data created in E/F is removed by G (no `zz_phase25_*` rows remain): `SELECT count(*) FROM member_publications WHERE publication_id LIKE 'zz_phase25_%';` → 0.
  </acceptance_criteria>
  <done>Live-DB verification A–H all match expected results: table/columns, partial unique index, RLS+single-SELECT-policy, backfill parity, INSERT/UPDATE sync, DELETE cascade, single-primary enforcement — and no zz_phase25_ test rows remain.</done>
  <resume-signal>Type "verified" and paste the A–H results, or describe any query that did not match expectations</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Confirm app code unchanged and green (Success Criteria #3)</name>
  <files></files>
  <read_first>
    - .planning/phases/25-publication-id/25-VALIDATION.md (Full suite command + Sampling Rate "Before verify-work")
    - .planning/phases/25-publication-id/25-CONTEXT.md <domain> "このフェーズで変更しないもの" list
  </read_first>
  <action>
    Run the full gate to prove the app builds, tests, and lints unchanged after the schema phase. Then assert no application code was modified by this phase: `git diff src/` must be empty (Success Criteria #3 — the sync is entirely DB-trigger-driven, src/lib/members.ts and friends are untouched). Also confirm `grep -rn member_publications src/` returns no matches (the app does not read the new table this phase — boundary intact). Do not modify any files in this task; it is a verification gate only.
  </action>
  <verify>
    <automated>npm run build && npm test && npm run lint && test -z "$(git diff --name-only -- src/)" && ! grep -rn "member_publications" src/ && echo SC3_OK</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0.
    - `npm test` exits 0 (vitest suite green).
    - `npm run lint` exits 0.
    - `git diff --name-only -- src/` outputs nothing (no app code changed).
    - `grep -rn member_publications src/` returns no matches.
    - Combined command prints SC3_OK.
  </acceptance_criteria>
  <done>App builds/tests/lints green; src/ has zero diff; member_publications appears nowhere in src/ — Success Criteria #3 satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human operator → Supabase SQL Editor → live Postgres | The migration is applied manually by a trusted operator into project xolhjcngrwwwqtklmoyk. |
| anon/authenticated client → live member_publications (RLS) | After apply, public clients can SELECT but not write member_publications. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | Information Disclosure / Tampering | live member_publications RLS posture | mitigate | Task 2 query C asserts relrowsecurity=true and that the ONLY policy is `public select member_publications` (SELECT) — no write policy exposed to anon/authenticated. Verified against the live DB, not just the file. |
| T-25-02 | Denial of Service | trigger write blocked by RLS | accept | service_role (BYPASSRLS) executes the trigger; Task 2 query E proves triggered writes succeed under RLS. |
| T-25-04 | Elevation of Privilege | trigger SECURITY DEFINER escalation | mitigate | Function is SECURITY INVOKER (default, not DEFINER) — confirmed in the applied migration; runs with caller privileges only. |
| T-25-AP | Tampering | partial/failed manual apply leaves inconsistent DB | mitigate | Migration is BEGIN/COMMIT-wrapped (Plan 01) — a failed paste rolls back atomically. Task 1 step 4 confirms idempotent re-run. |
| T-25-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — SQL-only. No slopcheck checkpoint required. |
</threat_model>

<verification>
- Migration applied in Supabase SQL Editor with no error (Task 1).
- Post-migration SQL verification A–H all match expected results (Task 2): table+columns, partial unique index, RLS+policy, backfill parity, INSERT/UPDATE sync, DELETE cascade, single-primary enforcement.
- `npm run build && npm test && npm run lint` green; `git diff src/` empty; `grep -rn member_publications src/` empty (Task 3).
</verification>

<success_criteria>
- member_publications is live with table + partial unique index + RLS public-select policy (SCHEMA-01).
- Existing members data is backfilled with one is_primary=true row each, parity confirmed (SCHEMA-02).
- Sync trigger demonstrably fires on INSERT/UPDATE; DELETE cascades; single-primary invariant enforced.
- App code unchanged and green (Success Criteria #3).
</success_criteria>

<output>
Create `.planning/phases/25-publication-id/25-02-SUMMARY.md` when done — record the A–H verification-query results.
</output>
