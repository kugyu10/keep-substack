---
phase: 4
plan: "04-01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/lib/types.ts
  - src/lib/redis.ts
  - src/lib/kvMembers.ts
autonomous: true
requirements:
  - KV-01

must_haves:
  truths:
    - "@upstash/redis が package.json の dependencies に存在する"
    - "tsx と dotenv が devDependencies に存在する"
    - "Member 型に substackId, teamId, addedAt フィールドがあり feedUrl フィールドが存在しない"
    - "redis.ts が Redis.fromEnv() を export const redis として export している"
    - "kvMembers.ts が getMembers(): Promise<Member[]> を export している"
    - "KVに members キーが存在しない場合 getMembers() が空配列を返す"
  artifacts:
    - path: "src/lib/redis.ts"
      provides: "Upstash Redis シングルトンクライアント"
      exports: ["redis"]
    - path: "src/lib/kvMembers.ts"
      provides: "KVからメンバー配列を取得する関数"
      exports: ["getMembers"]
    - path: "src/lib/types.ts"
      provides: "新 Member 型定義（KVスキーマ準拠）"
      contains: "substackId: string"
  key_links:
    - from: "src/lib/kvMembers.ts"
      to: "src/lib/redis.ts"
      via: "import { redis } from './redis'"
      pattern: "import.*redis.*from.*./redis"
    - from: "src/lib/kvMembers.ts"
      to: "Upstash KV"
      via: "redis.get<Member[]>('members')"
      pattern: "redis\\.get.*members"
---

<objective>
KVデータ層の基盤を確立する。@upstash/redis をインストールし、Member 型を新スキーマに更新し、redis シングルトンと getMembers() 関数を新設する。

Purpose: Phase 4の後続プラン（04-02）と Phase 5/6 が KV経由でメンバーデータを取得できる共通基盤を作る。
Output: src/lib/redis.ts、src/lib/kvMembers.ts（新設）、src/lib/types.ts（更新）、package.json（依存追加）
</objective>

<execution_context>
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-kv-data-layer/04-CONTEXT.md
@.planning/phases/04-kv-data-layer/04-RESEARCH.md

<interfaces>
<!-- 変更対象の既存インターフェース -->

現在の src/lib/types.ts（変更前）:
```typescript
export type Member = {
  name: string
  feedUrl: string
}

export type MemberFeedResult = {
  member: Member
  items: FeedItem[]
}
```

変更後の Member 型（D-03, D-07 に基づく）:
```typescript
export type Member = {
  name: string
  substackId: string
  teamId: string
  addedAt: string  // ISO 8601
}
```

新設する src/lib/redis.ts:
```typescript
import { Redis } from '@upstash/redis'
export const redis = Redis.fromEnv()
```

新設する src/lib/kvMembers.ts:
```typescript
import { redis } from './redis'
import type { Member } from './types'

export async function getMembers(): Promise<Member[]> {
  const members = await redis.get<Member[]>('members')
  return members ?? []
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 依存パッケージのインストール</name>
  <files>package.json</files>
  <read_first>
    - package.json（現在の依存関係を確認してから追加する）
  </read_first>
  <action>
以下のコマンドを実行して依存パッケージを追加する：

```bash
cd /Users/kugyu10/work/keep-substack
npm install @upstash/redis
npm install --save-dev tsx dotenv
```

インストール後、package.json を確認して以下が追加されていることを検証する：
- dependencies に `"@upstash/redis"` が存在する
- devDependencies に `"tsx"` と `"dotenv"` が存在する

注意: `@vercel/kv` は2024年12月に廃止済みのため絶対に使わない（D-02）。
  </action>
  <verify>
    <automated>cd /Users/kugyu10/work/keep-substack && node -e "const p=require('./package.json'); console.log('@upstash/redis:', p.dependencies['@upstash/redis'] ?? 'MISSING'); console.log('tsx:', p.devDependencies['tsx'] ?? 'MISSING'); console.log('dotenv:', p.devDependencies['dotenv'] ?? 'MISSING')"</automated>
  </verify>
  <acceptance_criteria>
    - package.json の dependencies に `"@upstash/redis"` キーが存在する（値は `^1.38.0` 以上）
    - package.json の devDependencies に `"tsx"` キーが存在する
    - package.json の devDependencies に `"dotenv"` キーが存在する
  </acceptance_criteria>
  <done>npm install が完了し、node_modules/@upstash/redis が存在する</done>
</task>

<task type="auto">
  <name>Task 2: Member 型の更新と redis/kvMembers モジュールの新設</name>
  <files>src/lib/types.ts, src/lib/redis.ts, src/lib/kvMembers.ts</files>
  <read_first>
    - src/lib/types.ts（現在の型定義を確認してから更新する）
    - src/lib/fetchFeed.ts（Member 型を参照しているため、変更後の影響を把握する）
  </read_first>
  <action>
**Step 1: src/lib/types.ts を更新する（D-07, D-03 に基づく）**

`Member` 型を以下に置き換える（`feedUrl` を削除し、`substackId`、`teamId`、`addedAt` を追加）：

```typescript
// RESEARCH.md §2「カレンダーUI向けのキーフィールド」に基づく
export type FeedItem = {
  title?: string
  link?: string
  pubDate?: string
  isoDate?: string // rss-parserが自動付与するISO 8601形式
}

export type Member = {
  name: string
  substackId: string
  teamId: string
  addedAt: string  // ISO 8601
}

// Phase 4 KV移行後のスキーマ（D-03）。feedUrl は substackId から動的生成する（D-08）

// Plan 01-02 の page.tsx が使う集約型
export type MemberFeedResult = {
  member: Member
  items: FeedItem[]
}
```

注意: `feedUrl` フィールドをコードベースに残さない（D-03）。

**Step 2: src/lib/redis.ts を新設する（D-01, D-02 に基づく）**

```typescript
import { Redis } from '@upstash/redis'

// D-02: @upstash/redis の自動シリアライズを使用（redis.json.* は使わない）
// D-01: 単一キー 'members' に Member 配列を JSON 格納
export const redis = Redis.fromEnv()
```

**Step 3: src/lib/kvMembers.ts を新設する（D-10 に基づく）**

```typescript
import { redis } from './redis'
import type { Member } from './types'

// D-10: KV からメンバーを取得する関数
// D-01: redis.get('members') で Member[] を取得。キーが存在しない場合は [] を返す
export async function getMembers(): Promise<Member[]> {
  const members = await redis.get<Member[]>('members')
  return members ?? []
}
```

注意事項:
- `redis.json.get` は絶対に使わない（D-02）
- `JSON.parse` を手動で呼ばない（@upstash/redis が自動デシリアライズする）
- `members ?? []` のフォールバックは必須（KV 未シード時に null が返る — Pitfall 3 対策）
  </action>
  <verify>
    <automated>cd /Users/kugyu10/work/keep-substack && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/types.ts に `substackId: string` が存在する（grep: `grep -c "substackId: string" src/lib/types.ts` が 1 以上）
    - src/lib/types.ts に `feedUrl` が存在しない（grep: `grep -c "feedUrl" src/lib/types.ts` が 0）
    - src/lib/redis.ts に `export const redis = Redis.fromEnv()` が存在する
    - src/lib/kvMembers.ts に `export async function getMembers(): Promise<Member[]>` が存在する
    - src/lib/kvMembers.ts に `return members ?? []` が存在する
    - `npx tsc --noEmit` がエラーゼロで完了する（ただし fetchFeed.ts の member.feedUrl 参照は次プランで修正するため、そこだけエラーが出ることがある — その場合は次プランで解消する旨をメモする）
  </acceptance_criteria>
  <done>
    types.ts・redis.ts・kvMembers.ts が正しく作成・更新されており、TypeScript 型定義として整合性がある。
    fetchFeed.ts や page.tsx の member.feedUrl 参照エラーは 04-02-PLAN で解消する。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| kvMembers.ts → Upstash KV REST API | KVから取得したデータは外部I/Oであり、型安全性を確認する必要がある |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | src/lib/redis.ts | mitigate | UPSTASH_REDIS_REST_TOKEN は .env.local に格納し、git にコミットしない。.gitignore に .env.local が含まれることを確認する |
| T-04-02 | Tampering | redis.get<Member[]>('members') | accept | KVへの書き込みは seed スクリプト経由のみ（Phase 4 スコープ）。管理画面認証は Phase 6 で実装 |
| T-04-03 | Denial of Service | Redis.fromEnv() | accept | Upstash 無料枠の rate limit (10,000 req/day) は開発・小規模運用では問題なし |
</threat_model>

<verification>
1. `npm install` が成功し、node_modules/@upstash/redis が存在する
2. src/lib/types.ts の Member 型に feedUrl が存在せず、substackId/teamId/addedAt が存在する
3. src/lib/redis.ts が存在し、`export const redis = Redis.fromEnv()` を含む
4. src/lib/kvMembers.ts が存在し、`getMembers()` が `return members ?? []` でフォールバックしている
5. `npx tsc --noEmit` が（fetchFeed.ts/page.tsx の参照エラーを除いて）型エラーなしで通る
</verification>

<success_criteria>
- @upstash/redis がインストール済みで import 可能
- Member 型が新スキーマ（name, substackId, teamId, addedAt）に更新されている
- getMembers() 関数が KV から Member[] を返す準備ができている
- KV 未シード時は空配列を返す（null safety）
</success_criteria>

<output>
完了後、`.planning/phases/04-kv-data-layer/04-01-SUMMARY.md` を作成する。
</output>
