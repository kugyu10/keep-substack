---
phase: 4
plan: "04-02"
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - src/lib/fetchFeed.ts
  - src/app/page.tsx
  - scripts/seed-kv.ts
autonomous: false
requirements:
  - KV-01
  - KV-02

user_setup:
  - service: upstash-redis
    why: "KVデータストアとして使用。アプリ起動・シードスクリプト実行に必須"
    env_vars:
      - name: UPSTASH_REDIS_REST_URL
        source: "Upstash Console (https://console.upstash.com) → データベース選択 → REST API → Endpoint"
      - name: UPSTASH_REDIS_REST_TOKEN
        source: "Upstash Console → データベース選択 → REST API → Read/Write Token"
    dashboard_config:
      - task: "Upstash Console でデータベースを作成（無料枠: 10,000 req/day）"
        location: "https://console.upstash.com → Create Database"
      - task: "Vercel ダッシュボードで環境変数を Build + Function + Preview + Production 全スコープに設定"
        location: "Vercel Project → Settings → Environment Variables"

must_haves:
  truths:
    - "page.tsx に members.json の import が存在しない"
    - "page.tsx が getMembers() を呼び出している"
    - "page.tsx に force-static が存在しない"
    - "fetchFeed.ts が member.feedUrl ではなく member.substackId から feedUrl を動的生成している"
    - "scripts/seed-kv.ts が存在し npx tsx scripts/seed-kv.ts で実行できる"
    - "シードスクリプトが .env.local を読み込み members.json の全メンバーを KV に登録できる"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "KV 経由でメンバー取得する Server Component"
      contains: "getMembers"
    - path: "src/lib/fetchFeed.ts"
      provides: "substackId から feedUrl を動的生成するフィード取得関数"
      contains: "substackId}.substack.com/feed"
    - path: "scripts/seed-kv.ts"
      provides: "members.json → KV 移行スクリプト"
      contains: "redis.set"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/lib/kvMembers.ts"
      via: "import { getMembers } from '@/lib/kvMembers'"
      pattern: "getMembers"
    - from: "src/lib/fetchFeed.ts"
      to: "Substack RSS"
      via: "https://${member.substackId}.substack.com/feed"
      pattern: "substackId.*substack\\.com/feed"
    - from: "scripts/seed-kv.ts"
      to: "Upstash KV"
      via: "redis.set('members', members)"
      pattern: "redis\\.set.*members"
---

<objective>
アプリを KV データ取得に切り替え、移行スクリプトを作成する。fetchFeed.ts を substackId 動的生成に更新し、page.tsx を getMembers() 呼び出しに切り替え、seed スクリプトを作成する。

Purpose: アプリが members.json ではなく Upstash KV からメンバーデータを取得できる状態にし、既存データの移行手段を提供する。
Output: fetchFeed.ts（更新）、page.tsx（更新）、scripts/seed-kv.ts（新設）
</objective>

<execution_context>
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-kv-data-layer/04-CONTEXT.md
@.planning/phases/04-kv-data-layer/04-RESEARCH.md
@.planning/phases/04-kv-data-layer/04-01-SUMMARY.md

<interfaces>
<!-- 04-01 で確立した新スキーマと関数シグネチャ -->

src/lib/types.ts（04-01 で更新済み）:
```typescript
export type Member = {
  name: string
  substackId: string
  teamId: string
  addedAt: string  // ISO 8601
}
```

src/lib/kvMembers.ts（04-01 で新設済み）:
```typescript
export async function getMembers(): Promise<Member[]>
```

src/lib/fetchFeed.ts の現在の feedUrl 参照箇所（変更対象）:
```typescript
// 変更前
members.map((member) => fetchWithRetry(member.feedUrl))

// 変更後（D-08）
members.map((member) =>
  fetchWithRetry(`https://${member.substackId}.substack.com/feed`)
)
```

src/app/page.tsx の現在の構造（変更対象）:
```typescript
// 変更前（削除する行）
export const dynamic = 'force-static'
import members from '@/data/members.json'
// ...
const results = await fetchAllFeedsCached(members as Member[])
// ...
const substackId = extractSubstackId(member.feedUrl)
if (!substackId) return null
// ...
<Link key={member.feedUrl} href={`/member/${substackId}`}>

// 変更後（D-05, D-08, D-10, Pattern 6, Pattern 7）
import { getMembers } from '@/lib/kvMembers'
// ...
const members = await getMembers()
const results = await fetchAllFeedsCached(members)
// ...
<Link key={member.substackId} href={`/member/${member.substackId}`}>
```

src/data/members.json の内容（シードスクリプトの入力）:
```json
[
  { "name": "キャリア孔明", "feedUrl": "https://careerkoumei.substack.com/feed" },
  { "name": "うおじゅん（オタクプロデューサー）", "feedUrl": "https://uojun.substack.com/feed" }
]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: fetchFeed.ts の feedUrl 動的生成への切り替え</name>
  <files>src/lib/fetchFeed.ts</files>
  <read_first>
    - src/lib/fetchFeed.ts（変更前の全コードを把握してから編集する）
    - src/lib/types.ts（新 Member 型の substackId フィールドを確認する）
  </read_first>
  <action>
`src/lib/fetchFeed.ts` の `fetchAllFeeds` 関数内で `member.feedUrl` を参照している箇所を `member.substackId` 動的生成に変更する（D-08）。

変更対象は `fetchAllFeeds` 関数内の以下の行：
```typescript
// 変更前
members.map((member) => fetchWithRetry(member.feedUrl))

// 変更後
members.map((member) =>
  fetchWithRetry(`https://${member.substackId}.substack.com/feed`)
)
```

関数シグネチャ `fetchAllFeedsCached(members: Member[])` は変更しない（D-06）。
  </action>
  <verify>
    <automated>cd /Users/kugyu10/work/keep-substack && npx tsc --noEmit 2>&1 | grep "fetchFeed" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/fetchFeed.ts に `substackId}.substack.com/feed` という文字列が存在する
    - src/lib/fetchFeed.ts に `member.feedUrl` が存在しない（grep: `grep -c "member\.feedUrl" src/lib/fetchFeed.ts` が 0）
    - `npx tsc --noEmit` で fetchFeed.ts に関するエラーが出ない
  </acceptance_criteria>
  <done>fetchFeed.ts が substackId から feedUrl を動的生成しており、TypeScript エラーがない</done>
</task>

<task type="auto">
  <name>Task 2: page.tsx の KV 取得への切り替え</name>
  <files>src/app/page.tsx</files>
  <read_first>
    - src/app/page.tsx（変更前の全コードを把握してから編集する）
    - src/lib/kvMembers.ts（getMembers の import パスを確認する）
  </read_first>
  <action>
`src/app/page.tsx` を以下の変更点に従って更新する：

**削除する内容:**
1. `export const dynamic = 'force-static'` — KV アクセスはランタイム I/O のため削除（Pattern 6、Pitfall 4 対策）
2. `import members from '@/data/members.json'` — D-05: members.json の import を削除
3. `import { buildArticleMap, extractSubstackId } from '@/lib/calendarUtils'` の `extractSubstackId` の import（`buildArticleMap` は残す）
4. `const substackId = extractSubstackId(member.feedUrl)` の行
5. `if (!substackId) return null` の行

**追加する内容:**
1. `import { getMembers } from '@/lib/kvMembers'` を追加
2. `const members = await getMembers()` を `Home()` 関数の先頭に追加
3. `fetchAllFeedsCached` の引数を `members as Member[]` から `members` に変更（型キャスト不要）

**変更する内容:**
1. `<Link key={member.feedUrl} href={`/member/${substackId}`}>` を `<Link key={member.substackId} href={`/member/${member.substackId}`}>` に変更（Pattern 7）

変更後の page.tsx 全体イメージ:
```typescript
import Link from 'next/link'
import { getMembers } from '@/lib/kvMembers'
import { fetchAllFeedsCached } from '@/lib/fetchFeed'
import { buildArticleMap } from '@/lib/calendarUtils'
import MiniCalendar from '@/components/MiniCalendar'

export default async function Home() {
  const members = await getMembers()
  const results = await fetchAllFeedsCached(members)

  return (
    <main className="max-w-6xl mx-auto p-6">
      <h1 className="text-2xl font-bold mb-6">Keep Substack</h1>
      <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">
        {results.map(({ member, items }) => {
          const map = buildArticleMap(items)
          const articleMapEntries = Array.from(map.entries())
          return (
            <Link key={member.substackId} href={`/member/${member.substackId}`}>
              <MiniCalendar
                memberName={member.name}
                articleMap={articleMapEntries}
              />
            </Link>
          )
        })}
      </div>
    </main>
  )
}
```

注意: `import type { Member } from '@/lib/types'` も不要になる場合は削除する（getMembers の戻り値が型推論されるため）。
  </action>
  <verify>
    <automated>cd /Users/kugyu10/work/keep-substack && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/app/page.tsx に `force-static` が存在しない（grep: `grep -c "force-static" src/app/page.tsx` が 0）
    - src/app/page.tsx に `members.json` の import が存在しない（grep: `grep -c "members.json" src/app/page.tsx` が 0）
    - src/app/page.tsx に `getMembers` の import が存在する（grep: `grep -c "getMembers" src/app/page.tsx` が 1 以上）
    - src/app/page.tsx に `member.substackId` が存在する（grep: `grep -c "member\.substackId" src/app/page.tsx` が 1 以上）
    - src/app/page.tsx に `extractSubstackId` が存在しない（grep: `grep -c "extractSubstackId" src/app/page.tsx` が 0）
    - `npx tsc --noEmit` がエラーゼロで完了する
    - `npm run build` が成功する（ローカルに .env.local が設定されている場合）
  </acceptance_criteria>
  <done>page.tsx が getMembers() 経由で KV からメンバーを取得し、TypeScript ビルドが通る</done>
</task>

<task type="auto">
  <name>Task 3: シードスクリプト（scripts/seed-kv.ts）の作成</name>
  <files>scripts/seed-kv.ts</files>
  <read_first>
    - src/data/members.json（移行元データのスキーマを確認する）
    - src/lib/types.ts（新 Member 型のフィールドを確認する）
  </read_first>
  <action>
`scripts/` ディレクトリを作成し、`scripts/seed-kv.ts` を以下の内容で作成する（D-09）：

```typescript
// D-09: 一度だけ実行する移行スクリプト。npx tsx scripts/seed-kv.ts で実行。
// Pitfall 2 対策: dotenv は .env.local を明示指定する
import * as dotenv from 'dotenv'
dotenv.config({ path: '.env.local' })

import { Redis } from '@upstash/redis'
import membersJson from '../src/data/members.json'

const redis = Redis.fromEnv()

// 旧スキーマ: { name: string, feedUrl: string }
// 新スキーマ (D-03): { name: string, substackId: string, teamId: string, addedAt: string }
function migrate(old: { name: string; feedUrl: string }) {
  const m = old.feedUrl.match(/^https?:\/\/([^.]+)\.substack\.com/)
  if (!m) throw new Error(`feedUrl から substackId を抽出できません: ${old.feedUrl}`)
  return {
    name: old.name,
    substackId: m[1],
    teamId: 'default',
    addedAt: new Date().toISOString(),
  }
}

async function main() {
  const members = membersJson.map(migrate)
  // D-01: 単一キー 'members' に Member 配列を格納
  // D-02: @upstash/redis の自動シリアライズ（JSON.stringify は不要）
  await redis.set('members', members)
  console.log(`Seeded ${members.length} members:`)
  members.forEach((m) => console.log(`  - ${m.name} (${m.substackId})`))
}

main().catch((err) => {
  console.error('Seed failed:', err)
  process.exit(1)
})
```

tsconfig.json に `resolveJsonModule: true` がない場合は追加が必要になる可能性があるが、Next.js のデフォルト tsconfig には含まれているため通常は不要。

**実行前の前提条件（ユーザーが設定する必要がある）:**
1. `.env.local` に `UPSTASH_REDIS_REST_URL` と `UPSTASH_REDIS_REST_TOKEN` が設定されている
2. Upstash Console でデータベースが作成済みである

**実行コマンド:**
```bash
npx tsx scripts/seed-kv.ts
```

期待する出力:
```
Seeded 2 members:
  - キャリア孔明 (careerkoumei)
  - うおじゅん（オタクプロデューサー） (uojun)
```
  </action>
  <verify>
    <automated>cd /Users/kugyu10/work/keep-substack && test -f scripts/seed-kv.ts && echo "FILE EXISTS" && grep -c "redis.set" scripts/seed-kv.ts</automated>
  </verify>
  <acceptance_criteria>
    - scripts/seed-kv.ts が存在する
    - scripts/seed-kv.ts に `dotenv.config({ path: '.env.local' })` が存在する（Pitfall 2 対策）
    - scripts/seed-kv.ts に `redis.set('members', members)` が存在する（D-01）
    - scripts/seed-kv.ts に `JSON.stringify` が存在しない（D-02: 自動シリアライズ使用）
    - scripts/seed-kv.ts に `teamId: 'default'` が存在する
    - `npx tsc --noEmit` がスクリプトの型エラーなしで通る（tsconfig の include に scripts/ が含まれない場合は別途確認）
  </acceptance_criteria>
  <done>scripts/seed-kv.ts が作成されており、環境変数設定後に npx tsx scripts/seed-kv.ts で実行できる</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    以下が自動実行済み:
    1. src/lib/fetchFeed.ts が substackId から feedUrl を動的生成するよう更新された
    2. src/app/page.tsx が getMembers() を使い KV からメンバーを取得するよう切り替えられた
    3. scripts/seed-kv.ts が作成された

    実行が必要な手動ステップ:
    1. Upstash Console (https://console.upstash.com) でデータベースを作成する
    2. .env.local に UPSTASH_REDIS_REST_URL と UPSTASH_REDIS_REST_TOKEN を追加する
    3. npx tsx scripts/seed-kv.ts を実行してデータを移行する
    4. npm run dev で動作確認する
  </what-built>
  <how-to-verify>
    **Step 1: 環境変数の設定**
    `.env.local` に以下を追加（Upstash Console の REST API タブから取得）:
    ```
    UPSTASH_REDIS_REST_URL=https://your-db.upstash.io
    UPSTASH_REDIS_REST_TOKEN=your-token
    ```

    **Step 2: シードスクリプトの実行**
    ```bash
    npx tsx scripts/seed-kv.ts
    ```
    期待する出力:
    ```
    Seeded 2 members:
      - キャリア孔明 (careerkoumei)
      - うおじゅん（オタクプロデューサー） (uojun)
    ```

    **Step 3: 開発サーバーで動作確認**
    ```bash
    npm run dev
    ```
    http://localhost:3000 を開いて:
    - [ ] 「Keep Substack」ページが表示される
    - [ ] キャリア孔明のカレンダーが表示される
    - [ ] うおじゅんのカレンダーが表示される
    - [ ] 各カードをクリックすると /member/{substackId} に遷移する

    **Step 4: KV データ確認（任意）**
    Upstash Console の Data Browser で `members` キーの値を確認し、
    `[{ "name": "キャリア孔明", "substackId": "careerkoumei", "teamId": "default", "addedAt": "..." }, ...]`
    の形式で保存されていることを確認する。

    **Step 5: TypeScript ビルド確認**
    ```bash
    npx tsc --noEmit
    npm run build
    ```
    エラーなしで完了すること。
  </how-to-verify>
  <resume-signal>
    動作確認が完了したら "approved" と入力してください。
    問題があれば発生したエラーを貼り付けてください（例: "UPSTASH_REDIS_REST_URL not set" など）。
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scripts/seed-kv.ts → Upstash KV REST API | シードスクリプトは開発者ローカルで実行。トークンは .env.local に格納 |
| page.tsx (Server Component) → kvMembers.ts | KV から取得したデータを UI に渡す。XSS は React のエスケープで対応 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Information Disclosure | scripts/seed-kv.ts | mitigate | `.env.local` を .gitignore に含める。スクリプト内にトークンをハードコードしない（dotenv 経由で読む） |
| T-04-05 | Tampering | redis.set('members', members) in seed-kv.ts | accept | シードスクリプトはローカルで一度だけ実行。本番の書き込みは Phase 6 の管理画面で実装 |
| T-04-06 | Spoofing | page.tsx の getMembers() | accept | サーバーサイドのみで実行。クライアントへのトークン露出なし |
</threat_model>

<verification>
1. `npx tsc --noEmit` がエラーゼロで完了する
2. `npm run build` が成功する（.env.local 設定後）
3. `npx tsx scripts/seed-kv.ts` が "Seeded N members" を出力する
4. `npm run dev` で http://localhost:3000 にアクセスするとメンバーのカレンダーが表示される
5. page.tsx に `force-static` が存在しない
6. コードベースに `feedUrl` のハードコードが存在しない（grep: `grep -r "feedUrl" src/` が空）
</verification>

<success_criteria>
- アプリが @upstash/redis 経由でメンバー一覧を取得し、ページが正常にレンダリングされる（KV-01）
- npx tsx scripts/seed-kv.ts を実行すると members.json の全メンバーが Upstash KV に登録される（KV-02）
- KV から取得したメンバーデータに name, substackId, teamId, addedAt が含まれる
- feedUrl のハードコードがコードベースに存在しない（D-08）
- src/data/members.json は手動削除の準備ができている（D-04）
</success_criteria>

<output>
完了後、`.planning/phases/04-kv-data-layer/04-02-SUMMARY.md` を作成する。
</output>
