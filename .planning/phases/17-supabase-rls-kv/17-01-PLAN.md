# Plan 17-01: PostgreSQL DDL + RLS SQL スクリプト

**Phase:** 17 — Supabaseスキーマ + RLS設定 + KVデータ移行
**Plan:** 17-01
**Wave:** 1
**Status:** draft

## Objective

Supabase管理画面のSQL Editorに貼り付けて実行するためのDDL SQLファイルを作成する。
4テーブル（members / teams / member_teams / articles）のCREATE TABLE文とRLSポリシーを定義する。

## Requirements Addressed

- MIGRATE-01: membersテーブル・articlesテーブルをSupabase PostgreSQLで定義し、RLSポリシーを設定できる

## Pre-implementation Notes

- このプランはアプリコードを変更しない。SQLファイルを1つ作成するだけ
- Supabase管理画面 > SQL Editor に貼り付けて実行する手順を前提とする
- UUIDはPostgreSQL組み込みの `gen_random_uuid()` を使用する（拡張不要）
- articlesテーブルのsubstack_id列はmembers.substack_idへのFK（ON DELETE CASCADE）
- thumbnailはPhase 18でFeedItem全フィールドを検討してから追加（DEFERRED）

## Success Criteria

1. `supabase/schema.sql` が存在し、members / teams / member_teams / articles の4テーブルのCREATE TABLE文が含まれること
2. 全テーブルに `ENABLE ROW LEVEL SECURITY` が設定されていること
3. anon keyでのSELECT全件許可ポリシーが4テーブルに存在すること
4. INSERT/UPDATE/DELETEはservice_roleのみ許可するポリシーが存在すること
5. `supabase/schema.sql` の先頭に実行手順コメントが記載されていること

## Tasks

### Task 1: supabase/schema.sql を作成する

**File:** `supabase/schema.sql`（新規作成）

Supabase管理画面のSQL Editorで実行するDDL + RLSポリシーSQL。

```sql
-- Keep Substack — Supabase初期スキーマ
-- 実行手順: Supabase管理画面 > SQL Editor に全文貼り付けて「Run」
-- 冪等性: 既にテーブルが存在する場合は IF NOT EXISTS でスキップ

-- ============================================================
-- 1. テーブル定義
-- ============================================================

CREATE TABLE IF NOT EXISTS members (
  id          UUID        PRIMARY KEY DEFAULT gen_random_uuid(),
  name        TEXT        NOT NULL,
  substack_id TEXT        UNIQUE NOT NULL,
  image_url   TEXT,
  added_at    TIMESTAMPTZ NOT NULL DEFAULT now()
);

CREATE TABLE IF NOT EXISTS teams (
  id   UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  name TEXT UNIQUE NOT NULL
);

CREATE TABLE IF NOT EXISTS member_teams (
  member_id UUID REFERENCES members(id) ON DELETE CASCADE,
  team_id   UUID REFERENCES teams(id)   ON DELETE CASCADE,
  PRIMARY KEY (member_id, team_id)
);

CREATE TABLE IF NOT EXISTS articles (
  id          UUID        PRIMARY KEY DEFAULT gen_random_uuid(),
  substack_id TEXT        NOT NULL REFERENCES members(substack_id) ON DELETE CASCADE,
  title       TEXT,
  link        TEXT        UNIQUE NOT NULL,
  pub_date    TIMESTAMPTZ
);

-- ============================================================
-- 2. Row Level Security
-- ============================================================

ALTER TABLE members      ENABLE ROW LEVEL SECURITY;
ALTER TABLE teams        ENABLE ROW LEVEL SECURITY;
ALTER TABLE member_teams ENABLE ROW LEVEL SECURITY;
ALTER TABLE articles     ENABLE ROW LEVEL SECURITY;

-- anon / authenticated: SELECT 全件許可（公開データ）
CREATE POLICY "public select members"
  ON members FOR SELECT USING (true);

CREATE POLICY "public select teams"
  ON teams FOR SELECT USING (true);

CREATE POLICY "public select member_teams"
  ON member_teams FOR SELECT USING (true);

CREATE POLICY "public select articles"
  ON articles FOR SELECT USING (true);

-- service_role: INSERT / UPDATE / DELETE 許可（Cron・管理スクリプト用）
-- service_role はデフォルトでRLSをバイパスするため追加ポリシー不要
-- （Supabase の service_role は BYPASSRLS 権限を持つ）
```

---

## Verification

- [ ] `supabase/schema.sql` が存在すること
- [ ] ファイル内に `CREATE TABLE IF NOT EXISTS members` が含まれること
- [ ] ファイル内に `CREATE TABLE IF NOT EXISTS teams` が含まれること
- [ ] ファイル内に `CREATE TABLE IF NOT EXISTS member_teams` が含まれること
- [ ] ファイル内に `CREATE TABLE IF NOT EXISTS articles` が含まれること
- [ ] ファイル内に `ENABLE ROW LEVEL SECURITY` が4件含まれること
- [ ] ファイル内に `CREATE POLICY` が4件含まれること

## Notes

- service_roleはSupabaseでBYPASSRLS権限を持つため、INSERT/UPDATE/DELETE用のポリシーは不要
- Phase 19でAuthロール追加時にこのファイルを参照して差分ポリシーを追加する
- thumbnailカラムはPhase 18のHISTORY-01実装時にALTER TABLEで追加する
