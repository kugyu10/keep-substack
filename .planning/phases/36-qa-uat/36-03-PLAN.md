---
phase: 36-qa-uat
plan: 03
type: execute
wave: 2
depends_on: ["36-01"]
files_modified:
  - .planning/phases/27-substack-handle-db/27-HUMAN-UAT.md
  - .planning/phases/27-substack-handle-db/27-VERIFICATION.md
autonomous: false
requirements: [QA-01, QA-02]
must_haves:
  truths:
    - "本番 Supabase の Site URL / Redirect URLs が https://keep-substack.com を許可していることが確認されている（D-06 landmine 解消）"
    - "本番（https://keep-substack.com）で Phase 27 の6シナリオ #1-#6 すべてが開発者自身のアカウントで一巡され、6 件それぞれに本番の pass/fail 結果が記録されている（SC#1 充足 — #2-#5 も TEST 自動 spec ではなく本番結果を個別記録）"
    - "本プランは D-01 ハイブリッド方式の「手動レイヤー」を担う — 視覚目視・実 Magic Link メール往復が必須の項目（D-02 の手動必須目安: 27 シナリオ6 Magic Link メール往復 + 本番ブラウザ目視、D-03 で実メール往復は強制自動化しない）を開発者が本番で手動実行する"
    - "検証で変更した本番データ（substack_handle 等）が元に戻されている（他メンバー非汚染、D-04）"
    - "27-HUMAN-UAT.md と 27-VERIFICATION.md が実結果で in-place 更新されている（D-09）"
  artifacts:
    - path: ".planning/phases/27-substack-handle-db/27-HUMAN-UAT.md"
      provides: "Phase 27 UAT 6 シナリオの実行結果（#1-#6 すべてに本番 result 行 + Summary カウント + status）"
      contains: "[pass]"
    - path: ".planning/phases/27-substack-handle-db/27-VERIFICATION.md"
      provides: "status: human_needed → verified + 各 human_verification の本番 pass 記録"
      contains: "status: verified"
  key_links:
    - from: "本番 https://keep-substack.com /my・/member・Magic Link"
      to: "27-HUMAN-UAT.md / 27-VERIFICATION.md"
      via: "手動実行結果を in-place 更新"
      pattern: "result: \\[pass\\]"
---

<objective>
SC#1 が要求する「本番環境で」の Phase 27 UAT 6 シナリオを、本番（https://keep-substack.com）で開発者自身のアカウント（メンバー行）に対して手動実行し（D-04）、結果を 27-HUMAN-UAT.md / 27-VERIFICATION.md に in-place 記録する（D-09）。これは D-01（ハイブリッド方式）の「手動レイヤー」であり、D-02 の手動必須目安（実 Magic Link メール往復・本番ブラウザ目視）と D-03（実メール往復のような困難項目は強制自動化しない）に対応する。⚠ SC#1 の「本番環境で」は #1/#6 だけでなく **#1-#6 の全6シナリオ** に及ぶ。#2-#5 は TEST project の自動 spec（36-01）が durable な追加レイヤーとして存在するが、SC#1 充足は本番手動の個別結果でのみ達成される — 自動 spec の green を #2-#5 の本番結果の代替にしてはならない（D-05）。⚠ 実行前に本番 Supabase の Redirect URL が新ドメインを許可しているか必ず確認する（D-06 landmine）— これを飛ばすと Magic Link シナリオ(#6)は確実に失敗する。

Purpose: QA-01（本番 UAT）+ QA-02 の本番側充足。失敗時は黙って通さず新規バグ化（D-10）。
Output: 27-HUMAN-UAT.md（#1-#6 すべての本番 result 行 + Summary + status）と 27-VERIFICATION.md（status + 本番 pass 記録）の in-place 更新。src/ は変更しない。
</objective>

<execution_context>
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-qa-uat/36-RESEARCH.md

<interfaces>
本番リファレンス:
  本番 Supabase project = xolhjcngrwwwqtklmoyk
  本番 URL = https://keep-substack.com（旧 keep-substack.vercel.app から移行済み、D-06）
  Magic Link login URL = https://keep-substack.com/login-51cf21389c56/?handle=hoge

DOM/振る舞い契約（本番も同一実装）:
  @handle は設定後 read-only（MyProfileForm.tsx:52、Drift D-A）。本番で #5「null に戻す」が
  UI 上できない場合は SQL Editor で UPDATE members SET substack_handle=NULL WHERE id='<自分のid>'。

更新対象の正典ファイル形（36-PATTERNS.md §In-Place .md 更新の正典フィールド形）:
  27-HUMAN-UAT.md: frontmatter status / 各 ### N の result 行 [pending]→[pass]/[fail] / Summary カウント / ## Current Test
  27-VERIFICATION.md: frontmatter status: human_needed → verified / 本文 ### Human Verification Required に pass 記録
</interfaces>

<context_compliance>
本プランが実装する CONTEXT.md 決定（traceability）:
- D-01: ハイブリッド方式の「手動レイヤー」— 視覚目視・実メール往復が必須の項目を開発者が本番で手動実行（自動レイヤーは 36-01）。
- D-02: 手動必須の目安に対応 — 27 シナリオ6（実 Magic Link メール往復）+ 本番ブラウザ目視を手動実行。
- D-03: 実 Magic Link メール往復のような困難項目は強制自動化せず手動で実施。
- D-04/D-05/D-06/D-09/D-10: 本番データ衛生 / 本番充足 / Redirect URL 前提 / in-place 記録 / 失敗時バグ化（本文各タスク参照）。
</context_compliance>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: 本番 Redirect URL 事前チェック（D-06 landmine）</name>
  <read_first>
    - 36-RESEARCH.md §Manual Production UAT Runbook §0（事前チェック手順）
    - MEMORY: project-prod-domain.md（本番は keep-substack.com に移行済み、Magic Link / Redirect URL に影響）
  </read_first>
  <what-built>
    自動化済み部分はない。これは Magic Link シナリオ(#6)の前提となる本番 Supabase 設定の人間目視確認。
  </what-built>
  <action>
    開発者が Supabase Dashboard（本番 project xolhjcngrwwwqtklmoyk）→ Authentication → URL Configuration を開き、Site URL が https://keep-substack.com であること、Redirect URLs に https://keep-substack.com/**（最低限 https://keep-substack.com/auth/callback）が含まれることを目視確認する。旧 keep-substack.vercel.app しか無ければ新ドメインを追加する。確認結果を Task 3 の記録に前提として渡す。Claude が自動化できる CLI/API は無い（Dashboard の URL 設定は人間操作）。
  </action>
  <how-to-verify>
    1. Supabase Dashboard で本番 project（xolhjcngrwwwqtklmoyk）を開く。
    2. Authentication → URL Configuration を開く。
    3. Site URL が `https://keep-substack.com` になっているか確認。
    4. Redirect URLs に `https://keep-substack.com/**`（最低限 `https://keep-substack.com/auth/callback`）が含まれているか確認。
    5. 旧 `keep-substack.vercel.app` しか無い場合は新ドメインを追加してから次へ進む。
  </how-to-verify>
  <acceptance_criteria>
    - 本番 Site URL = https://keep-substack.com を確認した
    - Redirect URLs に https://keep-substack.com/** （または /auth/callback）が含まれることを確認・必要なら追加した
    - この確認結果（OK / 追加した）を 27-VERIFICATION.md の Magic Link 項目記録に前提として残す
  </acceptance_criteria>
  <resume-signal>"確認OK" または追加した内容を報告</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 本番 6 シナリオ手動実行（QA-01, 27 #1-#6 すべて本番で）</name>
  <read_first>
    - 36-RESEARCH.md §Manual Production UAT Runbook §1-#1 / §2-#2〜#5 / §3-#6
    - .planning/phases/27-substack-handle-db/27-HUMAN-UAT.md（Tests #1-#6 の expected）
    - src/app/my/MyProfileForm.tsx:52-71（handle 設定後 read-only — #5 で null に戻せない可能性）
  </read_first>
  <what-built>
    検証対象は本番にデプロイ済みの既存実装。新規構築物なし。開発者が本番ブラウザ + 自分のアカウントで6シナリオを一巡する。
  </what-built>
  <action>
    開発者が本番 https://keep-substack.com で自分のアカウントに対し 27 の6シナリオ（#1 SQL migration 確認 / #2 @handle input 描画 / #3 "hoge"→"@hoge" 保存往復 read-only 表示 / #4 /member プロフィールリンクあり / #5 handle NULL でリンクなし / #6 Magic Link ?handle=hoge 伝播）を順に手動実行する。⚠ #2-#5 は TEST project の自動 spec（36-01）が並行して存在するが、SC#1「本番環境で」の充足には #2,#3,#4,#5 を **本番ブラウザ + 本番 DB（自分の行）で一つひとつ実際に通し、各シナリオ個別に pass/fail を出す** こと。自動 spec の green を #2-#5 の本番結果の代わりにしない（D-05）。#5 で UI から NULL に戻せない場合は SQL Editor で UPDATE members SET substack_handle=NULL WHERE id='<自分のid>'。#6 は Task 1 の Redirect URL 確認後に実行。検証後、変更した本番データ（自分の substack_handle 等）を元の値に戻す（D-04、他メンバー非汚染）。各シナリオ #1-#6 の本番 pass/fail を Task 3 の記録に渡す。実メール往復・本番ブラウザ目視は自動化不可（D-03）。
  </action>
  <how-to-verify>
    #1（SQL migration）: 本番 SQL Editor で `SELECT column_name FROM information_schema.columns WHERE table_name='members' AND column_name='substack_handle';` → 1 行返る。`SELECT substack_handle FROM members LIMIT 5;` で既存行が NULL を確認。
    #2（input 描画）: https://keep-substack.com/my にログイン → @handle 入力欄が描画される（本番ブラウザで目視）。
    #3（保存往復）: 本番で "hoge" を保存 → reload で "@hoge" が read-only 表示（設定後 read-only、Drift D-A）。
    #4（リンクあり）: 本番 /member/[自分の publicationId] で名前/アイコンが substack.com/@hoge を新タブで開くリンク。
    #5（リンクなし）: handle を NULL に戻す（UI で戻せなければ SQL Editor で UPDATE members SET substack_handle=NULL WHERE id='<自分のid>'）→ 本番 /member でリンクが消える。
    #6（Magic Link）: Task 1 完了後、https://keep-substack.com/login-51cf21389c56/?handle=hoge でメール送信 → Magic Link クリック → /my?handle=hoge 着地 → DB handle が NULL のとき input に "hoge" pre-fill。
    ⚠ 検証後、変更した本番データ（自分の substack_handle 等）を元に戻す（D-04）。
  </how-to-verify>
  <acceptance_criteria>
    - #1-#6 の各シナリオを本番（https://keep-substack.com）で実行し pass / fail を判定した
    - ⚠ #2, #3, #4, #5 のそれぞれについて、TEST project 自動 spec ではなく **本番（https://keep-substack.com）での pass/fail 結果** を個別に得た（4 シナリオ分の本番結果が揃っている。#1/#6 の本番結果だけでは不可）
    - #5 の handle NULL 復元を含め、変更した本番データを元に戻した（他メンバーのデータは触れていない、D-04）
    - 失敗したシナリオがあれば fail として記録し Task 4 でバグ化する（黙って通さない、D-10）
  </acceptance_criteria>
  <resume-signal>各シナリオの本番 pass/fail を #1-#6 個別に報告（例: "本番 #1 pass, #2 pass, #3 pass, #4 pass, #5 pass, #6 pass"）</resume-signal>
</task>

<task type="auto">
  <name>Task 3: 27-HUMAN-UAT.md / 27-VERIFICATION.md を実結果で in-place 更新（D-09）</name>
  <files>.planning/phases/27-substack-handle-db/27-HUMAN-UAT.md, .planning/phases/27-substack-handle-db/27-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/27-substack-handle-db/27-HUMAN-UAT.md（frontmatter + Tests #1-6 + Summary の現状形）
    - .planning/phases/27-substack-handle-db/27-VERIFICATION.md（frontmatter status + human_verification[] 6 件）
    - 36-PATTERNS.md §In-Place .md 更新の正典フィールド形（27 の編集仕様）
    - 36-01-SUMMARY.md（27 #2-#5 の自動 spec pass 結果 — 本番手動 pass と併記する補助証拠）
  </read_first>
  <action>
    27-HUMAN-UAT.md: frontmatter の `updated:` を実行日（2026-06-08 系）に、`status: partial` を全 pass なら完了相当（passed）に更新。各 `### N.` の `result: [pending]` を Task 2 の本番実結果に応じ `[pass]`/`[fail]` へ書き換える。⚠ #2,#3,#4,#5 の result 行には **本番（https://keep-substack.com）の pass/fail を主たる結果として明記** し、TEST project 自動 spec（36-01）の green は補助証拠として併記する（自動 spec を本番結果の代替にしない、D-05）。#1（SQL本番）/#6（Magic Link）も本番 manual-production の結果を記録。結果として #1-#6 の6行すべてに本番由来の result が入る。Summary ブロックの `total: 6 / passed / pending` カウントを実結果に更新。`## Current Test` の `[awaiting human testing]` を完了記述に。
    27-VERIFICATION.md: frontmatter `status: human_needed` を `verified` に（全 pass の場合。fail があれば human_needed のまま + Task 4 のバグ参照を記録）。本文 `### Human Verification Required`（#1〜#6）に各項目の pass 手段を追記する。⚠ #2-#5 は「本番手動 pass（主）+ automate spec 36-01（補助）」、#1 は本番 SQL、#6 は本番 Magic Link（Task 1 の Redirect URL 前提）として記録 — #2-#5 を automate のみで close しない。Drift D-A（handle 設定後 read-only）に検証が追従した旨を #3 記録に注記。
    src/ は変更しない。Phase 36 側に統合サマリーは作らない（D-09）。
  </action>
  <verify>
    <automated>grep -c "result: \[pass\]\|result: \[fail\]" .planning/phases/27-substack-handle-db/27-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - 27-HUMAN-UAT.md の Tests #1-#6 すべてに result: [pass] または [fail] が入っている（grep で [pending] が 0 件: `grep -c "result: \[pending\]" 27-HUMAN-UAT.md` == 0）
    - ⚠ #2, #3, #4, #5 それぞれの result 行に本番（keep-substack.com）の pass/fail が主結果として明記されている（automate spec の green だけで close されていない、D-05）
    - 27-HUMAN-UAT.md Summary の passed/pending カウントが実結果と整合
    - 27-VERIFICATION.md frontmatter が `status: verified`（全 pass 時）または fail の場合バグ参照付きで human_needed
    - 本文 Human Verification Required の #1-#6 に各項目の検証手段が追記され、#2-#5 は「本番手動 pass（主）+ automate 36-01（補助）」と読み取れる
    - #3 記録に Drift D-A（設定後 read-only）追従の注記がある
  </acceptance_criteria>
  <done>Phase 27 の正典 .md が実結果で in-place 更新され、#1-#6 すべての本番結果で QA-01/QA-02 の本番充足が記録されている</done>
</task>

<task type="auto">
  <name>Task 4: 失敗シナリオがあれば新規バグ化（D-10、条件付き）</name>
  <files>.planning/phases/27-substack-handle-db/27-VERIFICATION.md</files>
  <read_first>
    - 36-CONTEXT.md D-10（失敗時は /gsd:debug または新規フェーズで capture、黙って通さない）
    - Task 2/3 の結果（fail があるか）
  </read_first>
  <action>
    Task 2 で fail だったシナリオが 1 つでもあれば、その内容（再現手順・期待値・実際値）を 27-VERIFICATION.md の Gaps セクションに明記し、`/gsd:debug` セッションまたは新規フェーズとして capture すべき旨を記録する（修正そのものは本フェーズスコープ外 — Deferred）。fail が 0 件なら本タスクは「全 pass のため bug capture 不要」と記録して終了。既知の logout redirect bug 等に再遭遇した場合も参照のみ記録し修正しない（別トラック）。これにより v1.8 go/no-go 判定（D-11）に必要な情報が揃う。
  </action>
  <verify>
    <automated>test -f .planning/phases/27-substack-handle-db/27-VERIFICATION.md && grep -q "Gaps\|全 pass" .planning/phases/27-substack-handle-db/27-VERIFICATION.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - fail があれば 27-VERIFICATION.md に再現手順・期待/実際値・バグ化方針（/gsd:debug or 新規フェーズ）が記録されている
    - fail が 0 件なら「全 pass・bug capture 不要」が記録されている
    - 失敗が黙って pass 扱いになっていない（D-10）
  </acceptance_criteria>
  <done>Phase 27 検証の go/no-go 判断材料が揃っている（pass 記録 or バグ化）</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer browser → 本番 Supabase（xolhjcngrwwwqtklmoyk） | 手動 UAT が本番 DB に書き込む（開発者自身のメンバー行のみ） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-06 | Tampering | 手動本番 UAT が他メンバーのデータを汚染 | mitigate | 開発者自身のメンバー行のみ対象。検証後に変更値を元に戻す（D-04）。#5 で SQL Editor リセットは自分の id に限定 |
| T-36-07 | Spoofing/I.D. | Magic Link が旧ドメインへリダイレクトし失敗/誤遷移 | mitigate | Task 1 で本番 Redirect URL に keep-substack.com が含まれることを事前確認（D-06 landmine） |
| T-36-SC | Tampering | npm/pip/cargo installs | accept | install 無し。Package Legitimacy Gate は N/A |

**注:** 検証フェーズ・src/ 不変。唯一の実在リスクは手動本番 UAT のデータ衛生（D-04）で、自分の行限定 + 値復元で緩和する。
</threat_model>

<verification>
- 本番 6 シナリオ #1-#6 を一巡し、各シナリオ個別に本番 pass/fail を判定済み（#2-#5 も本番結果あり）
- 変更した本番データを元に戻した（D-04）
- 27-HUMAN-UAT.md に [pending] が 0 件、27-VERIFICATION.md の status が更新済み
</verification>

<success_criteria>
- QA-01（本番 UAT）が SC#1 を充足（#1-#6 すべての本番手動パス完了 + 結果記録、#2-#5 を automate で代替していない）
- 27 の正典 .md が in-place 更新され、Phase 36 側に統合サマリーを作らない（D-09）
- 失敗は黙って通さずバグ化記録（D-10）
</success_criteria>

<output>
Create `.planning/phases/36-qa-uat/36-03-SUMMARY.md` when done
</output>
</content>
</invoke>
