---
phase: 36-qa-uat
plan: 02
type: execute
wave: 2
depends_on: ["36-01"]
files_modified:
  - e2e/29-mobile.spec.ts
  - e2e/29-layout-nav.spec.ts
autonomous: true
requirements: [QA-04]
must_haves:
  truths:
    - "375px viewport で CommitGrid の week-0/week-1 が hidden、最新 week-2 のみ visible（29 #1 モバイル1週縮退）"
    - "/ で各メンバー行に /member/ への Link が存在する（29 #2 構造、ピクセル整列は手動目視に委譲）"
    - "ViewTabs に /daily へのナビが存在し /daily で旧 WeeklyHeatmapGrid が描画される（29 #3、⚠ ルートは /weekly-stamp ではなく /daily）"
  artifacts:
    - path: "e2e/29-mobile.spec.ts"
      provides: "375px viewport で hidden sm:flex 縮退を検証する回帰 spec"
      contains: "toBeHidden"
    - path: "e2e/29-layout-nav.spec.ts"
      provides: "3列構造 + /daily ナビ検証 spec（anonymous project）"
      contains: "/daily"
  key_links:
    - from: "e2e/29-mobile.spec.ts"
      to: "CommitGrid.tsx の hidden sm:flex 週ブロック"
      via: "mobile-375 project の viewport で toBeHidden/toBeVisible"
      pattern: "toBeHidden"
    - from: "e2e/29-layout-nav.spec.ts"
      to: "ViewTabs.tsx / src/app/daily/page.tsx"
      via: "anonymous project で href=/daily ナビと WeeklyHeatmapGrid 描画を検証"
      pattern: "daily"
---

<objective>
Phase 29 の視覚/ナビ検証項目を Playwright で自動化する。#1 モバイル1週縮退は mobile-375 viewport project（36-01 で playwright.config.ts に追加済み）で `hidden sm:flex` の display:none を toBeHidden で検出。#2 3列レイアウトは構造（/member/ Link の存在）のみ自動化しピクセル整列は本番手動目視に委譲（Pitfall 4）。#3 は ⚠ 重大ドリフト D-C により旧 `/weekly-stamp` が現行 `/daily` に移動済みのため、`/daily` ルートと ViewTabs ナビ・WeeklyHeatmapGrid 描画を検証する。

Purpose: QA-04（Phase 29 VERIFICATION ギャップ）のうち自動化可能な振る舞いを TEST project で回帰検証可能にする。29-HUMAN-UAT.md は既 resolved(3/3) のため再実行しない（D-07）。src/ は変更しない。
Output: e2e/29-mobile.spec.ts（mobile-375 project）+ e2e/29-layout-nav.spec.ts（anonymous project）。
</objective>

<execution_context>
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-qa-uat/36-RESEARCH.md
@.planning/phases/36-qa-uat/36-PATTERNS.md

<interfaces>
<!-- DOM 契約（現行ソース直読で確認済み）。executor は探索不要。 -->

CommitGrid.tsx:118-132 — root <div className="flex flex-1 gap-2">。直下 3 週ブロック:
  week-0 :120 / week-1 :126 = "hidden sm:flex ..."（< 640px で display:none）
  week-2 :132 = "flex flex-1 ..."（常時可視）
  → 375px viewport で week-0/1 が toBeHidden、week-2 が toBeVisible。

CommitGoalRow.tsx — 各行 Col1 = <Link href="/member/...">（avatar+name）, Col2 = CommitGrid または「未コミット」, Col3 = w-10 shrink-0 achievement。
  「未コミット」行（slots.length===0）には CommitGrid が描画されない。

ViewTabs.tsx — <nav> 内に <a href="/">コミット＆ゴール</a> と <a href="/daily">デイリー</a>（⚠ Drift D-C: /weekly-stamp ではなく /daily）。

src/app/daily/page.tsx — /daily で WeeklyHeatmapGrid を描画、All タブ href="/daily" + チームタブ href="/daily?team=..."。

既存 anonymous spec の手本: e2e/admin-guard.spec.ts（storageState なし + page.goto + DOM/href assert）。
playwright.config.ts: 36-01 で mobile-375 project（viewport 375x800, 29-mobile.spec.ts）と anonymous testMatch への 29-layout-nav.spec.ts 追加が完了済み。
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 29-mobile.spec.ts — 375px モバイル1週縮退（QA-04, 29 #1）</name>
  <files>e2e/29-mobile.spec.ts</files>
  <read_first>
    - e2e/admin-guard.spec.ts（anonymous + page.goto の手本、storageState 不要）
    - src/components/CommitGrid.tsx:118-137（root div.flex.flex-1.gap-2 と 3 週ブロックの className）
    - src/components/CommitGoalRow.tsx:42-50（未コミット行には CommitGrid が描画されない点 → CommitGrid を持つ行を .first() で選ぶ）
    - 36-PATTERNS.md §29-mobile.spec.ts（375px viewport の手本コード）
  </read_first>
  <behavior>
    - mobile-375 project（viewport 375x800）で page.goto('/')
    - 最初の CommitGrid root `div.flex.flex-1.gap-2` を .first() で取得し、その直下 `> div` 週ブロックを取る
    - week-0（nth(0)）が toBeHidden、week-1（nth(1)）が toBeHidden（hidden sm:flex の display:none）
    - week-2（nth(2)）が toBeVisible（flex flex-1）
    - もし / に CommitGrid を持つ行が無い場合に備え、createTestAdmin で seed member に commit slots を持たせる独立 seed を beforeAll で行い afterAll で member_id スコープ delete（28 spec の slots と競合しないよう独立管理）。または CommitGrid を持つ行を確実に得るための前提を seed で担保する。
  </behavior>
  <action>
    e2e/admin-guard.spec.ts と同型の anonymous spec を作る（storageState 不要、/ はログイン不要）。CommitGrid root の locator は `page.locator('div.flex.flex-1.gap-2').first()`、週ブロックは `.locator('> div')`。week-0/1 を toBeHidden、week-2 を toBeVisible で assert。/ に CommitGrid を持つ行が存在するよう beforeAll で createTestAdmin により seed member（TEST.publicationId）に commit slots を 1 件投入し（admin.rpc('replace_member_commit_slots', { p_member_id, p_slots: [{day_of_week, hour}] }) または member_commit_slots への直 insert）、afterAll で member_id スコープ delete して復元する。このファイルは mobile-375 project（config で割り当て済み）でのみ実行される。
  </action>
  <verify>
    <automated>npx playwright test e2e/29-mobile.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test e2e/29-mobile.spec.ts` が pass（mobile-375 project, viewport 375）
    - week-0/week-1 に toBeHidden、week-2 に toBeVisible の assert がある
    - seed した commit slots が afterAll で member_id スコープ delete され復元される（truncate 無し）
    - storageState を使わない（anonymous 同型、ログイン不要ページ）
  </acceptance_criteria>
  <done>375px で最新週のみ表示される CSS 縮退が自動回帰検証される（29 #1）</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 29-layout-nav.spec.ts — 3列構造 + /daily ナビ（QA-04, 29 #2構造, #3）</name>
  <files>e2e/29-layout-nav.spec.ts</files>
  <read_first>
    - e2e/admin-guard.spec.ts（anonymous で page.goto + href assert の手本）
    - src/components/CommitGoalRow.tsx:17-69（3カラム構造: Link /member/ → CommitGrid/未コミット → w-10 achievement）
    - src/components/ViewTabs.tsx（href="/" と href="/daily" のナビ — ⚠ Drift D-C）
    - src/app/daily/page.tsx:33,38,48,59（All/チームタブ href + WeeklyHeatmapGrid 描画）
    - 36-PATTERNS.md §29-layout-nav.spec.ts および §Drift Alerts D-C
  </read_first>
  <behavior>
    - #2 構造: page.goto('/') → `a[href^="/member/"]` の .first() が toBeVisible（各行に member link 存在）。ピクセル整列は assert しない（Pitfall 4 — 構造のみ）
    - #3 ナビ: / で nav 内 `a[href="/daily"]` が toBeVisible（ViewTabs のデイリータブ）→ page.goto('/daily') → `a[href="/daily"]`（All タブ）が visible かつ WeeklyHeatmapGrid 由来の DOM（daily/page.tsx:59）が描画されている
  </behavior>
  <action>
    e2e/admin-guard.spec.ts と同型の anonymous spec（storageState 不要）。#2 は `page.locator('a[href^="/member/"]').first()` の toBeVisible で構造存在のみ検証（ピクセル整列・スペーシングは assert しない — 本番手動目視に委譲、Pitfall 4 / 29 #2 自動化境界）。#3 は ⚠ Drift Alert D-C に従い旧 /weekly-stamp ではなく現行 /daily を対象に書く: / で `nav a[href="/daily"]` の toBeVisible、続いて page.goto('/daily') で All タブ `a[href="/daily"]` と WeeklyHeatmapGrid 描画を検証。ファイル冒頭コメントに「29-VERIFICATION の /weekly-stamp は後続フェーズで /daily にリネーム済み（src バグではない）」と明記。このファイルは anonymous project（config 割り当て済み）で実行。DB mutation は無いため afterEach 不要。
  </action>
  <verify>
    <automated>npx playwright test e2e/29-layout-nav.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test e2e/29-layout-nav.spec.ts` が pass
    - #3 spec が `/daily` を対象にしている（grep: `grep -c "/daily" e2e/29-layout-nav.spec.ts` >= 2）かつ `/weekly-stamp` をターゲットにしていない（リネーム追従、Drift D-C）
    - #2 は構造存在（a[href^="/member/"]）のみ assert し bounding-box ピクセル整列を assert していない（Pitfall 4）
    - ファイル冒頭に /weekly-stamp → /daily リネームの注記コメントがある
  </acceptance_criteria>
  <done>29 #2 構造 + #3 ナビが現行 /daily ルートに対して自動回帰検証される</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process → TEST Supabase | 29-mobile の seed/cleanup のみ service_role を使う。29-layout-nav は anonymous 読み取りのみ |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-04 | Tampering | 29-mobile の seed slots がテスト後残留 | mitigate | afterAll で member_id スコープ delete（truncate 禁止）。28 spec とは独立 seed |
| T-36-05 | Tampering | 本番 DB 汚染 | mitigate | .env.test = TEST project 限定。anonymous spec は読み取りのみ |
| T-36-SC | Tampering | npm/pip/cargo installs | accept | パッケージ install 無し。Package Legitimacy Gate は N/A |

**注:** 検証フェーズ・src/ 不変・新規攻撃面なし。29-layout-nav は DB 書き込みを行わない読み取り専用 anonymous spec。
</threat_model>

<verification>
- `npx playwright test e2e/29-mobile.spec.ts` pass（mobile-375 project）
- `npx playwright test e2e/29-layout-nav.spec.ts` pass（anonymous project）
- 実行後 29-mobile が seed した commit slots が member_id スコープで復元されている
- dev サーバ停止状態で実行（Pitfall 3）
</verification>

<success_criteria>
- 29 #1（モバイル縮退）と #3（/daily ナビ）が自動 pass、#2 は構造のみ自動 pass（整列は手動委譲）
- #3 spec が現行 /daily を対象（Drift D-C 追従）
- src/ に変更なし（git diff src/ が空）
</success_criteria>

<output>
Create `.planning/phases/36-qa-uat/36-02-SUMMARY.md` when done
</output>
