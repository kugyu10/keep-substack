---
phase: 36-qa-uat
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - playwright.config.ts
  - e2e/27-handle.spec.ts
  - e2e/28-commit-flow.spec.ts
autonomous: true
requirements: [QA-02, QA-03]
must_haves:
  truths:
    - "TEST project に replace_member_commit_slots RPC と member_commit_slots UNIQUE が適用済みであることが確認できる（28 spec の前提）"
    - "ログイン済み /my で @handle input が NULL 時に描画され、保存往復で DB に @hoge が書かれ reload 後 read-only 表示になる（27 #2,#3）"
    - "handle 設定時 /member にプロフィールリンクが出て、null 時は出ない（27 #4,#5）"
    - "/my の投稿スケジュール宣言フルフロー（モーダル/頻度/重複警告/保存往復）が通る（28 #2）"
    - "新 spec 実行後 TEST member の substack_handle が NULL / commit slots が空に復元される（他データ非汚染）"
  artifacts:
    - path: "e2e/27-handle.spec.ts"
      provides: "Phase 27 #2-#5 の logged-in DB round-trip 回帰 spec"
      contains: "resolveTestMemberId"
    - path: "e2e/28-commit-flow.spec.ts"
      provides: "Phase 28 #2 schedule-declaration フルフロー回帰 spec"
      contains: "replace_member_commit_slots round-trip via member_commit_slots count"
    - path: "playwright.config.ts"
      provides: "logged-in testMatch に 27/28 spec 追加 + mobile-375 project 定義"
      contains: "mobile-375"
  key_links:
    - from: "e2e/27-handle.spec.ts"
      to: "e2e/helpers/admin.ts createTestAdmin + e2e/fixtures/test-data.ts TEST"
      via: "service_role admin client で members 行を seed/assert/cleanup"
      pattern: "createTestAdmin"
    - from: "e2e/28-commit-flow.spec.ts"
      to: "member_commit_slots テーブル"
      via: "expect.poll で count==2 を確認 + afterEach で member_id スコープ delete"
      pattern: "member_commit_slots"
---

<objective>
Phase 27 の自動化可能項目（#2 @handle描画 / #3 保存往復 / #4 プロフィールリンクあり / #5 リンクなし）と Phase 28 #2（/my スケジュール宣言フルフロー）を、Phase 26 で確立済みの session-injection E2E ハーネス（mintAuthCookies + storageState + member_id スコープ afterEach）に durable な回帰 spec として追加する。これは D-04/D-05 が定める「自動レイヤー（TEST project に対する durable 追加レイヤー）」であり、SC#1 の本番充足は別プラン（36-03 手動 UAT）が担保する。

Purpose: QA-02（Phase 27 VERIFICATION ギャップ）と QA-03（Phase 28 #2 フルフロー）のコードレベル振る舞いを TEST project で回帰検証可能にする。src/ は一切変更しない（検証フェーズ）。
Output: playwright.config.ts（testMatch 追記 + mobile-375 project 追加）+ e2e/27-handle.spec.ts + e2e/28-commit-flow.spec.ts。28 spec 着手前に TEST project の RPC/UNIQUE 適用を fail-fast 確認する。
</objective>

<execution_context>
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-qa-uat/36-RESEARCH.md
@.planning/phases/36-qa-uat/36-PATTERNS.md

<interfaces>
<!-- 既存ハーネスの再利用 API。executor はこれらを直接使い、コードベース探索不要。 -->

From e2e/fixtures/test-data.ts:
  TEST.publicationId = 'e2e-test-publication'  // seed member を一意に引くキー
  TEST.teamName = 'e2e-public-team'
  TEST.memberName = 'e2e-test-member'
  TEST.userEmail = 'playwright-test-user@e2e.keep-substack.local'

From e2e/helpers/admin.ts:
  createTestAdmin(): SupabaseClient  // service_role、RLS バイパス、seed/assert/cleanup 用

From e2e/my-teams.spec.ts（踏襲する手本 — resolveTestMemberId + afterEach の形）:
  async function resolveTestMemberId(): Promise<string>  // publication_id → members.id を single() で解決

DOM 契約（現行ソース直読で確認済み）:
  MyProfileForm.tsx:52-71 — substackHandle が null のときのみ編集可能 input
    (id=substack_handle, label='Substack ハンドル', placeholder='@yourhandle')。
    非 null のとき hidden input + read-only <p>（編集不可、Drift Alert D-A）。
  actions.ts:50-60 — handle は '@' 正規化（"hoge" → "@hoge"）。
  CalendarGrid.tsx:65-66 — handle set 時 <a href="https://substack.com/@handle" target="_blank">。
  CommitScheduleModal.tsx — トリガー button「投稿スケジュールを宣言する」:101 /
    role="dialog" aria-modal :122-123 / select#frequency :149 / select#day-{i} :170 /
    警告「同じ曜日を複数選択しています」:200 / 送信 button「宣言する」:214
    (disabled は isPending || showDuplicateWarning)。
  actions.ts:176 — commit slots 保存は RPC replace_member_commit_slots（delete+insert ではない、Drift Alert D-B）。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TEST project の commit-slots スキーマ前提確認（fail-fast）</name>
  <files>e2e/28-commit-flow.spec.ts</files>
  <read_first>
    - supabase/schema.sql（replace_member_commit_slots 関数定義 :150 / member_commit_slots テーブル + UNIQUE）
    - src/app/my/actions.ts（:176 の admin.rpc('replace_member_commit_slots') 呼び出し）
    - 36-RESEARCH.md §Open Questions 1（TEST project への RPC/UNIQUE 適用状態が未確認）
  </read_first>
  <action>
    TEST project（otydhiumsdsyxepnjqjp）に member_commit_slots テーブル + (member_id, day_of_week) UNIQUE 制約 + replace_member_commit_slots RPC が適用済みかを確認する。確認手段: createTestAdmin() で `admin.rpc('replace_member_commit_slots', { p_member_id: '<resolveTestMemberId の結果>', p_slots: [] })` を空配列で 1 度呼び、rpcError が null（= 関数存在）であることを確かめるスモークを 28 spec の先頭テストとして組み込む（関数が無ければ PostgREST が 'function ... does not exist' を返し fail-fast）。もし RPC が TEST project に未適用だった場合は、supabase/schema.sql の durable 定義（CREATE OR REPLACE FUNCTION replace_member_commit_slots + member_commit_slots の CREATE TABLE/UNIQUE 部分）を TEST project の Supabase SQL Editor 経由で適用する（これはテスト環境 readiness 確認であり本番スキーマ push ではない。本番 xolhjcngrwwwqtklmoyk には触れない）。afterEach は空配列呼びの後で member_id スコープ delete を行い副作用を残さない。
  </action>
  <verify>
    <automated>npx playwright test e2e/28-commit-flow.spec.ts -g "RPC 前提" </automated>
  </verify>
  <acceptance_criteria>
    - `admin.rpc('replace_member_commit_slots', { p_member_id, p_slots: [] })` が rpcError===null を返す（RPC が TEST project に存在）
    - 確認テストが member_commit_slots に余計な行を残さない（afterEach の member_id スコープ delete で空）
    - 本番 project（xolhjcngrwwwqtklmoyk）への接続・変更が一切ない（.env.test = TEST project のみ）
  </acceptance_criteria>
  <done>TEST project に RPC/UNIQUE が存在することが自動確認でき、28 フルフロー spec の前提が満たされている</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 27-handle.spec.ts — @handle 描画/保存往復/プロフィールリンク（QA-02, 27 #2-#5）</name>
  <files>e2e/27-handle.spec.ts, playwright.config.ts</files>
  <read_first>
    - e2e/my-teams.spec.ts（resolveTestMemberId + member_id スコープ afterEach の手本、構造を 1:1 踏襲）
    - src/app/my/MyProfileForm.tsx:52-71（@handle の null/非 null 分岐 DOM 契約 — Drift Alert D-A: 設定後 read-only）
    - src/app/my/actions.ts:50-60（"hoge" → "@hoge" 正規化）
    - src/components/CalendarGrid.tsx:60-74（handle set 時 <a href="https://substack.com/@handle" target="_blank">、null 時はリンクなし素 div）
    - playwright.config.ts:30-41（logged-in/anonymous project の testMatch 形 — self-analog）
  </read_first>
  <behavior>
    - #2: seed member.substack_handle=NULL のとき /my に getByLabel('Substack ハンドル') の編集 input が描画され placeholder='@yourhandle'
    - #3: #substack_handle に "hoge" を入力 → button「保存する」→ DB が '@hoge' に（expect.poll）→ reload 後 #substack_handle が toHaveCount(0) で read-only の '@hoge' テキストが visible（Drift Alert D-A）
    - #4: admin で members.substack_handle='@hoge' をセット → /member/${TEST.publicationId} で a[href="https://substack.com/@hoge"][target="_blank"] が visible
    - #5: handle=NULL で /member/${TEST.publicationId} → a[href^="https://substack.com/"] が toHaveCount(0)、名前見出しは visible
    - afterEach: 全テスト後 members.substack_handle を NULL に復元（設定後 read-only のため復元必須）
  </behavior>
  <action>
    e2e/my-teams.spec.ts の構造（import createTestAdmin / TEST、ローカル resolveTestMemberId、test.afterEach）をそのまま複製し、afterEach を `admin.from('members').update({ substack_handle: null }).eq('id', memberId)` に置き換える。4 テスト（#2,#3,#4,#5）を上記 behavior 通りに記述。waitForTimeout 禁止、DB 往復は expect.poll で吸収。playwright.config.ts の logged-in project の testMatch に '27-handle.spec.ts' を追加し、同じ編集で（Task 3 と重複編集を避けるため）'28-commit-flow.spec.ts' も追加、さらに mobile-375 project（name:'mobile-375', testMatch:['29-mobile.spec.ts'], use: Desktop Chrome + viewport {width:375,height:800}, storageState なし）と anonymous project の testMatch への '29-layout-nav.spec.ts' 追加もこの 1 回の config 編集でまとめて行う（config はこのプランが唯一の owner）。fullyParallel/workers/webServer は変更しない（Pitfall 3,5）。
  </action>
  <verify>
    <automated>npx playwright test e2e/27-handle.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test e2e/27-handle.spec.ts` が 4 テスト全 pass
    - reload 後 assert が `expect(page.locator('#substack_handle')).toHaveCount(0)` を含む（Drift Alert D-A の read-only 振る舞いを検証）
    - afterEach に `update({ substack_handle: null })` が member_id スコープで存在（truncate 無し）
    - playwright.config.ts に 'mobile-375' project と 4 spec の testMatch エントリが存在（grep 確認: `grep -c "27-handle.spec.ts\|28-commit-flow.spec.ts\|29-mobile.spec.ts\|29-layout-nav.spec.ts\|mobile-375" playwright.config.ts` が 5 以上）
  </acceptance_criteria>
  <done>27 #2-#5 が TEST project で回帰検証され、config に全 spec の project 割り当てが入っている</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 28-commit-flow.spec.ts — スケジュール宣言フルフロー（QA-03, 28 #2）</name>
  <files>e2e/28-commit-flow.spec.ts</files>
  <read_first>
    - e2e/my-teams.spec.ts（認証済み /my 操作 + expect.poll + afterEach の手本）
    - src/app/my/CommitScheduleModal.tsx（トリガー:101 / dialog:122 / #frequency:149 / #day-{i}:170 / 警告:200 / 宣言する:214、disabled 条件）
    - src/app/my/actions.ts:176（RPC replace_member_commit_slots — ブラックボックス往復で検証、Drift Alert D-B）
    - 36-PATTERNS.md §28-commit-flow.spec.ts（フルフロー手本コード）
  </read_first>
  <behavior>
    - /my で button「投稿スケジュールを宣言する」click → role=dialog が visible
    - select#frequency を '2' に → #day-1 が visible（スロット行 2 つ）
    - #day-0 と #day-1 を同じ '1' に → 「同じ曜日を複数選択しています」visible + button「宣言する」が disabled
    - #day-1 を '3' に変更 → 「宣言する」click → dialog が hidden
    - member_commit_slots に member_id スコープで count==2（expect.poll、RPC 往復をブラックボックス検証）
    - reload 後サマリーテキスト /週2回 —/ が visible
    - afterEach: member_commit_slots を member_id スコープ delete
  </behavior>
  <action>
    Task 1 で組み込んだ RPC 前提テストと同一ファイルに、上記 behavior のフルフローテストを追加する。resolveTestMemberId はファイル内ローカル関数として複製（my-teams.spec.ts と同形）。モーダル操作は getByRole('button', { name: '投稿スケジュールを宣言する' }) / getByRole('dialog') / locator('#frequency').selectOption('2') / locator('#day-{i}').selectOption(...) / getByText('同じ曜日を複数選択しています') / getByRole('button', { name: '宣言する' }) を使う。DB 往復は createTestAdmin + member_commit_slots の count を expect.poll で 2 に。internal の delete+insert vs RPC には依存しない（行数のみ検証）。afterEach は member_commit_slots を member_id で delete。playwright.config への testMatch 追加は Task 2 で完了済みのため本タスクでは config を触らない。
  </action>
  <verify>
    <automated>npx playwright test e2e/28-commit-flow.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test e2e/28-commit-flow.spec.ts` が全 pass（RPC 前提テスト + フルフローテスト）
    - DB 往復 assert が `member_commit_slots` の count を expect.poll で 2 に検証している（waitForTimeout 不使用）
    - afterEach に `from('member_commit_slots').delete().eq('member_id', ...)` が存在（truncate 無し）
    - 重複曜日で button「宣言する」が toBeDisabled() を assert している
  </acceptance_criteria>
  <done>28 #2 のフルフローが TEST project で RPC 往復含め回帰検証される</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process → TEST Supabase | service_role admin client が RLS をバイパスして seed/assert/cleanup する（e2e/ 限定） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-01 | Tampering | E2E が誤って本番 DB を汚染 | mitigate | .env.test = TEST project（otydhiumsdsyxepnjqjp）限定。本番 xolhjcngrwwwqtklmoyk には接続しない。webServer は build+start（dev サーバ停止前提、Pitfall 3） |
| T-36-02 | Information Disclosure | service_role key の漏洩 | mitigate | createTestAdmin は e2e/ のみ・.env.test は gitignore。src/ に混入させない（src/ 不変） |
| T-36-03 | Tampering | 共有 TEST member 行のデータ残留 | mitigate | afterEach で member_id スコープ delete/NULL 復元（truncate 禁止）。他メンバー非汚染 |
| T-36-SC | Tampering | npm/pip/cargo installs | accept | このプランはパッケージを一切インストールしない（既存依存のみ）。Package Legitimacy Gate は N/A |

**注:** これは検証フェーズで src/ を変更しない。新規エンドポイント・新規攻撃面なし。テストコードは隔離された TEST project に対し member_id スコープで実行される。上記は実在する最小限の運用上の考慮のみ。
</threat_model>

<verification>
- `npx playwright test e2e/27-handle.spec.ts` 全 pass（4 テスト）
- `npx playwright test e2e/28-commit-flow.spec.ts` 全 pass（RPC 前提 + フルフロー）
- dev サーバ（:3000）を停止した状態で実行（Pitfall 3）
- 実行後 TEST member の substack_handle=NULL / member_commit_slots=空 に復元されている
</verification>

<success_criteria>
- 27 #2,#3,#4,#5 と 28 #2 が TEST project で自動回帰検証され green
- playwright.config.ts に 4 新 spec の project 割り当て + mobile-375 project が定義済み
- src/ に変更が 1 行も無い（git diff src/ が空）
</success_criteria>

<output>
Create `.planning/phases/36-qa-uat/36-01-SUMMARY.md` when done
</output>
