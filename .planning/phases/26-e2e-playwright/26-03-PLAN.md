---
phase: 26-e2e-playwright
plan: 03
type: execute
wave: 3
depends_on: ["26-01", "26-02"]
files_modified:
  - e2e/login.spec.ts
  - e2e/my-teams.spec.ts
  - e2e/admin-guard.spec.ts
autonomous: true
requirements: [E2E-01, E2E-02, E2E-03]
user_setup: []

must_haves:
  truths:
    - "An injected (test-account) session reaches /my without redirect — proving proxy.ts getUser() validated the minted cookie (E2E-01)"
    - "Toggling a public-team checkbox and saving creates a member_teams row, and the test cleans it up after (E2E-02)"
    - "Anonymous access to /admin and /my redirects to / ; the non-admin logged-in user is also redirected from /admin (E2E-03)"
    - "npx playwright test passes all three specs AND npx vitest run still passes (no runner collision)"
  artifacts:
    - path: "e2e/login.spec.ts"
      provides: "E2E-01 logged-in session reaches /my"
      contains: "マイページ"
    - path: "e2e/my-teams.spec.ts"
      provides: "E2E-02 public team join → member_teams delta + afterEach cleanup"
      contains: "保存する"
    - path: "e2e/admin-guard.spec.ts"
      provides: "E2E-03 anon/non-admin redirect to /"
      contains: "/admin"
  key_links:
    - from: "e2e/my-teams.spec.ts"
      to: "member_teams table"
      via: "createTestAdmin expect.poll on row count"
      pattern: "member_teams"
    - from: "e2e/admin-guard.spec.ts"
      to: "proxy.ts redirect"
      via: "goto('/admin') → toHaveURL('/')"
      pattern: "toHaveURL"
    - from: "e2e/login.spec.ts"
      to: "e2e/.auth/user.json storageState"
      via: "logged-in project"
      pattern: "/my"
---

<objective>
Author the three E2E spec files that satisfy the phase's three ROADMAP success criteria, wire them to the projects defined in `playwright.config.ts`, and prove the full suite green: E2E-01 (injected session reaches `/my`), E2E-02 (`/my` public-team join → `member_teams` INSERT, with afterEach cleanup), E2E-03 (anon + non-admin redirected from `/admin`/`/my`). No production code is modified — these tests exercise the existing `proxy.ts` gate and `/my` server action exactly as deployed.

Purpose: Deliver the observable, automated verification that the three locked flows work end-to-end against the real (test) Supabase project.
Output: Three `e2e/*.spec.ts` files; `npx playwright test` green; `npx vitest run` still green.
</objective>

<execution_context>
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/26-e2e-playwright/26-RESEARCH.md
@.planning/phases/26-e2e-playwright/26-PATTERNS.md
@.planning/phases/26-e2e-playwright/26-01-SUMMARY.md

<interfaces>
<!-- Harness contracts created by Plan 01 (consume directly, do not re-derive) -->
e2e/helpers/admin.ts        → export createTestAdmin()  (service_role, bypasses RLS)
e2e/fixtures/test-data.ts   → export const TEST = { userEmail, teamName, memberName, publicationId }
e2e/global-setup.ts         → seeds non-admin user + public team(status='public') + linked member; writes e2e/.auth/user.json
playwright.config.ts        → projects: 'logged-in' (storageState user.json, matches login.spec.ts + my-teams.spec.ts), 'anonymous' (no storageState, matches admin-guard.spec.ts)

<!-- DOM contract from src/app/my/MyProfileForm.tsx:59-87 + page.tsx:48 -->
<input type="checkbox" name="teams" value={t.name}>  inside <label> with text t.name  → getByRole('checkbox', { name: TEST.teamName })
<button type="submit">保存する</button>              → getByRole('button', { name: '保存する' })
heading 「マイページ」 on /my                          → getByRole('heading', { name: 'マイページ' })

<!-- DB write contract from src/app/my/actions.ts:89-105 -->
member_teams INSERT { member_id, team_id } on join; DELETE eq member_id (+ in team_id) on leave

<!-- Guard contract from src/proxy.ts:34-45 -->
/admin: !user || app_metadata.role !== 'admin' → redirect '/'
/my:    !user → redirect '/'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: E2E-01 login + E2E-03 admin-guard specs</name>
  <files>e2e/login.spec.ts, e2e/admin-guard.spec.ts</files>
  <read_first>
    - src/proxy.ts (lines 26-51: getUser() gate, /admin role check, /my login check, matcher)
    - src/app/my/page.tsx (line 48: 「マイページ」 heading text)
    - src/__tests__/proxy.test.ts (lines 39-72: the unit-test analog asserting the same redirect contract)
    - e2e/fixtures/test-data.ts (TEST constants — created by Plan 01)
    - .planning/phases/26-e2e-playwright/26-RESEARCH.md (Code Examples lines 440-468 login + admin-guard; Open Question 1 lines 532-535 non-admin double-duty)
  </read_first>
  <action>
    Create `e2e/login.spec.ts` (runs under the `logged-in` project per playwright.config testMatch): `import { test, expect } from '@playwright/test'`; one test that `page.goto('/my')`, asserts `expect(page).toHaveURL(/\/my$/)` (NOT redirected to `/`), and asserts `page.getByRole('heading', { name: 'マイページ' })` is visible — this proves `proxy.ts` `getUser()` validated the injected storageState cookie (E2E-01). Create `e2e/admin-guard.spec.ts` (runs under the `anonymous` project — NO storageState): two tests asserting `page.goto('/admin')` → `expect(page).toHaveURL('http://localhost:3000/')` and `page.goto('/my')` → same redirect (E2E-03 anonymous case). Add a THIRD test in admin-guard.spec.ts for the non-admin logged-in case using `test.use({ storageState: 'e2e/.auth/user.json' })` on a nested `describe` (the seeded user is intentionally non-admin per Open Question 1): `page.goto('/admin')` → `expect(page).toHaveURL('http://localhost:3000/')`. Keep an explanatory top-of-file comment per the existing test-file convention. Do NOT mock Supabase — these are real end-to-end assertions.
  </action>
  <verify>
    <automated>npx playwright test e2e/admin-guard.spec.ts e2e/login.spec.ts --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test e2e/login.spec.ts` exits 0; the test asserts final URL matches `/\/my$/` and 「マイページ」 heading visible
    - `npx playwright test e2e/admin-guard.spec.ts` exits 0; anonymous `/admin` and `/my` both assert `toHaveURL('http://localhost:3000/')`
    - admin-guard.spec.ts includes a non-admin logged-in case (storageState user.json) asserting `/admin` redirects to `/`
    - Neither spec imports from `@/` (src) nor modifies any src file
  </acceptance_criteria>
  <done>E2E-01 (injected session reaches /my) and E2E-03 (anon + non-admin blocked from /admin, anon blocked from /my) pass against the running test app.</done>
</task>

<task type="auto">
  <name>Task 2: E2E-02 my-teams join spec with afterEach cleanup, then full suite + vitest gate</name>
  <files>e2e/my-teams.spec.ts</files>
  <read_first>
    - src/app/my/MyProfileForm.tsx (lines 59-87: checkbox name="teams" value=team name inside label; 保存する submit button)
    - src/app/my/actions.ts (lines 89-105: member_teams INSERT/DELETE scoped by member_id)
    - src/app/my/__tests__/updateMyProfileAction.test.ts (unit analog of the same join/leave behavior)
    - e2e/helpers/admin.ts (createTestAdmin — created by Plan 01)
    - e2e/fixtures/test-data.ts (TEST constants)
    - .planning/phases/26-e2e-playwright/26-RESEARCH.md (Code Examples lines 470-495 my-teams; Pattern 4 lines 344-357 afterEach cleanup; Pitfall 5 workers:1)
  </read_first>
  <action>
    Create `e2e/my-teams.spec.ts` (runs under the `logged-in` project): import `{ test, expect }`, `createTestAdmin` from `./helpers/admin`, `TEST` from `./fixtures/test-data`. One test: `page.goto('/my')`, `page.getByRole('checkbox', { name: TEST.teamName }).check()`, `page.getByRole('button', { name: '保存する' }).click()`, then assert the DB delta using `expect.poll` with `createTestAdmin()` — resolve the member id via `from('members').select('id').eq('publication_id', TEST.publicationId).single()`, then `from('member_teams').select('*', {count:'exact', head:true}).eq('member_id', m.id)` and `.toBe(1)` (poll absorbs server-action latency; use auto-waiting, NEVER `waitForTimeout`). Add `test.afterEach` (Pattern 4) that uses `createTestAdmin()` to resolve the member id by `publication_id` and `from('member_teams').delete().eq('member_id', m.id)` — scoped to the test member only, NEVER a broad truncate (D-09); service_role bypasses RLS so the DELETE succeeds. After authoring, run the FULL suite `npx playwright test` (webServer builds+starts the app under .env.test) and confirm all three specs pass, then run `npx vitest run` to confirm no runner collision regression (Pitfall 2). Note: `workers:1` is already set in config so the shared member_teams rows do not race.
  </action>
  <verify>
    <automated>npx playwright test && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test e2e/my-teams.spec.ts` exits 0: checkbox `TEST.teamName` checked → 保存する clicked → `member_teams` count for the test member polls to `1`
    - `test.afterEach` deletes only the test member's `member_teams` rows (scoped by `member_id`, no truncate)
    - The spec uses `expect.poll` / auto-waiting and contains NO `waitForTimeout`
    - `npx playwright test` (full suite, all 3 specs) exits 0
    - `npx vitest run` still exits 0 and executes only `src/**` tests (no e2e specs)
    - No file under `src/` is modified
  </acceptance_criteria>
  <done>E2E-02 passes (join writes member_teams, cleaned up afterEach); full 3-spec Playwright suite green; vitest unit suite still green with no collision — all three ROADMAP success criteria met.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright browser → Next.js app (:3000) | injected auth cookie crosses; validated by proxy.ts getUser() |
| test (service_role admin) → TEST member_teams | mutable test data written then cleaned |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-04 | Elevation of Privilege | weakening proxy.ts to make E2E-03 pass | mitigate | Phase boundary forbids src/ changes; acceptance criteria assert no src file modified; E2E-03 tests the REAL gate (non-admin user truly blocked) |
| T-26-03 | Tampering | leftover member_teams polluting reruns / hitting prod | mitigate | afterEach scoped delete by member_id (D-09 no truncate); suite runs against .env.test TEST project only (Plan 02) |
| T-26-06 | Tampering | flaky parallel writes corrupting assertions | accept | workers:1 + fullyParallel:false (config); single shared test user, deterministic |
</threat_model>

<verification>
- `npx playwright test` → all 3 specs green (E2E-01/02/03)
- `npx vitest run` → green, src/** only (no collision regression)
- `git diff --name-only` shows no file under `src/`
- member_teams left empty after suite (afterEach cleanup verified by rerunnability)
</verification>

<success_criteria>
All three ROADMAP success criteria are satisfied by passing automated tests: (1) injected test-account session reaches `/my`; (2) `/my` public-team join writes `member_teams`; (3) anonymous and non-admin access to `/admin` (and anon to `/my`) redirects to `/`. Full Playwright suite green AND existing vitest suite green, with zero production-code changes.
</success_criteria>

<output>
Create `.planning/phases/26-e2e-playwright/26-03-SUMMARY.md` when done
</output>
