---
phase: 26-e2e-playwright
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - playwright.config.ts
  - vitest.config.ts
  - .gitignore
  - .env.test.example
  - e2e/helpers/admin.ts
  - e2e/helpers/session.ts
  - e2e/fixtures/test-data.ts
  - e2e/global-setup.ts
autonomous: true
requirements: [E2E-01, E2E-02, E2E-03]
user_setup: []

must_haves:
  truths:
    - "npx vitest run executes only src/** unit tests and does NOT pick up any e2e/*.spec.ts file"
    - "Playwright is installed at 1.60.0 with a chromium browser available"
    - "global-setup mints production-identical Supabase auth cookies into e2e/.auth/user.json via @supabase/ssr round-trip (no hand-rolled encoding) — session injection bypasses email/PKCE so proxy.ts getUser() validates a real session (D-01, D-02, D-03)"
    - "Static fixtures (test auth user, public team, user_id-linked member) are seeded idempotently in global-setup, kept separate from mutable member_teams data (D-07, D-08)"
    - "Secrets (.env.test, e2e/.auth/) are never committable (D-05)"
  artifacts:
    - path: "playwright.config.ts"
      provides: "testDir e2e, globalSetup, webServer build+start, logged-in + anonymous projects"
      contains: "testDir"
    - path: "vitest.config.ts"
      provides: "vitest include scoped to src/**, excludes e2e/**"
      contains: "src/**"
    - path: "e2e/helpers/admin.ts"
      provides: "createTestAdmin() service_role client"
      contains: "createClient"
    - path: "e2e/helpers/session.ts"
      provides: "mintAuthCookies(email) via @supabase/ssr setSession round-trip"
      contains: "setSession"
    - path: "e2e/global-setup.ts"
      provides: "idempotent seed + storageState minting"
      contains: "global"
    - path: "e2e/fixtures/test-data.ts"
      provides: "TEST constants (email/team/member/publicationId)"
      contains: "TEST"
    - path: ".env.test.example"
      provides: "placeholder env template for test Supabase project"
      contains: "NEXT_PUBLIC_SUPABASE_URL"
  key_links:
    - from: "e2e/helpers/session.ts"
      to: "@supabase/ssr createServerClient.setSession"
      via: "in-memory getAll/setAll capture"
      pattern: "setAll"
    - from: "playwright.config.ts"
      to: ".env.test"
      via: "dotenv loadEnv before defineConfig"
      pattern: "dotenv|\\.env\\.test"
    - from: "vitest.config.ts"
      to: "src/**"
      via: "test.include glob"
      pattern: "src/\\*\\*"
---

<objective>
Stand up the complete Playwright E2E harness with ZERO production-code changes: install `@playwright/test@1.60.0` + chromium, author `playwright.config.ts` and a new `vitest.config.ts` (to prevent the two runners from globbing each other's files), add the `e2e/` helper/fixture/global-setup scaffolding (admin client clone, `@supabase/ssr` cookie-minting, idempotent seed), and harden secret handling (`.gitignore` + `.env.test.example`).

Purpose: Every downstream spec (Plans 03's E2E-01/02/03) depends on this harness existing and on `global-setup` producing a valid logged-in `storageState`. This plan creates the contracts and infrastructure; the specs that consume them come later.
Output: Installed Playwright, two config files, `e2e/` harness (helpers/fixtures/global-setup), gitignore hardening, `.env.test.example`.
</objective>

<execution_context>
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-e2e-playwright/26-CONTEXT.md
@.planning/phases/26-e2e-playwright/26-RESEARCH.md
@.planning/phases/26-e2e-playwright/26-PATTERNS.md

<interfaces>
<!-- Production analogs the executor MUST mirror. Use these directly — do not re-derive. -->

From src/lib/supabase/admin.ts (clone verbatim into e2e/helpers/admin.ts, rename to createTestAdmin):
```typescript
import { createClient } from '@supabase/supabase-js'
export function createSupabaseAdminClient() {
  return createClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.SUPABASE_SERVICE_ROLE_KEY!,
    { auth: { autoRefreshToken: false, persistSession: false } }
  )
}
```

From src/lib/supabase/server.ts (cookie-adapter shape to mirror in e2e/helpers/session.ts, replacing next/headers cookieStore with an in-memory capture array):
```typescript
createServerClient(URL, ANON, {
  cookies: {
    getAll() { return cookieStore.getAll() },
    setAll(cookiesToSet) { cookiesToSet.forEach(({name,value,options}) => cookieStore.set(name,value,options)) },
  },
})
```

From src/proxy.ts (the gate the minted cookie must satisfy — informational):
```typescript
// matcher: ['/admin', '/admin/:path*', '/my', '/my/:path*']
// const { data: { user } } = await supabase.auth.getUser()
// /admin: !user || user.app_metadata?.role !== 'admin' → redirect '/'
// /my:    !user → redirect '/'
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Playwright + chromium, add npm scripts, harden secrets</name>
  <files>package.json, .gitignore, .env.test.example</files>
  <read_first>
    - package.json (current scripts block lines 5-11; "test": "vitest run" must stay unchanged)
    - .gitignore (line 34 `.env*` already covers .env.test; e2e/.auth/ is NOT yet ignored)
    - .env.example (Supabase block lines 11-16 — the three vars to mirror)
  </read_first>
  <action>
    Run `npm install -D @playwright/test@1.60.0` (exact pin per RESEARCH Standard Stack), then `npx playwright install chromium` to fetch the browser binary. Add two scripts to package.json alongside the existing ones (do NOT replace `"test": "vitest run"`): `"test:e2e": "playwright test"` and `"test:e2e:ui": "playwright test --ui"`. Append `e2e/.auth/` to .gitignore (the `.env*` entry already covers `.env.test`, so do NOT add `.env.test` again — only add the `e2e/.auth/` storageState directory, which holds real access tokens). Create `.env.test.example` mirroring the four vars from .env.example with placeholder values: `NEXT_PUBLIC_SUPABASE_URL=https://<test-project-ref>.supabase.co`, `NEXT_PUBLIC_SUPABASE_ANON_KEY=<test-anon-key>`, `SUPABASE_SERVICE_ROLE_KEY=<test-service-role-key>`, `ADMIN_PASSWORD=test`. Add a comment in `.env.test.example` noting that the actual `.env.test` is gitignored and MUST point at the dedicated TEST project (never production), and that the `db push` connection string uses the direct port 5432 (not the app's 6543 transaction pooler).
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(!p.devDependencies['@playwright/test']) throw new Error('playwright missing'); if(p.scripts['test:e2e']!=='playwright test') throw new Error('test:e2e missing'); if(p.scripts.test!=='vitest run') throw new Error('vitest test script changed'); console.log('ok')" && grep -q 'e2e/.auth/' .gitignore && grep -q 'NEXT_PUBLIC_SUPABASE_URL' .env.test.example && npx playwright --version</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright --version` prints `Version 1.60.0`
    - package.json devDependencies contains `@playwright/test` and scripts contains `test:e2e` and `test:e2e:ui`; `"test": "vitest run"` is unchanged
    - `.gitignore` contains a line `e2e/.auth/`
    - `.env.test.example` exists and contains `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`
    - No real secret values appear in `.env.test.example` (placeholders only)
  </acceptance_criteria>
  <done>Playwright 1.60.0 + chromium installed; npm scripts added without touching the vitest script; e2e/.auth/ gitignored; .env.test.example committed with placeholders only.</done>
</task>

<task type="auto">
  <name>Task 2: Write playwright.config.ts and vitest.config.ts</name>
  <files>playwright.config.ts, vitest.config.ts</files>
  <read_first>
    - .planning/phases/26-e2e-playwright/26-RESEARCH.md (Pattern 2 lines 276-308 = the config baseline; Pitfall 1 lines 396-401 = build-time NEXT_PUBLIC inlining; Pitfall 2 lines 402-406 = vitest collision; Pitfall 5/6 = workers:1 + port)
    - .planning/phases/26-e2e-playwright/26-PATTERNS.md (vitest.config.ts template lines 164-173; playwright.config.ts notes lines 156-160)
    - package.json (build/start scripts that webServer invokes)
  </read_first>
  <action>
    Create `playwright.config.ts` at repo root authored from RESEARCH Pattern 2: import `defineConfig, devices` from `@playwright/test`; load `.env.test` via `dotenv`'s `config({ path: '.env.test' })` BEFORE the `defineConfig(...)` call (so `process.env` is populated when the config object and webServer.env are built). Set `testDir: './e2e'`, `testMatch: '**/*.spec.ts'`, `globalSetup: './e2e/global-setup.ts'`, `fullyParallel: false`, `workers: 1` (Pitfall 5: single shared test user races on member_teams), `reporter: 'list'`, `use: { baseURL: 'http://localhost:3000', trace: 'on-first-retry' }`. Define two projects: `logged-in` (testMatch `['login.spec.ts','my-teams.spec.ts']`, `use.storageState: 'e2e/.auth/user.json'` + Desktop Chrome) and `anonymous` (testMatch `['admin-guard.spec.ts']`, Desktop Chrome, NO storageState). Configure `webServer` with `command: 'npm run build && npm run start'` (Pitfall 1: NEXT_PUBLIC_* are build-time inlined, so the build MUST run under test env — never `next dev`), `url: 'http://localhost:3000'`, `reuseExistingServer: !process.env.CI`, `timeout: 180_000`, and an explicit `env:` block forwarding `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`, `ADMIN_PASSWORD` from `process.env`. Add a top-of-file comment warning to stop any stale `npm run dev` on :3000 before running (Pitfall 6 reuseExistingServer attaching to prod-env dev server). Create `vitest.config.ts` at repo root with `test.include: ['src/**/*.{test,spec}.{ts,tsx}']` and `test.exclude: ['e2e/**','node_modules/**']` so `vitest run` never globs `e2e/*.spec.ts` (Pitfall 2). Do NOT inline full implementations into this plan beyond what read_first references.
  </action>
  <verify>
    <automated>npx vitest run 2>&1 | tee /tmp/vitest_out.txt; grep -qv 'e2e/' /tmp/vitest_out.txt && ! grep -E 'e2e/.*\.spec\.ts' /tmp/vitest_out.txt && grep -q "testDir" playwright.config.ts && grep -q "src/\*\*" vitest.config.ts && echo CONFIG_OK</automated>
  </verify>
  <acceptance_criteria>
    - `playwright.config.ts` contains `testDir: './e2e'`, `globalSetup: './e2e/global-setup.ts'`, `workers: 1`, and `command: 'npm run build && npm run start'`
    - `playwright.config.ts` loads `.env.test` via dotenv before `defineConfig`
    - `vitest.config.ts` `test.include` is scoped to `src/**` and `exclude` lists `e2e/**`
    - `npx vitest run` completes WITHOUT executing any `e2e/*.spec.ts` file (existing src/ unit tests behave as before)
    - Neither config modifies any file under `src/`
  </acceptance_criteria>
  <done>Both config files exist; vitest run is scoped to src/** and ignores e2e/; playwright config has webServer build+start, two projects, workers:1, dotenv .env.test loading.</done>
</task>

<task type="auto">
  <name>Task 3: Build e2e harness — admin/session helpers, fixtures, idempotent global-setup</name>
  <files>e2e/helpers/admin.ts, e2e/helpers/session.ts, e2e/fixtures/test-data.ts, e2e/global-setup.ts</files>
  <read_first>
    - src/lib/supabase/admin.ts (clone verbatim as createTestAdmin)
    - src/lib/supabase/server.ts (createServerClient getAll/setAll adapter shape to mirror with in-memory capture)
    - src/app/my/page.tsx (lines 14-39: member resolved by user_id; teams selected by status='public')
    - src/app/my/actions.ts (lines 60-105: members/teams/member_teams query vocabulary)
    - .planning/phases/26-e2e-playwright/26-RESEARCH.md (Pattern 1 lines 195-268 mintAuthCookies reference impl; Pattern 3 lines 315-342 idempotent seed; Pitfall 3 cookie domain='localhost'; Pitfall 4 generateLink hashed_token; note lines 270 on properties.hashed_token)
    - .planning/phases/26-e2e-playwright/26-PATTERNS.md (session.ts/global-setup analog sections lines 51-101)
  </read_first>
  <action>
    Create `e2e/helpers/admin.ts` as a verbatim clone of `src/lib/supabase/admin.ts` exporting `createTestAdmin()` (same `createClient` with `NEXT_PUBLIC_SUPABASE_URL` + `SUPABASE_SERVICE_ROLE_KEY`, `autoRefreshToken:false`, `persistSession:false`). It is cloned (not imported via `@/`) because e2e lives outside `src/` and loads `.env.test`. Create `e2e/fixtures/test-data.ts` exporting a single `TEST` object `{ userEmail, teamName, memberName, publicationId }` with collision-safe values (executor discretion; recommend an `e2e-`/`playwright-test-` prefix on every value so seeded rows are visually distinguishable from real data). Create `e2e/helpers/session.ts` exporting `mintAuthCookies(email)` following RESEARCH Pattern 1: build the service_role admin client, call `auth.admin.generateLink({type:'magiclink', email})`, read the token from `link.properties.hashed_token` and ASSERT it is defined before proceeding (Pitfall 4), call `auth.verifyOtp({type:'email', token_hash})` to obtain `{access_token, refresh_token}`, then drive `@supabase/ssr` `createServerClient(URL, ANON_KEY, {cookies:{getAll:()=>[], setAll:(toSet)=>captured.push(...toSet)}})` and `await ssr.auth.setSession({access_token, refresh_token})` to capture the exact cookies; map each captured cookie to Playwright shape overriding `domain:'localhost'`, `path:'/'`, `secure:false`, `sameSite:'Lax'`, `httpOnly:false`, `expires` ~1 day out (Pitfall 3 — never copy the Supabase host into the cookie domain). Do NOT hand-roll the `sb-<ref>-auth-token` name/base64/chunk encoding — the round-trip produces it. Create `e2e/global-setup.ts` as the default-exported async function that (1) idempotently seeds via the admin client: `auth.admin.createUser({email:TEST.userEmail, email_confirm:true})` (the test user is intentionally NON-admin — do NOT set app_metadata.role='admin'; on "already registered" fetch the existing id via listUsers), `teams` upsert `{name:TEST.teamName, status:'public'}` onConflict 'name' (MUST be status='public' or no checkbox renders — E2E-02 blocker), `members` upsert `{name:TEST.memberName, publication_id:TEST.publicationId, user_id:<userId>}` onConflict 'publication_id'; (2) calls `mintAuthCookies(TEST.userEmail)`, `mkdirSync('e2e/.auth',{recursive:true})`, and `writeFileSync('e2e/.auth/user.json', JSON.stringify({cookies, origins:[]}))`. Add a fail-fast guard at the top of global-setup that throws if any of `NEXT_PUBLIC_SUPABASE_URL`/`NEXT_PUBLIC_SUPABASE_ANON_KEY`/`SUPABASE_SERVICE_ROLE_KEY` is unset (so a missing `.env.test` produces a clear error rather than a silent prod hit).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E 'e2e/' || echo NO_E2E_TS_ERRORS; test -f e2e/helpers/admin.ts && test -f e2e/helpers/session.ts && test -f e2e/fixtures/test-data.ts && test -f e2e/global-setup.ts && grep -q 'createTestAdmin' e2e/helpers/admin.ts && grep -q 'setSession' e2e/helpers/session.ts && grep -q "status: 'public'\|status:'public'" e2e/global-setup.ts && echo HARNESS_OK</automated>
  </verify>
  <acceptance_criteria>
    - `e2e/helpers/admin.ts` exports `createTestAdmin()` using `SUPABASE_SERVICE_ROLE_KEY` with `autoRefreshToken:false, persistSession:false`
    - `e2e/helpers/session.ts` `mintAuthCookies` derives cookies via `@supabase/ssr` `setSession` round-trip (contains `setSession` and `setAll`), asserts `hashed_token` defined, and sets cookie `domain` to `'localhost'`
    - `e2e/helpers/session.ts` contains NO hand-written `base64-` prefix string and NO manual `.0`/`.1` chunk naming
    - `e2e/global-setup.ts` seeds a `teams` row with `status: 'public'`, creates a NON-admin auth user (no `app_metadata.role:'admin'`), and writes `e2e/.auth/user.json`
    - `e2e/global-setup.ts` throws if any required Supabase env var is unset
    - TypeScript compile of `e2e/` files reports no type errors
    - No file under `src/` is modified
  </acceptance_criteria>
  <done>e2e harness compiles: createTestAdmin clone, mintAuthCookies via ssr round-trip with localhost domain, TEST fixtures, idempotent non-admin seed + public team + linked member, storageState writer, env fail-fast guard. No production code touched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness (Node) → TEST Supabase project | service_role key crosses here; must target TEST project only |
| repo → version control | secrets (.env.test, minted storageState tokens) must NOT cross into git |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | Information Disclosure | `e2e/.auth/user.json` (real access/refresh tokens) | mitigate | Task 1 adds `e2e/.auth/` to `.gitignore`; storageState is generated at runtime, never committed |
| T-26-02 | Information Disclosure | `.env.test` service_role key | mitigate | `.env*` already gitignored (verified .gitignore:34); only `.env.test.example` with placeholders is committed; keys never logged |
| T-26-03 | Tampering | tests writing to PRODUCTION Supabase | mitigate | global-setup fail-fast on unset env vars; dedicated TEST project (D-04); Plan 02 enforces test-URL ≠ prod-URL; build runs under .env.test (Pitfall 1) |
| T-26-04 | Elevation of Privilege | seeded test user gaining admin role | mitigate | Task 3 seed explicitly does NOT set `app_metadata.role='admin'`; the non-admin user is required for E2E-03 negative case |
| T-26-SC | Tampering | npm install of `@playwright/test` | accept | Single new package = official Microsoft `@playwright/test` (RESEARCH Package Legitimacy Audit: Approved, canonical org microsoft/playwright); no third-party Supabase-Playwright helper added |
</threat_model>

<verification>
- `npx playwright --version` → 1.60.0
- `npx vitest run` passes and globs only `src/**` (no e2e specs executed)
- `e2e/` TypeScript compiles with no errors
- `git status` shows neither `.env.test` nor `e2e/.auth/` as trackable (gitignored)
- No file under `src/` appears in this plan's diff
</verification>

<success_criteria>
Playwright harness fully scaffolded and type-correct, vitest scoped away from e2e, secrets hardened, and global-setup ready to seed + mint storageState — all with zero production-code modification. (Actual test execution is gated on Plan 02's provisioned `.env.test`.)
</success_criteria>

<output>
Create `.planning/phases/26-e2e-playwright/26-01-SUMMARY.md` when done
</output>
