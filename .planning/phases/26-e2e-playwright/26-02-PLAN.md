---
phase: 26-e2e-playwright
plan: 02
type: execute
wave: 2
depends_on: ["26-01"]
files_modified: []
autonomous: false
requirements: [E2E-01, E2E-02, E2E-03]
user_setup:
  - service: supabase
    why: "Dedicated TEST Supabase cloud project (D-04) — production data must stay fully isolated"
    env_vars:
      - name: NEXT_PUBLIC_SUPABASE_URL
        source: "TEST project → Settings → API (must NOT equal the production URL)"
      - name: NEXT_PUBLIC_SUPABASE_ANON_KEY
        source: "TEST project → Settings → API"
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "TEST project → Settings → API (service_role — secret)"
    dashboard_config:
      - task: "Create a new dedicated TEST project; copy the direct (port 5432) DB connection string for migrations"
        location: "Supabase Dashboard → New project / Settings → Database"

must_haves:
  truths:
    - "A dedicated TEST Supabase cloud project exists, distinct from production"
    - ".env.test exists with the TEST project's URL/anon/service_role keys and is gitignored"
    - "The TEST project's NEXT_PUBLIC_SUPABASE_URL is NOT equal to the production URL"
    - "All supabase/migrations/ are applied to the TEST project (teams.status, members.publication_id, member_publications all exist)"
  artifacts:
    - path: ".env.test"
      provides: "TEST project connection secrets (gitignored, never committed)"
      contains: "NEXT_PUBLIC_SUPABASE_URL"
  key_links:
    - from: "supabase/migrations/"
      to: "TEST Supabase project"
      via: "supabase db push --db-url <direct-5432-connection-string>"
      pattern: "db push"
---

<objective>
[BLOCKING / autonomous:false] Provision the dedicated cloud TEST Supabase project (D-04/D-06: NO local Docker stack), populate `.env.test` with its keys, and apply the existing `supabase/migrations/` to it via `supabase db push --db-url`. This is the human-only prerequisite gating ALL test execution — without a provisioned project + applied schema, every spec in Plan 03 would falsely fail (no tables) or, worse, hit production data.

Purpose: Establish a production-isolated test database whose schema matches the app. Sequenced AFTER Plan 01 (so `.env.test.example` and `global-setup.ts` exist to validate against) and BEFORE Plan 03 (the specs).
Output: A live TEST project, a valid `.env.test`, and migrated schema.
</objective>

<execution_context>
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/26-e2e-playwright/26-CONTEXT.md
@.planning/phases/26-e2e-playwright/26-RESEARCH.md

<interfaces>
<!-- Migration files to apply (verified present in supabase/migrations/) -->
20260516_rename_substack_id_to_publication_id.sql
20260517_add_team_status.sql           (adds teams.status — REQUIRED for E2E-02 public-team checkbox)
20260530_add_member_publications.sql   (adds member_publications + sync trigger on members INSERT/UPDATE)

<!-- db push command shape (RESEARCH lines 497-505; CLI 2.75.0, --db-url verified) -->
supabase db push --db-url "postgresql://postgres:<PWD>@db.<test-ref>.supabase.co:5432/postgres"
<!-- direct port 5432 for migrations (NOT the app's 6543 transaction pooler) -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Provision TEST Supabase project + populate .env.test (HUMAN-ONLY)</name>
  <files>.env.test</files>
  <what-built>Plan 01 created `.env.test.example` (template) and `e2e/global-setup.ts` (which fail-fasts on unset Supabase env vars). This step requires the human to create the external resource and supply secrets — Claude cannot create a Supabase cloud project or type the service_role key.</what-built>
  <action>HUMAN-ONLY external provisioning. The operator creates a dedicated TEST Supabase cloud project and populates `.env.test` from `.env.test.example`. Claude cannot create the cloud project nor type the secret service_role key, so this is a blocking checkpoint. After the human signals, Claude runs the automated post-condition check (placeholder-rejection + presence of the three keys). See how-to-verify for the operator steps.</action>
  <how-to-verify>
    1. In the Supabase Dashboard, create a NEW project dedicated to E2E testing (do NOT reuse production). Confirm email auto-confirm / `admin.generateLink` is permitted under service_role (default cloud config is fine; no SMTP needed).
    2. Copy `.env.test.example` to `.env.test` and fill in the TEST project's `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY` (Settings → API). Set `ADMIN_PASSWORD=test`.
    3. CRITICAL: ensure the test `NEXT_PUBLIC_SUPABASE_URL` is NOT the production URL (compare against your production .env). Tests writing/deleting `member_teams` against production would corrupt real data.
    4. From Settings → Database, copy the DIRECT connection string (port 5432) for the migration push in Task 2.
  </how-to-verify>
  <verify>
    <automated>test -f .env.test && grep -q 'NEXT_PUBLIC_SUPABASE_URL' .env.test && grep -q 'SUPABASE_SERVICE_ROLE_KEY' .env.test && node -e "const fs=require('fs');const t=fs.readFileSync('.env.test','utf8');if(/<test-(project-ref|anon-key|service-role-key)>/.test(t))throw new Error('.env.test still has placeholder values — fill in real TEST keys');console.log('env.test populated')"</automated>
  </verify>
  <acceptance_criteria>
    - `.env.test` exists at repo root and is gitignored (`git check-ignore .env.test` succeeds)
    - `.env.test` contains real (non-placeholder) `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`
    - The test `NEXT_PUBLIC_SUPABASE_URL` differs from the production Supabase URL (human-confirmed)
  </acceptance_criteria>
  <done>A dedicated TEST Supabase project exists and `.env.test` holds its real (non-placeholder) keys, gitignored, with URL confirmed distinct from production; the direct 5432 connection string is in hand for Task 2.</done>
  <resume-signal>Type "approved" once .env.test is populated with TEST-project keys (and confirmed distinct from production), with the direct 5432 connection string ready for Task 2.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Apply migrations to the TEST project via supabase db push --db-url</name>
  <files>supabase/migrations/</files>
  <what-built>The schema-provisioning step. Claude runs `supabase db push --db-url` but the human supplies the secret direct connection string (contains the DB password) interactively, so this is gated.</what-built>
  <action>Run `supabase db push --db-url "<direct-5432-connection-string-from-Task-1>"` (CLI 2.75.0; `--db-url` applies all files under `supabase/migrations/` in order with history tracking; no `link`, no local Docker per D-06). The connection string contains the DB password (a secret supplied interactively by the operator), so this is gated. After push, run the automated schema-check (verify block) confirming `teams.status` and `member_publications` exist on the TEST project. See how-to-verify for fallback (session pooler) if direct 5432 is unavailable.</action>
  <how-to-verify>
    1. Run `supabase db push --db-url "<direct-5432-connection-string-from-Task-1>"` (CLI 2.75.0; `--db-url` applies all `supabase/migrations/` in order with history tracking; no `link`, no local Docker per D-06). The three migrations are: `20260516_rename_substack_id_to_publication_id.sql`, `20260517_add_team_status.sql`, `20260530_add_member_publications.sql`.
    2. If the direct (5432) connection is unavailable on the plan tier, use the session pooler connection string instead (also works for migrations).
    3. Confirm push reports success and all three migrations applied.
  </how-to-verify>
  <verify>
    <automated>node -e "require('dotenv').config({path:'.env.test'});const{createClient}=require('@supabase/supabase-js');const a=createClient(process.env.NEXT_PUBLIC_SUPABASE_URL,process.env.SUPABASE_SERVICE_ROLE_KEY,{auth:{persistSession:false}});(async()=>{const t=await a.from('teams').select('status').limit(1);if(t.error)throw new Error('teams.status missing: '+t.error.message);const mp=await a.from('member_publications').select('id').limit(1);if(mp.error)throw new Error('member_publications missing: '+mp.error.message);console.log('schema OK on TEST project')})()"</automated>
  </verify>
  <acceptance_criteria>
    - `supabase db push --db-url <test>` completes successfully
    - Querying the TEST project: `teams` has a `status` column and `member_publications` table exists (the verify script selecting both returns no error)
    - The TEST project schema matches `supabase/schema.sql` durable definitions
  </acceptance_criteria>
  <done>All three migrations applied to the TEST project; schema-check confirms `teams.status` and `member_publications` are queryable on the TEST URL. Plan 03 specs can now run against a correct, isolated schema.</done>
  <resume-signal>Type "approved" once `supabase db push` succeeds and the schema-check script confirms teams.status + member_publications on the TEST project.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → Supabase Dashboard/CLI | service_role key + DB connection string (passwords) entered here |
| migration CLI → TEST DB | schema DDL applied; must target TEST, never production |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-03 | Tampering | tests/migrations targeting PRODUCTION data | mitigate | Task 1 requires test URL ≠ prod URL (human-confirmed + placeholder-rejection script); dedicated TEST project (D-04) |
| T-26-02 | Information Disclosure | `.env.test` + DB connection string | mitigate | `.env.test` gitignored (verified via `git check-ignore`); connection string passed only on the CLI invocation, not committed |
| T-26-05 | Denial of Service | `db push` against wrong/prod DB altering schema | mitigate | Direct 5432 string copied from the TEST project dashboard only; schema-check script confirms it landed on the TEST URL |
</threat_model>

<verification>
- `git check-ignore .env.test` exits 0 (file is ignored)
- `.env.test` has real keys, no `<placeholder>` tokens remain
- TEST-project URL ≠ production URL (human-confirmed)
- Schema check: `teams.status` and `member_publications` queryable on the TEST project
</verification>

<success_criteria>
A dedicated, production-isolated TEST Supabase project is live with all migrations applied, and `.env.test` holds its keys (gitignored). Plan 03's specs can now run against a correct schema without risk to production.
</success_criteria>

<output>
Create `.planning/phases/26-e2e-playwright/26-02-SUMMARY.md` when done
</output>
