---
phase: 28-db-my
plan: 02
type: execute
wave: 2
depends_on:
  - 28-01
files_modified:
  - src/app/my/CommitScheduleModal.tsx
  - src/app/my/actions.ts
  - src/app/my/page.tsx
  - src/app/my/__tests__/updateCommitSlotsAction.test.ts
autonomous: false
requirements:
  - SCHED-01
  - SCHED-02
  - SCHED-03

must_haves:
  truths:
    - "D-01: updateCommitSlotsAction が src/app/my/actions.ts に独立した Server Action として実装されている"
    - "D-02: プロフィール保存とスケジュール宣言は独立したボタンと Server Action で処理される"
    - "D-03: CommitScheduleModal.tsx が src/app/my/ に Client Component として存在する"
    - "D-04: スケジュール未設定時は「投稿スケジュールを宣言する」ボタンが表示される"
    - "D-05: スケジュール設定済み時は「週N回 — 月曜 8:00、水曜 20:00…」形式のサマリーテキストが表示される"
    - "D-06: モーダルの開閉状態（isOpen）は CommitScheduleModal 自身の useState で管理される"
    - "D-07: /my/page.tsx がサーバーサイドで member_commit_slots を SELECT し props として CommitScheduleModal に渡す"
    - "D-08: 頻度 <select> で 1〜4 を選択すると曜日+時刻ペアの入力欄が動的に増減する"
    - "D-09: 各スロットに曜日 <select>（月〜日 = 1〜7）が存在する"
    - "D-10: 各スロットに時刻 <select>（00〜23）が存在する"
    - "D-11: 同一曜日を複数選択するとクライアントサイドで警告が表示される"
    - "D-12: 送信ボタンのテキストは「宣言する」である"
    - "D-17: updateCommitSlotsAction が DELETE してから INSERT する全置換パターンで実装されている"
    - "updateCommitSlotsAction のユニットテストが GREEN になる"
  artifacts:
    - path: "src/app/my/CommitScheduleModal.tsx"
      provides: "コミットスケジュール設定モーダル（Client Component）"
      exports: ["CommitScheduleModal"]
    - path: "src/app/my/actions.ts"
      provides: "updateCommitSlotsAction Server Action"
      exports: ["updateCommitSlotsAction"]
    - path: "src/app/my/page.tsx"
      provides: "member.id + commitSlots を CommitScheduleModal に渡す統合"
      contains: "CommitScheduleModal"
    - path: "src/app/my/__tests__/updateCommitSlotsAction.test.ts"
      provides: "GREEN テストスイート"
      exports: ["updateCommitSlotsAction テスト全件"]
  key_links:
    - from: "src/app/my/CommitScheduleModal.tsx"
      to: "src/app/my/actions.ts"
      via: "useActionState(updateCommitSlotsAction, null)"
      pattern: "useActionState.*updateCommitSlotsAction"
    - from: "src/app/my/page.tsx"
      to: "src/app/my/CommitScheduleModal.tsx"
      via: "props: memberId + initialSlots"
      pattern: "CommitScheduleModal.*memberId.*initialSlots"
    - from: "src/app/my/actions.ts"
      to: "member_commit_slots テーブル (Supabase)"
      via: "admin.from('member_commit_slots').delete().eq('member_id', member.id)"
      pattern: "member_commit_slots.*delete.*member_id"
---

<objective>
`CommitScheduleModal` Client Component と `updateCommitSlotsAction` Server Action を実装し、`/my` ページでコミットスケジュールを設定・保存できるようにする。Plan 01 で作成した `member_commit_slots` テーブルに実際にデータを書き込む。

Purpose: SCHED-01（頻度設定）、SCHED-02（曜日+時刻設定）、SCHED-03（DB 永続化）をすべて満たす。
Output: CommitScheduleModal.tsx（新規）、actions.ts 更新、page.tsx 更新、テスト GREEN
</objective>

<execution_context>
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/28-db-my/28-CONTEXT.md
@.planning/phases/28-db-my/28-RESEARCH.md
@.planning/phases/28-db-my/28-PATTERNS.md
@.planning/phases/28-db-my/28-UI-SPEC.md
@.planning/phases/28-db-my/28-VALIDATION.md

<interfaces>
<!-- Key types the executor needs. Extracted from codebase patterns in 28-PATTERNS.md. -->

From src/app/my/actions.ts (既存パターン):
```typescript
'use server'
import { revalidatePath } from 'next/cache'
import { createSupabaseServerClient } from '@/lib/supabase/server'
import { createSupabaseAdminClient } from '@/lib/supabase/admin'

// 既存の Server Action シグネチャ（updateCommitSlotsAction も同形式）
export async function updateMyProfileAction(
  prevState: string | null,
  formData: FormData
): Promise<string | null>
```

From src/app/my/MyProfileForm.tsx (useActionState パターン):
```typescript
'use client'
import { useActionState } from 'react'
// const [state, action, isPending] = useActionState(serverAction, null)
// <form action={action}> で送信
// {state && <p role="alert" className="text-sm text-red-600">{state}</p>}
```

CommitScheduleModal props (新規定義):
```typescript
interface CommitScheduleModalProps {
  memberId: string  // UUID — members.id
  initialSlots: { id: number; day_of_week: number; hour: number }[]
}
```

updateCommitSlotsAction のシグネチャ (新規):
```typescript
export async function updateCommitSlotsAction(
  prevState: string | null,
  formData: FormData
): Promise<string | null>
// formData.get('slots') → JSON.parse → { day_of_week: number; hour: number }[]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: updateCommitSlotsAction を actions.ts に追加し テストを GREEN に</name>
  <files>src/app/my/actions.ts, src/app/my/__tests__/updateCommitSlotsAction.test.ts</files>

  <read_first>
    - src/app/my/actions.ts — 全文。既存の `updateMyProfileAction` の実装を読む（import 群、認証パターン lines 55-67、delete+insert パターン lines 91-113、revalidatePath + return null パターン lines 115-116）
    - src/app/my/__tests__/updateCommitSlotsAction.test.ts — Plan 01 で作成した RED テスト。6 件のテストケースの期待値を確認する
    - .planning/phases/28-db-my/28-PATTERNS.md — 「updateCommitSlotsAction に適用するパターン (合成)」セクション（具体的な実装の全パターン）
    - .planning/phases/28-db-my/28-RESEARCH.md — 「Pitfall 3: Server Action の FormData からのスロット配列 parse」「Pitfall 4: RLS ポリシーと admin client の組み合わせ」
  </read_first>

  <behavior>
    - 正常系: slots=[{day_of_week:1, hour:8}] → delete 実行 → insert 実行 → revalidatePath('/my') → null を返す
    - 空スロット: slots=[] → delete 実行 → insert スキップ → null を返す
    - 未認証: user=null → 'ログインセッションが切れました。再ログインしてください' → DB 操作ゼロ
    - バリデーション: slots.length > 4 → '不正なスロット数です'
    - バリデーション: day_of_week=0 (範囲外) → '曜日の値が不正です'
    - バリデーション: hour=24 (範囲外) → '時刻の値が不正です'
  </behavior>

  <action>
    `src/app/my/actions.ts` の末尾に `updateCommitSlotsAction` を追加する。既存の import 群（'use server', revalidatePath, createSupabaseServerClient, createSupabaseAdminClient）はすでに先頭にあるため追加不要。

    実装の流れ（per D-01, D-17 + 28-PATTERNS.md の合成パターン）:
    1. `formData.get('slots') as string | null` で slots を取り出す。null の場合は 'スロットデータが見つかりません' を返す
    2. `JSON.parse(rawSlots)` で parse。parse 失敗は 'スロットデータが不正です' を返す
    3. バリデーション: `!Array.isArray(slots) || slots.length > 4` → '不正なスロット数です'（per planning_rules: スロット数 <= 4 チェック）
    4. 各スロットの `day_of_week` が 1〜7 の範囲外なら '曜日の値が不正です'（per D-15 ISO 8601）
    5. 各スロットの `hour` が 0〜23 の範囲外なら '時刻の値が不正です'
    6. `createSupabaseServerClient()` → `auth.getUser()` → user が null なら 'ログインセッションが切れました。再ログインしてください'（AUTH_ERROR）
    7. `createSupabaseAdminClient()` → `admin.from('members').select('id').eq('user_id', user.id).single()` → member が null なら '保存に失敗しました。もう一度お試しください'（クライアントから member_id を受け取らない — Pitfall 4）
    8. `admin.from('member_commit_slots').delete().eq('member_id', member.id)` — deleteError なら '保存に失敗しました。もう一度お試しください'
    9. `slots.length > 0` の場合のみ `admin.from('member_commit_slots').insert(slots.map(s => ({ member_id: member.id, day_of_week: s.day_of_week, hour: s.hour })))` — insertError なら '保存に失敗しました。もう一度お試しください'
    10. `revalidatePath('/my')` → `return null`

    実装後、`npm test -- updateCommitSlotsAction` を実行して全テストが GREEN になることを確認する。
  </action>

  <verify>
    <automated>npm test -- updateCommitSlotsAction 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - `grep "export async function updateCommitSlotsAction" src/app/my/actions.ts` が 1 行マッチする
    - `grep "member_commit_slots" src/app/my/actions.ts` が 2 行以上マッチする（delete + insert）
    - `grep "JSON.parse" src/app/my/actions.ts` が 1 行以上マッチする（FormData parse）
    - `grep "slots.length > 4" src/app/my/actions.ts` が 1 行以上マッチする（バリデーション）
    - `grep "day_of_week.*BETWEEN\|day_of_week < 1\|day_of_week > 7" src/app/my/actions.ts` が 1 行以上マッチする
    - `npm test -- updateCommitSlotsAction` が exit 0 で全テスト GREEN になる
    - `npm test` 全体が引き続き GREEN になる
  </acceptance_criteria>

  <done>updateCommitSlotsAction が actions.ts に実装され、6 件のユニットテストが GREEN になる。</done>
</task>

<task type="auto">
  <name>Task 2: CommitScheduleModal.tsx を新規作成</name>
  <files>src/app/my/CommitScheduleModal.tsx</files>

  <read_first>
    - src/app/my/MyProfileForm.tsx — 全文。'use client' 宣言、useActionState パターン、<form action={action}> 構造、role="alert" エラー表示、ボタンの Tailwind クラス（lines 128-135）、label + select パターン（lines 39-49）
    - .planning/phases/28-db-my/28-UI-SPEC.md — Component Inventory セクション全体（modal overlay 構造、frequency select、slot rows、submit button、close behavior、accessibility 要件）
    - .planning/phases/28-db-my/28-PATTERNS.md — 「CommitScheduleModal.tsx 新規 Client Component」セクション（useEffect でのモーダルクローズ・isFirstRender パターン、JSON hidden input パターン、modal overlay 構造）
    - .planning/phases/28-db-my/28-RESEARCH.md — 「Pitfall 6: モーダルで useActionState の formAction を直接呼び出す」「Code Examples: 曜日サマリーテキスト生成・重複曜日バリデーション」
    - .planning/phases/28-db-my/28-CONTEXT.md — D-04〜D-12 の決定事項（モーダルの開閉方法・スロット入力UI・バリデーション・送信ボタンテキスト）
  </read_first>

  <action>
    `src/app/my/CommitScheduleModal.tsx` を新規作成する。`'use client'` で始まる Client Component。

    props インターフェース:
    - `memberId: string`（UUID — 未使用だが props として受け取る。Server Action は auth.getUser() で本人確認するため）
    - `initialSlots: { id: number; day_of_week: number; hour: number }[]`

    状態管理（per D-06, D-08）:
    - `isOpen: boolean` — useState でモーダル開閉
    - `frequency: number` — useState で頻度（1〜4）。initialSlots.length || 1 で初期化
    - `slots: { day_of_week: number; hour: number }[]` — useState で動的スロット配列。initialSlots || デフォルト({day_of_week:1, hour:8}×frequency) で初期化

    `useActionState(updateCommitSlotsAction, null)` で `[state, action, isPending]` を取得。

    成功時クローズ（RESEARCH.md Pitfall 6 回避）:
    ```
    const isFirstRender = useRef(true)
    useEffect(() => {
      if (isFirstRender.current) { isFirstRender.current = false; return }
      if (state === null && !isPending) setIsOpen(false)
    }, [state, isPending])
    ```

    未設定状態（per D-04）: `initialSlots.length === 0` かつ `!isOpen` のとき、「投稿スケジュールを宣言する」ボタンを表示。クラス: `border rounded px-4 py-2 text-sm font-semibold` + border色 `border-[#d8d8d8]` + テキスト色 `text-[#363737]`（UI-SPEC に準拠）。

    設定済み状態（per D-05）: `initialSlots.length > 0` かつ `!isOpen` のとき、`formatSummary(initialSlots)` で生成したサマリーテキストを表示。クラス: `text-sm text-[#363737] cursor-pointer underline-on-hover`。

    `formatSummary` 関数: RESEARCH.md Code Examples のパターン通り。`DAY_LABELS = {1:'月曜', 2:'火曜', ..., 7:'日曜'}`。出力例: 「週2回 — 月曜 8:00、水曜 20:00」。

    モーダルオーバーレイ（UI-SPEC の modal overlay structure）:
    - `<div className="fixed inset-0 z-50 flex items-center justify-center" onClick={() => setIsOpen(false)}>`（overlay、クリックで閉じる）
    - 内側: `<div role="dialog" aria-modal="true" aria-labelledby="commit-schedule-modal-title" onKeyDown={(e) => { if (e.key === 'Escape') setIsOpen(false) }} onClick={(e) => e.stopPropagation()} className="bg-[#fafafa] rounded shadow-xl w-full max-w-sm mx-4 p-6 relative">`
    - ヘッダー: `<h2 id="commit-schedule-modal-title" className="text-lg font-semibold">投稿スケジュールを宣言</h2>` + × ボタン（`aria-label="閉じる"` `className="absolute top-3 right-3 text-gray-400 hover:text-gray-700"`）

    頻度セレクト（per D-08, UI-SPEC）:
    - `<label htmlFor="frequency" className="block text-sm font-semibold mb-1">週の投稿回数</label>`
    - `<select id="frequency" className="border rounded px-3 py-2 text-sm w-full">` — options: 1, 2, 3, 4。onChange で `setFrequency(n)` + slots 配列を M 要素に増減（既存値を保持しつつ末尾に追加またはトリムする）

    スロット行（per D-09, D-10, UI-SPEC）: `slots.map((slot, i) => ...)` で動的レンダリング。
    - `<p className="text-xs text-gray-500 mb-1">スロット {i+1}</p>`
    - 曜日 `<select>`: options は 月(1)〜日(7)（ISO 8601 per D-15）。className: `border rounded px-3 py-2 text-sm flex-1`
    - 時刻 `<select>`: options は 0〜23（表示は `String(h).padStart(2,'0') + ':00'`）。className: `border rounded px-3 py-2 text-sm w-24`
    - onChange で `slots` state を更新

    重複曜日バリデーション（per D-11）:
    - `hasDuplicateDays` 関数: RESEARCH.md Code Examples のパターン通り
    - 重複時に `<p className="text-xs text-red-600">同じ曜日を複数選択しています</p>` を表示
    - 「宣言する」ボタンは無効化しない（D-11: クライアントサイドで警告のみ）

    フォーム（per D-12, UI-SPEC）:
    - `<form action={action}>`
    - `<input type="hidden" name="slots" value={JSON.stringify(slots)} />`
    - state エラー表示: `{state && <p role="alert" className="text-sm text-red-600">{state}</p>}`
    - 送信ボタン: `<button type="submit" disabled={isPending} className="w-full bg-orange-500 text-white rounded px-4 py-2 text-sm font-semibold disabled:opacity-50">{isPending ? '宣言中...' : '宣言する'}</button>`

    アクセシビリティ（UI-SPEC Accessibility セクション）:
    - `aria-labelledby="commit-schedule-modal-title"` をモーダルコンテナに付与
    - 全 `<select>` に `<label htmlFor="...">` + `id` を付与
    - role="alert" は既存パターン通り
  </action>

  <verify>
    <automated>npm test 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - `src/app/my/CommitScheduleModal.tsx` が存在する
    - `grep "'use client'" src/app/my/CommitScheduleModal.tsx` が 1 行マッチする
    - `grep "useActionState.*updateCommitSlotsAction" src/app/my/CommitScheduleModal.tsx` が 1 行マッチする
    - `grep "投稿スケジュールを宣言する" src/app/my/CommitScheduleModal.tsx` が 1 行以上マッチする（per D-04）
    - `grep '宣言する' src/app/my/CommitScheduleModal.tsx` が 1 行以上マッチする（per D-12）
    - `grep 'role="dialog"' src/app/my/CommitScheduleModal.tsx` が 1 行マッチする
    - `grep 'aria-modal="true"' src/app/my/CommitScheduleModal.tsx` が 1 行マッチする
    - `grep 'type="hidden".*name="slots"' src/app/my/CommitScheduleModal.tsx` が 1 行以上マッチする
    - `grep "isFirstRender" src/app/my/CommitScheduleModal.tsx` が 1 行以上マッチする（Pitfall 6 防止）
    - `grep "同じ曜日を複数選択しています" src/app/my/CommitScheduleModal.tsx` が 1 行マッチする（per D-11）
    - `npm test` 全体が GREEN になる
  </acceptance_criteria>

  <done>CommitScheduleModal が UI-SPEC 通りに実装され、未設定→モーダル開→選択→宣言→クローズの完全なフローが機能する。</done>
</task>

<task type="auto">
  <name>Task 3: page.tsx を更新（member.id + commitSlots SELECT + CommitScheduleModal 統合）</name>
  <files>src/app/my/page.tsx</files>

  <read_first>
    - src/app/my/page.tsx — 全文。現在の admin クエリ構造（lines 15-27）と JSX 構造（lines 51-70）を確認する。members SELECT に `id` が含まれていないことを確認する（Pitfall 2）
    - .planning/phases/28-db-my/28-PATTERNS.md — 「page.tsx の変更点 1/2/3」セクション（変更箇所の具体的な前後）
    - .planning/phases/28-db-my/28-UI-SPEC.md — 「/my page integration point」セクション（JSX の追加位置と className）
    - src/app/my/CommitScheduleModal.tsx — 直前のタスクで作成したファイル（props: memberId, initialSlots の型確認）
  </read_first>

  <action>
    `src/app/my/page.tsx` の3箇所を更新する（per D-07, D-13, RESEARCH.md Pitfall 2）。

    変更点 1 — members SELECT に `id` を追加する:
    既存クエリの `.select()` に `id,` を追加する。現在は `name, publication_id, substack_handle, member_teams(...)` だけなので先頭に `id,` を追加する。これにより TypeScript の型推論が `member.id: string` を認識する。

    変更点 2 — commitSlots クエリを追加する:
    member クエリの直後（member が存在する場合のみ実行されるブロック）に以下を追加する:
    `admin.from('member_commit_slots').select('id, day_of_week, hour').eq('member_id', member.id).order('day_of_week')`
    結果を `const commitSlots = commitSlotsData ?? []` に格納する。member が null の場合（未リンク状態）はこのクエリをスキップして `commitSlots = []` とする。

    変更点 3 — JSX に `CommitScheduleModal` を追加する:
    既存の `<MyProfileForm ...>` の後に以下を追加する（UI-SPEC の「/my page integration point」通り）:
    ```
    <div className="mt-8">
      <h2 className="text-sm font-semibold mb-2">投稿スケジュール</h2>
      <CommitScheduleModal memberId={member.id} initialSlots={commitSlots} />
    </div>
    ```
    `CommitScheduleModal` を `import CommitScheduleModal from './CommitScheduleModal'` で import する。

    注意: `CommitScheduleModal` は member が存在する場合のみレンダリングする（`!member` の場合は `<LinkMemberForm>` のみ表示する既存ロジックに統合する）。
  </action>

  <verify>
    <automated>npm run build 2>&1 | tail -30</automated>
  </verify>

  <acceptance_criteria>
    - `grep "id," src/app/my/page.tsx` が 1 行以上マッチする（members SELECT への id 追加）
    - `grep "member_commit_slots" src/app/my/page.tsx` が 1 行以上マッチする（commitSlots クエリ）
    - `grep "CommitScheduleModal" src/app/my/page.tsx` が 2 行以上マッチする（import + 使用）
    - `grep "memberId={member.id}" src/app/my/page.tsx` が 1 行マッチする
    - `grep "initialSlots={commitSlots}" src/app/my/page.tsx` が 1 行マッチする
    - `grep "投稿スケジュール" src/app/my/page.tsx` が 1 行マッチする（セクション見出し）
    - `npm run build` が TypeScript エラーなしで exit 0 で完了する
    - `npm test` 全体が GREEN になる
  </acceptance_criteria>

  <done>page.tsx が member.id を SELECT し、commitSlots を取得して CommitScheduleModal に渡す。`npm run build` が通り、/my ページがコミットスケジュール設定セクション付きでレンダリングされる。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4 [BLOCKING]: /my ページでコミットスケジュール設定の動作確認</name>

  <what-built>
    Task 1: `updateCommitSlotsAction` を `actions.ts` に実装し、ユニットテストが GREEN になった。
    Task 2: `CommitScheduleModal.tsx` を新規作成した（モーダルUI + 動的スロット + バリデーション）。
    Task 3: `page.tsx` を更新し（member.id + commitSlots + CommitScheduleModal）、ビルドが通った。
  </what-built>

  <how-to-verify>
    1. 開発サーバーを起動する: `npm run dev`
    2. ブラウザで http://localhost:3000/my にアクセスする（ログイン済みであること）
    3. **スケジュール未設定の確認:**
       - ページ下部に「投稿スケジュール」セクションが表示されることを確認する
       - 「投稿スケジュールを宣言する」ボタンが表示されることを確認する
    4. **モーダルを開く:**
       - ボタンをクリックしてモーダルが開くことを確認する
       - 「投稿スケジュールを宣言」というタイトルが表示されることを確認する
       - 「週の投稿回数」セレクトが表示されることを確認する
    5. **動的スロット増減の確認:**
       - 頻度を「2」に変更すると曜日+時刻ペアが 2 行表示されることを確認する
       - 「3」に変更すると 3 行になることを確認する
    6. **重複曜日バリデーション:**
       - 2 つのスロットで同じ曜日を選択すると「同じ曜日を複数選択しています」と表示されることを確認する
    7. **保存（宣言）:**
       - 曜日と時刻を設定して「宣言する」ボタンをクリックする
       - ボタンが「宣言中...」に変わり、完了後にモーダルが閉じることを確認する
       - ページをリロードし、設定したスケジュール（例: 「週1回 — 月曜 8:00」）が表示されることを確認する
    8. **設定済みサマリー表示と編集:**
       - 設定済みサマリーテキストをクリックしてモーダルが開くことを確認する
       - 設定済みの曜日・時刻が反映されていることを確認する
    9. **モーダルクローズ:**
       - × ボタンでモーダルが閉じることを確認する
       - オーバーレイ（モーダル外）をクリックしてモーダルが閉じることを確認する
       - Escape キーでモーダルが閉じることを確認する

    **Supabase Dashboard 確認:**
    - https://supabase.com/dashboard/project/xolhjcngrwwwqtklmoyk → Table Editor → `member_commit_slots`
    - 保存後に自分のスロットデータが登録されていることを確認する
  </how-to-verify>

  <resume-signal>
    すべての動作を確認できたら「approved」と入力してください。問題がある場合は症状を報告してください。
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Server Action | FormData の slots 値はクライアントから来るため信頼できない。Server Action 側でバリデーションと認証が必須 |
| Server Action → Supabase | admin client (service_role) は BYPASSRLS。Server Action 内で user_id → member.id を解決して本人スコープを強制する |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01 | Tampering | updateCommitSlotsAction — member_id なりすまし | mitigate | `auth.getUser()` → `.eq('user_id', user.id)` で member.id を解決。FormData から member_id を受け取らない（Pitfall 4）|
| T-28-02 | Tampering | updateCommitSlotsAction — 範囲外 day_of_week/hour | mitigate | Server Action 内で `day_of_week < 1 or > 7` と `hour < 0 or > 23` をチェック。DB の CHECK 制約がバックストップ |
| T-28-03 | Denial of Service | updateCommitSlotsAction — 大量スロット INSERT | mitigate | `slots.length > 4` チェックで '不正なスロット数です' を返す |
| T-28-04 | Tampering | CommitScheduleModal — JSON hidden input の改ざん | mitigate | hidden input の値はクライアントで JSON.stringify するが、Server Action で再バリデーションする（D-11 クライアントバリデーションのみでは不十分な部分をサーバーで補完）|
| T-28-SC | Tampering | npm/pip/cargo installs | accept | このプランで外部パッケージのインストールなし |
</threat_model>

<verification>
## Phase 28 Plan 02 完了確認

- [ ] `src/app/my/CommitScheduleModal.tsx` が存在し 'use client' + useActionState + role="dialog" を含む
- [ ] `src/app/my/actions.ts` に `updateCommitSlotsAction` が追加されている
- [ ] `npm test -- updateCommitSlotsAction` が全件 GREEN になる
- [ ] `npm test` 全体が GREEN になる
- [ ] `npm run build` が TypeScript エラーなしで通る
- [ ] /my ページでモーダルが開き、スロットを設定して「宣言する」で保存できる
- [ ] 保存後のリロードでサマリーテキストが表示される
- [ ] Supabase Dashboard で `member_commit_slots` にデータが保存されている
</verification>

<success_criteria>
1. SCHED-01: /my ページで週1〜4の頻度を選択でき、選択に合わせた数の入力欄が表示される
2. SCHED-02: 各スロットで曜日（ISO 8601 1〜7）と時刻（0〜23）を設定できる
3. SCHED-03: 「宣言する」ボタンで DB に保存され、ページリロード後も設定が維持される
4. ユニットテスト 6 件が GREEN になり `npm test` 全体が GREEN になる
5. TypeScript ビルド (`npm run build`) がエラーなしで通る
</success_criteria>

<output>
Create `.planning/phases/28-db-my/28-02-SUMMARY.md` when done
</output>
