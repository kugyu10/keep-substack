---
phase: 28-db-my
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260602000001_add_member_commit_slots.sql
  - supabase/schema.sql
  - src/app/my/__tests__/updateCommitSlotsAction.test.ts
autonomous: false
requirements:
  - SCHED-03

must_haves:
  truths:
    - "member_commit_slots テーブルが Supabase prod DB に存在する"
    - "member_id は UUID 型で members.id への FK 制約が成立する"
    - "RLS が有効で public SELECT ポリシーと本人書き込みポリシーが登録されている"
    - "supabase/schema.sql が member_commit_slots の最終状態を反映している"
    - "updateCommitSlotsAction のユニットテストが RED 状態で存在する（Wave 0）"
  artifacts:
    - path: "supabase/migrations/20260602000001_add_member_commit_slots.sql"
      provides: "CREATE TABLE member_commit_slots DDL + RLS ポリシー"
      contains: "member_id UUID NOT NULL REFERENCES members(id) ON DELETE CASCADE"
    - path: "supabase/schema.sql"
      provides: "全テーブル定義の最終状態ミラー"
      contains: "CREATE TABLE IF NOT EXISTS member_commit_slots"
    - path: "src/app/my/__tests__/updateCommitSlotsAction.test.ts"
      provides: "Wave 0 テストスキャフォールド（RED）"
      exports: ["updateCommitSlotsAction テストスイート"]
  key_links:
    - from: "supabase/migrations/20260602000001_add_member_commit_slots.sql"
      to: "supabase prod DB (xolhjcngrwwwqtklmoyk)"
      via: "supabase db push"
      pattern: "member_commit_slots.*member_id.*UUID"
    - from: "supabase/schema.sql"
      to: "supabase/migrations/20260602000001_add_member_commit_slots.sql"
      via: "内容同期 (Phase 26 確立ルール)"
      pattern: "CREATE TABLE IF NOT EXISTS member_commit_slots"
---

<objective>
`member_commit_slots` テーブルを Supabase prod DB に追加する。migration ファイル作成・schema.sql 同期・supabase db push の3ステップで完結する。

Purpose: Phase 29 の CommitGoalView がこのテーブルを SELECT するため、Plan 02 より先に DB 構造を確定させる。
Output: migration ファイル、更新済み schema.sql、Wave 0 テストスキャフォールド（RED）
</objective>

<execution_context>
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/28-db-my/28-CONTEXT.md
@.planning/phases/28-db-my/28-RESEARCH.md
@.planning/phases/28-db-my/28-PATTERNS.md
@.planning/phases/28-db-my/28-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (Wave 0): updateCommitSlotsAction テストスキャフォールドを作成（RED）</name>
  <files>src/app/my/__tests__/updateCommitSlotsAction.test.ts</files>

  <read_first>
    - src/app/my/__tests__/updateMyProfileAction.test.ts — mock 構造全体（vi.mock 宣言・setupAdminMock・makeFormData・beforeEach パターン）を転用する
    - .planning/phases/28-db-my/28-PATTERNS.md — 「updateCommitSlotsAction.test.ts 向けの admin mock 構造」セクション（具体的な setupAdminMock と makeFormData 実装）
    - .planning/phases/28-db-my/28-VALIDATION.md — Per-Task Verification Map の Wave 0 テスト要件（SCHED-01/02/03 のケース）
  </read_first>

  <behavior>
    - テスト 1 (SCHED-03): `updateCommitSlotsAction(null, makeFormData([{day_of_week:1, hour:8}]))` が認証済みユーザーで delete → insert を実行し null を返す
    - テスト 2 (SCHED-03): 未認証時（user: null）は 'ログインセッションが切れました。再ログインしてください' を返し、DB 操作ゼロ
    - テスト 3 (SCHED-01/02): slots が空配列のとき delete のみ実行し insert はスキップする
    - テスト 4 (SCHED-01): slots.length > 4 のとき '不正なスロット数です' を返す（バリデーション）
    - テスト 5 (SCHED-02): day_of_week が 1〜7 の範囲外（例: 0 や 8）のとき '曜日の値が不正です' を返す
    - テスト 6 (SCHED-02): hour が 0〜23 の範囲外（例: 24）のとき '時刻の値が不正です' を返す
  </behavior>

  <action>
    `src/app/my/__tests__/updateCommitSlotsAction.test.ts` を新規作成する。

    import は `updateMyProfileAction.test.ts` の冒頭と同一パターン（vi.mock 3本: next/cache, @/lib/supabase/server, @/lib/supabase/admin）を使う。

    `setupAdminMock` 関数は 28-PATTERNS.md の「updateCommitSlotsAction.test.ts 向けの admin mock 構造」をそのまま転用する。テーブルは `members`（select → eq → single）と `member_commit_slots`（delete → eq, insert）の2つのみ。

    `makeFormData` 関数: `slots: { day_of_week: number; hour: number }[]` を受け取り、`fd.append('slots', JSON.stringify(slots))` で FormData を構築して返す。

    定数: `MEMBER_ID = 'test-member-uuid'`、`AUTH_ERROR = 'ログインセッションが切れました。再ログインしてください'`。

    `beforeEach`: `vi.clearAllMocks()` + `mockGetUser.mockResolvedValue({ data: { user: { id: 'u1' } } })`。

    このファイル作成時点では `updateCommitSlotsAction` が `src/app/my/actions.ts` に存在しないため、`import { updateCommitSlotsAction } from '../actions'` は TypeScript エラーになる。これは意図的な RED 状態であり、`// TODO: implement in Plan 02` コメントを追加しておく。`npm test` を実行して import エラーまたはテスト失敗で RED であることを確認する。
  </action>

  <verify>
    <automated>npm test -- updateCommitSlotsAction 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - `src/app/my/__tests__/updateCommitSlotsAction.test.ts` が存在する
    - ファイルに `vi.mock('next/cache'`、`vi.mock('@/lib/supabase/server'`、`vi.mock('@/lib/supabase/admin'` の3行が含まれる
    - `setupAdminMock` 関数が定義されている
    - `makeFormData` 関数が定義されている
    - `describe('updateCommitSlotsAction'` ブロックに 6 件以上の `it(` が含まれる
    - `npm test -- updateCommitSlotsAction` がテスト失敗または import エラーで終了（RED 確認）
  </acceptance_criteria>

  <done>Wave 0 テストスキャフォールドが RED 状態で存在し、Plan 02 の実装後に GREEN になる準備が整っている。</done>
</task>

<task type="auto">
  <name>Task 2: member_commit_slots migration ファイルを作成</name>
  <files>supabase/migrations/20260602000001_add_member_commit_slots.sql</files>

  <read_first>
    - supabase/migrations/20260602000000_add_substack_handle.sql — BEGIN/COMMIT ラップの形式（直近 migration）
    - supabase/migrations/20260531000000_consolidated_schema.sql — DROP POLICY IF EXISTS + CREATE POLICY パターン（RLS ポリシー）
    - supabase/schema.sql lines 9-17 — `members.id UUID PRIMARY KEY DEFAULT gen_random_uuid()` を確認（FK 型が UUID であることを証明）
    - .planning/phases/28-db-my/28-PATTERNS.md — 「Phase 28 の DDL 全体 (上記パターン合成)」セクション
  </read_first>

  <action>
    `supabase/migrations/20260602000001_add_member_commit_slots.sql` を新規作成する。

    構造は BEGIN/COMMIT ラップ（直近 migration と同形式）。

    DDL 内容（per D-13, D-14 修正済み, D-15, D-16, D-18）:
    - `CREATE TABLE IF NOT EXISTS member_commit_slots` — テーブル名は D-13 で確定
    - カラム: `id BIGINT GENERATED ALWAYS AS IDENTITY PRIMARY KEY`、`member_id UUID NOT NULL REFERENCES members(id) ON DELETE CASCADE`（CRITICAL: BIGINT ではなく UUID — members.id が UUID であるため）、`day_of_week INT NOT NULL CHECK (day_of_week BETWEEN 1 AND 7)`（ISO 8601、per D-15）、`hour INT NOT NULL CHECK (hour BETWEEN 0 AND 23)`
    - D-16: UNIQUE 制約なし（UIバリデーションのみ）
    - `ALTER TABLE member_commit_slots ENABLE ROW LEVEL SECURITY`
    - RLS ポリシー: `DROP POLICY IF EXISTS "public select member_commit_slots"` の後に `CREATE POLICY "public select member_commit_slots" ON member_commit_slots FOR SELECT USING (true)` を定義
    - RLS ポリシー: `DROP POLICY IF EXISTS "member write own commit slots"` の後に `CREATE POLICY "member write own commit slots" ON member_commit_slots FOR ALL USING (member_id = (SELECT id FROM members WHERE user_id = auth.uid())) WITH CHECK (...)` を定義（per D-18）

    28-PATTERNS.md の「Phase 28 の DDL 全体 (上記パターン合成)」の SQL をそのまま使用する。
  </action>

  <verify>
    <automated>grep -c "member_id.*UUID.*NOT NULL.*REFERENCES members" /Users/kugyu10/work/keep-substack/supabase/migrations/20260602000001_add_member_commit_slots.sql</automated>
  </verify>

  <acceptance_criteria>
    - `supabase/migrations/20260602000001_add_member_commit_slots.sql` が存在する
    - `grep "member_id.*UUID.*NOT NULL.*REFERENCES members"` で 1 行以上マッチする（BIGINT でないことの確認）
    - ファイルに `BEGIN;` と `COMMIT;` が含まれる
    - `CHECK (day_of_week BETWEEN 1 AND 7)` が含まれる（ISO 8601 per D-15）
    - `CHECK (hour BETWEEN 0 AND 23)` が含まれる
    - `ENABLE ROW LEVEL SECURITY` が含まれる
    - `DROP POLICY IF EXISTS "public select member_commit_slots"` が含まれる
    - `DROP POLICY IF EXISTS "member write own commit slots"` が含まれる
    - `auth.uid()` を使う書き込みポリシーが含まれる（per D-18）
  </acceptance_criteria>

  <done>migration ファイルが正しい UUID FK と RLS ポリシーを含んで存在する。</done>
</task>

<task type="auto">
  <name>Task 3: supabase/schema.sql に member_commit_slots を追加</name>
  <files>supabase/schema.sql</files>

  <read_first>
    - supabase/schema.sql — 全文を読む。`member_publications` テーブル定義（最後のテーブル）と `-- 2. Row Level Security` セクションの境界を確認し、挿入位置を特定する。既存の RLS セクションのパターン（ALTER TABLE + CREATE POLICY）を確認する
    - supabase/migrations/20260602000001_add_member_commit_slots.sql — 直前のタスクで作成した DDL（schema.sql に追加する内容と一致させる）
    - .planning/phases/28-db-my/28-PATTERNS.md — 「schema.sql の変更点」セクション（追加位置と既存パターン）
  </read_first>

  <action>
    `supabase/schema.sql` を更新する。Phase 26 P02 で確立されたルール: migration と schema.sql は常に同期する。

    変更点 1 — テーブル定義を追加する:
    `member_publications` テーブル定義の直後（`-- 2. Row Level Security` セクションの直前）に `CREATE TABLE IF NOT EXISTS member_commit_slots (...)` を挿入する。DDL は migration ファイルの CREATE TABLE 部分と同一内容（id/member_id/day_of_week/hour）。

    変更点 2 — RLS 有効化を追加する:
    既存の `ALTER TABLE member_publications ENABLE ROW LEVEL SECURITY;` の直後に `ALTER TABLE member_commit_slots ENABLE ROW LEVEL SECURITY;` を追加する。

    変更点 3 — RLS ポリシーを追加する:
    既存のポリシー定義群の末尾に、migration と同内容の `CREATE POLICY "public select member_commit_slots"` と `CREATE POLICY "member write own commit slots"` を追加する。schema.sql では `DROP POLICY IF EXISTS` は不要（冪等性はすでに `IF NOT EXISTS` で確保）。

    注意: schema.sql は最終状態のミラーであり incremental diff ではない。migration の BEGIN/COMMIT ラップは不要。
  </action>

  <verify>
    <automated>grep -c "CREATE TABLE IF NOT EXISTS member_commit_slots" /Users/kugyu10/work/keep-substack/supabase/schema.sql</automated>
  </verify>

  <acceptance_criteria>
    - `grep "CREATE TABLE IF NOT EXISTS member_commit_slots" supabase/schema.sql` が 1 行マッチする
    - `grep "member_id.*UUID.*NOT NULL.*REFERENCES members" supabase/schema.sql` が 1 行以上マッチする
    - `grep "ALTER TABLE member_commit_slots ENABLE ROW LEVEL SECURITY" supabase/schema.sql` が 1 行マッチする
    - `grep '"public select member_commit_slots"' supabase/schema.sql` が 1 行マッチする
    - `grep '"member write own commit slots"' supabase/schema.sql` が 1 行マッチする
  </acceptance_criteria>

  <done>schema.sql が migration ファイルと同期し、member_commit_slots の完全な定義（テーブル + RLS）を含む。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4 [BLOCKING]: supabase db push でテーブルを prod DB に適用</name>

  <what-built>
    Task 2 で `supabase/migrations/20260602000001_add_member_commit_slots.sql` を作成した。
    Task 3 で `supabase/schema.sql` を更新した。
  </what-built>

  <how-to-verify>
    1. プロジェクトルートで以下を実行する:
       ```
       supabase db push
       ```
    2. 出力に `member_commit_slots` のマイグレーションが適用されたメッセージが表示されること（またはエラーなしで完了すること）を確認する
    3. Supabase Dashboard (https://supabase.com/dashboard/project/xolhjcngrwwwqtklmoyk) → Table Editor で `member_commit_slots` テーブルが存在することを確認する
    4. カラムが `id (int8)`, `member_id (uuid)`, `day_of_week (int4)`, `hour (int4)` であることを確認する
    5. Authentication → RLS タブで `member_commit_slots` の RLS が有効で 2 つのポリシーが登録されていることを確認する

    **エラーが出た場合:**
    - `foreign key constraint` エラー → migration の `member_id` が UUID になっているか確認（BIGINT のままだとエラー）
    - `already exists` エラー → `IF NOT EXISTS` ガードを確認
  </how-to-verify>

  <resume-signal>
    `supabase db push` が成功し、Dashboard で `member_commit_slots` テーブルが確認できたら「approved」と入力してください。エラーが出た場合は内容を貼り付けてください。
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration DDL → prod DB | DDL の型不一致（UUID vs BIGINT）は FK 制約エラーでブロックされる |
| Supabase RLS | service_role（admin client）は BYPASSRLS のため RLS ポリシーは直接書き込みを防ぐ直接の防壁にはならない。防壁は Server Action 側の user_id スコープ |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01 | Tampering | migration FK 型 | mitigate | `member_id UUID NOT NULL REFERENCES members(id)` — BIGINT FK は PostgreSQL が拒否する |
| T-28-02 | Tampering | day_of_week/hour 範囲外値 | mitigate | `CHECK (day_of_week BETWEEN 1 AND 7)` + `CHECK (hour BETWEEN 0 AND 23)` を DDL に定義 |
| T-28-SC | Tampering | npm/pip/cargo installs | accept | このプランで外部パッケージのインストールなし |
</threat_model>

<verification>
## Phase 28 Plan 01 完了確認

- [ ] `supabase/migrations/20260602000001_add_member_commit_slots.sql` が存在し `member_id UUID NOT NULL REFERENCES members(id)` を含む
- [ ] `supabase/schema.sql` に `CREATE TABLE IF NOT EXISTS member_commit_slots` が追加されている
- [ ] `supabase db push` が exit 0 で完了した
- [ ] Supabase Dashboard で `member_commit_slots` テーブルと 2 つの RLS ポリシーが確認できる
- [ ] `src/app/my/__tests__/updateCommitSlotsAction.test.ts` が存在し RED 状態（Plan 02 で GREEN になる）
- [ ] `npm test` が既存テストを壊さずにパスする（Wave 0 テストは RED 可）
</verification>

<success_criteria>
1. `member_commit_slots` テーブルが Supabase prod DB に存在し、RLS が有効で 2 つのポリシー（public SELECT + 本人書き込み）が登録されている
2. `member_id` は `UUID` 型で `members(id)` への FK 制約が成立している
3. `supabase/schema.sql` が migration と同期しており `member_commit_slots` の完全な定義を含む
4. `updateCommitSlotsAction.test.ts` が RED 状態で存在し、Plan 02 完了後に GREEN になる準備が整っている
</success_criteria>

<output>
Create `.planning/phases/28-db-my/28-01-SUMMARY.md` when done
</output>
