# Plan 16-01: ポップオーバー刷新（ダーク化 + 横並びレイアウト）

**Phase:** 16 — ポップオーバー刷新
**Plan:** 16-01
**Status:** pending

## Objective

`src/components/HeatmapTooltip.tsx` のポップオーバーを刷新する。
- 背景をダーク（zinc-800）・白テキスト・強い影に変更する（POPOVER-02）
- 記事リストをサムネイル左 + 2行タイトル右の横並びに変更する（POPOVER-01）
- × ボタンのカラーをダークテーマに合わせる（POPOVER-04 polishing）
- Click Outside に `touchstart` を追加してモバイルタップにも対応する（POPOVER-03 enhancement）

## Requirements Addressed

- POPOVER-01: 縦並びレイアウトを廃止し、横並びリスト（サムネイル＋2行タイトル）に刷新する
- POPOVER-02: ダークモード化する（zinc-800背景、白テキスト、強い影）
- POPOVER-03: Click Outsideでポップオーバーを閉じられるようにする（既存実装 + touchstart追加）
- POPOVER-04: 右上に「×」ボタンを配置する（既存実装 + カラー調整）

## Pre-implementation Notes

- POPOVER-03 (Click Outside): `mousedown` リスナーは既に実装済み。`touchstart` を追加してモバイル対応を強化する
- POPOVER-04 (× ボタン): 既に実装済み。ダークテーマに合わせてカラーを調整する
- `line-clamp-2` は Tailwind v4 の flex 内で効かない既知問題のため、inline style で対応する

## Success Criteria

1. ポップオーバーの背景が `bg-zinc-800` のダーク表示になること
2. ポップオーバー内テキストが白（`text-white`）で表示されること
3. 各記事がサムネイル（左）＋2行タイトル（右）の横並びで表示されること
4. `touchstart` リスナーが `mousedown` と共に追加されていること
5. `npm run build` がエラーなく完了すること

## Tasks

### Task 1: ポップオーバー本体をダーク化する（POPOVER-02）

**File:** `src/components/HeatmapTooltip.tsx`

ポップオーバーコンテナの背景・影を変更する。

**Before:**
```tsx
          <div className="bg-white border border-gray-200 rounded shadow-lg p-2 min-w-[220px] max-w-xs relative">
```

**After:**
```tsx
          <div className="bg-zinc-800 rounded shadow-2xl p-2 min-w-[220px] max-w-xs relative">
```

---

### Task 2: × ボタンのカラーをダークテーマに合わせる（POPOVER-04）

**File:** `src/components/HeatmapTooltip.tsx`

× ボタンのテキストカラーをダーク背景に合わせて変更する。

**Before:**
```tsx
              className="absolute top-1 right-1 text-gray-400 hover:text-gray-600 text-xs"
```

**After:**
```tsx
              className="absolute top-1 right-1 text-zinc-400 hover:text-white text-xs"
```

---

### Task 3: 記事リストを横並びレイアウトに変更する（POPOVER-01）

**File:** `src/components/HeatmapTooltip.tsx`

各記事の `<li>` 内を縦並び（画像上・タイトル下）から横並び（サムネイル左・2行タイトル右）に変更する。

**Before:**
```tsx
                <li key={i} className="mb-3 last:mb-0">
                  <a
                    href={withUtm(article.link ?? '#')}
                    target="_blank"
                    rel="noopener noreferrer"
                    className="block"
                  >
                    {(article.thumbnail || imageUrl) && (
                      <img
                        src={article.thumbnail ?? imageUrl!}
                        alt=""
                        className="w-full rounded mb-1 object-cover max-h-24"
                        style={!article.thumbnail ? { maskImage: 'linear-gradient(to bottom, black 50%, transparent 100%)' } : undefined}
                      />
                    )}
                    <span className="text-xs text-blue-600 hover:underline break-words block">
                      {article.title}
                    </span>
                  </a>
                </li>
```

**After:**
```tsx
                <li key={i} className="mb-2 last:mb-0">
                  <a
                    href={withUtm(article.link ?? '#')}
                    target="_blank"
                    rel="noopener noreferrer"
                    className="flex items-start gap-2"
                  >
                    {(article.thumbnail || imageUrl) && (
                      <img
                        src={article.thumbnail ?? imageUrl!}
                        alt=""
                        className="w-12 h-12 rounded object-cover shrink-0"
                      />
                    )}
                    <span
                      className="text-xs text-white hover:underline flex-1 min-w-0"
                      style={{ overflow: 'hidden', display: '-webkit-box', WebkitBoxOrient: 'vertical', WebkitLineClamp: 2 }}
                    >
                      {article.title}
                    </span>
                  </a>
                </li>
```

注意:
- `w-full rounded mb-1 object-cover max-h-24` → `w-12 h-12 rounded object-cover shrink-0`（固定サイズに変更）
- `maskImage` の inline style は不要になるため削除
- `text-blue-600` → `text-white`（ダークテーマ対応）
- `line-clamp-2` は既知問題のため webkit inline style で対応

---

### Task 4: touchstart を Click Outside に追加する（POPOVER-03 enhancement）

**File:** `src/components/HeatmapTooltip.tsx`

`mousedown` に加えて `touchstart` もリッスンしてモバイルタップでポップオーバーを閉じられるようにする。

**Before:**
```tsx
  useEffect(() => {
    if (!open) return
    function handleClickOutside(e: MouseEvent) {
      if (cellRef.current && !cellRef.current.contains(e.target as Node)) {
        setOpen(false)
      }
    }
    document.addEventListener('mousedown', handleClickOutside)
    return () => document.removeEventListener('mousedown', handleClickOutside)
  }, [open])
```

**After:**
```tsx
  useEffect(() => {
    if (!open) return
    function handleClickOutside(e: MouseEvent | TouchEvent) {
      const target = e instanceof TouchEvent ? e.touches[0]?.target : (e as MouseEvent).target
      if (cellRef.current && target instanceof Node && !cellRef.current.contains(target)) {
        setOpen(false)
      }
    }
    document.addEventListener('mousedown', handleClickOutside as EventListener)
    document.addEventListener('touchstart', handleClickOutside as EventListener)
    return () => {
      document.removeEventListener('mousedown', handleClickOutside as EventListener)
      document.removeEventListener('touchstart', handleClickOutside as EventListener)
    }
  }, [open])
```

---

## Verification

実装後に以下を確認する:

- [ ] `src/components/HeatmapTooltip.tsx` に `bg-zinc-800` が1件あること
- [ ] `src/components/HeatmapTooltip.tsx` に `bg-white` が0件であること
- [ ] `src/components/HeatmapTooltip.tsx` に `flex items-start gap-2` が1件あること（横並びレイアウト）
- [ ] `src/components/HeatmapTooltip.tsx` に `touchstart` が1件あること
- [ ] `src/components/HeatmapTooltip.tsx` に `WebkitLineClamp` が1件あること（2行truncate）
- [ ] `npm run build` がエラーなく完了すること

## Notes

- `HeatmapRow.tsx` は変更しない（`HeatmapTooltip` の呼び出し方は変わらない）
- `maskImage` の gradient style は横並びレイアウトでは不要なため削除する
- `shadow-2xl` は Tailwind のデフォルトユーティリティで動作する
- TypeScript: `TouchEvent` はブラウザ標準型、追加 import 不要
