---
phase: 06-admin-team-filter
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01-PLAN.md
files_modified:
  - src/app/admin/actions.ts
  - src/app/admin/page.tsx
  - src/app/admin/AdminAddForm.tsx
  - src/app/admin/AdminMemberList.tsx
  - src/app/page.tsx
autonomous: true
requirements:
  - ADM-01
  - ADM-02
  - ADM-03
  - HEAT-04

must_haves:
  truths:
    - "/admin ページにメンバー一覧（name, substackId, teamId, addedAt）が表形式で表示される"
    - "メンバー追加フォーム（name + substackId + teamId）が存在し、送信するとKVに追加される"
    - "削除ボタンをクリックするとwindow.confirm()が表示され、確認後にKVから削除される"
    - "追加・削除後にrevalidatePath('/admin')でページが即時再レンダリングされる"
    - "エラー時（重複substackId等）はフォーム下にエラーメッセージが表示される"
    - "トップページにAll + 各チームのタブUIが表示される"
    - "/?team=teamIdでアクセスするとそのチームのメンバーのみのヒートマップが表示される"
  artifacts:
    - path: "src/app/admin/actions.ts"
      provides: "Server Actions: addMemberAction, deleteMemberAction"
      exports:
        - addMemberAction
        - deleteMemberAction
    - path: "src/app/admin/page.tsx"
      provides: "管理画面 Server Component"
      contains: "getMembers"
    - path: "src/app/admin/AdminAddForm.tsx"
      provides: "メンバー追加フォーム Client Component"
      contains: "useActionState"
    - path: "src/app/admin/AdminMemberList.tsx"
      provides: "メンバー一覧 + 削除ボタン Client Component"
      contains: "window.confirm"
    - path: "src/app/page.tsx"
      provides: "チームタブUI + searchParamsフィルタリング"
      contains: "searchParams"
  key_links:
    - from: "src/app/admin/AdminAddForm.tsx"
      to: "src/app/admin/actions.ts"
      via: "useActionState(addMemberAction, null)"
      pattern: "Server Actions"
    - from: "src/app/admin/AdminMemberList.tsx"
      to: "src/app/admin/actions.ts"
      via: "deleteMemberAction.bind(null, substackId)"
      pattern: "Server Actions"
    - from: "src/app/page.tsx"
      to: "src/lib/kvMembers.ts"
      via: "getMembers() + teamId filter"
      pattern: "searchParams filtering"
---

<objective>
Phase 6の管理UIとチームフィルターを実装する。

Purpose: Plan 01で確立したKV操作関数（addMember/deleteMember）とBasic認証（middleware.ts）を前提に、/adminページとトップページのチームフィルタータブを実装する。
Output:
- src/app/admin/actions.ts（新規: Server Actions）
- src/app/admin/page.tsx（新規: 管理画面 Server Component）
- src/app/admin/AdminAddForm.tsx（新規: 追加フォーム Client Component）
- src/app/admin/AdminMemberList.tsx（新規: 一覧 + 削除 Client Component）
- src/app/page.tsx（更新: チームタブUI + searchParamsフィルタリング）
</objective>

<execution_context>
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/kugyu10/work/keep-substack/.planning/PROJECT.md
@/Users/kugyu10/work/keep-substack/.planning/ROADMAP.md
@/Users/kugyu10/work/keep-substack/.planning/STATE.md

<interfaces>
<!-- 既存コード — executorはこれを参照して実装する。再探索不要。 -->

From src/lib/kvMembers.ts (Plan 01完了後):
```typescript
export async function getMembers(): Promise<Member[]>
export async function addMember(member: Omit<Member, 'addedAt'>): Promise<void>
export async function deleteMember(substackId: string): Promise<void>
```

From src/lib/types.ts:
```typescript
export type Member = {
  name: string
  substackId: string
  teamId: string
  addedAt: string  // ISO 8601
}
```

From src/app/page.tsx (現状):
```typescript
import { getMembers } from '@/lib/kvMembers'
import { fetchAllFeedsCached } from '@/lib/fetchFeed'
import { getRecentDays, sortByWeeklyCount } from '@/lib/heatmapUtils'
import WeeklyHeatmapGrid from '@/components/WeeklyHeatmapGrid'

export default async function Home() {
  const members = await getMembers()
  const results = await fetchAllFeedsCached(members)
  const dates = getRecentDays()
  const sorted = sortByWeeklyCount(results, dates)

  return (
    <main className="max-w-[600px] mx-auto p-6 pb-64">
      <h1 className="text-2xl font-semibold mb-6">Keep Substack</h1>
      <WeeklyHeatmapGrid results={sorted} dates={dates} />
    </main>
  )
}
```

From src/components/WeeklyHeatmapGrid.tsx (参照パターン):
- props: `{ results: MemberFeedResult[], dates: string[] }` を受け取るServer/Client Component
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: src/app/admin/actions.tsを新規作成する（Server Actions）</name>
  <files>src/app/admin/actions.ts</files>
  <action>
`src/app/admin/actions.ts` を新規作成する。`'use server'` ディレクティブを先頭に記述する。

- **addMemberAction**: フォームデータを受け取り、addMember()を呼び出す。成功時はrevalidatePath('/admin')。エラー時はエラーメッセージ文字列を返す。
- **deleteMemberAction**: substackIdを受け取り、deleteMember()を呼び出す。成功時はrevalidatePath('/admin')。

`useActionState` との連携のため、addMemberActionは `(prevState: string | null, formData: FormData) => Promise<string | null>` シグネチャとする。

```typescript
'use server'

import { revalidatePath } from 'next/cache'
import { addMember, deleteMember } from '@/lib/kvMembers'

export async function addMemberAction(
  prevState: string | null,
  formData: FormData
): Promise<string | null> {
  const name = formData.get('name') as string
  const substackId = formData.get('substackId') as string
  const teamId = formData.get('teamId') as string

  if (!name || !substackId) {
    return 'name と substackId は必須です'
  }

  try {
    await addMember({ name, substackId, teamId: teamId ?? '' })
    revalidatePath('/admin')
    return null
  } catch (e) {
    return e instanceof Error ? e.message : '追加に失敗しました'
  }
}

export async function deleteMemberAction(substackId: string): Promise<void> {
  await deleteMember(substackId)
  revalidatePath('/admin')
}
```
  </action>
  <verify>
`npx tsc --noEmit` がエラーなし。
  </verify>
  <done>
- src/app/admin/actions.ts が存在する
- addMemberAction がexportされている
- deleteMemberAction がexportされている
- 'use server' ディレクティブが存在する
  </done>
</task>

<task type="auto">
  <name>Task 2: src/app/admin/AdminAddForm.tsxを新規作成する（追加フォーム）</name>
  <files>src/app/admin/AdminAddForm.tsx</files>
  <action>
`src/app/admin/AdminAddForm.tsx` を新規作成する。`'use client'` ディレクティブを先頭に記述する。

- `useActionState` でaddMemberActionのエラーメッセージを管理する（D-15）
- フィールド: name（必須）, substackId（必須）, teamId（任意、テキスト自由入力 D-08）
- エラーがある場合はフォーム下に赤文字で表示する
- 送信ボタンのラベル: "追加"

```typescript
'use client'

import { useActionState } from 'react'
import { addMemberAction } from './actions'

export default function AdminAddForm() {
  const [error, formAction] = useActionState(addMemberAction, null)

  return (
    <form action={formAction} className="space-y-3 mb-8">
      <h2 className="text-lg font-semibold">メンバー追加</h2>
      <div className="flex gap-2">
        <input
          name="name"
          placeholder="名前"
          required
          className="border rounded px-2 py-1 text-sm flex-1"
        />
        <input
          name="substackId"
          placeholder="substackId"
          required
          className="border rounded px-2 py-1 text-sm flex-1"
        />
        <input
          name="teamId"
          placeholder="teamId (任意)"
          className="border rounded px-2 py-1 text-sm flex-1"
        />
        <button
          type="submit"
          className="bg-blue-600 text-white px-4 py-1 rounded text-sm hover:bg-blue-700"
        >
          追加
        </button>
      </div>
      {error && <p className="text-red-600 text-sm">{error}</p>}
    </form>
  )
}
```
  </action>
  <verify>
`npx tsc --noEmit` がエラーなし。
  </verify>
  <done>
- src/app/admin/AdminAddForm.tsx が存在する
- 'use client' ディレクティブが存在する
- useActionState が使用されている
- name, substackId, teamId フィールドが存在する
  </done>
</task>

<task type="auto">
  <name>Task 3: src/app/admin/AdminMemberList.tsxを新規作成する（一覧 + 削除）</name>
  <files>src/app/admin/AdminMemberList.tsx</files>
  <action>
`src/app/admin/AdminMemberList.tsx` を新規作成する。`'use client'` ディレクティブを先頭に記述する。

- meberをpropsで受け取りテーブル形式で表示する（D-05）
- 表示フィールド: name, substackId, teamId, addedAt（ISO 8601をそのまま表示）
- 削除ボタンのonClick内で `window.confirm()` を呼ぶ（D-06）
- 確認後にServer Action（deleteMemberAction）を呼ぶ

```typescript
'use client'

import { deleteMemberAction } from './actions'
import type { Member } from '@/lib/types'

type Props = { members: Member[] }

export default function AdminMemberList({ members }: Props) {
  async function handleDelete(substackId: string) {
    if (!window.confirm(`"${substackId}" を削除しますか？`)) return
    await deleteMemberAction(substackId)
  }

  return (
    <div className="overflow-x-auto">
      <table className="w-full text-sm border-collapse">
        <thead>
          <tr className="bg-gray-100 text-left">
            <th className="border px-3 py-2">名前</th>
            <th className="border px-3 py-2">substackId</th>
            <th className="border px-3 py-2">teamId</th>
            <th className="border px-3 py-2">addedAt</th>
            <th className="border px-3 py-2"></th>
          </tr>
        </thead>
        <tbody>
          {members.map((m) => (
            <tr key={m.substackId} className="hover:bg-gray-50">
              <td className="border px-3 py-2">{m.name}</td>
              <td className="border px-3 py-2">{m.substackId}</td>
              <td className="border px-3 py-2">{m.teamId}</td>
              <td className="border px-3 py-2 text-xs text-gray-500">{m.addedAt}</td>
              <td className="border px-3 py-2">
                <button
                  onClick={() => handleDelete(m.substackId)}
                  className="text-red-600 hover:underline text-xs"
                >
                  削除
                </button>
              </td>
            </tr>
          ))}
        </tbody>
      </table>
      {members.length === 0 && (
        <p className="text-gray-400 text-sm mt-2">メンバーがいません</p>
      )}
    </div>
  )
}
```
  </action>
  <verify>
`npx tsc --noEmit` がエラーなし。
  </verify>
  <done>
- src/app/admin/AdminMemberList.tsx が存在する
- 'use client' ディレクティブが存在する
- window.confirm が使用されている
- deleteMemberAction が呼ばれている
  </done>
</task>

<task type="auto">
  <name>Task 4: src/app/admin/page.tsxを新規作成する（管理画面 Server Component）</name>
  <files>src/app/admin/page.tsx</files>
  <action>
`src/app/admin/page.tsx` を新規作成する。Server ComponentとしてKVからメンバーを取得し、AdminAddFormとAdminMemberListを配置する。

```typescript
import { getMembers } from '@/lib/kvMembers'
import AdminAddForm from './AdminAddForm'
import AdminMemberList from './AdminMemberList'

export default async function AdminPage() {
  const members = await getMembers()

  return (
    <main className="max-w-3xl mx-auto p-6">
      <h1 className="text-2xl font-semibold mb-6">管理画面</h1>
      <AdminAddForm />
      <AdminMemberList members={members} />
    </main>
  )
}
```
  </action>
  <verify>
`npx tsc --noEmit` がエラーなし。
  </verify>
  <done>
- src/app/admin/page.tsx が存在する
- getMembers() が呼ばれている
- AdminAddForm が使用されている
- AdminMemberList が使用されている
  </done>
</task>

<task type="auto">
  <name>Task 5: src/app/page.tsxにチームタブUIとsearchParamsフィルタリングを追加する</name>
  <files>src/app/page.tsx</files>
  <action>
`src/app/page.tsx` を更新する。以下の変更を加える:

1. **searchParams受け取り**: Next.js 15以降はPromise型（D-13）
2. **チームフィルタリング**: `searchParams.team` がある場合は `teamId` が一致するメンバーのみ
3. **チームタブUI**: Allタブ + KVメンバーから動的生成した各チームタブ（D-11, D-12, D-14）
   - `[...new Set(members.map(m => m.teamId).filter(Boolean))]` でユニークなteamIdを抽出
   - 各タブは `<a href>` リンク（D-12）
   - アクティブタブは強調スタイル

```typescript
import { getMembers } from '@/lib/kvMembers'
import { fetchAllFeedsCached } from '@/lib/fetchFeed'
import { getRecentDays, sortByWeeklyCount } from '@/lib/heatmapUtils'
import WeeklyHeatmapGrid from '@/components/WeeklyHeatmapGrid'

type Props = {
  searchParams: Promise<{ team?: string }>
}

export default async function Home({ searchParams }: Props) {
  const { team } = await searchParams
  const allMembers = await getMembers()

  const teams = [...new Set(allMembers.map((m) => m.teamId).filter(Boolean))]
  const filteredMembers = team ? allMembers.filter((m) => m.teamId === team) : allMembers

  const results = await fetchAllFeedsCached(filteredMembers)
  const dates = getRecentDays()
  const sorted = sortByWeeklyCount(results, dates)

  return (
    <main className="max-w-[600px] mx-auto p-6 pb-64">
      <h1 className="text-2xl font-semibold mb-4">Keep Substack</h1>

      {teams.length > 0 && (
        <div className="flex gap-2 mb-6 flex-wrap">
          <a
            href="/"
            className={`px-3 py-1 rounded text-sm border ${
              !team ? 'bg-gray-800 text-white border-gray-800' : 'text-gray-600 border-gray-300 hover:bg-gray-50'
            }`}
          >
            All
          </a>
          {teams.map((t) => (
            <a
              key={t}
              href={`/?team=${encodeURIComponent(t)}`}
              className={`px-3 py-1 rounded text-sm border ${
                team === t ? 'bg-gray-800 text-white border-gray-800' : 'text-gray-600 border-gray-300 hover:bg-gray-50'
              }`}
            >
              {t}
            </a>
          ))}
        </div>
      )}

      <WeeklyHeatmapGrid results={sorted} dates={dates} />
    </main>
  )
}
```

**注意:** `teams.length > 0` の場合のみタブUIを表示する。全メンバーのteamIdが空文字の場合はタブ非表示（シンプルな表示を維持）。
  </action>
  <verify>
`npx tsc --noEmit` がエラーなし。
  </verify>
  <done>
- src/app/page.tsx が searchParams を受け取っている
- teamId でのフィルタリングロジックが存在する
- チームタブUI（All + 各チーム）が存在する
- タブが `<a href>` リンクで実装されている
- tsc --noEmit がエラーなし
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| フォーム入力 → Server Actions | ユーザー入力（name, substackId, teamId）がサーバーに送信される |
| URL searchParams → フィルタリング | /?team=xxx のURLパラメータがサーバーサイドで処理される |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Injection | addMemberAction (formData) | mitigate | KVにはオブジェクトとして格納。SQLなし。name/substackId/teamIdは文字列として保存するのみ（クエリ実行なし）。 |
| T-06-05 | XSS | AdminMemberList (members表示) | mitigate | Reactの自動エスケープでHTMLインジェクションを防ぐ。dangerouslySetInnerHTMLは使用しない。 |
| T-06-06 | XSS | page.tsx (team URLパラメータ) | mitigate | `encodeURIComponent(t)` でURLエンコード。ReactのhrefはXSSを自動防止。 |
| T-06-07 | Elevation of Privilege | /admin Server Actions | accept | middleware.tsでBasic認証済み（Plan 01）。ブラウザからの直接呼び出しはミドルウェアでブロック済み。 |
</threat_model>

<verification>
1. `npx tsc --noEmit` がゼロエラー
2. `test -f src/app/admin/page.tsx && echo "exists"` が "exists"
3. `test -f src/app/admin/actions.ts && echo "exists"` が "exists"
4. `grep -c "useActionState" src/app/admin/AdminAddForm.tsx` が 1 以上
5. `grep -c "window.confirm" src/app/admin/AdminMemberList.tsx` が 1 以上
6. `grep -c "searchParams" src/app/page.tsx` が 1 以上
7. `grep -c "team" src/app/page.tsx` が 1 以上
</verification>

<success_criteria>
- /admin ページにメンバー一覧・追加フォーム・削除ボタンが存在する（ADM-01, ADM-02）
- Basic認証（middleware.ts）で /admin が保護されている（ADM-03）
- /?team=teamId でヒートマップをフィルタリングできる（HEAT-04）
- トップページにチームタブUIが表示される
- TypeScriptコンパイルエラーなし
</success_criteria>

<output>
完了後、`.planning/phases/06-admin-team-filter/06-02-SUMMARY.md` を作成する。
</output>
