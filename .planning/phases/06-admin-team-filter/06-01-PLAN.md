---
phase: 06-admin-team-filter
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/kvMembers.ts
  - middleware.ts
  - .env.example
autonomous: true
requirements:
  - ADM-03

must_haves:
  truths:
    - "kvMembers.tsにaddMember(member: Omit<Member, 'addedAt'>)関数がexportされている"
    - "kvMembers.tsにdeleteMember(substackId: string)関数がexportされている"
    - "middleware.tsが存在し、/adminへのリクエストをBasic認証でインターセプトする"
    - "ADMIN_PASSWORDが一致しない場合に401 + WWW-Authenticateヘッダーを返す"
    - ".env.exampleにADMIN_PASSWORD=が追記されている"
  artifacts:
    - path: "src/lib/kvMembers.ts"
      provides: "getMembers/addMember/deleteMemberの3関数"
      exports:
        - getMembers
        - addMember
        - deleteMember
    - path: "middleware.ts"
      provides: "Basic認証でのadmin保護"
      contains: "ADMIN_PASSWORD"
    - path: ".env.example"
      provides: "ADMIN_PASSWORD環境変数のドキュメント"
      contains: "ADMIN_PASSWORD"
  key_links:
    - from: "middleware.ts"
      to: "ADMIN_PASSWORD"
      via: "process.env.ADMIN_PASSWORD"
      pattern: "Basic認証"
    - from: "src/lib/kvMembers.ts"
      to: "redis"
      via: "redis.set('members', updated)"
      pattern: "KV更新"
---

<objective>
Phase 6のデータ層基盤と認証基盤を整備する。

Purpose: Plan 02で実装する管理UI（Server Actions）が依存するKV操作関数と、/adminへのアクセスを保護するBasic認証middlewareを確立する。
Output:
- src/lib/kvMembers.ts（addMember + deleteMember追加）
- middleware.ts（新規: Basic認証）
- .env.example（ADMIN_PASSWORD追記）
</objective>

<execution_context>
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/kugyu10/work/keep-substack/.planning/PROJECT.md
@/Users/kugyu10/work/keep-substack/.planning/ROADMAP.md
@/Users/kugyu10/work/keep-substack/.planning/STATE.md

<interfaces>
<!-- 既存コード — executorはこれを参照して実装する。再探索不要。 -->

From src/lib/kvMembers.ts (現状):
```typescript
import { redis } from './redis'
import type { Member } from './types'

export async function getMembers(): Promise<Member[]> {
  const members = await redis.get<Member[]>('members')
  return members ?? []
}
```

From src/lib/types.ts:
```typescript
export type Member = {
  name: string
  substackId: string
  teamId: string
  addedAt: string  // ISO 8601
}
```

From .env.example (現状):
```
# ISRのrevalidate間隔（秒）。デフォルト300秒（5分）
REVALIDATE_SECONDS=300

UPSTASH_REDIS_REST_URL=https://your-db.upstash.io
UPSTASH_REDIS_REST_TOKEN=your-token
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: kvMembers.tsにaddMemberとdeleteMemberを追加する</name>
  <files>src/lib/kvMembers.ts</files>
  <action>
`src/lib/kvMembers.ts` に以下の2関数を追記する。既存の `getMembers()` は変更しない。

**addMember**: getMembers() → 重複チェック → 新メンバー追加 → redis.set
- `addedAt` はサーバー側で `new Date().toISOString()` を付与する（D-07）
- 同一substackIdが既に存在する場合はErrorをthrowする（D-15のエラーハンドリング用）

```typescript
export async function addMember(member: Omit<Member, 'addedAt'>): Promise<void> {
  const members = await getMembers()
  if (members.some((m) => m.substackId === member.substackId)) {
    throw new Error(`substackId "${member.substackId}" は既に登録されています`)
  }
  const newMember: Member = { ...member, addedAt: new Date().toISOString() }
  await redis.set('members', [...members, newMember])
}
```

**deleteMember**: getMembers() → filter → redis.set
- 存在しないsubstackIdを削除しようとしても静かに成功する（エラーなし）

```typescript
export async function deleteMember(substackId: string): Promise<void> {
  const members = await getMembers()
  const updated = members.filter((m) => m.substackId !== substackId)
  await redis.set('members', updated)
}
```
  </action>
  <verify>
`npx tsc --noEmit` がエラーなし。
  </verify>
  <done>
- src/lib/kvMembers.ts に addMember がexportされている
- src/lib/kvMembers.ts に deleteMember がexportされている
- tsc --noEmit がエラーなし
  </done>
</task>

<task type="auto">
  <name>Task 2: middleware.tsを新規作成する（Basic認証）</name>
  <files>middleware.ts</files>
  <action>
プロジェクトルート（`src/` と同階層）に `middleware.ts` を新規作成する。

- Next.js Edge Middlewareとして `/admin` パスをインターセプトする（D-02）
- `ADMIN_PASSWORD` ENV変数との照合のみ。ユーザー名は不要（D-01）
- 認証失敗時は `401 Unauthorized` + `WWW-Authenticate: Basic realm="Admin"` を返す（D-03）

```typescript
import { NextRequest, NextResponse } from 'next/server'

export function middleware(request: NextRequest) {
  const authHeader = request.headers.get('authorization')
  const adminPassword = process.env.ADMIN_PASSWORD ?? ''
  const encoded = Buffer.from(`:${adminPassword}`).toString('base64')

  if (authHeader !== `Basic ${encoded}`) {
    return new NextResponse('Unauthorized', {
      status: 401,
      headers: { 'WWW-Authenticate': 'Basic realm="Admin"' },
    })
  }

  return NextResponse.next()
}

export const config = {
  matcher: ['/admin', '/admin/:path*'],
}
```

**注意:** `Buffer` はNode.js Edge Runtimeで利用可能。`btoa()` / `atob()` はUnicode対応が不完全なため使用しない。
  </action>
  <verify>
`npx tsc --noEmit` がエラーなし。
  </verify>
  <done>
- middleware.ts が存在する
- matcher に '/admin' と '/admin/:path*' が含まれている
- ADMIN_PASSWORD を使ったBasic認証ロジックが存在する
- tsc --noEmit がエラーなし
  </done>
</task>

<task type="auto">
  <name>Task 3: .env.exampleにADMIN_PASSWORDを追記する</name>
  <files>.env.example</files>
  <action>
`.env.example` の末尾に以下を追記する（D-04）。既存行は変更しない。

```
ADMIN_PASSWORD=your-admin-password
```
  </action>
  <verify>
`grep "ADMIN_PASSWORD" .env.example` が1件ヒットする。
  </verify>
  <done>
- .env.example に ADMIN_PASSWORD= が存在する
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP Request → middleware.ts | 外部からの /admin アクセスを認証でブロック |
| ENV → ADMIN_PASSWORD | デプロイ環境の環境変数からパスワードを読み取る |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Spoofing | middleware.ts Basic認証 | mitigate | ADMIN_PASSWORD一致のみでBasic認証。HTTPS前提（Vercelデプロイ）でBase64盗聴リスクを軽減。 |
| T-06-02 | Elevation of Privilege | /admin パス保護 | mitigate | matcher: ['/admin', '/admin/:path*'] で全サブパスを保護。認証なしでServer Actionsへの到達を防ぐ。 |
| T-06-03 | Tampering | deleteMember / addMember | accept | KV操作はServer Actions経由のみ（Plan 02で実装）。直接呼び出しはサーバーサイドのみ可能。 |
</threat_model>

<verification>
1. `npx tsc --noEmit` がゼロエラー
2. `grep -c "addMember\|deleteMember" src/lib/kvMembers.ts` が 2 以上
3. `test -f middleware.ts && echo "exists"` が "exists"
4. `grep -c "ADMIN_PASSWORD" .env.example` が 1 以上
5. `grep -c "matcher" middleware.ts` が 1 以上
</verification>

<success_criteria>
- kvMembers.tsにaddMemberとdeleteMemberがexportされている
- middleware.tsが存在し、/adminをBasic認証で保護している
- .env.exampleにADMIN_PASSWORD=が追記されている
- TypeScriptコンパイルエラーなし
</success_criteria>

<output>
完了後、`.planning/phases/06-admin-team-filter/06-01-SUMMARY.md` を作成する。
</output>
