---
context: phase
phase: 39-og-og
status: blocked
work_type: verify-work (UAT)
last_updated: 2026-06-16T10:17:06.039Z
---

# Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Stale UAT vs current code | Phase 39 SUMMARY described `opengraph-image.tsx` (ImageResponse), but v1.9 (commit e697e03) deleted those and migrated to screenshot `/api/og`. UAT tests targeted dead URLs → false 404 "issues". | advisory | Before generating UAT from an old SUMMARY, check git log / current files for later supersession. |

<current_state>
Phase 39 (OG images/meta) UAT, status=partial. Started against original SUMMARY URLs, found 404s, diagnosed as not-a-bug (URLs superseded by v1.9 /api/og). Re-verified against /api/og and hit a REAL blocker: timeout + OOM crash.
</current_state>

<completed_work>
- Test 1 (OG/Twitter meta tags top/daily/member): PASS
- Test 2 (metadataBase absolute https://keep-substack.com): PASS
- Test 3 (member OG via /api/og?view=member&publicationId=): ISSUE / blocker (timeout+OOM)
- Tests 4,5,6: BLOCKED by test 3 (same /api/og endpoint)
- Diagnosed 404s as not-a-bug → .planning/debug/og-image-404.md
</completed_work>

<remaining_work>
- Fix the /api/og blocker (see blocker below)
- Re-run blocked tests 3-6 via /gsd:verify-work 39
</remaining_work>

<decisions_made>
- 404 on /opengraph-image + /member/[id]/opengraph-image is NOT a bug: those files deleted in e697e03 (v1.9 screenshot migration). Current OG = /api/og?view=goal|daily|member.
- User chose to re-verify Phase 39 against current /api/og.
</decisions_made>

<blockers>
- BLOCKER (test 3): Local `/api/og?view=member&publicationId=uojun` resolved screenshot target to PRODUCTION `https://keep-substack.com/og-view/member?publicationId=uojun` (not localhost) → page.goto networkidle 25000ms timeout → screenshot fallback → immediately FATAL: JavaScript heap out of memory (~15GB) crashing the dev server.
  - Likely causes: (a) getSiteUrl / NEXT_PUBLIC_SITE_URL returns prod domain in local env; (b) memory leak / Chromium browser/page not released on the timeout+fallback path.
  - Files: src/lib/screenshot.ts:99 (page.goto networkidle), src/app/api/og/route.tsx:106 (screenshotOg call + fallback), getSiteUrl + buildOgImagePath.
</blockers>

## Required Reading (in order)
1. `.planning/phases/39-og-og/39-UAT.md` — full UAT state (status: partial), Gaps section has the blocker
2. `.planning/debug/og-image-404.md` — why the 404s are not-a-bug
3. Memory `project-og-screenshot-method.md` — local needs NEXT_PUBLIC_SITE_URL + restart; prod needs Vercel Pro maxDuration

## Infrastructure State
- Dev server CRASHED via OOM during test 3. Must restart before re-testing.
- Valid /api/og views: goal | daily | member (member requires publicationId param).

<context>
The real work left is a debug+fix of /api/og: stop it screenshotting the production origin from local, and fix the OOM (free Chromium browser/page on timeout/fallback). Two likely-separate fixes: origin resolution + resource cleanup.
</context>

<next_action>
Start with: /gsd:debug the /api/og timeout+OOM blocker (fresh context). Inspect getSiteUrl/NEXT_PUBLIC_SITE_URL origin resolution and screenshot.ts browser/page lifecycle on the timeout path. After fix + dev restart, run /gsd:verify-work 39 to re-verify blocked tests 3-6.
</next_action>
