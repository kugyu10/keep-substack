---
phase: "03-dashboard-ux"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/calendarUtils.ts
  - src/components/MiniCalendar.tsx
  - src/app/page.tsx
autonomous: true
requirements:
  - DASH-01
  - DASH-02
  - DASH-03

must_haves:
  truths:
    - "ダッシュボードで全メンバーのミニカレンダーがグリッド表示される"
    - "各ミニカレンダーカードをクリックすると /member/[substackId] へ遷移する"
    - "スマートフォン（1列）/ タブレット（2列）/ PC（3列）でレスポンシブ表示される"
    - "substackId が feedUrl のサブドメインから自動的に抽出される"
  artifacts:
    - path: "src/lib/calendarUtils.ts"
      provides: "extractSubstackId 関数のエクスポート"
      contains: "extractSubstackId"
    - path: "src/components/MiniCalendar.tsx"
      provides: "今月固定・月ナビなしのミニカレンダー Server Component"
      exports: ["MiniCalendar"]
    - path: "src/app/page.tsx"
      provides: "グリッドレイアウトのダッシュボード"
      contains: "grid-cols-1 sm:grid-cols-2 lg:grid-cols-3"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/components/MiniCalendar.tsx"
      via: "import と props 渡し"
      pattern: "MiniCalendar"
    - from: "src/app/page.tsx"
      to: "/member/[substackId]"
      via: "next/link の href"
      pattern: "href.*member"
    - from: "src/components/MiniCalendar.tsx"
      to: "src/lib/calendarUtils.ts"
      via: "buildDayGrid のインポート"
      pattern: "buildDayGrid"
---

<objective>
ダッシュボード（/）を全メンバーのミニカレンダーグリッドに刷新する。

Purpose: Phase 2 で縦並びのフルカレンダーだったトップページを、全メンバーを俯瞰できるレスポンシブグリッドレイアウトに変更する。各カードをクリックすると個人詳細ページへ遷移できる。
Output:
- src/lib/calendarUtils.ts — extractSubstackId 関数の追加
- src/components/MiniCalendar.tsx — 今月固定・月ナビなしのミニカレンダー（新規）
- src/app/page.tsx — グリッドレイアウトのダッシュボードに更新
</objective>

<execution_context>
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/kugyu10/work/keep-substack/.planning/PROJECT.md
@/Users/kugyu10/work/keep-substack/.planning/ROADMAP.md
@/Users/kugyu10/work/keep-substack/.planning/STATE.md

<interfaces>
<!-- 既存コードから抽出した型・シグネチャ。executor はこれを参照して実装すること。 -->

src/lib/calendarUtils.ts（現在のエクスポート）:
```typescript
export type DayInfo = {
  date: number      // 1-31
  colStart?: number // 月初日のみ: 1（日曜）〜7（土曜）
}
export type ArticleMap = Map<string, { title?: string; link?: string }[]>
export function parseIsoDate(isoDate: string): { year: number; month: number; day: number } | null
export function buildDayGrid(year: number, month: number): DayInfo[]
export function buildArticleMap(items: { title?: string; link?: string; isoDate?: string }[]): ArticleMap
```

src/components/CalendarGrid.tsx（Props — MiniCalendar で同型を使う）:
```typescript
type Article = { title?: string; link?: string }
type Props = {
  memberName: string
  articleMap: [string, Article[]][]  // Map はシリアライズ不可のため配列で渡す
}
```

src/lib/fetchFeed.ts:
```typescript
export const fetchAllFeedsCached: (members: Member[]) => Promise<MemberFeedResult[]>
// MemberFeedResult = { member: Member; items: FeedItem[] }
```

src/data/members.json（現在の内容）:
```json
[
  { "name": "キャリア孔明", "feedUrl": "https://careerkoumei.substack.com/feed" },
  { "name": "うおじゅん", "feedUrl": "https://uojun.substack.com/feed" }
]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: calendarUtils.ts に extractSubstackId 関数を追加する</name>
  <files>src/lib/calendarUtils.ts</files>
  <action>
既存の src/lib/calendarUtils.ts の末尾に以下の関数を追加する（per D-02）。

```typescript
/**
 * SubstackフィードURLからサブドメイン（substackId）を抽出する。
 * 例: "https://uojun.substack.com/feed" -> "uojun"
 * Substack形式でないURLはnullを返す。
 */
export function extractSubstackId(feedUrl: string): string | null {
  const m = feedUrl.match(/^https?:\/\/([^.]+)\.substack\.com/)
  return m ? m[1] : null
}
```

既存の関数（parseIsoDate, buildDayGrid, buildArticleMap）は一切変更しないこと。
  </action>
  <verify>
    <automated>grep -c "extractSubstackId" /Users/kugyu10/work/keep-substack/src/lib/calendarUtils.ts</automated>
  </verify>
  <done>extractSubstackId がエクスポートされており、"https://uojun.substack.com/feed" を渡すと "uojun" が返る</done>
</task>

<task type="auto">
  <name>Task 2: MiniCalendar.tsx — 今月固定のミニカレンダー Server Component を新規作成する</name>
  <files>src/components/MiniCalendar.tsx</files>
  <action>
src/components/MiniCalendar.tsx を新規作成する。CalendarGrid とは別のファイルに独立させる（KISS: 責務を分離）。

要件:
- Server Component（'use client' なし、useState なし）
- 今月（new Date() で取得）のみ表示。月ナビなし
- 記事のある日付セルは bg-green-500 text-white で色付け
- 記事のない日付セルは bg-gray-100 text-gray-400
- カード全体の外枠: border rounded-lg p-3 hover:shadow-md transition-shadow cursor-pointer
- メンバー名: text-sm font-semibold mb-2 truncate
- グリッド: grid grid-cols-7 gap-0.5
- 日付セル: aspect-square rounded-sm text-xs flex items-center justify-center
- colStart は style={{ gridColumnStart: day.colStart }} で設定（Tailwind 動的クラス col-start-${n} はビルドで検知されないため style を使う。Phase 2 確立済みパターン）
- ツールチップは実装しない（クリックで個人詳細ページへ誘導する設計のため YAGNI）

実装テンプレート:
```tsx
import { buildDayGrid } from '@/lib/calendarUtils'

type Article = { title?: string; link?: string }

type Props = {
  memberName: string
  articleMap: [string, Article[]][]
}

export default function MiniCalendar({ memberName, articleMap }: Props) {
  const now = new Date()
  const year = now.getFullYear()
  const month = now.getMonth() + 1

  const days = buildDayGrid(year, month)
  const map = new Map(articleMap)

  return (
    <div className="border rounded-lg p-3 hover:shadow-md transition-shadow cursor-pointer">
      <p className="text-sm font-semibold mb-2 truncate">{memberName}</p>
      <div className="grid grid-cols-7 gap-0.5">
        {days.map((day) => {
          const key = `${year}-${String(month).padStart(2, '0')}-${String(day.date).padStart(2, '0')}`
          const hasArticle = (map.get(key) ?? []).length > 0
          return (
            <div
              key={day.date}
              style={day.colStart ? { gridColumnStart: day.colStart } : undefined}
              className={`aspect-square rounded-sm text-xs flex items-center justify-center ${
                hasArticle ? 'bg-green-500 text-white' : 'bg-gray-100 text-gray-400'
              }`}
            >
              {day.date}
            </div>
          )
        })}
      </div>
    </div>
  )
}
```
  </action>
  <verify>
    <automated>cd /Users/kugyu10/work/keep-substack && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>src/components/MiniCalendar.tsx が存在し、TypeScript コンパイルエラーなし</done>
</task>

<task type="auto">
  <name>Task 3: page.tsx をミニカレンダーグリッドのダッシュボードに更新する</name>
  <files>src/app/page.tsx</files>
  <action>
src/app/page.tsx を全面的に書き換えてダッシュボードに変更する（per DASH-01, DASH-02, DASH-03）。

変更点:
1. max-w-2xl -> max-w-6xl（3列グリッドが崩れないよう幅を拡大。Pitfall 4 参照）
2. CalendarGrid の縦並びを廃止し、MiniCalendar のグリッドに変更
3. each メンバーを next/link でラップして /member/[substackId] へ遷移（per D-01, D-02）
4. substackId が null の場合は null を返してスキップ

書き換え後の全体コード:
```tsx
// RESEARCH.md §3「D-06の実現方法」に従い、unstable_cacheがrevalidateを制御するため
// export const revalidate は使わず export const dynamic = 'force-static' を使う
export const dynamic = 'force-static'

import Link from 'next/link'
import { fetchAllFeedsCached } from '@/lib/fetchFeed'
import { buildArticleMap, extractSubstackId } from '@/lib/calendarUtils'
import MiniCalendar from '@/components/MiniCalendar'
import members from '@/data/members.json'
import type { Member } from '@/lib/types'

export default async function Home() {
  const results = await fetchAllFeedsCached(members as Member[])

  return (
    <main className="max-w-6xl mx-auto p-6">
      <h1 className="text-2xl font-bold mb-6">Keep Substack</h1>
      <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">
        {results.map(({ member, items }) => {
          const substackId = extractSubstackId(member.feedUrl)
          if (!substackId) return null
          const map = buildArticleMap(items)
          const articleMapEntries = Array.from(map.entries())
          return (
            <Link key={member.feedUrl} href={`/member/${substackId}`}>
              <MiniCalendar
                memberName={member.name}
                articleMap={articleMapEntries}
              />
            </Link>
          )
        })}
      </div>
    </main>
  )
}
```

注意: CalendarGrid のインポートは削除する（ダッシュボードでは使わない）。
  </action>
  <verify>
    <automated>cd /Users/kugyu10/work/keep-substack && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>TypeScript コンパイルエラーなし。page.tsx に grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 と /member/${substackId} へのリンクが含まれる</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| members.json → extractSubstackId | ローカルファイルのみ。外部入力なし |
| URL path (/member/[substackId]) | dynamicParams=false で事前生成パス以外は404（Plan 03-02 で設定） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | MiniCalendar（公開ページ） | accept | 認証なし公開ページが設計通り。フィードデータはSubstackが公開している情報のみ |
| T-03-02 | Spoofing | feedUrl → substackId 抽出 | accept | members.json はサーバー管理のローカルファイル。外部からの改ざん経路なし |
</threat_model>

<verification>
Plan 03-01 完了確認:

```bash
# 1. TypeScript コンパイルエラーなし
cd /Users/kugyu10/work/keep-substack && npx tsc --noEmit

# 2. extractSubstackId 関数が存在する
grep "extractSubstackId" /Users/kugyu10/work/keep-substack/src/lib/calendarUtils.ts

# 3. MiniCalendar コンポーネントが存在する
ls /Users/kugyu10/work/keep-substack/src/components/MiniCalendar.tsx

# 4. page.tsx にグリッドレイアウトとリンクが含まれる
grep "grid-cols-1 sm:grid-cols-2 lg:grid-cols-3" /Users/kugyu10/work/keep-substack/src/app/page.tsx
grep "member.*substackId" /Users/kugyu10/work/keep-substack/src/app/page.tsx

# 5. 開発サーバーでビルドエラーなし（オプション）
cd /Users/kugyu10/work/keep-substack && npm run build 2>&1 | tail -20
```
</verification>

<success_criteria>
- extractSubstackId("https://uojun.substack.com/feed") が "uojun" を返す
- MiniCalendar.tsx が存在し、'use client' なし（Server Component）
- page.tsx が MiniCalendar のグリッドを描画し、各カードが /member/[substackId] へのリンクになっている
- npx tsc --noEmit がエラーなしで完了する
</success_criteria>

<output>
完了後、.planning/phases/03-dashboard-ux/03-01-SUMMARY.md を作成すること。
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/templates/summary.md のテンプレートに従う。
</output>
