---
phase: 12-chameleon-hidden-team
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/types.ts
  - src/app/page.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "All ビュー（/?）に chameleon 所属メンバーが表示されない"
    - "チームタブの一覧に 'chameleon' タブが表示されない"
    - "/?team=chameleon を直打ちすると chameleon メンバーが表示される（D-02）"
    - "/?team=teamA では teamA と chameleon 両方に所属するメンバーが表示される（D-01）"
    - "管理画面 AdminMemberList は変更なし — chameleon メンバーは全表示（D-03）"
  artifacts:
    - path: "src/lib/types.ts"
      provides: "HIDDEN_TEAM 定数のエクスポート"
      contains: "HIDDEN_TEAM"
    - path: "src/app/page.tsx"
      provides: "teams フィルター + All ビュー filteredMembers フィルター"
      contains: "HIDDEN_TEAM"
  key_links:
    - from: "src/lib/types.ts"
      to: "src/app/page.tsx"
      via: "import { HIDDEN_TEAM }"
      pattern: "HIDDEN_TEAM"
    - from: "src/app/page.tsx"
      to: "teams 配列"
      via: ".filter(t => t !== HIDDEN_TEAM)"
      pattern: "filter.*HIDDEN_TEAM"
    - from: "src/app/page.tsx"
      to: "filteredMembers"
      via: "All ビュー時に chameleon メンバーを除外"
      pattern: "teamNames.*includes.*HIDDEN_TEAM"
---

<objective>
シークレットチーム "chameleon" の非表示ロジックを実装する。

Purpose: All ビューとチームタブから chameleon メンバー/タブを隠しつつ、URL直打ち・他チームタブ・管理画面では通常通り表示を維持する。
Output: src/lib/types.ts に HIDDEN_TEAM 定数、src/app/page.tsx に2箇所のフィルタリング変更。
</objective>

<execution_context>
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/kugyu10/work/keep-substack/.planning/PROJECT.md
@/Users/kugyu10/work/keep-substack/.planning/ROADMAP.md
@/Users/kugyu10/work/keep-substack/.planning/STATE.md
@/Users/kugyu10/work/keep-substack/.planning/phases/12-chameleon-hidden-team/12-CONTEXT.md

<interfaces>
<!-- 変更対象のキー定義。Executor はそのまま使用する。 -->

From src/lib/types.ts:
```typescript
export type Member = {
  name: string
  substackId: string
  teamNames: string[]  // Phase 11 D-01 で string[] に移行済み
  addedAt: string
}
```

From src/app/page.tsx (変更前):
```typescript
const teams = [...new Set(allMembers.flatMap((m) => m.teamNames).filter(Boolean))]
const filteredMembers = team ? allMembers.filter((m) => m.teamNames.includes(team)) : allMembers
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: HIDDEN_TEAM 定数を types.ts に追加する</name>
  <files>src/lib/types.ts</files>
  <behavior>
    - HIDDEN_TEAM を import した側で teams フィルターに使用できる
    - 定数値は小文字の文字列 'chameleon'
    - export されているので page.tsx から import できる
  </behavior>
  <action>
src/lib/types.ts のファイル末尾に以下を追記する（per Claude's Discretion — KISS/YAGNI）:

```typescript
// シークレットチーム名。タブ一覧・All ビューから非表示にする（Phase 12）
export const HIDDEN_TEAM = 'chameleon'
```

既存の型定義や他のコードには一切手を加えない。
  </action>
  <verify>
    <automated>grep -n "export const HIDDEN_TEAM" /Users/kugyu10/work/keep-substack/src/lib/types.ts</automated>
  </verify>
  <done>types.ts に `export const HIDDEN_TEAM = 'chameleon'` が存在する</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: page.tsx に chameleon フィルタリングを適用する</name>
  <files>src/app/page.tsx</files>
  <behavior>
    - teams 配列から 'chameleon' が除外される（タブ非表示）
    - All ビュー（team 未指定）時に chameleon 所属メンバーが filteredMembers から除外される
    - /?team=chameleon 直打ちでは chameleon メンバーが表示される（team 指定時のフィルタは変更しない）
    - /?team=teamA では teamA かつ chameleon に所属するメンバーも表示される（team 指定フィルタは従来通り）
  </behavior>
  <action>
src/app/page.tsx を以下の通り変更する:

1. import 行に HIDDEN_TEAM を追加:
```typescript
import { HIDDEN_TEAM } from '@/lib/types'
```

2. teams 生成ロジックを変更（'chameleon' をタブ一覧から除外）:
```typescript
// 変更前
const teams = [...new Set(allMembers.flatMap((m) => m.teamNames).filter(Boolean))]

// 変更後
const teams = [...new Set(allMembers.flatMap((m) => m.teamNames).filter(Boolean))].filter(
  (t) => t !== HIDDEN_TEAM
)
```

3. filteredMembers ロジックを変更（All ビュー時のみ chameleon メンバーを除外）:
```typescript
// 変更前
const filteredMembers = team ? allMembers.filter((m) => m.teamNames.includes(team)) : allMembers

// 変更後
const filteredMembers = team
  ? allMembers.filter((m) => m.teamNames.includes(team))
  : allMembers.filter((m) => !m.teamNames.includes(HIDDEN_TEAM))
```

注意: team が指定された場合（/?team=teamA や /?team=chameleon）は従来ロジックのまま。
変更箇所は上記3点のみ。JSX や他のロジックは一切触らない（D-01, D-02, D-03 を遵守）。
  </action>
  <verify>
    <automated>cd /Users/kugyu10/work/keep-substack && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - TypeScript コンパイルエラーなし
    - grep -n "HIDDEN_TEAM" src/app/page.tsx が3行ヒットする（import 1行 + filter 2箇所）
    - grep -n "HIDDEN_TEAM" src/app/page.tsx で確認: import行・teams生成行・filteredMembers行の3箇所
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    chameleon チームの非表示ロジックを実装した。
    - All ビューから chameleon メンバーを除外
    - チームタブから 'chameleon' タブを除外
    - URL直打ち・他チームタブ・管理画面は従来通り
  </what-built>
  <how-to-verify>
    1. `npm run dev` でローカル起動 (http://localhost:3000)
    2. http://localhost:3000 を開き、チームタブに "chameleon" が存在しないことを確認
    3. All ビューで chameleon のみに所属するメンバーが表示されないことを確認
       （テスト用に chameleon メンバーを KV に追加して確認するか、既存データで確認）
    4. http://localhost:3000/?team=chameleon を直打ちし、chameleon メンバーが表示されることを確認（D-02）
    5. http://localhost:3000/admin を開き、chameleon メンバーが通常通り表示されることを確認（D-03）
    6. chameleon と他チームの両方に所属するメンバーがいる場合、そのチームタブでは表示されることを確認（D-01）
  </how-to-verify>
  <resume-signal>"approved" または発見した問題を記述してください</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → Next.js page | searchParams.team は URL クエリパラメータ（未信頼入力） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Information Disclosure | page.tsx / teams フィルタ | accept | chameleon タブは非表示だが /?team=chameleon 直打ちで閲覧可能（D-02 で意図的仕様）。PII なし・低価値ターゲット |
| T-12-02 | Tampering | searchParams.team | accept | team パラメータは既存フィルタで string 比較のみ。SQL/クエリインジェクションなし。KV読み取り専用パス |
| T-12-03 | Information Disclosure | AdminMemberList | accept | 管理画面は別ルート（/admin）。chameleon メンバーの全表示は管理者向け仕様（D-03）。認証は上位フェーズのスコープ |
</threat_model>

<verification>
以下のコマンドで実装を確認する:

```bash
# 1. TypeScript 型チェック
cd /Users/kugyu10/work/keep-substack && npx tsc --noEmit

# 2. HIDDEN_TEAM 定数の存在確認
grep -n "export const HIDDEN_TEAM" src/lib/types.ts

# 3. page.tsx の変更箇所確認（3行ヒットするはず）
grep -n "HIDDEN_TEAM" src/app/page.tsx

# 4. AdminMemberList が変更されていないことを確認
grep -n "HIDDEN_TEAM\|chameleon" src/app/admin/AdminMemberList.tsx 2>/dev/null || echo "変更なし (期待通り)"

# 5. ビルド確認
npm run build 2>&1 | tail -20
```
</verification>

<success_criteria>
- `src/lib/types.ts` に `export const HIDDEN_TEAM = 'chameleon'` が存在する
- `src/app/page.tsx` の teams 生成に `.filter((t) => t !== HIDDEN_TEAM)` が追加されている
- `src/app/page.tsx` の All ビュー filteredMembers に `.filter((m) => !m.teamNames.includes(HIDDEN_TEAM))` が追加されている
- `src/app/admin/AdminMemberList.tsx` に変更なし（grep で確認）
- TypeScript コンパイルエラーなし
- ブラウザ確認（checkpoint）で D-01, D-02, D-03 の挙動がすべて確認できている
</success_criteria>

<output>
完了後、`.planning/phases/12-chameleon-hidden-team/12-01-SUMMARY.md` を作成すること。
</output>
