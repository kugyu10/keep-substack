---
phase: 35-google-analytics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/page.tsx
  - src/app/__tests__/page.test.tsx
autonomous: true
requirements:
  - TEAM-01

must_haves:
  truths:
    - "private チームが未ログインユーザーに対してトップページのタブとして表示される"
    - "hidden チームはタブに表示されない"
    - "All タブに private チームのメンバーが引き続き表示される"
    - "全テストスイートがグリーンのまま"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "private チームをタブに含む teams 配列（status !== 'hidden' フィルタ）"
      contains: "t.status !== 'hidden'"
    - path: "src/app/__tests__/page.test.tsx"
      provides: "TEAM-01 の新仕様に適合した TEAM-03 テスト"
      contains: "shows public AND private team names as tabs"
  key_links:
    - from: "src/app/page.tsx"
      to: "teams (line 18-24)"
      via: "flatMap filter change"
      pattern: "t\\.status !== 'hidden'"
---

<objective>
TEAM-01 要件を実装する: トップページのチームタブに private チームを表示する。

Purpose: status === 'public' から status !== 'hidden' へのフィルタ変更により、private チームがタブに表示されるようになる。hidden チームは引き続き除外される。

Output:
- `src/app/page.tsx` line 21 の 1 行変更
- `src/app/__tests__/page.test.tsx` の TEAM-03 テストを新仕様に更新
</objective>

<execution_context>
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/35-google-analytics/35-CONTEXT.md
@.planning/phases/35-google-analytics/35-PATTERNS.md

Key decisions from CONTEXT.md:
- D-01: teams 計算を `t.status === 'public'` → `t.status !== 'hidden'` に変更（1行）
- D-02: private チームタブは public チームと同一の外観。鍵アイコン等の追加なし
- D-03: filteredMembers フィルタ (`m.teams.every((t) => t.status !== 'hidden')`) は変更なし
- D-04: /my ページの private チーム readonly 制限は変更なし
</context>

<interfaces>
<!-- src/app/page.tsx の変更対象箇所（lines 18-24） -->
<!-- 変更前: -->
const teams = [
  ...new Set(
    allMembers
      .flatMap((m) => m.teams.filter((t) => t.status === 'public').map((t) => t.name))
      .filter(Boolean)
  ),
]

<!-- 変更後（line 21 のみ変更）: -->
const teams = [
  ...new Set(
    allMembers
      .flatMap((m) => m.teams.filter((t) => t.status !== 'hidden').map((t) => t.name))
      .filter(Boolean)
  ),
]

<!-- filteredMembers は変更しない（lines 25-30）: -->
const filteredMembers = team
  ? allMembers.filter((m) => m.teams.some((t) => t.name === team))
  : allMembers.filter((m) => m.teams.every((t) => t.status !== 'hidden'))
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TEAM-03 テストを新仕様に更新し、TEAM-01 の page.tsx 変更を実装する</name>
  <files>src/app/__tests__/page.test.tsx, src/app/page.tsx</files>
  <read_first>
    - src/app/__tests__/page.test.tsx — 変更対象の TEAM-03 テスト（lines 139-161）の現状を確認する
    - src/app/page.tsx — 変更対象の teams 計算（lines 18-24）と変更してはならない filteredMembers（lines 25-30）を確認する
    - .planning/phases/35-google-analytics/35-PATTERNS.md — TEAM-03 更新後のテストコードパターン（lines 112-145）を参照する
    - .planning/phases/35-google-analytics/35-RESEARCH.md — Pitfall 1（lines 219-243）でテスト変更内容の詳細を確認する
  </read_first>
  <behavior>
    TDD サイクル（RED → GREEN）:

    RED フェーズ（テスト更新のみ）:
    - `src/app/__tests__/page.test.tsx` の TEAM-03 `it` ブロック（line 139）を以下の通り更新する:
      - it の説明文: 'shows ONLY public team names as tabs — private/hidden teams absent' → 'shows public AND private team names as tabs — hidden teams absent'
      - `expect(labels).not.toContain('PrivateTeam')` → `expect(labels).toContain('PrivateTeam')`
      - `expect(labels).not.toContain('SecretTeam')` → `expect(labels).toContain('SecretTeam')`
      - `expect(labels).not.toContain('HiddenTeam')` は変更なし（hidden は引き続き除外）
    - この時点でテストを実行し、TEAM-03 が RED になることを確認する（page.tsx はまだ変更していない）

    GREEN フェーズ（実装）:
    - `src/app/page.tsx` line 21 を変更する:
      `.flatMap((m) => m.teams.filter((t) => t.status === 'public').map((t) => t.name))`
      →
      `.flatMap((m) => m.teams.filter((t) => t.status !== 'hidden').map((t) => t.name))`
    - filteredMembers（lines 25-30）は変更しない（D-03）
    - テストを再実行して GREEN になることを確認する

    変更しないもの（重要）:
    - `it('dedupes public team names...')` テスト（lines 163-177）
    - `it('All view: excludes members belonging to any hidden team')` テスト（lines 180-201）
    - `it('team-selected view: shows members of selected team REGARDLESS of status')` テスト（lines 204-220）
    - page.tsx の filteredMembers フィルタ（lines 25-30）
  </behavior>
  <action>
    1. src/app/__tests__/page.test.tsx を開き、line 139 の it ブロックを変更する:
       - it の説明文を 'shows public AND private team names as tabs — hidden teams absent' に変更する（per D-01）
       - line 158 の `expect(labels).not.toContain('PrivateTeam')` → `expect(labels).toContain('PrivateTeam')` に変更する
       - line 159 の `expect(labels).not.toContain('SecretTeam')` → `expect(labels).toContain('SecretTeam')` に変更する
       - line 160 の `expect(labels).not.toContain('HiddenTeam')` は変更しない

    2. `npx vitest run src/app/__tests__/page.test.tsx` を実行して TEAM-03 が RED であることを確認する（page.tsx 未変更のため失敗するはず）

    3. src/app/page.tsx の line 21 を変更する:
       `t.status === 'public'` → `t.status !== 'hidden'`
       （それ以外の行は変更しない）

    4. `npx vitest run src/app/__tests__/page.test.tsx` を実行して全テストが GREEN になることを確認する
  </action>
  <verify>
    <automated>npx vitest run src/app/__tests__/page.test.tsx</automated>
  </verify>
  <done>
    - page.test.tsx の TEAM-03 テストの説明文が 'shows public AND private team names as tabs — hidden teams absent' になっている
    - page.test.tsx の TEAM-03 テストで `expect(labels).toContain('PrivateTeam')` と `expect(labels).toContain('SecretTeam')` がある
    - page.test.tsx の TEAM-03 テストで `expect(labels).not.toContain('HiddenTeam')` がある
    - page.tsx line 21 が `t.status !== 'hidden'` を含む
    - page.tsx の filteredMembers（lines 25-30）が変更されていない（`m.teams.every((t) => t.status !== 'hidden')` のまま）
    - `npx vitest run src/app/__tests__/page.test.tsx` が全テスト GREEN
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → RSC page.tsx | team クエリパラメータは URL から読み取られる（searchParams）|
| DB → page.tsx | getMembers() が teams[].status を返す。DB 値をそのまま表示 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-01 | Information Disclosure | page.tsx teams array | accept | private チームは「チーム名のみ」が公開される。メンバー情報は filteredMembers ロジックで制御される。private チーム名の公開は D-02 で意図的に承認済み |
| T-35-02 | Tampering | searchParams team parameter | accept | team パラメータは DB の teams[].name と照合される（`t.name === team`）。不正な値を渡しても filteredMembers が空になるだけで DB 書き込みなし。XSS リスクなし（encodeURIComponent で URL エンコード済み） |
| T-35-SC | Tampering | npm/pip/cargo installs | accept | このフェーズは新規パッケージインストールなし。@next/third-parties は既インストール済み（Vercel first-party）|
</threat_model>

<verification>
- `npx vitest run src/app/__tests__/page.test.tsx` が全テストグリーン（4テスト）
- `npx tsc --noEmit` がエラーなし
- page.tsx の teams 配列が `t.status !== 'hidden'` フィルタを使用している
- page.tsx の filteredMembers が変更されていない
</verification>

<success_criteria>
- private チームがトップページのチームタブに表示されることを assert するテストがグリーン
- hidden チームがタブに表示されないことを assert するテストがグリーン
- 全 22 テストファイル、150 テストがグリーン（新規テストファイルは不要）
</success_criteria>

<output>
Create `.planning/phases/35-google-analytics/35-01-SUMMARY.md` when done
</output>
