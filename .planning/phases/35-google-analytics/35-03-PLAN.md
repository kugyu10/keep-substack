---
phase: 35-google-analytics
plan: 03
type: execute
wave: 2
depends_on:
  - 35-01
  - 35-02
files_modified: []
autonomous: false
requirements:
  - ANLT-01
  - ANLT-02

must_haves:
  truths:
    - "全テストスイート（22 ファイル・150 テスト以上）がグリーン"
    - "TypeScript コンパイルがエラーなし"
    - "Vercel 本番環境変数に NEXT_PUBLIC_GA_MEASUREMENT_ID が設定されている"
    - "本番デプロイ後に GA4 プロパティでページビューが記録される"
  artifacts:
    - path: "src/app/layout.tsx"
      provides: "GoogleAnalytics コンポーネントの実装"
      contains: "NEXT_PUBLIC_GA_MEASUREMENT_ID"
  key_links:
    - from: "Vercel Dashboard (Production)"
      to: "process.env.NEXT_PUBLIC_GA_MEASUREMENT_ID"
      via: "Environment Variables → Production"
      pattern: "G-[A-Z0-9]+"

user_setup:
  - service: vercel
    why: "本番環境での GA4 ページビュートラッキング有効化。NEXT_PUBLIC_* 変数はビルド時インライン化されるため、設定後に本番デプロイが必要"
    env_vars:
      - name: NEXT_PUBLIC_GA_MEASUREMENT_ID
        source: "Google Analytics → 管理 → データストリーム → ウェブストリーム → 測定 ID（例: G-XXXXXXXXXX）"
    dashboard_config:
      - task: "Vercel Production 環境変数に NEXT_PUBLIC_GA_MEASUREMENT_ID を追加する"
        location: "Vercel Dashboard → [keep-substack] → Settings → Environment Variables → 'Production' のみチェック → Name: NEXT_PUBLIC_GA_MEASUREMENT_ID, Value: G-XXXXXXXXXX"
      - task: "本番ブランチ（main）への push または Vercel Redeploy で本番ビルドを再実行する"
        location: "Vercel Dashboard → [keep-substack] → Deployments → 最新デプロイメント → Redeploy"
---

<objective>
Phase 35 の全変更（Plans 01 と 02）が正しく統合されていることを確認し、Vercel 本番環境変数の設定をユーザーが完了するためのチェックポイントを提供する。

Purpose: Plans 01 と 02 は独立して並列実行されるため、統合後に全スイートが通過することを最終確認する。その後、NEXT_PUBLIC_GA_MEASUREMENT_ID の本番設定はユーザーが Vercel ダッシュボードで実行する必要があり、自動化不可。

Output:
- 全テストスイートのグリーン確認
- Vercel 本番環境変数設定の完了確認
</objective>

<execution_context>
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/35-google-analytics/35-01-SUMMARY.md
@.planning/phases/35-google-analytics/35-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 統合後の全テストスイートと TypeScript 型チェックを実行する</name>
  <files></files>
  <read_first>
    - .planning/phases/35-google-analytics/35-01-SUMMARY.md — Plan 01 の完了状態を確認する
    - .planning/phases/35-google-analytics/35-02-SUMMARY.md — Plan 02 の完了状態を確認する
  </read_first>
  <action>
    1. `npx tsc --noEmit` を実行してビルドエラーがないことを確認する
    2. `npx vitest run` を実行して全 22 テストファイル・150 テスト以上がグリーンであることを確認する
    3. 以下の grep コマンドで Plan 01/02 の実装が存在することを確認する:
       - `grep -n "t\.status !== 'hidden'" src/app/page.tsx` — 1 件ヒット（line 21 付近）
       - `grep -n "GoogleAnalytics" src/app/layout.tsx` — 2 件ヒット（import と JSX）
       - `grep -v '^#' src/app/layout.tsx | grep -c "NEXT_PUBLIC_GA_MEASUREMENT_ID"` — 1 以上
    4. いずれかのチェックが失敗した場合は、該当する Plan（01 または 02）の SUMMARY.md を読んで原因を特定し修正する
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run</automated>
  </verify>
  <done>
    - `npx tsc --noEmit` がエラー 0
    - `npx vitest run` が Test Files N passed (22+)、Tests N passed (150+) を出力する
    - `grep "t\.status !== 'hidden'" src/app/page.tsx` が 1 件ヒット
    - `grep "GoogleAnalytics" src/app/layout.tsx` が 2 件ヒット
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Vercel 本番環境変数 NEXT_PUBLIC_GA_MEASUREMENT_ID を設定する</name>
  <what-built>Plans 01 と 02 で src/app/page.tsx（private チームタブ表示）と src/app/layout.tsx（GoogleAnalytics 条件付きレンダリング）の実装が完了している。GA スクリプトの実際の有効化には Vercel での環境変数設定と本番デプロイが必要。</what-built>
  <how-to-verify>
    1. Google Analytics 管理画面で測定 ID を取得する:
       - Google Analytics → 管理 → データストリーム → ウェブストリームを選択 → 測定 ID（G-XXXXXXXXXX 形式）をコピーする

    2. Vercel ダッシュボードで本番環境変数を設定する:
       - https://vercel.com/dashboard → keep-substack プロジェクト → Settings → Environment Variables
       - 「Add New」をクリック
       - Name: `NEXT_PUBLIC_GA_MEASUREMENT_ID`
       - Value: `G-XXXXXXXXXX`（手順 1 でコピーした実際の ID）
       - Environment: 「Production」のみを選択（Preview/Development は選択しない — D-08/D-09）
       - 「Save」をクリック

    3. 本番ビルドを再実行する:
       - Deployments タブ → 最新の Production デプロイメント → 「...」メニュー → 「Redeploy」をクリック
       - または、main ブランチに push して自動デプロイを起動する

    4. デプロイ完了後、本番 URL（https://keep-substack.vercel.app）をブラウザで開き、DevTools の Network タブで `gtag/js` へのリクエストが発生していることを確認する

    5. GA4 管理画面の「リアルタイム」レポートで自分のアクセスがカウントされることを確認する（数分かかる場合あり）
  </how-to-verify>
  <resume-signal>Vercel での環境変数設定と本番デプロイが完了したら "approved" と入力してください。GA4 リアルタイムレポートで確認できた場合はその旨も記載してください。</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vercel Dashboard → env vars | NEXT_PUBLIC_GA_MEASUREMENT_ID はビルド時にインライン化される。本番ビルドにのみ適用される |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-05 | Information Disclosure | Vercel env var NEXT_PUBLIC_GA_MEASUREMENT_ID | accept | NEXT_PUBLIC_* 変数はクライアントサイドで公開される設計。GA Measurement ID は意図的に公開される値（全 Web サイトで同様）|
| T-35-SC | Tampering | npm/pip/cargo installs | accept | このプランはソースコード変更なし。環境変数設定と検証のみ |
</threat_model>

<verification>
- `npx tsc --noEmit` エラー 0
- `npx vitest run` 全 150 テスト以上グリーン
- 本番 URL で `window.gtag` が定義されている（ブラウザ DevTools コンソールで確認）
- GA4 リアルタイムレポートでページビューが記録される
</verification>

<success_criteria>
Phase 35 の全 success criteria が満たされている:
1. トップページのチームタブに private チームが表示され、未ログインユーザーが閲覧できる（TEAM-01 ✅）
2. /my ページからの private チーム参加・退出制限は変更なし（TEAM-01 ✅）
3. 本番環境で GA4 プロパティにページビューが記録される（ANLT-01 ✅）
4. ローカル開発では GA スクリプトが読み込まれない（ANLT-02 ✅）
</success_criteria>

<output>
Create `.planning/phases/35-google-analytics/35-03-SUMMARY.md` when done
</output>
