---
phase: 35-google-analytics
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/layout.tsx
autonomous: true
requirements:
  - ANLT-01
  - ANLT-02

must_haves:
  truths:
    - "本番環境（NEXT_PUBLIC_GA_MEASUREMENT_ID 設定済み）では GoogleAnalytics スクリプトが全ページに注入される"
    - "開発環境（env var 未設定）では GA スクリプトが読み込まれない"
    - "TypeScript ビルドエラーが発生しない"
    - "root layout の既存構造（Header / {children} / Footer）は変更されない"
  artifacts:
    - path: "src/app/layout.tsx"
      provides: "GoogleAnalytics コンポーネントの条件付きレンダリング"
      contains: "GoogleAnalytics"
  key_links:
    - from: "src/app/layout.tsx"
      to: "@next/third-parties/google"
      via: "import { GoogleAnalytics }"
      pattern: "from '@next/third-parties/google'"
    - from: "src/app/layout.tsx"
      to: "process.env.NEXT_PUBLIC_GA_MEASUREMENT_ID"
      via: "conditional render"
      pattern: "NEXT_PUBLIC_GA_MEASUREMENT_ID &&"

user_setup:
  - service: vercel
    why: "本番環境での GA4 ページビュートラッキング有効化"
    env_vars:
      - name: NEXT_PUBLIC_GA_MEASUREMENT_ID
        source: "Google Analytics → 管理 → データストリーム → ウェブストリーム → 測定 ID（G-XXXXXXXXXX 形式）"
    dashboard_config:
      - task: "Vercel 本番環境変数に NEXT_PUBLIC_GA_MEASUREMENT_ID を追加"
        location: "Vercel Dashboard → Project Settings → Environment Variables → Production のみを選択して追加"
---

<objective>
ANLT-01/02 要件を実装する: `src/app/layout.tsx` に `GoogleAnalytics` コンポーネントを追加し、本番環境の全ページで GA4 ページビュートラッキングを有効にする。

Purpose: root layout に追加することで Next.js のすべてのルート（/、/daily、/my、/admin/* 等）に自動適用される。`NEXT_PUBLIC_GA_MEASUREMENT_ID` 環境変数が未設定の場合は短絡評価でスキップされ、ローカル開発では GA スクリプトが読み込まれない。

Output:
- `src/app/layout.tsx` に `import { GoogleAnalytics } from '@next/third-parties/google'` 追加
- `<body>` 末尾に条件付き `<GoogleAnalytics>` レンダリング追加
</objective>

<execution_context>
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/35-google-analytics/35-CONTEXT.md
@.planning/phases/35-google-analytics/35-PATTERNS.md

Key decisions from CONTEXT.md:
- D-05: @next/third-parties v16.2.7 はインストール済み。追加パッケージ不要
- D-06: layout.tsx に GoogleAnalytics を追加。NEXT_PUBLIC_GA_MEASUREMENT_ID が設定されている場合のみレンダリング
- D-07: 実装パターン: `{process.env.NEXT_PUBLIC_GA_MEASUREMENT_ID && <GoogleAnalytics gaId={process.env.NEXT_PUBLIC_GA_MEASUREMENT_ID} />}`
- D-08: .env.local に NEXT_PUBLIC_GA_MEASUREMENT_ID を設定しない（ローカルでは GA 無効）
- D-09: Vercel 本番環境変数に NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX を設定（ユーザーが直接設定）
- D-10: NODE_ENV によるコード内チェック不要。env var の有無のみで制御
</context>

<interfaces>
<!-- src/app/layout.tsx 現状（全体）-->
import type { Metadata } from 'next'
import './globals.css'
import Header from '@/components/Header'
import Footer from '@/components/Footer'

export const metadata: Metadata = {
  metadataBase: new URL('https://keep-substack.vercel.app'),
  title: 'Keep Substack',
  description: 'Substackコミュニティメンバーの記事更新を確認するツール',
  openGraph: { ... },
}

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="ja">
      <body>
        <Header />
        {children}
        <Footer />
      </body>
    </html>
  )
}

<!-- @next/third-parties/google の GoogleAnalytics シグネチャ（node_modules 確認済み）-->
export declare function GoogleAnalytics(props: { gaId: string; dataLayerName?: string }): JSX.Element
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: layout.tsx に GoogleAnalytics コンポーネントを追加する（ANLT-01/02 per D-06/D-07）</name>
  <files>src/app/layout.tsx</files>
  <read_first>
    - src/app/layout.tsx — 現状の import 行・body 構造を確認する（既に context に記載済みだが、編集前に必ず再確認する）
    - .planning/phases/35-google-analytics/35-PATTERNS.md — layout.tsx の変更パターン（lines 59-103）を参照する
    - .planning/phases/35-google-analytics/35-RESEARCH.md — Pitfall 2（lines 248-254）と Pitfall 3（lines 256-261）を確認する
  </read_first>
  <action>
    1. src/app/layout.tsx の import ブロック末尾（Footer import の次の行）に以下を追加する:
       `import { GoogleAnalytics } from '@next/third-parties/google'`

    2. `<body>` 内の `<Footer />` の直後（`</body>` の直前）に以下を追加する:
       `{process.env.NEXT_PUBLIC_GA_MEASUREMENT_ID && (
         <GoogleAnalytics gaId={process.env.NEXT_PUBLIC_GA_MEASUREMENT_ID} />
       )}`

    3. それ以外の箇所（metadata、html lang、Header、children、Footer）は変更しない

    禁止事項（per D-10）:
    - `NODE_ENV !== 'production'` による条件チェックを追加しない
    - `NEXT_PUBLIC_GA_MEASUREMENT_ID!` のような non-null assertion を使用しない（型安全のため && による narrowing を使用）
    - `(auth)/layout.tsx` は変更しない（root layout の継承で /login 等もカバーされる）
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run</automated>
  </verify>
  <done>
    - src/app/layout.tsx に `import { GoogleAnalytics } from '@next/third-parties/google'` が存在する
    - src/app/layout.tsx の `<body>` 末尾に `process.env.NEXT_PUBLIC_GA_MEASUREMENT_ID &&` を含む条件付きレンダリングが存在する
    - src/app/layout.tsx に `<GoogleAnalytics gaId={process.env.NEXT_PUBLIC_GA_MEASUREMENT_ID}` が存在する
    - `npx tsc --noEmit` がエラーなし
    - `npx vitest run` が 150 テスト全グリーン（layout.tsx のユニットテストは存在しないが全スイート通過で確認）
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| layout.tsx → browser | GoogleAnalytics コンポーネントが next/script 経由で gtag.js をブラウザに注入する |
| Vercel env var → build | NEXT_PUBLIC_GA_MEASUREMENT_ID はビルド時インライン化される |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-03 | Information Disclosure | NEXT_PUBLIC_GA_MEASUREMENT_ID | accept | GA Measurement ID（G-XXXXXXXXXX）はフロントエンドで公開される性質の値。悪用されても自プロパティのデータは保護される（GA プロパティのアクセス制御は Google 側で管理）|
| T-35-04 | Elevation of Privilege | GoogleAnalytics component | accept | @next/third-parties は Vercel first-party パッケージ。gtag.js は Google CDN から読み込まれる。サプライチェーンリスクは低い（Vercel 管理）|
| T-35-SC | Tampering | npm/pip/cargo installs | accept | 新規パッケージインストールなし。@next/third-parties v16.2.7 は既インストール済み・検証済み |
</threat_model>

<verification>
- `npx tsc --noEmit` がエラーなし（TypeScript 型エラーなし）
- `npx vitest run` が 150 テスト全グリーン
- `grep -n "GoogleAnalytics" src/app/layout.tsx` が 2 行（import と JSX）を返す
- `grep -n "NEXT_PUBLIC_GA_MEASUREMENT_ID" src/app/layout.tsx` が条件付きレンダリング行を返す
- `grep -v "NODE_ENV" src/app/layout.tsx | wc -l` が元の行数 + 2 程度（NODE_ENV チェックが追加されていないこと）
</verification>

<success_criteria>
- layout.tsx が @next/third-parties/google の GoogleAnalytics をインポートしている
- NEXT_PUBLIC_GA_MEASUREMENT_ID が設定されている場合のみ GoogleAnalytics がレンダリングされる（&& 短絡評価）
- NODE_ENV チェックを含まない
- TypeScript エラーなし・全テストグリーン
</success_criteria>

<output>
Create `.planning/phases/35-google-analytics/35-02-SUMMARY.md` when done
</output>
