---
phase: 42-1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/schema.sql
  - src/lib/comments.ts
  - src/lib/__tests__/comments.test.ts
  - src/lib/__tests__/fixtures/comment_reader.json
  - src/lib/__tests__/fixtures/comment_replies.json
autonomous: true
requirements: [COMMENT-01, COMMENT-02, COMMENT-03, COMMENT-04, COMMENT-05, COMMENT-06]
user_setup:
  - service: supabase
    why: "note_comments テーブルを本番 DB に作成（service_role 書き込み・public select）。schema.sql は正規ソースだが本番反映は SQL Editor 経由マイグレーションが必須"
    dashboard_config:
      - task: "schema.sql に追記した note_comments の CREATE TABLE / ENABLE RLS / public select policy を本番 Supabase の SQL Editor で実行（dev=otydhiumsdsyxepnjqjp / prod=xolhjcngrwwwqtklmoyk）"
        location: "Supabase Dashboard -> SQL Editor"

must_haves:
  truths:
    - "parseNoteId が URL / c-付き / 裸の ID を寛容にパースし、不正入力は null を返す"
    - "parseComment が reader レスポンスから children_count を件数として取り出す"
    - "parseReplies が commentBranches[].comment をフラットな CommentItem[] に変換し、photo_url を string|null 正規化する"
    - "isFresh が fetched_at と CACHE_TTL_MS(30分) で鮮度を判定する"
    - "getNoteComments が cache hit / miss / stale / force を分岐し、miss/stale/force 時に 2 エンドポイントを fetch して upsert する"
    - "fetch 異常時は stale フォールバックせず {status:'error'} を返す"
    - "getNoteComments が invalid_input / error / ok(0件含む) を判別ユニオンで区別する"
  artifacts:
    - path: "supabase/schema.sql"
      provides: "note_comments テーブル定義 + RLS + public select policy"
      contains: "note_comments"
    - path: "src/lib/comments.ts"
      provides: "純関数 parse 群 + 鮮度判定 + fetch&retry + キャッシュ層 getNoteComments"
      exports: ["parseNoteId", "parseComment", "parseReplies", "isFresh", "getNoteComments", "CommentItem", "CommentsResult", "CACHE_TTL_MS"]
      min_lines: 120
    - path: "src/lib/__tests__/comments.test.ts"
      provides: "COMMENT-01..06 の単体テスト（fixture ベース・fetch/supabase モック）"
      contains: "getNoteComments"
  key_links:
    - from: "src/lib/comments.ts"
      to: "https://substack.com/api/v1/reader/comment"
      via: "fetch (cache:'no-store' + AbortSignal.timeout)"
      pattern: "reader/comment"
    - from: "src/lib/comments.ts"
      to: "note_comments"
      via: "supabase admin upsert / anon select"
      pattern: "note_comments"
---

<objective>
Note コメントのキャッシュ基盤を構築する。`note_comments` テーブル（JSONB 1行保存）を schema.sql に追記し、純関数 parse 群 + 鮮度判定 + fetch&retry + Supabase キャッシュ層を `src/lib/comments.ts` に実装し、COMMENT-01..06 の単体テストを通す。

Purpose: Phase 41 の notes.ts パターンを 1:1 踏襲してコメント取得・永続化のデータ層を確立する。UI（Plan 02）はこの層だけに依存する。
Output: schema.sql 追記 / src/lib/comments.ts / src/lib/__tests__/comments.test.ts + fixtures
</objective>

<execution_context>
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-1/42-RESEARCH.md

# 1:1 踏襲する既存パターン（notes.ts の判別ユニオン + 型ガード + cache:'no-store' + AbortSignal.timeout(5000) + res.ok→throw→1秒1回リトライ）
@src/lib/notes.ts
@src/lib/__tests__/notes.test.ts

# Supabase clients（書き込みは service_role / 読みは anon）
@src/lib/supabase/server.ts
@src/lib/supabase/admin.ts

# スパイク実測の生 JSON（フィクスチャ元データ。Note id 276780760）
@.planning/spikes/002-substack-note-comments/raw/comment_reader.json
@.planning/spikes/002-substack-note-comments/raw/comment_replies.json

<interfaces>
<!-- notes.ts が確立した契約。comments.ts は同型の判別ユニオン + 純関数 + fetch&retry を踏襲する。 -->

既存 notes.ts:
- RETRY_DELAY_MS = 1000
- fetch は cache:'no-store' + AbortSignal.timeout(5000)、res.ok でなければ throw、1回目失敗→1秒待ち→1回リトライ→なお失敗で {status:'error'}（空フォールバックしない）
- parseNoteFeed: 純関数・型ガードで不正 item を除外・throw しない・不正 json は [] を返す
- 判別ユニオン NoteListResult = {status:'ok', notes} | {status:'error'}

schema.sql 規約:
- CREATE TABLE IF NOT EXISTS <name> (...);
- ALTER TABLE <name> ENABLE ROW LEVEL SECURITY;
- CREATE POLICY "public select <name>" ON <name> FOR SELECT USING (true);
- service_role は BYPASSRLS のため書き込み用 policy 不要（コメントで明記）
- TIMESTAMPTZ NOT NULL DEFAULT now()

supabase admin client: createSupabaseAdminClient() (service_role, src/lib/supabase/admin.ts)
supabase anon server client: createSupabaseServerClient() (src/lib/supabase/server.ts)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: note_comments テーブルを schema.sql に追記</name>
  <files>supabase/schema.sql</files>
  <action>
    schema.sql の既存テーブル群と同じ規約で `note_comments` を追記する（A3: JSONB 1行保存）。カラム: `note_id TEXT PRIMARY KEY`（parseNoteId が返す正規化 ID）、`comment_count INTEGER NOT NULL`（COMMENT-02 / reader の children_count）、`comments JSONB NOT NULL`（COMMENT-03/04 のフラット CommentItem 配列）、`fetched_at TIMESTAMPTZ NOT NULL DEFAULT now()`（COMMENT-05 鮮度判定）。`CREATE TABLE IF NOT EXISTS` を使う。続けて `ALTER TABLE note_comments ENABLE ROW LEVEL SECURITY;` と `CREATE POLICY "public select note_comments" ON note_comments FOR SELECT USING (true);` を既存 policy 群の隣に追記する。書き込みは service_role が BYPASSRLS で行うため write policy は不要（既存コメント規約に倣いコメントを残す）。本番反映は SQL Editor 経由マイグレーションが必須（user_setup に申し送り済み）— schema.sql 冒頭/該当箇所のコメントにもその旨を残す。
  </action>
  <verify>
    <automated>grep -q "CREATE TABLE IF NOT EXISTS note_comments" supabase/schema.sql && grep -q "public select note_comments" supabase/schema.sql && grep -q "ENABLE ROW LEVEL SECURITY" supabase/schema.sql && echo OK</automated>
  </verify>
  <done>note_comments の CREATE TABLE / RLS 有効化 / public select policy が schema.sql に既存規約どおり追記されている</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: comments.ts 純関数 + 鮮度判定（parseNoteId / parseComment / parseReplies / isFresh）</name>
  <files>src/lib/comments.ts, src/lib/__tests__/comments.test.ts, src/lib/__tests__/fixtures/comment_reader.json, src/lib/__tests__/fixtures/comment_replies.json</files>
  <behavior>
    - parseNoteId('https://substack.com/.../note/c-276780760') === '276780760'（c- 除去）
    - parseNoteId('c-276780760') === '276780760' / parseNoteId('276780760') === '276780760'
    - parseNoteId('') / parseNoteId('garbage') / parseNoteId(非数字) === null（COMMENT-01）
    - parseComment(reader fixture) が children_count を件数として返す（COMMENT-02）
    - parseReplies(replies fixture) が commentBranches[].comment をフラットな CommentItem[] に変換（COMMENT-03）。各 item は id/name/body/date/photoUrl を持ち、photo_url は string|null に正規化（取得不可は null＝COMMENT-04）。reactionCount は持たない（COMMENT-07 リアクション数表示は将来フェーズへ defer・RESEARCH 確定型に準拠）
    - parseReplies(不正 json / commentBranches 欠落) → [] を返し throw しない（型ガード）
    - isFresh(now - 1分) === true / isFresh(now - 31分) === false（CACHE_TTL_MS=30分・COMMENT-05）
    - CACHE_TTL_MS === 30 * 60 * 1000（定数値を直接アサートし回帰耐性を確保）
  </behavior>
  <action>
    フィクスチャをコピー: `.planning/spikes/002-substack-note-comments/raw/comment_reader.json` → `src/lib/__tests__/fixtures/comment_reader.json`、`comment_replies.json` 同様。comments.ts に CommentItem 型（id:number, name:string, body:string, photoUrl:string|null, date:string — RESEARCH 確定型と完全一致。reactionCount は含めない＝COMMENT-07 は将来フェーズ）と `CACHE_TTL_MS` 定数（30*60*1000）を定義。純関数 parseNoteId（URL/c-/裸 ID を寛容にパース、数字以外は null）、parseComment（reader json から children_count を取り出す型ガード付き）、parseReplies（commentBranches[].comment をフラット化、photo_url を string|null 正規化、不正 json は [] を返し throw しない）、isFresh（fetched_at 文字列を Date 化し now との差が CACHE_TTL_MS 未満なら true）を notes.ts の parseNoteFeed と同じ型ガードスタイルで実装。comments.test.ts に behavior の各ケースを fixture ベースで記述。fetch/supabase には依存しない純関数テスト。
  </action>
  <verify>
    <automated>npx vitest run src/lib/__tests__/comments.test.ts -t "parseNoteId" && npx vitest run src/lib/__tests__/comments.test.ts -t "parseComment" && npx vitest run src/lib/__tests__/comments.test.ts -t "parseReplies" && npx vitest run src/lib/__tests__/comments.test.ts -t "isFresh"</automated>
  </verify>
  <done>parseNoteId/parseComment/parseReplies/isFresh が behavior 全ケースで green。photo_url が string|null 正規化される</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: getNoteComments — fetch&retry + キャッシュ hit/stale/force + upsert</name>
  <files>src/lib/comments.ts, src/lib/__tests__/comments.test.ts</files>
  <behavior>
    - cache hit（fresh & !force）: Supabase select のみで {status:'ok', count, comments, fetchedAt, fromCache:true} を返し fetch しない（COMMENT-05）
    - cache miss / stale / force: 2エンドポイント fetch → upsert → {status:'ok', ..., fromCache:false}
    - parseNoteId が null: {status:'invalid_input'}（fetch も select もしない・COMMENT-01）
    - fetch 異常（res.ok でない / timeout）: 1秒1回リトライ後も失敗なら {status:'error'}。stale をフォールバック返却しない（A2 / COMMENT-06）
    - 0件（children_count===0 / commentBranches 空）: {status:'ok', count:0, comments:[]}（error とは区別・COMMENT-06）
  </behavior>
  <action>
    comments.ts に CommentsResult 判別ユニオン（{status:'ok', count, comments, fetchedAt, fromCache} | {status:'invalid_input'} | {status:'error'}）と非公開 fetch ヘルパ（GET `https://substack.com/api/v1/reader/comment/{id}`→parseComment で件数、GET `.../comment/{id}/replies`→parseReplies で一覧。両方 cache:'no-store' + AbortSignal.timeout(5000)、res.ok でなければ throw、notes.ts と同じ 1秒1回リトライ）を実装。`getNoteComments(input: string, opts?: {force?: boolean}): Promise<CommentsResult>` を実装: (1) parseNoteId→null なら invalid_input、(2) createSupabaseServerClient で note_comments select、(3) cached && !force && isFresh(fetched_at) なら fromCache:true で返す、(4) miss/stale/force なら 2エンドポイント fetch（try/catch で失敗時 error 返却・stale フォールバックしない）、(5) 成功時 createSupabaseAdminClient で upsert（note_id 衝突時 update）して fromCache:false で返す。comments.test.ts に behavior の各ケースを vi.mock で fetch と supabase clients をモックして記述（A4: ページング非対応・件数は children_count を正とする旨をテスト/コメントで明示）。
  </action>
  <verify>
    <automated>npx vitest run src/lib/__tests__/comments.test.ts -t "getNoteComments"</automated>
  </verify>
  <done>getNoteComments が hit/miss/stale/force/invalid_input/error/0件を behavior どおり分岐し green。fetch 失敗時に stale を返さず error を返す</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ユーザー入力(Note URL/ID) → comments.ts | 寛容パースの前に検証が必要。parseNoteId が数字以外を null 化 |
| comments.ts → 非公式 Substack API | 信頼できない外部レスポンス。型ガードで不正フィールドを除外 |
| comments.ts → Supabase(service_role 書き込み) | service_role キーは server-only。クライアントに露出させない |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-01 | Tampering | 外部 API レスポンス(parseComment/parseReplies) | mitigate | 型ガードで id/body/date/count を検証、不正 item を除外、photo_url を string|null 正規化（XSS は Plan 02 のテキストノード描画で防ぐ） |
| T-42-02 | Information Disclosure | SUPABASE_SERVICE_ROLE_KEY | mitigate | createSupabaseAdminClient は server-only module（src/lib）でのみ使用、client component から import しない |
| T-42-03 | Denial of Service | 非公式 API への無制限 fetch | accept | AbortSignal.timeout(5000) + 1回のみリトライで上限。PoC 規模・低トラフィックのため許容 |
| T-42-04 | Information Disclosure | note_comments(public select) | accept | コメントは元々公開 Note の公開コメント。PII 機微なし |
| T-42-SC | Tampering | npm/pip/cargo installs | mitigate | 新規パッケージ install なし（RESEARCH 確定）。install 発生時は slopcheck + blocking checkpoint |
</threat_model>

<verification>
- `npx vitest run src/lib/__tests__/comments.test.ts` 全 green（COMMENT-01..06 の純関数 + キャッシュ層）
- schema.sql に note_comments + RLS + public select policy が追記されている
- **SCHEMA_PUSH（本番申し送り）**: schema.sql の note_comments DDL を本番 Supabase の SQL Editor で実行する必要がある（user_setup 参照）。実行は開発者の手動作業
- 新規 npm パッケージなし / 新規 env なし
</verification>

<success_criteria>
- note_comments テーブル定義が既存規約どおり schema.sql に存在
- comments.ts が parseNoteId/parseComment/parseReplies/isFresh/getNoteComments と型を export
- getNoteComments が hit/miss/stale/force/invalid_input/error/0件を判別ユニオンで区別
- fetch 失敗時に stale フォールバックせず error を返す（A2）
- comments.test.ts が COMMENT-01..06 をカバーし全 green
</success_criteria>

<output>
Create `.planning/phases/42-1/42-01-SUMMARY.md` when done
</output>
