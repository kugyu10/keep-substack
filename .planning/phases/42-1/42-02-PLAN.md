---
phase: 42-1
plan: 02
type: execute
wave: 2
depends_on: ["42-01"]
files_modified:
  - src/app/(main)/notes/page.tsx
autonomous: false
requirements: [COMMENT-01, COMMENT-02, COMMENT-03, COMMENT-04, COMMENT-06]
must_haves:
  truths:
    - "ユーザーは公開 /notes ページで Note の URL/ID を入力フォームに指定できる（ログイン不要）"
    - "指定 Note のコメント件数が表示される"
    - "コメント本文がフラットな一覧で、各コメント者の名前とアイコン付きで表示される"
    - "アイコン取得不可時はイニシャル/プレースホルダにフォールバックする"
    - "手動 force 再取得ができる"
    - "取得失敗 / 0件 / 未入力(キャッシュ無)の状態が画面に明示される"
  artifacts:
    - path: "src/app/(main)/notes/page.tsx"
      provides: "公開 /notes コメント表示ページ（入力フォーム + 件数 + フラット一覧 + 名前/アイコン fallback + force ボタン + 3状態描画）"
      min_lines: 60
      contains: "getNoteComments"
  key_links:
    - from: "src/app/(main)/notes/page.tsx"
      to: "src/lib/comments.ts"
      via: "getNoteComments(input, {force})"
      pattern: "getNoteComments"
---

<objective>
公開ルート `/notes` のコメント表示ページを実装する。Note URL/ID 入力フォーム（COMMENT-01）、コメント件数（COMMENT-02）、フラットなコメント一覧（COMMENT-03）、各コメントの名前+アイコン（取得不可時イニシャル fallback・COMMENT-04）、手動 force 再取得（COMMENT-05）、取得失敗/0件/未入力の3状態明示（COMMENT-06）を提供する。

Purpose: Plan 01 のキャッシュ層 getNoteComments を唯一の依存として、ユーザー向けコメント可視化 UI を完成させる。
Output: src/app/(main)/notes/page.tsx
</objective>

<execution_context>
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-1/42-RESEARCH.md

# 1:1 踏襲する RSC ページパターン（force-dynamic / status 3分岐 / whitespace-pre-wrap break-words / JST 整形）
@src/app/(main)/admin/notes/page.tsx

<interfaces>
<!-- Plan 01 が export する契約。ページはこれだけに依存する。 -->
src/lib/comments.ts:
- getNoteComments(input: string, opts?: { force?: boolean }): Promise<CommentsResult>
- CommentsResult =
    | { status: 'ok'; count: number; comments: CommentItem[]; fetchedAt: string; fromCache: boolean }
    | { status: 'invalid_input' }
    | { status: 'error' }
- CommentItem = { id: number; name: string; body: string; date: string; photoUrl: string | null; reactionCount: number }
  （正確な型名/フィールド名は 42-01-SUMMARY.md で確認すること）

既存 RSC ページ規約（admin/notes/page.tsx）:
- export const dynamic = 'force-dynamic'
- status 分岐で error/0件/一覧を別文言で描画
- 本文は <p className="... whitespace-pre-wrap break-words">{body}</p>（テキストノード描画で XSS 安全）
- 日時は new Date(date).toLocaleString('ja-JP', { timeZone: 'Asia/Tokyo' })
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 公開 /notes ページ実装（フォーム + 3状態 + 一覧 + アイコン fallback + force）</name>
  <files>src/app/(main)/notes/page.tsx</files>
  <action>
    admin/notes/page.tsx を踏襲して公開ルート `/notes` の RSC ページを作る（D-公開ルート: middleware matcher は /admin・/my のみのため /notes は素通り＝認証ガード不要・LogoutButton も置かない）。`export const dynamic = 'force-dynamic'`。searchParams で `id`（入力値）と `force`（'1' で force 再取得）を受け取る（Next.js App Router の searchParams は await が必要なバージョンなら await する）。GET フォーム（method なし＝GET）: テキスト input name="id"（プレースホルダに URL or c-付き ID の例）+ 送信ボタンで COMMENT-01。id が未指定なら「Note の URL または ID を入力してください」の未入力(キャッシュ無)状態を表示し getNoteComments を呼ばない。id 指定時は `const result = await getNoteComments(id, { force: force === '1' })`。result.status で3分岐（COMMENT-06）: 'invalid_input'→「URL/ID を認識できませんでした」、'error'→「コメントの取得に失敗しました。時間をおいて再読み込みしてください。」、'ok' へ。'ok' の描画: 件数を「コメント {result.count} 件」表示（COMMENT-02）。result.count===0 / comments.length===0 なら「まだコメントがありません。」（error と別文言・COMMENT-06）。comments.length>0 ならフラット一覧（COMMENT-03）を ul/li で map。各 li: アイコン（photoUrl が string なら plain `<img>` で表示、null/空なら名前の先頭1文字イニシャルを丸背景プレースホルダで表示＝COMMENT-04、next/image は使わない）+ 名前 + 本文を `<p className="whitespace-pre-wrap break-words">{body}</p>` + 日時 JST。force 再取得ボタン: 現在の id を保持して `?id={id}&force=1` に GET するリンク/フォーム（COMMENT-05）。fromCache の真偽に応じて「キャッシュ済み（{fetchedAt} JST）」等の軽い注記を出してもよい。img の onError でイニシャル fallback まで作り込む必要はない（photoUrl===null 分岐で十分・PoC）。
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -v "node_modules" | grep -i "notes/page" || echo "TYPECHECK_OK"</automated>
  </verify>
  <done>/notes が入力フォーム・件数・フラット一覧・名前+アイコン(null時イニシャル)・force ボタン・3状態(invalid_input/error/0件)・未入力状態を描画し、getNoteComments のみに依存。型エラーなし</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 公開 /notes ページの動作確認（human-verify）</name>
  <action>Claude による自動実装（Task 1）後、開発者が公開 /notes ページの動作を目視確認する。Claude は追加の自動化作業を行わず、下記 how-to-verify 手順の結果待ちで一時停止する。</action>
  <what-built>公開 /notes ページ: Note URL/ID 入力フォーム、コメント件数、名前+アイコン付きフラット一覧、force 再取得、取得失敗/0件/未入力の状態表示。Supabase キャッシュ（30分 TTL・force で再取得）連携。</what-built>
  <how-to-verify>
    1. ローカル/preview で http://localhost:3000/notes を開く（ログインせず＝公開ルートとして到達できることを確認）
    2. フィクスチャの Note URL（https://substack.com/.../note/c-276780760）または ID `276780760` を入力して送信 → コメント件数とフラットなコメント一覧（名前+アイコン+本文+JST日時）が表示される
    3. アイコンが取得できないコメントで、名前のイニシャル/プレースホルダが表示される（画像割れしない）
    4. もう一度同じ ID で送信 → 即時表示（キャッシュ hit。注記があれば「キャッシュ済み」表示を確認）
    5. force 再取得ボタンを押す → 再 fetch される
    6. 不正な文字列（例 "abc"）を入力 → 「URL/ID を認識できませんでした」が出る（invalid_input）
    7. 未入力で /notes を開く → 入力を促す文言が出る（キャッシュ無/未入力状態）
    8. 本番反映前に: note_comments テーブルの DDL を本番 Supabase の SQL Editor で実行済みか確認（42-01 user_setup）
  </how-to-verify>
  <verify>
    <human-check>上記 how-to-verify 1-8 を開発者が確認</human-check>
  </verify>
  <done>1-7 が期待どおり動作し、8 の本番 SQL Editor 反映が確認済み</done>
  <resume-signal>「approved」または問題点を記述</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ブラウザ → /notes(公開ルート) | 認証なしで到達可能。ユーザー入力の id は comments.ts の parseNoteId で検証される |
| コメント本文/名前 → DOM 描画 | 外部由来テキスト。React テキストノード描画で XSS を防ぐ |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-05 | Tampering(XSS) | コメント body/name の描画 | mitigate | `{body}`/`{name}` をテキストノードとして描画（dangerouslySetInnerHTML 不使用）。whitespace-pre-wrap で改行保持のみ |
| T-42-06 | Tampering | photoUrl を img src に使用 | mitigate | photoUrl は parse 段で string|null 正規化済み。plain `<img>` は javascript: 等を実行しない。null 時はイニシャル fallback |
| T-42-07 | Information Disclosure | /notes が公開ルート | accept | 表示対象は公開 Note の公開コメントのみ。機微情報なし（D-公開ルート） |
</threat_model>

<verification>
- /notes が公開ルートとして認証なしで到達できる（middleware matcher 確認）
- 入力フォーム/件数/フラット一覧/名前+アイコン fallback/force/3状態が表示される（human-verify checkpoint）
- getNoteComments のみに依存（fetch/supabase を page で直接叩かない）
- 型エラーなし（tsc --noEmit）
</verification>

<success_criteria>
- 公開 /notes で COMMENT-01,02,03,04,06 がユーザー操作で確認できる
- アイコン取得不可時にイニシャル/プレースホルダが表示される
- force 再取得が機能する（COMMENT-05 の UI 経路）
- human-verify checkpoint が approved
</success_criteria>

<output>
Create `.planning/phases/42-1/42-02-SUMMARY.md` when done
</output>
