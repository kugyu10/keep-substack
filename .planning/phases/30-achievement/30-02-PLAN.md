---
phase: 30-achievement
plan: 02
type: execute
wave: 2
depends_on:
  - 30-01
files_modified:
  - src/components/CommitGoalRow.tsx
  - src/components/CommitGoalView.tsx
  - src/components/__tests__/CommitGoalRow.test.tsx
autonomous: true
requirements:
  - ACHIEV-01
  - ACHIEV-02

must_haves:
  truths:
    - "streak=1 のメンバー行に 👑 アイコンが表示される"
    - "streak≥2 のメンバー行に 👑🔥 アイコンが並んで表示される"
    - "streak=0 のメンバー行にアイコンが表示されない（aria-hidden div のみ）"
    - "CommitGoalView がヘッダースペーサーを w-10 に統一して列ずれがない"
    - "CommitGoalRow の streak prop が CommitGoalView から計算済み数値で渡される"
  artifacts:
    - path: "src/components/CommitGoalRow.tsx"
      provides: "streak prop 受け取り + Column 3 条件分岐表示"
      contains: "streak: number"
    - path: "src/components/CommitGoalView.tsx"
      provides: "consecutiveWeekStreak 呼び出し + ヘッダー幅同期"
      contains: "consecutiveWeekStreak"
    - path: "src/components/__tests__/CommitGoalRow.test.tsx"
      provides: "streak=0/1/≥2 の3状態表示テスト（element-tree パターン）"
      contains: "streak=1"
  key_links:
    - from: "src/components/CommitGoalView.tsx"
      to: "src/lib/commitUtils.ts"
      via: "consecutiveWeekStreak named import"
      pattern: "consecutiveWeekStreak"
    - from: "src/components/CommitGoalView.tsx"
      to: "src/components/CommitGoalRow.tsx"
      via: "streak prop"
      pattern: "streak={streak}"
---

<objective>
`CommitGoalRow.tsx` に `streak: number` prop を追加して Column 3 を 👑/👑🔥/空 の条件分岐に差し替え、`CommitGoalView.tsx` で `consecutiveWeekStreak` を呼び出して streak を渡す。`CommitGoalRow.test.tsx` を新規作成して3状態をテストする。

Purpose: Plan 01 のロジックを UI に接続し ACHIEV-01/02 を満足させる。
Output: 動作する achievement アイコン表示 + テスト 3ケース + vitest フルスイート全パス
</objective>

<execution_context>
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/30-achievement/30-CONTEXT.md
@.planning/phases/30-achievement/30-UI-SPEC.md
@.planning/phases/30-achievement/30-PATTERNS.md

<interfaces>
From src/lib/commitUtils.ts (after Plan 01 implementation):
```typescript
export function consecutiveWeekStreak(
  slots: CommitSlot[],
  items: FeedItem[]
): number
// 0 = 未達成 / 1 = 今週のみ(👑) / 2以上 = 連続達成(👑🔥)
```

From src/components/CommitGoalRow.tsx (current state — to be modified):
```typescript
type CommitGoalRowProps = {
  member: Member
  items: FeedItem[]
  slots: CommitSlot[]
  imageUrl?: string
  // streak: number  ← Plan 02 で追加
}
// Column 3 (line 49–50):
<div className="w-8 shrink-0" aria-hidden="true" />
// ↑ この行を streak に応じた条件分岐に差し替える
```

From src/components/CommitGoalView.tsx (current state — to be modified):
```typescript
// line 3: import { sortMembersForCommitView } from '@/lib/commitUtils'
//   → consecutiveWeekStreak を追加インポート
// line 35: <div className="w-8 shrink-0" />
//   → w-8 → w-10 に変更（Header Spacer Sync — Pitfall 3 対策）
// lines 37–48: sorted.map loop
//   → streak 計算して CommitGoalRow に渡す
```

From src/components/__tests__/CommitGrid.test.tsx (element-tree helpers to copy):
```typescript
type AnyEl = ReactElement<Record<string, unknown>> & { type: unknown }
function isElement(node: unknown): node is AnyEl
function flattenChildren(children: unknown): unknown[]
function findByType(node: unknown, target: unknown): AnyEl | null
function findAllByTag(node: unknown, tag: string): AnyEl[]
function collectText(node: unknown): string[]
function findAllByClassName(node: unknown, predicate: (cls: string) => boolean): AnyEl[]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CommitGoalRow.test.tsx 新規作成 — 3状態テスト（RED→GREEN前提）</name>
  <files>src/components/__tests__/CommitGoalRow.test.tsx</files>
  <read_first>
    - src/components/__tests__/CommitGrid.test.tsx — element-tree ヘルパー全文（lines 1–75）+ フィクスチャパターン
    - src/components/CommitGoalRow.tsx — 現在のコンポーネント構造・props 型・Column 3 の位置（line 49–50）
    - src/lib/types.ts — Member / CommitSlot / FeedItem 型定義
  </read_first>
  <behavior>
    - streak=0: collectText(el) に '👑' も '🔥' も含まない
    - streak=1: collectText(el) に '👑' を含む文字列が存在する
    - streak=2: collectText(el) に '👑' と '🔥' の両方を含む文字列（または隣接）が存在する
  </behavior>
  <action>
    新規ファイル src/components/__tests__/CommitGoalRow.test.tsx を作成する。

    構成:
    1. CommitGrid.test.tsx の element-tree ヘルパー（isElement, flattenChildren, findByType, findAllByTag, collectText, findAllByClassName）を全コピーする
    2. CommitGoalRow のインポート: `import CommitGoalRow from '../CommitGoalRow'`
    3. 型インポート: `import type { Member, CommitSlot, FeedItem } from '@/lib/types'`
    4. フィクスチャ関数 `makeMember()` を定義する（Member 型を満たす最小オブジェクト: id は `'00000000-0000-0000-0000-000000000001'`、name は `'Test User'`、publicationId は `'test-pub'`、teams は `[]`、addedAt は `'2026-01-01T00:00:00.000Z'`）
    5. 以下の3つの describe ブロックを追加する:
       - `describe('CommitGoalRow streak=0: アイコンなし')` — streak=0 を渡した CommitGoalRow の collectText 結果に '👑'/'🔥' が含まれないことを検証
       - `describe('CommitGoalRow streak=1: 👑 のみ (ACHIEV-01)')` — streak=1 を渡した CommitGoalRow の collectText 結果に '👑' が含まれることを検証
       - `describe('CommitGoalRow streak=2: 👑🔥 (ACHIEV-02)')` — streak=2 を渡した CommitGoalRow の collectText 結果に '👑' と '🔥' の両方が含まれることを検証

    この時点では CommitGoalRow に streak prop がないため型エラーまたはテスト失敗になる（RED 確認用）。
    Task 2 完了後にこのテストが GREEN になる。
  </action>
  <verify>
    <automated>npx vitest run src/components/__tests__/CommitGoalRow.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/__tests__/CommitGoalRow.test.tsx が存在する
    - ファイルに `describe('CommitGoalRow streak=0` / `describe('CommitGoalRow streak=1` / `describe('CommitGoalRow streak=2` の3ブロックが含まれる
    - element-tree ヘルパー collectText が定義されている
    - makeMember() 関数が Member 型を満たす（id, name, publicationId, teams, addedAt フィールドを持つ）
    - （Task 2 完了後）`npx vitest run src/components/__tests__/CommitGoalRow.test.tsx` が全パスする
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: CommitGoalRow — streak prop 追加 + Column 3 条件分岐実装</name>
  <files>src/components/CommitGoalRow.tsx</files>
  <read_first>
    - src/components/CommitGoalRow.tsx — 全文（現在の props 型・Column 3 プレースホルダーの正確な位置）
    - .planning/phases/30-achievement/30-UI-SPEC.md — Achievement Icon Display Contract（States 表、Layout 項目、aria-label 値）
    - .planning/phases/30-achievement/30-CONTEXT.md — D-02（列幅・アイコン表示ルール）
  </read_first>
  <action>
    src/components/CommitGoalRow.tsx に以下の変更を加える（Server Component の 'use client' 禁止を維持）:

    1. `CommitGoalRowProps` 型に `streak: number` フィールドを追加する（imageUrl?: string の直後）
    2. 関数シグネチャのデストラクチャリングに `streak` を追加する
    3. Column 3（現在の `<div className="w-8 shrink-0" aria-hidden="true" />`、line 49–50）を以下の条件分岐に置き換える:
       - streak === 0: `<div className="w-8 shrink-0" aria-hidden="true" />`（変更なし）
       - streak === 1: `<div className="w-8 shrink-0 flex items-center justify-center"><span role="img" aria-label="今週達成" className="text-sm leading-none">👑</span></div>`
       - streak >= 2: `<div className="w-10 shrink-0 flex items-center justify-center gap-1"><span role="img" aria-label="連続達成" className="text-sm leading-none">👑🔥</span></div>`

    変更後の列幅ルール（per D-02・UI-SPEC）:
    - streak=0 または streak=1: Column 3 幅 = `w-8`（32px）
    - streak >= 2: Column 3 幅 = `w-10`（40px）

    emoji はそのまま JSX に書く（Unicode リテラル）。`text-primary` などカラーユーティリティは適用しない（UI-SPEC Color 項）。
  </action>
  <verify>
    <automated>npx vitest run src/components/__tests__/CommitGoalRow.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - src/components/CommitGoalRow.tsx の CommitGoalRowProps に `streak: number` が含まれる
    - ファイルに `aria-label="今週達成"` と `aria-label="連続達成"` の両方が存在する
    - ファイルに `w-10 shrink-0` クラスを持つ div が streak≥2 のブランチに存在する
    - ファイルに `aria-label="今週達成"` の span に `👑` が、`aria-label="連続達成"` の span に `👑🔥` が含まれる
    - `npx vitest run src/components/__tests__/CommitGoalRow.test.tsx` が全パスする（streak=0/1/2 の3ケース）
    - ファイル先頭に `'use client'` が存在しない（Server Component 維持）
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: CommitGoalView — streak 計算 + ヘッダースペーサー同期</name>
  <files>src/components/CommitGoalView.tsx</files>
  <read_first>
    - src/components/CommitGoalView.tsx — 全文（import行・ヘッダースペーサー line 35・sorted.map ループ lines 37–48）
    - src/lib/commitUtils.ts — consecutiveWeekStreak シグネチャ（Plan 01 実装後）
    - .planning/phases/30-achievement/30-RESEARCH.md — Pitfall 3（ヘッダースペーサー幅漏れ）・Pitfall 5（streak を CommitGoalRow 内で計算してはいけない）
  </read_first>
  <action>
    src/components/CommitGoalView.tsx に以下の変更を加える:

    1. import 行（line 3）を更新する:
       変更前: `import { sortMembersForCommitView } from '@/lib/commitUtils'`
       変更後: `import { sortMembersForCommitView, consecutiveWeekStreak } from '@/lib/commitUtils'`

    2. ヘッダースペーサー（line 35）を更新する（Pitfall 3 対策 — 原子的変更）:
       変更前: `<div className="w-8 shrink-0" />`
       変更後: `<div className="w-10 shrink-0" />`
       ※ streak≥2 時に Column 3 が w-10 になるため、ヘッダーも w-10 に固定して列ずれを防ぐ

    3. sorted.map ループ（lines 37–48）内の CommitGoalRow 呼び出しを更新する:
       - `memberSlots` の filter は既存のまま維持
       - `const streak = consecutiveWeekStreak(memberSlots, items)` を memberSlots の直後に追加
       - `<CommitGoalRow>` の props に `streak={streak}` を追加する

    Pitfall 5 厳守: streak 計算は CommitGoalView で行い CommitGoalRow に計算済み数値を渡す。CommitGoalRow に isoToJSTDateKey や getWeekDates の import を追加してはいけない。
  </action>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - src/components/CommitGoalView.tsx の import 行に `consecutiveWeekStreak` が含まれる
    - ファイル内のヘッダースペーサー `<div className=` に `w-10 shrink-0` が含まれ、`w-8 shrink-0` のスペーサー div が残っていない（ヘッダー行のスペーサーとして）
    - ファイルに `const streak = consecutiveWeekStreak(` が含まれる
    - ファイルに `streak={streak}` が CommitGoalRow props として含まれる
    - `npx vitest run` でフルスイートが全パスする（89 tests 以上）
    - ファイル先頭に `'use client'` が存在しない（Server Component 維持）
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consecutiveWeekStreak 返り値 → JSX 条件分岐 | number 型のため型安全。XSS リスクなし（emoji リテラル） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-03 | Information Disclosure | CommitGoalRow: aria-label | accept | aria-label は "今週達成"/"連続達成" の固定文字列。ユーザーデータを含まない |
| T-30-04 | Tampering | JSX emoji rendering | accept | React は文字列をエスケープするため Unicode emoji の XSS リスクはない |
| T-30-SC | Tampering | npm installs | accept | このフェーズは外部パッケージ追加なし |
</threat_model>

<verification>
1. `npx vitest run src/components/__tests__/CommitGoalRow.test.tsx` が全パスする（streak=0/1/2 の3ケース）
2. `npx vitest run` でフルスイートが全パスする
3. `grep -n "streak" src/components/CommitGoalRow.tsx` で props 型・デストラクチャ・条件分岐の3箇所にヒットする
4. `grep -n "w-10 shrink-0" src/components/CommitGoalView.tsx` でヘッダースペーサーがヒットする
5. `grep -n "consecutiveWeekStreak" src/components/CommitGoalView.tsx` でインポートと呼び出しの2箇所にヒットする
</verification>

<success_criteria>
- streak=0: 行にアイコンなし（テストパス）
- streak=1: 行に 👑 のみ表示（テストパス、aria-label="今週達成"）
- streak≥2: 行に 👑🔥 並列表示（テストパス、aria-label="連続達成"、w-10 列幅）
- CommitGoalView ヘッダースペーサーが w-10 でアイコン列と揃っている
- vitest フルスイートが全パス
</success_criteria>

<output>
Create `.planning/phases/30-achievement/30-02-SUMMARY.md` when done
</output>
