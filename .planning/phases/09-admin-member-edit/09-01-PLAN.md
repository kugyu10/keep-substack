---
phase: 09-admin-member-edit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/types.ts
  - src/lib/kvMembers.ts
  - src/app/admin/actions.ts
  - src/app/admin/AdminMemberList.tsx
  - src/app/admin/AdminAddForm.tsx
  - src/app/member/[substackId]/page.tsx
  - src/app/page.tsx
autonomous: true
requirements:
  - ADMIN-01

must_haves:
  truths:
    - "管理画面の各メンバー行に「編集」ボタンが表示される"
    - "編集ボタンを押すと同行のtdがinputに切り替わる（インライン編集）"
    - "substackIdは編集モード中も読み取り専用テキスト表示のまま"
    - "name・teamName・addedAtを変更して保存できる"
    - "保存後にメンバー一覧が最新データで更新される（revalidatePath）"
    - "空のnameや無効なaddedAt日付でバリデーションエラーが行内赤字表示される"
    - "teamIdフィールドがtreamNameにリネームされ、既存KVデータは後方互換で読み込まれる"
  artifacts:
    - path: "src/lib/types.ts"
      provides: "Member型のteamId→teamNameリネーム"
    - path: "src/lib/kvMembers.ts"
      provides: "getMembers後方互換読み込み + updateMember関数"
    - path: "src/app/admin/actions.ts"
      provides: "updateMemberAction Server Action + teamNameリネーム対応"
    - path: "src/app/admin/AdminMemberList.tsx"
      provides: "editingId stateによるインライン編集UI"
    - path: "src/app/admin/AdminAddForm.tsx"
      provides: "teamNameフォームフィールド名変更"
    - path: "src/app/member/[substackId]/page.tsx"
      provides: "teamName参照変更"
    - path: "src/app/page.tsx"
      provides: "teamName参照変更"
  key_links:
    - from: "src/app/admin/AdminMemberList.tsx"
      to: "src/app/admin/actions.ts"
      via: "updateMemberAction直接呼び出し（onClick）"
      pattern: "updateMemberAction\\(m\\.substackId"
    - from: "src/app/admin/actions.ts"
      to: "src/lib/kvMembers.ts"
      via: "updateMember関数呼び出し"
      pattern: "await updateMember\\(substackId"
    - from: "src/lib/kvMembers.ts"
      to: "redis"
      via: "redis.set('members', members)"
      pattern: "redis\\.set\\('members'"
    - from: "src/lib/kvMembers.ts"
      to: "既存KVデータ（teamIdキー）"
      via: "getMembers後方互換フォールバック"
      pattern: "teamName.*m\\.teamId"
---

<objective>
Phase 9: 管理画面メンバー編集機能の全実装。

3タスクで完結する単一プラン:
1. teamId→teamNameリネーム（型定義・全コンポーネント・後方互換）
2. updateMember KV関数 + updateMemberAction Server Action追加
3. AdminMemberList インライン編集UI実装

Purpose: 管理者が既存メンバーの name / teamName / addedAt を管理画面から直接変更できるようにする（ADMIN-01）。
Output: インライン編集UI付きの管理画面 + teamIdフィールドのtreamNameへの完全リネーム。
</objective>

<execution_context>
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/kugyu10/work/keep-substack/.planning/ROADMAP.md
@/Users/kugyu10/work/keep-substack/.planning/phases/09-admin-member-edit/09-RESEARCH.md

<interfaces>
<!-- 現在の型定義・実装。Task 1完了後に下記の通り変わる。 -->

From src/lib/types.ts (現在):
```typescript
export type Member = {
  name: string
  substackId: string
  teamId: string    // → teamName にリネーム（Task 1）
  addedAt: string   // ISO 8601
}
```

From src/lib/kvMembers.ts (現在):
```typescript
export async function getMembers(): Promise<Member[]>
export async function addMember(member: Omit<Member, 'addedAt'>): Promise<void>
export async function deleteMember(substackId: string): Promise<void>
// updateMember は未存在（Task 2で追加）
```

From src/app/admin/actions.ts (現在):
```typescript
export async function addMemberAction(prevState: string | null, formData: FormData): Promise<string | null>
export async function deleteMemberAction(substackId: string): Promise<void>
// updateMemberAction は未存在（Task 2で追加）
```

From src/app/admin/AdminMemberList.tsx (現在):
```typescript
// useState未使用、handleDelete のみ、teamId列を表示
type Props = { members: Member[] }
export default function AdminMemberList({ members }: Props)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: teamId→teamNameリネーム（全7ファイル）+ 後方互換読み込み</name>
  <files>
    src/lib/types.ts
    src/lib/kvMembers.ts
    src/app/admin/actions.ts
    src/app/admin/AdminAddForm.tsx
    src/app/member/[substackId]/page.tsx
    src/app/page.tsx
  </files>
  <action>
以下の変更を順番に実施する。Task 2・3の基盤となるためTypeScriptエラーが0件になることを確認してから次タスクに進む。

**src/lib/types.ts**
`teamId: string` を `teamName: string` に変更する。
```typescript
// 変更前（line 13）
teamId: string
// 変更後
teamName: string
```

**src/lib/kvMembers.ts**
`getMembers` を後方互換フォールバック付きに書き換える（D-05）。
既存KVデータには `teamId` キーが残っているため、読み込み時に `teamName ?? teamId ?? ''` で透過的にマッピングする。
```typescript
// 変更後の getMembers
export async function getMembers(): Promise<Member[]> {
  const members = await redis.get<any[]>('members')
  if (!members) return []
  return members.map((m) => ({
    ...m,
    teamName: m.teamName ?? m.teamId ?? '',
  }))
}
```
`addMember` の呼び出し引数 `Omit<Member, 'addedAt'>` は型が `Member` に追従するため変更不要。
ただし `addMember` 内で `teamId` を直接参照していないことを確認すること（kvMembers.ts line 18 付近）。

**src/app/admin/actions.ts**
`addMemberAction` 内の `teamId` 参照を `teamName` にリネームする。
```typescript
// 変更前
const teamId = formData.get('teamId') as string
await addMember({ name, substackId, teamId: teamId ?? '' })
// 変更後
const teamName = formData.get('teamName') as string
await addMember({ name, substackId, teamName: teamName ?? '' })
```

**src/app/admin/AdminAddForm.tsx**
フォームinputのname属性とplaceholderを変更する。
```tsx
// 変更前
name="teamId"
placeholder="teamId (任意)"
// 変更後
name="teamName"
placeholder="teamName (任意)"
```
AdminMemberListは Task 3で全面改修するためこのタスクでは変更不要。

**src/app/member/[substackId]/page.tsx**
`memberResult.member.teamId` を `memberResult.member.teamName` に変更する（line 29の2箇所）。
```tsx
// 変更前
href={memberResult.member.teamId ? `/?team=${memberResult.member.teamId}` : '/'}
// 変更後
href={memberResult.member.teamName ? `/?team=${memberResult.member.teamName}` : '/'}
```

**src/app/page.tsx**
`m.teamId` を `m.teamName` に変更する（line 14-15の2箇所）。
```typescript
// 変更前
const teams = [...new Set(allMembers.map((m) => m.teamId).filter(Boolean))]
const filteredMembers = team ? allMembers.filter((m) => m.teamId === team) : allMembers
// 変更後
const teams = [...new Set(allMembers.map((m) => m.teamName).filter(Boolean))]
const filteredMembers = team ? allMembers.filter((m) => m.teamName === team) : allMembers
```
  </action>
  <verify>
TypeScriptの型エラーが0件であること:
```bash
cd /Users/kugyu10/work/keep-substack && npx tsc --noEmit 2>&1 | head -30
```
teamId参照が残っていないこと（types.ts・kvMembers.ts・actions.ts・AdminAddForm.tsx・member/page.tsx・page.tsx）:
```bash
cd /Users/kugyu10/work/keep-substack && grep -rn "teamId" src/ --include="*.ts" --include="*.tsx" | grep -v "node_modules"
```
上記grepの出力は0件であること（`// teamId` コメント行を含む場合は内容を確認して問題なければOK）。
  </verify>
  <done>
- `src/lib/types.ts` の `Member.teamId` が `teamName` にリネームされている
- `src/lib/kvMembers.ts` の `getMembers` が `teamName ?? teamId ?? ''` フォールバック付きに変わっている
- `src/app/admin/actions.ts` の `addMemberAction` が `teamName` を参照している
- `src/app/admin/AdminAddForm.tsx` の input name が `teamName` になっている
- `src/app/member/[substackId]/page.tsx` と `src/app/page.tsx` で `teamName` を参照している
- `npx tsc --noEmit` がエラー0件で完了する
  </done>
</task>

<task type="auto">
  <name>Task 2: updateMember KV関数 + updateMemberAction Server Action追加</name>
  <files>
    src/lib/kvMembers.ts
    src/app/admin/actions.ts
  </files>
  <action>
**src/lib/kvMembers.ts**
`deleteMember` の直後に `updateMember` を追加する。`getMembers()` で全件取得→対象をfindIndex→スプレッド代入→`redis.set` で全件上書きのパターン（deleteMemberと同じ構造）。
```typescript
export async function updateMember(
  substackId: string,
  updates: Partial<Omit<Member, 'substackId'>>
): Promise<void> {
  const members = await getMembers()
  const idx = members.findIndex((m) => m.substackId === substackId)
  if (idx === -1) throw new Error(`メンバーが見つかりません: ${substackId}`)
  members[idx] = { ...members[idx], ...updates }
  await redis.set('members', members)
}
```

**src/app/admin/actions.ts**
1. importを `addMember, deleteMember, updateMember` に更新する（line 4）。
2. `deleteMemberAction` の直後に `updateMemberAction` を追加する。
   - シグネチャ: `(substackId: string, formData: FormData): Promise<string | null>`
   - バリデーション①: `!name` → `'name は必須です'` を返す（D-06）
   - バリデーション②: `new Date(addedAt).toString() === 'Invalid Date'` → `'addedAt は有効なISO日付文字列を入力してください'` を返す（D-03・D-06）
   - 成功: `revalidatePath('/admin')` して `null` を返す（D-07）
   - 例外キャッチ: `e instanceof Error ? e.message : '更新に失敗しました'`

```typescript
export async function updateMemberAction(
  substackId: string,
  formData: FormData
): Promise<string | null> {
  const name = formData.get('name') as string
  const teamName = formData.get('teamName') as string
  const addedAt = formData.get('addedAt') as string

  if (!name) return 'name は必須です'
  if (!addedAt || new Date(addedAt).toString() === 'Invalid Date') {
    return 'addedAt は有効なISO日付文字列を入力してください'
  }

  try {
    await updateMember(substackId, { name, teamName: teamName ?? '', addedAt })
    revalidatePath('/admin')
    return null
  } catch (e) {
    return e instanceof Error ? e.message : '更新に失敗しました'
  }
}
```
  </action>
  <verify>
TypeScriptエラーが0件であること:
```bash
cd /Users/kugyu10/work/keep-substack && npx tsc --noEmit 2>&1 | head -30
```
updateMember関数が存在すること:
```bash
grep -n "export async function updateMember" /Users/kugyu10/work/keep-substack/src/lib/kvMembers.ts
```
updateMemberActionが存在すること:
```bash
grep -n "export async function updateMemberAction" /Users/kugyu10/work/keep-substack/src/app/admin/actions.ts
```
  </verify>
  <done>
- `src/lib/kvMembers.ts` に `updateMember(substackId, updates)` が追加されている
- `src/app/admin/actions.ts` に `updateMemberAction(substackId, formData)` が追加されている
- importに `updateMember` が含まれている
- `npx tsc --noEmit` がエラー0件で完了する
  </done>
</task>

<task type="auto">
  <name>Task 3: AdminMemberList インライン編集UI実装</name>
  <files>
    src/app/admin/AdminMemberList.tsx
  </files>
  <action>
`AdminMemberList.tsx` を以下の仕様で全面改修する。

**追加import:**
```typescript
import { useState, useRef } from 'react'
import { deleteMemberAction, updateMemberAction } from './actions'
```

**追加state:**
```typescript
const [editingId, setEditingId] = useState<string | null>(null)
const [editError, setEditError] = useState<string | null>(null)
```

**handleUpdate関数を追加（handleDeleteの直後）:**
`<form>` タグは使わない（table内でのHTML仕様問題を回避。D-01 form-in-table問題）。
各行の input 要素に `useRef` は使わず、`button onClick` ハンドラ内で `event.currentTarget` の最近傍の `<tr>` を `closest('tr')` で取得し、その中の `input[name]` 要素から `FormData` を手動構築する。

```typescript
async function handleUpdate(substackId: string, e: React.MouseEvent<HTMLButtonElement>) {
  const tr = e.currentTarget.closest('tr')
  if (!tr) return
  const formData = new FormData()
  const inputs = tr.querySelectorAll<HTMLInputElement>('input[name]')
  inputs.forEach((input) => formData.append(input.name, input.value))
  const error = await updateMemberAction(substackId, formData)
  if (error) {
    setEditError(error)
  } else {
    setEditingId(null)
    setEditError(null)
  }
}
```

**テーブルヘッダー変更:**
`teamId` → `teamName` に変更する（line 21）。

**行レンダリングを条件分岐で書き換える:**
`editingId === m.substackId` のとき編集モード、そうでないとき通常表示モード。

通常表示モード（変更前の表示に「編集」ボタンを追加）:
```tsx
<tr key={m.substackId} className="hover:bg-gray-50">
  <td className="border px-3 py-2">{m.name}</td>
  <td className="border px-3 py-2">{m.substackId}</td>
  <td className="border px-3 py-2">{m.teamName}</td>
  <td className="border px-3 py-2 text-xs text-gray-500">
    {new Date(m.addedAt).toLocaleString('ja-JP', { timeZone: 'Asia/Tokyo' })}
  </td>
  <td className="border px-3 py-2">
    <button
      onClick={() => { setEditingId(m.substackId); setEditError(null) }}
      className="text-blue-600 hover:underline text-xs mr-2"
    >
      編集
    </button>
    <button
      onClick={() => handleDelete(m.substackId)}
      className="text-red-600 hover:underline text-xs"
    >
      削除
    </button>
  </td>
</tr>
```

編集モード（`editingId === m.substackId` のとき）:
- `name`: `<input defaultValue={m.name} name="name" className="border rounded px-1 w-full text-sm" />`
- `substackId`: テキスト表示のみ（D-02 変更禁止）
- `teamName`: `<input defaultValue={m.teamName} name="teamName" className="border rounded px-1 w-full text-sm" />`
- `addedAt`: `<input defaultValue={m.addedAt} name="addedAt" className="border rounded px-1 w-full text-sm font-mono text-xs" />`（ISO文字列直接入力、D-03）
- 操作列: 「保存」ボタン（`onClick={(e) => handleUpdate(m.substackId, e)}`）と「キャンセル」ボタン（`onClick={() => { setEditingId(null); setEditError(null) }}`）を表示。editError がある場合は `<p className="text-red-600 text-xs mt-1">{editError}</p>` を操作列内に表示。

```tsx
<tr key={m.substackId} className="hover:bg-gray-50">
  <td className="border px-3 py-2">
    <input defaultValue={m.name} name="name" className="border rounded px-1 w-full text-sm" />
  </td>
  <td className="border px-3 py-2 text-gray-400">{m.substackId}</td>
  <td className="border px-3 py-2">
    <input defaultValue={m.teamName} name="teamName" className="border rounded px-1 w-full text-sm" />
  </td>
  <td className="border px-3 py-2">
    <input defaultValue={m.addedAt} name="addedAt" className="border rounded px-1 w-full text-sm font-mono text-xs" />
  </td>
  <td className="border px-3 py-2">
    <button
      onClick={(e) => handleUpdate(m.substackId, e)}
      className="text-blue-600 hover:underline text-xs mr-2"
    >
      保存
    </button>
    <button
      onClick={() => { setEditingId(null); setEditError(null) }}
      className="text-gray-500 hover:underline text-xs"
    >
      キャンセル
    </button>
    {editError && <p className="text-red-600 text-xs mt-1">{editError}</p>}
  </td>
</tr>
```

`useRef` は使わないこと（D-04 KISS原則）。`defaultValue` を使い非制御コンポーネントとして扱うこと（`value` + `onChange` は不要）。
  </action>
  <verify>
TypeScriptエラーが0件であること:
```bash
cd /Users/kugyu10/work/keep-substack && npx tsc --noEmit 2>&1 | head -30
```
editingId stateが追加されていること:
```bash
grep -n "editingId" /Users/kugyu10/work/keep-substack/src/app/admin/AdminMemberList.tsx
```
updateMemberActionがimportされていること:
```bash
grep -n "updateMemberAction" /Users/kugyu10/work/keep-substack/src/app/admin/AdminMemberList.tsx
```
teamIdの残存参照がないこと:
```bash
grep -n "teamId" /Users/kugyu10/work/keep-substack/src/app/admin/AdminMemberList.tsx
```
ビルドが通ること:
```bash
cd /Users/kugyu10/work/keep-substack && npm run build 2>&1 | tail -20
```
  </verify>
  <done>
- `AdminMemberList.tsx` に `editingId` / `editError` stateが追加されている
- 各行に「編集」ボタンが表示され、クリックするとtdがinputに切り替わる
- `substackId` は編集モード中も読み取り専用テキスト表示
- 「保存」ボタンで `updateMemberAction` が呼ばれ、成功後 `editingId` がリセットされる
- バリデーションエラーは行内赤字表示される
- `npm run build` がエラーなく完了する
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ブラウザ→Server Action | `updateMemberAction` が受け取る `substackId` と `formData` は信頼できない入力 |
| Server Action→KV | `updateMember` が受け取る `substackId` が存在しない場合のエラー処理 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | `updateMemberAction` — `name` フィールド | mitigate | `if (!name) return 'name は必須です'` — Task 2で実装（D-06） |
| T-09-02 | Tampering | `updateMemberAction` — `addedAt` フィールド | mitigate | `new Date(addedAt).toString() === 'Invalid Date'` チェック — Task 2で実装（D-03・D-06） |
| T-09-03 | Tampering | `updateMember` — 存在しない `substackId` での更新 | mitigate | `findIndex === -1` 時に `throw new Error(...)` — Task 2で実装 |
| T-09-04 | Tampering | `updateMemberAction` — `substackId` の改ざん | accept | 管理画面はBasic認証で保護済み（既存認証）。このフェーズの対象外 |
| T-09-05 | Information Disclosure | KV後方互換フォールバック — `(m as any).teamId` | accept | 管理画面内部のデータ読み込みのみ。外部露出なし |
</threat_model>

<verification>
全タスク完了後の確認手順:

1. TypeScriptコンパイルエラーがないこと:
```bash
cd /Users/kugyu10/work/keep-substack && npx tsc --noEmit
```

2. ビルドが成功すること:
```bash
cd /Users/kugyu10/work/keep-substack && npm run build
```

3. `teamId` の残存参照がないこと（後方互換コード内の `m.teamId` は除外）:
```bash
grep -rn "teamId" /Users/kugyu10/work/keep-substack/src/ --include="*.ts" --include="*.tsx" | grep -v "m\.teamId\|teamId ??\|as any"
```
上記コマンドの出力が0件であること。

4. `updateMember` と `updateMemberAction` が定義されていること:
```bash
grep -rn "export async function update" /Users/kugyu10/work/keep-substack/src/ --include="*.ts"
```

5. `editingId` stateが `AdminMemberList.tsx` に存在すること:
```bash
grep -c "editingId" /Users/kugyu10/work/keep-substack/src/app/admin/AdminMemberList.tsx
```
出力が `0` でないこと。
</verification>

<success_criteria>
1. 管理画面（`/admin`）の各メンバー行に「編集」ボタンが表示される
2. 「編集」ボタンを押すと同行の name / teamName / addedAt セルが input に切り替わる
3. substackId は編集モード中も読み取り専用テキスト表示のまま（D-02）
4. 「保存」を押すとサーバーバリデーションが実行され、エラー時は行内赤字表示される（D-06）
5. 保存成功後にメンバー一覧が最新データで更新される（revalidatePath）
6. teamId フィールドが全体で teamName にリネームされており、既存KVデータが後方互換で読み込まれる（D-04・D-05）
7. `npx tsc --noEmit` および `npm run build` がエラー0件で完了する
</success_criteria>

<output>
Complete後、`.planning/phases/09-admin-member-edit/09-01-SUMMARY.md` を作成する。
</output>
