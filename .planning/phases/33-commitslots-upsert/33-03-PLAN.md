---
phase: 33-commitslots-upsert
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/schema.sql
  - src/app/my/actions.ts
autonomous: true
requirements:
  - DB-01

must_haves:
  truths:
    - "updateCommitSlotsAction が admin.rpc('replace_member_commit_slots', ...) を呼び出す（delete+insert は使わない）"
    - "RPC 呼び出しにエラーがあれば '保存に失敗しました。もう一度お試しください' を返す"
    - "既存のバリデーション（slots.length > 4, day_of_week 範囲, hour 範囲）は変更なし"
    - "schema.sql に replace_member_commit_slots 関数定義が追加されている"
  artifacts:
    - path: "supabase/schema.sql"
      provides: "replace_member_commit_slots RPC 関数定義（canonical source）"
      contains: "CREATE OR REPLACE FUNCTION replace_member_commit_slots"
    - path: "src/app/my/actions.ts"
      provides: "updateCommitSlotsAction が RPC 経由でアトミック保存"
  key_links:
    - from: "src/app/my/actions.ts"
      to: "member_commit_slots テーブル"
      via: "admin.rpc('replace_member_commit_slots', { p_member_id, p_slots })"
      pattern: "rpc.*replace_member_commit_slots"
---

<objective>
DB-01 のコードサイド変更を行う。updateCommitSlotsAction の delete+insert ブロック（非アトミック）を admin.rpc('replace_member_commit_slots', ...) 呼び出し1つに置き換える。また supabase/schema.sql に RPC 関数定義を追加する（canonical source として）。migration ファイルは Plan 04 の prod DB 適用直前に作成する（このプランではコード変更のみ）。

Purpose: commitSlots 保存をアトミックにし、ネットワーク遅延・再試行時の DELETE 後 INSERT 失敗によるデータ消失を排除する。
Output: src/app/my/actions.ts（DB write 部分置換）、supabase/schema.sql（RPC 関数追加）
</objective>

<execution_context>
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/kugyu10/work/keep-substack/.planning/PROJECT.md
@/Users/kugyu10/work/keep-substack/.planning/ROADMAP.md
@/Users/kugyu10/work/keep-substack/.planning/phases/33-commitslots-upsert/33-CONTEXT.md
@/Users/kugyu10/work/keep-substack/.planning/phases/33-commitslots-upsert/33-RESEARCH.md
@/Users/kugyu10/work/keep-substack/.planning/phases/33-commitslots-upsert/33-PATTERNS.md

<interfaces>
<!-- Key current code extracted from codebase. -->

src/app/my/actions.ts — updateCommitSlotsAction (lines 132-200):
- Lines 132-171: validation + auth check + member lookup — keep UNCHANGED
- Lines 173-199: TODO comment + delete + insert block — REPLACE ENTIRELY with RPC call
- Line 198: revalidatePath('/my') — keep after RPC call
- Imports at top include: createSupabaseAdminClient from '@/lib/supabase/admin'

The replacement block for lines 173-199 (from PATTERNS.md):
  const { error: rpcError } = await admin.rpc('replace_member_commit_slots', {
    p_member_id: member.id,
    p_slots: slots,
  })
  if (rpcError) {
    console.error('[updateCommitSlots] rpc failed:', rpcError)
    return '保存に失敗しました。もう一度お試しください'
  }
  revalidatePath('/my')
  return null

supabase/schema.sql — add after the last existing function definition:
  The file has sync_member_publications function ending around line 143.
  Add a new section "4. RPC Functions" with the replace_member_commit_slots function.
  See PATTERNS.md (schema.sql section) for the exact SQL block to append.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace delete+insert with admin.rpc in updateCommitSlotsAction</name>
  <files>src/app/my/actions.ts</files>
  <read_first>
    - src/app/my/actions.ts — read the full file before modifying; identify lines 132-200 (updateCommitSlotsAction), specifically lines 173-199 (the TODO + delete + insert block to replace)
    - .planning/phases/33-commitslots-upsert/33-PATTERNS.md (actions.ts section) — exact replacement block
    - .planning/phases/33-commitslots-upsert/33-RESEARCH.md (Pattern 3) — RPC call signature
  </read_first>
  <behavior>
    - Test 1 (SCHED-03): 認証済みユーザーで valid slots → rpc('replace_member_commit_slots', { p_member_id: MEMBER_ID, p_slots: [...] }) が呼ばれ null を返す
    - Test 2: 未認証 → AUTH_ERROR を返し rpc は呼ばれない（既存テスト2と同じ assertion, mockAdminRpc not called）
    - Test 3: slots が空配列 → rpc が p_slots: [] で呼ばれ null を返す（削除のみのケース）
    - Test 4-6: 既存のバリデーションテスト（slots.length > 4, day_of_week 0, hour 24）は変更なし
    - Test: rpc がエラーを返した場合 → '保存に失敗しました。もう一度お試しください' を返す
  </behavior>
  <action>
    In src/app/my/actions.ts, replace lines 173-199 (the TODO comment and the delete+insert block) with the RPC call block. Keep all lines 132-172 (validation through member lookup) and line 198's revalidatePath call position.

    Specifically:
    - Remove: the TODO comment block starting with `// TODO: delete+insert は非アトミック...`
    - Remove: the `const { error: deleteError } = await admin.from('member_commit_slots').delete()...` block and its error check
    - Remove: the `if (slots.length > 0) { const { error: insertError } = ...}` block and its error check
    - Add: `const { error: rpcError } = await admin.rpc('replace_member_commit_slots', { p_member_id: member.id, p_slots: slots })`
    - Add: error check `if (rpcError) { console.error('[updateCommitSlots] rpc failed:', rpcError); return '保存に失敗しました。もう一度お試しください' }`
    - Keep: `revalidatePath('/my')` and `return null`

    No import changes needed — `createSupabaseAdminClient` is already imported and `admin.rpc()` is a method on the client object.

    Note: slots is typed as `{ day_of_week: number; hour: number }[]` and will be passed directly as p_slots. supabase-js serializes it to JSONB automatically.
  </action>
  <verify>
    <automated>cd /Users/kugyu10/work/keep-substack && npm test -- src/app/my/__tests__/updateCommitSlotsAction.test.ts</automated>
  </verify>
  <done>
    - actions.ts updateCommitSlotsAction contains admin.rpc('replace_member_commit_slots', ...) and no longer has .delete() or .insert() calls for member_commit_slots
    - npm test (full suite) exits 0
  </done>
  <acceptance_criteria>
    - src/app/my/actions.ts contains `admin.rpc('replace_member_commit_slots',`
    - src/app/my/actions.ts does NOT contain `.from('member_commit_slots').delete()` (the old non-atomic pattern is gone)
    - src/app/my/actions.ts does NOT contain the `// TODO: delete+insert は非アトミック` comment
    - npm test exits 0 (note: updateCommitSlotsAction tests will FAIL until Plan 05 updates the test mocks — this is expected; full suite may show failures in that file only)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add replace_member_commit_slots function to supabase/schema.sql</name>
  <files>supabase/schema.sql</files>
  <read_first>
    - supabase/schema.sql — read the full file to find the correct insertion point (after the last existing function/trigger definition, around line 143); find the sync_member_publications function definition for structure reference
    - .planning/phases/33-commitslots-upsert/33-PATTERNS.md (schema.sql section) — exact SQL block to append and section header format
    - supabase/migrations/20260602000001_add_member_commit_slots.sql — confirms table structure (member_id UUID, day_of_week INT, hour INT)
  </read_first>
  <action>
    Append to supabase/schema.sql after the existing function/trigger definitions. Add:

    1. A section comment: `-- ============================================================`
       `-- 4. RPC Functions — アトミック書き込み`
       `-- ============================================================`

    2. A function description comment: `-- commitSlots の DELETE → INSERT をアトミックに行う。`
       `-- p_slots が空配列の場合は DELETE のみ実行（全スロット解除）。`

    3. The CREATE OR REPLACE FUNCTION statement:
       - Function name: replace_member_commit_slots
       - Parameters: p_member_id UUID, p_slots JSONB
       - Returns: void
       - Language: plpgsql
       - Body: DELETE FROM member_commit_slots WHERE member_id = p_member_id; then IF jsonb_array_length(p_slots) > 0 THEN INSERT ... FROM jsonb_array_elements(p_slots); END IF;

    See PATTERNS.md schema.sql section for the exact SQL to append. The function body performs:
    - DELETE FROM member_commit_slots WHERE member_id = p_member_id
    - IF jsonb_array_length(p_slots) > 0 THEN INSERT INTO member_commit_slots (member_id, day_of_week, hour) SELECT p_member_id, (elem->>'day_of_week')::INT, (elem->>'hour')::INT FROM jsonb_array_elements(p_slots) AS elem; END IF;

    Do NOT create a migration file in this task — that is done in Plan 04 immediately before the prod DB push.
  </action>
  <verify>
    <automated>cd /Users/kugyu10/work/keep-substack && grep -c "replace_member_commit_slots" supabase/schema.sql</automated>
  </verify>
  <done>
    - supabase/schema.sql contains the CREATE OR REPLACE FUNCTION replace_member_commit_slots definition
    - The function includes both DELETE and conditional INSERT logic
  </done>
  <acceptance_criteria>
    - grep -c "replace_member_commit_slots" supabase/schema.sql returns 1 or more (function present)
    - supabase/schema.sql contains `LANGUAGE plpgsql` for the new function
    - supabase/schema.sql contains `jsonb_array_length(p_slots)` in the function body
    - supabase/schema.sql contains `DELETE FROM member_commit_slots` inside the function body
    - npm test exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server Action → Supabase RPC | admin client (service role) calls DB function with member_id + slots array |
| Supabase RPC → member_commit_slots table | Function runs with SECURITY DEFINER implicitly — operates on the table directly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-06 | Tampering | updateCommitSlotsAction p_member_id | mitigate | member.id is derived from `admin.from('members').select('id').eq('user_id', user.id)` — user cannot spoof another member's ID since user.id comes from getUser() (server-validated) |
| T-33-07 | Tampering | p_slots JSONB injection | mitigate | slots is validated in TypeScript before being passed to RPC: Array.isArray, slot count ≤ 4, day_of_week 1-7, hour 0-23. Invalid shapes are rejected before DB call. |
| T-33-08 | Denial of Service | RPC bulk INSERT | accept | Slot count capped at 4 by server-side validation; INSERT is bounded |
| T-33-SC | Tampering | npm/pip/cargo installs | accept | No new packages in this plan |
</threat_model>

<verification>
After both tasks complete:
1. src/app/my/actions.ts contains admin.rpc('replace_member_commit_slots') and no .from('member_commit_slots').delete()
2. supabase/schema.sql contains CREATE OR REPLACE FUNCTION replace_member_commit_slots
3. npm test exits 0 (updateCommitSlotsAction tests in Plan 05 will update the mocks — full suite may show that file's tests failing until Plan 05)
</verification>

<success_criteria>
- updateCommitSlotsAction calls admin.rpc('replace_member_commit_slots', { p_member_id, p_slots }) instead of delete+insert
- schema.sql contains the RPC function definition (canonical source)
- All existing validation logic (slots count, day_of_week range, hour range) preserved unchanged
- npm test exits 0 (modulo updateCommitSlotsAction.test.ts which is updated in Plan 05)
</success_criteria>

<output>
Create `.planning/phases/33-commitslots-upsert/33-03-SUMMARY.md` when done
</output>
