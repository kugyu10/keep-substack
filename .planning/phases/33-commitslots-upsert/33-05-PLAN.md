---
phase: 33-commitslots-upsert
plan: 05
type: execute
wave: 2
depends_on:
  - "33-01"
  - "33-03"
files_modified:
  - src/app/my/__tests__/updateCommitSlotsAction.test.ts
  - e2e/admin-guard.spec.ts
autonomous: true
requirements:
  - BUG-01
  - BUG-02
  - DB-01

must_haves:
  truths:
    - "updateCommitSlotsAction のテストが admin.rpc モックを使って動作する（delete/insert スパイなし）"
    - "e2e/admin-guard.spec.ts の '未認証 /my → /' テストが '/login' リダイレクトアサーションに更新されている"
    - "npm test で全テストスイートがグリーンになる"
    - "npm run test:e2e で Playwright スイートがグリーンになる"
  artifacts:
    - path: "src/app/my/__tests__/updateCommitSlotsAction.test.ts"
      provides: "RPC モックで updateCommitSlotsAction をテストする suite"
    - path: "e2e/admin-guard.spec.ts"
      provides: "middleware 追加後の /my リダイレクト先 /login を検証する E2E"
  key_links:
    - from: "src/app/my/__tests__/updateCommitSlotsAction.test.ts"
      to: "mockAdminRpc.mockResolvedValue"
      via: "vi.mock createSupabaseAdminClient with rpc method"
      pattern: "mockAdminRpc"
    - from: "e2e/admin-guard.spec.ts"
      to: "/login redirect"
      via: "toHaveURL regex /login/"
      pattern: "haveURL.*login"
---

<objective>
Plan 01, 03 の変更に合わせてテストスイートを更新し、全テストをグリーンに戻す。updateCommitSlotsAction.test.ts は admin.rpc モックに対応する形に全面改訂。e2e/admin-guard.spec.ts は /my リダイレクト先が middleware により '/' から '/login?next=...' に変わったためアサーションを更新する。

Purpose: Phase 33 の全変更が正しく動作することを自動テストで保証する。npm test && npm run test:e2e が両方グリーンになることが phase gate 条件。
Output: updateCommitSlotsAction.test.ts（RPC mock 対応）、e2e/admin-guard.spec.ts（/login redirect アサーション更新）
</objective>

<execution_context>
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/kugyu10/work/keep-substack/.planning/PROJECT.md
@/Users/kugyu10/work/keep-substack/.planning/ROADMAP.md
@/Users/kugyu10/work/keep-substack/.planning/phases/33-commitslots-upsert/33-CONTEXT.md
@/Users/kugyu10/work/keep-substack/.planning/phases/33-commitslots-upsert/33-RESEARCH.md
@/Users/kugyu10/work/keep-substack/.planning/phases/33-commitslots-upsert/33-PATTERNS.md

<interfaces>
<!-- Key current test file contents extracted from codebase. -->

src/app/my/__tests__/updateCommitSlotsAction.test.ts (current — 149 lines):
- mockAdminFrom is the ONLY mock on the admin client (no rpc mock)
- setupAdminMock uses deleteSpy + insertSpy for member_commit_slots table
- Test 1 asserts deleteSpy + insertSpy called
- Test 3 asserts deleteSpy called, insertSpy NOT called (empty slots)
- Final two tests assert deleteError and insertError paths

e2e/admin-guard.spec.ts (current — 32 lines):
- Line 16-19: test '未認証で /my → / にリダイレクト (E2E-03)' asserts toHaveURL 'http://localhost:3000/'
- Line 11-14: test '未認証で /admin → / にリダイレクト' — DO NOT CHANGE (admin uses proxy.ts, not new middleware)
- Lines 25-32: non-admin logged-in /admin test — DO NOT CHANGE
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update updateCommitSlotsAction.test.ts for admin.rpc mock</name>
  <files>src/app/my/__tests__/updateCommitSlotsAction.test.ts</files>
  <read_first>
    - src/app/my/__tests__/updateCommitSlotsAction.test.ts — read the full current file (149 lines) before rewriting; understand all existing tests (テスト1〜テスト6 + deleteError + insertError)
    - src/app/my/actions.ts lines 132-200 — read the updated function (after Plan 03) to understand what mockAdminRpc receives
    - .planning/phases/33-commitslots-upsert/33-PATTERNS.md (updateCommitSlotsAction.test.ts section) — exact mock structure and updated test cases
    - src/app/login/__tests__/sendMagicLinkAction.test.ts — reference for vi.mock pattern style in this project
  </read_first>
  <behavior>
    - Test 1: 認証済み + valid slots → mockAdminRpc called with ('replace_member_commit_slots', { p_member_id: MEMBER_ID, p_slots: [{day_of_week:1, hour:8}] }) → returns null
    - Test 2: 未認証 → AUTH_ERROR returned, mockAdminRpc NOT called (unchanged behavior)
    - Test 3: slots = [] → mockAdminRpc called with p_slots: [] → returns null (全削除ケース)
    - Test 4: slots.length > 4 → '不正なスロット数です' (validation unchanged)
    - Test 5: day_of_week = 0 → '曜日の値が不正です' (validation unchanged)
    - Test 6: hour = 24 → '時刻の値が不正です' (validation unchanged)
    - Test: rpcError → '保存に失敗しました。もう一度お試しください'
  </behavior>
  <action>
    Rewrite src/app/my/__tests__/updateCommitSlotsAction.test.ts. Keep the overall describe block and the top-level mock structure, but replace the admin client mock and setupAdminMock function as follows:

    1. Add `mockAdminRpc` alongside `mockAdminFrom`:
       `const mockAdminFrom = vi.fn()`
       `const mockAdminRpc = vi.fn()`

    2. Update the vi.mock for '@/lib/supabase/admin' to include rpc:
       `createSupabaseAdminClient: vi.fn(() => ({ from: mockAdminFrom, rpc: mockAdminRpc }))`

    3. Replace setupAdminMock to accept `rpcError?: unknown` instead of `deleteError`/`insertError`:
       - member lookup via mockAdminFrom('members') stays the same (selectSpy pattern)
       - Remove the mock setup for 'member_commit_slots' table (no longer needed)
       - Add: `mockAdminRpc.mockResolvedValue({ error: opts.rpcError ?? null })`
       - Return `{ selectSpy, mockAdminRpc }`

    4. Update Test 1 assertion: replace `deleteSpy`/`insertSpy` assertions with:
       `expect(mockAdminRpc).toHaveBeenCalledWith('replace_member_commit_slots', { p_member_id: MEMBER_ID, p_slots: [{day_of_week: 1, hour: 8}] })`

    5. Update Test 2 assertion: replace `deleteSpy.not.toHaveBeenCalled()` with `mockAdminRpc.not.toHaveBeenCalled()` (or equivalent)

    6. Update Test 3: replace `deleteSpy called + insertSpy not called` with `mockAdminRpc called with p_slots: []`

    7. Tests 4-6 (validation): no changes needed — they return before reaching the DB call

    8. Replace `delete エラー` and `insert エラー` tests with a single `rpc エラー時` test:
       `setupAdminMock({ rpcError: { message: 'db error' } })` → expects '保存に失敗しました。もう一度お試しください'

    Preserve: MEMBER_ID constant, AUTH_ERROR constant, makeFormData helper, beforeEach clearAllMocks, mockGetUser.
  </action>
  <verify>
    <automated>cd /Users/kugyu10/work/keep-substack && npm test -- src/app/my/__tests__/updateCommitSlotsAction.test.ts</automated>
  </verify>
  <done>
    - All tests in updateCommitSlotsAction.test.ts pass with green status
    - No deleteSpy or insertSpy references remain in the test file
    - mockAdminRpc assertion appears in Tests 1 and 3
    - npm test (full suite) exits 0
  </done>
  <acceptance_criteria>
    - src/app/my/__tests__/updateCommitSlotsAction.test.ts contains `mockAdminRpc`
    - src/app/my/__tests__/updateCommitSlotsAction.test.ts contains `'replace_member_commit_slots'` in a toHaveBeenCalledWith assertion
    - src/app/my/__tests__/updateCommitSlotsAction.test.ts does NOT contain `deleteSpy` or `insertSpy`
    - `npm test -- src/app/my/__tests__/updateCommitSlotsAction.test.ts` exits 0 with all tests passing
    - npm test (full suite) exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Update e2e/admin-guard.spec.ts for new /my → /login redirect target</name>
  <files>e2e/admin-guard.spec.ts</files>
  <read_first>
    - e2e/admin-guard.spec.ts — read the full current file (32 lines); identify line 16-19 (the /my redirect test that must change) and lines 11-14 (the /admin test that must NOT change)
    - .planning/phases/33-commitslots-upsert/33-PATTERNS.md (e2e/admin-guard.spec.ts section) — exact updated assertion with regex pattern
    - src/middleware.ts — read after Plan 01 completes to verify the redirect URL shape (should produce /login?next=%2Fmy)
  </read_first>
  <action>
    In e2e/admin-guard.spec.ts, modify only the test on lines 16-19:

    OLD test name and assertion:
      test('未認証で /my → / にリダイレクト (E2E-03)', async ({ page }) => {
        await page.goto('/my')
        await expect(page).toHaveURL('http://localhost:3000/')
      })

    NEW test name and assertion:
      test('未認証で /my → /login にリダイレクト (E2E-03)', async ({ page }) => {
        await page.goto('/my')
        await expect(page).toHaveURL(/http:\/\/localhost:3000\/login(\?next=.*)?/)
      })

    Do NOT change:
    - The comment block at lines 1-8
    - The /admin test on lines 11-14 (still redirects to /)
    - The non-admin logged-in describe block on lines 25-32
    - Any imports or other test configuration

    Note: The regex /http:\/\/localhost:3000\/login(\?next=.*)?/ matches both '/login' and '/login?next=%2Fmy' to be resilient to query string encoding variations.
  </action>
  <verify>
    <automated>cd /Users/kugyu10/work/keep-substack && npm run build && npm run start &amp; sleep 5 &amp;&amp; npm run test:e2e -- e2e/admin-guard.spec.ts; kill %1 2>/dev/null</automated>
  </verify>
  <done>
    - admin-guard.spec.ts line 16 test name updated to '未認証で /my → /login にリダイレクト (E2E-03)'
    - Assertion uses regex matching /login( ?next=...)?
    - /admin test (line 11) unchanged
    - E2E suite passes for admin-guard.spec.ts
  </done>
  <acceptance_criteria>
    - e2e/admin-guard.spec.ts contains `/login` in the /my test assertion (not `'http://localhost:3000/'`)
    - e2e/admin-guard.spec.ts still contains `http://localhost:3000/` assertion for the /admin test (unchanged)
    - e2e/admin-guard.spec.ts does NOT contain `toHaveURL('http://localhost:3000/')` in the /my test block
    - npm run test:e2e exits 0 (all E2E specs pass, including admin-guard)
    - npm test exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test mocks → production code | Test mocks must accurately reflect the actual admin.rpc() call signature |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-11 | Tampering | test mock accuracy | mitigate | mockAdminRpc.toHaveBeenCalledWith asserts exact function name and params — detects signature drift |
| T-33-SC | Tampering | npm/pip/cargo installs | accept | No new packages |
</threat_model>

<verification>
After both tasks complete:
1. npm test exits 0 (all unit tests pass, including updateCommitSlotsAction with RPC mocks)
2. npm run test:e2e exits 0 (admin-guard.spec.ts passes with /login regex assertion)
3. No test file contains dead references to deleteSpy/insertSpy for member_commit_slots
</verification>

<success_criteria>
- updateCommitSlotsAction.test.ts uses mockAdminRpc (not delete/insert spies)
- e2e/admin-guard.spec.ts '/my' test asserts redirect to /login (not /)
- npm test exits 0
- npm run test:e2e exits 0 (full Playwright suite green)
- Phase 33 all requirements (BUG-01 skipped/confirmed, BUG-02, DB-01) have test coverage
</success_criteria>

<output>
Create `.planning/phases/33-commitslots-upsert/33-05-SUMMARY.md` when done
</output>
