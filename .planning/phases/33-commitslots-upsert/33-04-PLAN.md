---
phase: 33-commitslots-upsert
plan: 04
type: execute
wave: 2
depends_on:
  - "33-03"
files_modified:
  - supabase/migrations/20260608000000_add_replace_member_commit_slots_rpc.sql
autonomous: false
requirements:
  - DB-01

must_haves:
  truths:
    - "本番 DB (xolhjcngrwwwqtklmoyk) に replace_member_commit_slots 関数が存在する"
    - "migration ファイルが supabase/migrations/ に追加されている（プロジェクト規約）"
    - "/my でコミットスケジュールを保存すると本番環境でエラーなく保存される"
  artifacts:
    - path: "supabase/migrations/20260608000000_add_replace_member_commit_slots_rpc.sql"
      provides: "replace_member_commit_slots RPC 関数の差分 migration"
      contains: "CREATE OR REPLACE FUNCTION replace_member_commit_slots"
  key_links:
    - from: "supabase/migrations/20260608000000_add_replace_member_commit_slots_rpc.sql"
      to: "本番 DB (prod=xolhjcngrwwwqtklmoyk)"
      via: "Supabase SQL Editor 手動実行"
      pattern: "FUNCTION replace_member_commit_slots"
---

<objective>
DB-01 の本番 DB 反映フェーズ。migration ファイルを作成した後、そのSQL を Supabase SQL Editor (prod project: xolhjcngrwwwqtklmoyk) で実行して replace_member_commit_slots 関数を本番 DB に適用する。この手順は CLI では実行できないため checkpoint:human-action として設計されている。

Purpose: Plan 03 で変更した actions.ts の RPC 呼び出しが本番 DB で失敗しないよう、関数を事前にデプロイする。
Output: migration ファイル作成 + 本番 DB への SQL 適用
</objective>

<execution_context>
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/kugyu10/work/keep-substack/.planning/PROJECT.md
@/Users/kugyu10/work/keep-substack/.planning/ROADMAP.md
@/Users/kugyu10/work/keep-substack/.planning/phases/33-commitslots-upsert/33-CONTEXT.md
@/Users/kugyu10/work/keep-substack/.planning/phases/33-commitslots-upsert/33-RESEARCH.md
@/Users/kugyu10/work/keep-substack/.planning/phases/33-commitslots-upsert/33-PATTERNS.md

<interfaces>
<!-- Analog migration for structure reference -->

supabase/migrations/20260602000001_add_member_commit_slots.sql structure:
- BEGIN;
- -- Phase XX: description comment
- SQL body
- COMMIT;

Naming convention: YYYYMMDDHHMMSS_description.sql
New file name: 20260608000000_add_replace_member_commit_slots_rpc.sql

Supabase prod project ID: xolhjcngrwwwqtklmoyk
SQL Editor URL: https://supabase.com/dashboard/project/xolhjcngrwwwqtklmoyk/editor
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create migration file for replace_member_commit_slots RPC</name>
  <files>supabase/migrations/20260608000000_add_replace_member_commit_slots_rpc.sql</files>
  <read_first>
    - supabase/migrations/20260602000001_add_member_commit_slots.sql — read for BEGIN/COMMIT wrapper and comment style
    - supabase/schema.sql — verify the exact function definition already added in Plan 03 (copy from there to ensure consistency)
    - .planning/phases/33-commitslots-upsert/33-PATTERNS.md (migration file section) — exact file content
  </read_first>
  <action>
    Create supabase/migrations/20260608000000_add_replace_member_commit_slots_rpc.sql with the following structure:

    BEGIN;

    -- Phase 33: add replace_member_commit_slots RPC for atomic commit slot updates
    CREATE OR REPLACE FUNCTION replace_member_commit_slots(
      p_member_id UUID,
      p_slots     JSONB
    )
    RETURNS void
    LANGUAGE plpgsql
    AS $$
    BEGIN
      DELETE FROM member_commit_slots
      WHERE member_id = p_member_id;

      IF jsonb_array_length(p_slots) > 0 THEN
        INSERT INTO member_commit_slots (member_id, day_of_week, hour)
        SELECT
          p_member_id,
          (elem->>'day_of_week')::INT,
          (elem->>'hour')::INT
        FROM jsonb_array_elements(p_slots) AS elem;
      END IF;
    END;
    $$;

    COMMIT;

    The SQL content must be identical to what was added to schema.sql in Plan 03. Verify by reading supabase/schema.sql before writing.
  </action>
  <verify>
    <automated>cd /Users/kugyu10/work/keep-substack && grep -c "replace_member_commit_slots" supabase/migrations/20260608000000_add_replace_member_commit_slots_rpc.sql</automated>
  </verify>
  <done>
    - Migration file exists at the correct path
    - File contains BEGIN/COMMIT wrapper
    - File contains CREATE OR REPLACE FUNCTION replace_member_commit_slots
  </done>
  <acceptance_criteria>
    - supabase/migrations/20260608000000_add_replace_member_commit_slots_rpc.sql exists
    - grep -c "replace_member_commit_slots" supabase/migrations/20260608000000_add_replace_member_commit_slots_rpc.sql returns 1 or more
    - File contains `BEGIN;` and `COMMIT;`
    - File contains `jsonb_array_length(p_slots)` in the function body
  </acceptance_criteria>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [BLOCKING] Apply RPC migration to production Supabase database</name>
  <what-built>
    Migration file supabase/migrations/20260608000000_add_replace_member_commit_slots_rpc.sql has been created with the replace_member_commit_slots function SQL wrapped in BEGIN/COMMIT.

    The production Supabase project (xolhjcngrwwwqtklmoyk) does not have this function yet. The updateCommitSlotsAction code (Plan 03) now calls admin.rpc('replace_member_commit_slots', ...) — without applying this SQL to prod, the /my schedule save will fail with "function does not exist" in production.
  </what-built>
  <how-to-verify>
    1. Open: https://supabase.com/dashboard/project/xolhjcngrwwwqtklmoyk/editor
    2. Copy the entire contents of:
       supabase/migrations/20260608000000_add_replace_member_commit_slots_rpc.sql
    3. Paste into the SQL Editor and click "Run"
    4. Verify: the output shows no errors (expected: success or "function already exists" which is fine since it uses CREATE OR REPLACE)
    5. Verify the function exists: run this query in SQL Editor:
       SELECT proname FROM pg_proc WHERE proname = 'replace_member_commit_slots';
       Expected result: 1 row with proname = replace_member_commit_slots
    6. Optional smoke test: open https://keep-substack.vercel.app/my, set a commit schedule, save — confirm no error in the UI
  </how-to-verify>
  <resume-signal>
    Type "applied" after confirming the function exists in production (step 5 shows 1 row).
    If you encountered an error, describe it so we can diagnose.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQL Editor operator → prod DB | Human executes DDL in Supabase dashboard; no automated path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-09 | Tampering | RPC function DDL in prod | accept | CREATE OR REPLACE is idempotent. Function only deletes + inserts rows for the specific member_id passed — no cross-member access possible. |
| T-33-10 | Denial of Service | accidental full-table DELETE | mitigate | Function scopes DELETE to WHERE member_id = p_member_id — cannot accidentally delete all rows. Reviewed in migration file before execution. |
| T-33-SC | Tampering | npm/pip/cargo installs | accept | No packages installed |
</threat_model>

<verification>
After both tasks complete:
1. Migration file exists: supabase/migrations/20260608000000_add_replace_member_commit_slots_rpc.sql
2. Human confirmed: prod DB query `SELECT proname FROM pg_proc WHERE proname = 'replace_member_commit_slots'` returns 1 row
3. /my schedule save works in production (smoke test optional but recommended)
</verification>

<success_criteria>
- Migration file created at supabase/migrations/20260608000000_add_replace_member_commit_slots_rpc.sql
- replace_member_commit_slots function exists in production Supabase DB (xolhjcngrwwwqtklmoyk)
- Human has confirmed "applied" after verifying function existence via SQL Editor query
</success_criteria>

<output>
Create `.planning/phases/33-commitslots-upsert/33-04-SUMMARY.md` when done
</output>
