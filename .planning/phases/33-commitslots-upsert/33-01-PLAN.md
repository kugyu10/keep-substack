---
phase: 33-commitslots-upsert
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/middleware.ts
  - src/app/auth/callback/route.ts
autonomous: true
requirements:
  - BUG-02

must_haves:
  truths:
    - "未認証ユーザーが /my にアクセスすると /login?next=%2Fmy にリダイレクトされる"
    - "認証済みユーザーが /my にアクセスするとリダイレクトされずページが表示される"
    - "Magic Link クリック後に /auth/callback が next パラメータのパスへリダイレクトする（例: /my）"
    - "next パラメータが外部 URL の場合（// 始まり）は /my へフォールバックする"
  artifacts:
    - path: "src/middleware.ts"
      provides: "/my 認証ガード + セッションリフレッシュ"
      exports: ["middleware", "config"]
    - path: "src/app/auth/callback/route.ts"
      provides: "Magic Link コールバック — next パラメータを使ったリダイレクト"
  key_links:
    - from: "src/middleware.ts"
      to: "/login?next=..."
      via: "NextResponse.redirect"
      pattern: "redirect.*login.*next"
    - from: "src/app/auth/callback/route.ts"
      to: "next variable (line 14)"
      via: "NextResponse.redirect(new URL(next, origin))"
      pattern: "redirect.*new URL\\(next"
---

<objective>
BUG-02 のサーバーサイド修正を行う。新規ミドルウェア src/middleware.ts を作成して /my の認証ガードをミドルウェア層に移し、未認証時に /login?next=<currentPath> へリダイレクトする。また auth/callback/route.ts の line 59 を修正して、既に計算済みの next 変数をリダイレクトに使用するようにする（現在はハードコード /my）。

Purpose: ミドルウェアが /my を保護し、Magic Link 後に元ページへ戻れる UX を実現する第1段階。
Output: src/middleware.ts（新規）、src/app/auth/callback/route.ts（1行修正）
</objective>

<execution_context>
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/kugyu10/work/keep-substack/.planning/PROJECT.md
@/Users/kugyu10/work/keep-substack/.planning/ROADMAP.md
@/Users/kugyu10/work/keep-substack/.planning/phases/33-commitslots-upsert/33-CONTEXT.md
@/Users/kugyu10/work/keep-substack/.planning/phases/33-commitslots-upsert/33-RESEARCH.md
@/Users/kugyu10/work/keep-substack/.planning/phases/33-commitslots-upsert/33-PATTERNS.md

<interfaces>
<!-- Key patterns extracted from codebase. Use directly — no exploration needed. -->

From src/proxy.ts (exact analog for middleware.ts):
- Uses createServerClient from @supabase/ssr with getAll/setAll cookies API
- Calls supabase.auth.getUser() immediately after createServerClient (no code between)
- Returns NextResponse.next({ request }) as supabaseResponse
- matcher: ['/admin', '/admin/:path*', '/my', '/my/:path*']

From src/app/auth/callback/route.ts (lines 10-14 already correct, line 59 is the fix):
- Line 10: const nextParam = searchParams.get('next') ?? '/my'
- Line 14: const next = nextParam.startsWith('/') && !nextParam.startsWith('//') ? nextParam : '/my'
- Line 59 (CURRENT): return NextResponse.redirect(new URL('/my', origin))
- Line 59 (TARGET):   return NextResponse.redirect(new URL(next, origin))
- The TODO comment on lines 12-13 should also be removed.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create src/middleware.ts — /my auth guard with /login?next= redirect</name>
  <files>src/middleware.ts</files>
  <read_first>
    - src/proxy.ts — exact analog; copy the createServerClient + cookies + getUser() pattern verbatim, then apply the three deltas listed below
    - src/app/auth/callback/route.ts lines 10-14 — Open Redirect guard pattern to apply in middleware
    - .planning/phases/33-commitslots-upsert/33-RESEARCH.md (Pattern 1) — middleware implementation reference
    - .planning/phases/33-commitslots-upsert/33-PATTERNS.md (middleware.ts section) — delta list
  </read_first>
  <behavior>
    - Test 1: GET /my with no session cookie → response redirects to URL matching /login?next=%2Fmy
    - Test 2: GET /my with valid session → response is NextResponse.next() (no redirect)
    - Test 3: GET /my/settings with no session → response redirects to URL matching /login?next=%2Fmy%2Fsettings
  </behavior>
  <action>
    Create src/middleware.ts (new file — does not exist yet). Base it on src/proxy.ts with these three specific changes:

    1. Export name: rename `proxy` → `middleware` (the function export)
    2. Redirect target for !user case: change from `new URL('/', request.url)` to a URL built as:
       - pathname = '/login'
       - search = `?next=${encodeURIComponent(request.nextUrl.pathname)}`
       Use `request.nextUrl.clone()` to build the URL safely (see RESEARCH.md Pattern 1 code example).
    3. matcher in `export const config`: keep only ['/my', '/my/:path*'] — remove '/admin' and '/admin/:path*'

    Remove the /admin guard block entirely (lines 34-38 in proxy.ts analog). Keep only the /my guard.

    Do NOT touch src/proxy.ts — it is a unit-testable module for the existing proxy.test.ts and must remain unchanged (Pitfall 1 in RESEARCH.md).

    The session refresh (supabaseResponse / let response pattern with setAll) must be preserved exactly as in proxy.ts so tokens are refreshed on every request.

    Write src/middleware.test.ts alongside the implementation with the three behavior tests above, mocking @supabase/ssr createServerClient. The test file goes in src/ (not src/__tests__) to match vitest.config.ts include pattern src/**.
  </action>
  <verify>
    <automated>cd /Users/kugyu10/work/keep-substack && npm test -- --reporter=verbose src/middleware.test.ts</automated>
  </verify>
  <done>
    - src/middleware.ts exists and exports `middleware` function and `config` object
    - config.matcher is ['/my', '/my/:path*']
    - src/middleware.test.ts passes: 3 tests green (redirect to /login?next=..., pass-through for authed, /my/sub path encoded)
    - npm test exits 0 for the middleware test file
  </done>
  <acceptance_criteria>
    - src/middleware.ts contains `export async function middleware(`
    - src/middleware.ts contains `export const config` with matcher `['/my', '/my/:path*']`
    - src/middleware.ts contains `encodeURIComponent(` for building the next param
    - src/middleware.ts contains `/login` as redirect target (not `/`)
    - src/middleware.ts does NOT contain `/admin` in the guard logic
    - src/middleware.test.ts exists and `npm test -- src/middleware.test.ts` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Fix auth/callback/route.ts — use next variable instead of hardcoded '/my'</name>
  <files>src/app/auth/callback/route.ts</files>
  <read_first>
    - src/app/auth/callback/route.ts — read the full current file; identify line 59 (hardcoded /my redirect) and lines 12-13 (TODO comment to remove)
  </read_first>
  <action>
    In src/app/auth/callback/route.ts, make two changes:

    1. Line 59: Change `return NextResponse.redirect(new URL('/my', origin))` to `return NextResponse.redirect(new URL(next, origin))`.
       The `next` variable is already correctly computed at line 14 with Open Redirect validation.

    2. Remove the TODO comment on lines 12-13:
       Remove lines: `// TODO: next は現在ハードコードの '/my' リダイレクトに上書きされており未使用。`
       and: `//       signin/login 両アクションが next= を渡すようになったら復活させること。`

    No other changes. The rest of the file (exchangeCodeForSession, pid/handle member linking logic) is untouched.
  </action>
  <verify>
    <automated>cd /Users/kugyu10/work/keep-substack && npm test</automated>
  </verify>
  <done>
    - Line 59 of auth/callback/route.ts uses `new URL(next, origin)` (not `new URL('/my', origin)`)
    - TODO comment on lines 12-13 is removed
    - Full test suite passes (npm test exits 0)
  </done>
  <acceptance_criteria>
    - src/app/auth/callback/route.ts contains `return NextResponse.redirect(new URL(next, origin))`
    - src/app/auth/callback/route.ts does NOT contain `new URL('/my', origin)` (the hardcoded redirect is gone)
    - src/app/auth/callback/route.ts does NOT contain the "TODO: next は現在ハードコード" comment
    - npm test exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → middleware | Unauthenticated request with arbitrary path; next param derived from pathname (internal, not query input) |
| Magic Link URL → /auth/callback | `next` query param arrives from email link; attacker could craft a link with malicious next value |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-01 | Spoofing | /auth/callback next param | mitigate | Open Redirect guard already at line 14: `startsWith('/') && !startsWith('//')` — this plan's Task 2 activates it by using the `next` variable instead of hardcoded '/my' |
| T-33-02 | Elevation of Privilege | middleware.ts getUser() | mitigate | Use getUser() (server-validated) not getSession() (client trust); pattern from proxy.ts — no change required |
| T-33-03 | Tampering | middleware cookie setAll | accept | Supabase SSR handles concurrent refresh; setAll pattern from proxy.ts is established and correct |
| T-33-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan — all deps already in production |
</threat_model>

<verification>
After both tasks complete:
1. `npm test` exits 0 (all existing tests pass + new middleware.test.ts passes)
2. src/middleware.ts exists with correct export name and matcher
3. src/app/auth/callback/route.ts line using `new URL(next, origin)` is present
4. grep -c "new URL('/my', origin)" src/app/auth/callback/route.ts returns 0
</verification>

<success_criteria>
- src/middleware.ts created, exports `middleware` + `config`, matcher = ['/my', '/my/:path*']
- Unauthenticated /my → /login?next=%2Fmy (verified by middleware unit test)
- auth/callback uses `next` variable for redirect (not hardcoded '/my')
- npm test exits 0
</success_criteria>

<output>
Create `.planning/phases/33-commitslots-upsert/33-01-SUMMARY.md` when done
</output>
