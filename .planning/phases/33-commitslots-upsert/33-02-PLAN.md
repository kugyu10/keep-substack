---
phase: 33-commitslots-upsert
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/login/page.tsx
  - src/app/login/LoginForm.tsx
  - src/app/login/actions.ts
  - src/app/login/__tests__/sendMagicLinkAction.test.ts
autonomous: true
requirements:
  - BUG-02

must_haves:
  truths:
    - "/login?next=/my にアクセスすると LoginForm に hidden input name='next' value='/my' が含まれる"
    - "sendMagicLinkAction が next 付きで呼び出されると callbackUrl が /auth/callback?next=%2Fmy になる"
    - "sendMagicLinkAction が next なしで呼び出されると callbackUrl が /auth/callback のまま（後方互換）"
    - "next が外部パス（// 始まり）の場合、page.tsx でバリデーション除外され hidden input に渡らない"
  artifacts:
    - path: "src/app/login/page.tsx"
      provides: "searchParams.next を読み取り LoginForm に渡す Server Component"
    - path: "src/app/login/LoginForm.tsx"
      provides: "next prop を受け取り hidden input を render する Client Component"
    - path: "src/app/login/actions.ts"
      provides: "next に応じて callbackUrl に ?next= を付与する Server Action"
  key_links:
    - from: "src/app/login/page.tsx"
      to: "LoginForm next prop"
      via: "<LoginForm next={safeNext} />"
      pattern: "LoginForm.*next="
    - from: "src/app/login/LoginForm.tsx"
      to: "sendMagicLinkAction formData"
      via: "<input type='hidden' name='next'"
      pattern: "input.*type.*hidden.*name.*next"
    - from: "src/app/login/actions.ts"
      to: "/auth/callback?next="
      via: "formData.get('next') → callbackUrl"
      pattern: "callbackUrl.*next"
---

<objective>
BUG-02 のログインサイド修正を行う。/login ページが searchParams から next パラメータを受け取り LoginForm に渡す。LoginForm は next を hidden input として form に埋め込む。sendMagicLinkAction は formData.get('next') を読み取り emailRedirectTo に ?next= を付与する。この3つの変更で next パラメータが Magic Link メールの callbackUrl まで伝搬する。

Purpose: BUG-02 の login-side フロー完成。Plan 01（middleware + callback）と組み合わせて end-to-end のリダイレクト復元が完成する。
Output: page.tsx・LoginForm.tsx・actions.ts の3ファイル修正 + sendMagicLinkAction.test.ts 更新
</objective>

<execution_context>
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/kugyu10/work/keep-substack/.planning/PROJECT.md
@/Users/kugyu10/work/keep-substack/.planning/ROADMAP.md
@/Users/kugyu10/work/keep-substack/.planning/phases/33-commitslots-upsert/33-CONTEXT.md
@/Users/kugyu10/work/keep-substack/.planning/phases/33-commitslots-upsert/33-RESEARCH.md
@/Users/kugyu10/work/keep-substack/.planning/phases/33-commitslots-upsert/33-PATTERNS.md

<interfaces>
<!-- Key current file contents extracted from codebase. -->

src/app/login/page.tsx (current — 16 lines):
- Exports default async function LoginPage() with no props
- Creates supabase server client, calls getUser(), if user redirect('/my')
- Returns <main> with <h1>ログイン</h1> and <LoginForm />

src/app/login/LoginForm.tsx (current — 48 lines):
- 'use client' component
- export default function LoginForm() — no props
- useActionState(sendMagicLinkAction, null) for state/action/isPending
- Returns <form action={action}> with email input and submit button

src/app/login/actions.ts (current — 39 lines):
- callbackUrl = `${origin}/auth/callback` (line 17 — static, no next)
- formData only reads 'email' — does not read 'next'

src/app/login/__tests__/sendMagicLinkAction.test.ts (current — 69 lines):
- Test L-2 asserts emailRedirectTo contains '/auth/callback' and NOT 'pid=' / NOT 'handle='
- makeFormData(email: string) helper — only sets email field
- No test for next= param yet
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update login page, LoginForm, and sendMagicLinkAction for next param propagation</name>
  <files>src/app/login/page.tsx, src/app/login/LoginForm.tsx, src/app/login/actions.ts</files>
  <read_first>
    - src/app/login/page.tsx — read full current file before modifying (16 lines)
    - src/app/login/LoginForm.tsx — read full current file before modifying (48 lines)
    - src/app/login/actions.ts — read full current file before modifying (39 lines)
    - .planning/phases/33-commitslots-upsert/33-PATTERNS.md (sections for login/page.tsx, LoginForm.tsx, actions.ts) — exact change instructions and code patterns
    - .planning/phases/33-commitslots-upsert/33-RESEARCH.md (Pattern 2) — searchParams prop pattern + Open Redirect guard
  </read_first>
  <behavior>
    - Test: page.tsx with searchParams { next: '/my' } renders <LoginForm next="/my" /> (safeNext = '/my')
    - Test: page.tsx with searchParams { next: '//evil.com' } renders <LoginForm /> with no next prop (safeNext = undefined, Open Redirect blocked)
    - Test: page.tsx with no searchParams renders <LoginForm /> with no next prop
    - Test: LoginForm with next="/my" renders <input type="hidden" name="next" value="/my" />
    - Test: LoginForm with no next prop renders no hidden input
    - Test (sendMagicLinkAction): formData with email + next='/my' → emailRedirectTo ends with /auth/callback?next=%2Fmy
    - Test (sendMagicLinkAction): formData with email only (no next) → emailRedirectTo is exactly `${origin}/auth/callback` (no ?next=)
  </behavior>
  <action>
    Modify three files as follows:

    **src/app/login/page.tsx:**
    Add `searchParams: Promise<{ next?: string }>` prop to the component function signature. After the existing auth check (`if (user) redirect('/my')`), add:
    - `const { next } = await searchParams`
    - `const safeNext = next && next.startsWith('/') && !next.startsWith('//') ? next : undefined`
    Pass `next={safeNext}` to `<LoginForm />` (when safeNext is defined). When safeNext is undefined, LoginForm receives no next prop.

    **src/app/login/LoginForm.tsx:**
    Add `next?: string` prop to the component function signature.
    Inside `<form action={action}>`, before the email input div, add: `{next && <input type="hidden" name="next" value={next} />}`

    **src/app/login/actions.ts:**
    After the existing `const origin = headersList.get('origin') ?? ''` line, add:
    - `const next = formData.get('next') as string | null`
    Replace the static `const callbackUrl = \`${origin}/auth/callback\`` with:
    - `const callbackUrl = next ? \`${origin}/auth/callback?next=${encodeURIComponent(next)}\` : \`${origin}/auth/callback\``

    Do NOT change the error handling lines (27-38). Do NOT change the signInWithOtp call structure.

    Note on double-encoding (Pitfall 3): page.tsx validates and passes `next` as a plain string (e.g., '/my'). actions.ts encodes it with encodeURIComponent into the URL. auth/callback receives it already URL-decoded via searchParams.get('next'). This is the correct single-encode flow.
  </action>
  <verify>
    <automated>cd /Users/kugyu10/work/keep-substack && npm test</automated>
  </verify>
  <done>
    - page.tsx accepts searchParams prop and computes safeNext with Open Redirect guard
    - LoginForm accepts next?: string prop and renders hidden input when next is defined
    - sendMagicLinkAction reads formData.get('next') and appends ?next= to callbackUrl when present
    - npm test exits 0
  </done>
  <acceptance_criteria>
    - src/app/login/page.tsx contains `searchParams: Promise<{ next?: string }>`
    - src/app/login/page.tsx contains `startsWith('/') && !next.startsWith('//')` (Open Redirect guard)
    - src/app/login/LoginForm.tsx contains `{ next?: string }` in its props type
    - src/app/login/LoginForm.tsx contains `type="hidden" name="next"`
    - src/app/login/actions.ts contains `formData.get('next')`
    - src/app/login/actions.ts contains `encodeURIComponent(next)` in the callbackUrl construction
    - npm test exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update sendMagicLinkAction.test.ts — add Test L-2b for ?next= callbackUrl</name>
  <files>src/app/login/__tests__/sendMagicLinkAction.test.ts</files>
  <read_first>
    - src/app/login/__tests__/sendMagicLinkAction.test.ts — read full current file (69 lines) to understand existing tests before modifying
    - .planning/phases/33-commitslots-upsert/33-PATTERNS.md (sendMagicLinkAction.test.ts section) — describes what changes are needed to the test
    - .planning/phases/33-commitslots-upsert/33-RESEARCH.md (Validation Architecture section) — Test L-2 update description
  </read_first>
  <behavior>
    - Existing Test L-1: email のみ → SENT (keep as-is)
    - Existing Test L-2: callbackUrl に pid/handle が含まれない — update makeFormData helper to accept optional next, and update this test or add L-2b
    - New Test L-2b: email + next='/my' → emailRedirectTo = `http://localhost/auth/callback?next=%2Fmy`
    - New Test L-2c: email + no next → emailRedirectTo = `http://localhost/auth/callback` (no ?next=)
    - Existing Test L-3, L-4: keep unchanged
  </behavior>
  <action>
    In src/app/login/__tests__/sendMagicLinkAction.test.ts:

    1. Update `makeFormData` helper to accept an optional second arg `next?: string`:
       - When next is provided, append it: `fd.append('next', next)`

    2. Keep existing Test L-2 passing — it calls makeFormData('user@example.com') with no next, so callbackUrl will be `/auth/callback` only. The existing assertions still hold.

    3. Add new test "Test L-2b: next='/my' のとき callbackUrl に ?next=%2Fmy が付与される":
       - Call makeFormData('user@example.com', '/my')
       - Expect emailRedirectTo to equal `http://localhost/auth/callback?next=%2Fmy`

    4. Add new test "Test L-2c: next なし のとき callbackUrl に ?next= が付与されない":
       - Call makeFormData('user@example.com') (no next)
       - Expect emailRedirectTo to equal `http://localhost/auth/callback` (exact, no query string)

    Do not change the existing describe block name or the existing test names (L-1, L-2, L-3, L-4).
  </action>
  <verify>
    <automated>cd /Users/kugyu10/work/keep-substack && npm test -- src/app/login/__tests__/sendMagicLinkAction.test.ts</automated>
  </verify>
  <done>
    - sendMagicLinkAction.test.ts has Tests L-1, L-2, L-2b, L-2c, L-3, L-4 all passing
    - Test L-2b verifies ?next=%2Fmy is appended
    - Test L-2c verifies no next= when formData has no next field
    - npm test exits 0
  </done>
  <acceptance_criteria>
    - src/app/login/__tests__/sendMagicLinkAction.test.ts contains `L-2b` test
    - src/app/login/__tests__/sendMagicLinkAction.test.ts contains `%2Fmy` in an expectation (encoded /my)
    - `npm test -- src/app/login/__tests__/sendMagicLinkAction.test.ts` exits 0 with all tests passing
    - npm test (full suite) exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser query string → page.tsx | next param arrives as URL query; may be attacker-controlled |
| LoginForm hidden input → Server Action | form POST carries next value; user could modify DOM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-04 | Spoofing | page.tsx searchParams.next | mitigate | Open Redirect guard in page.tsx: `startsWith('/') && !startsWith('//')` before passing to LoginForm (per D-04) |
| T-33-05 | Spoofing | actions.ts formData.get('next') | accept | page.tsx validates before embedding in hidden input; client-side DOM manipulation would only affect the acting user (self-harm); auth/callback has its own validation layer |
| T-33-SC | Tampering | npm/pip/cargo installs | accept | No new packages — all deps already in production |
</threat_model>

<verification>
After both tasks complete:
1. npm test exits 0 (all suites pass including sendMagicLinkAction tests L-1 through L-2c)
2. page.tsx contains searchParams prop and safeNext validation
3. LoginForm.tsx contains hidden input for next
4. actions.ts callbackUrl includes encodeURIComponent(next) branch
</verification>

<success_criteria>
- Login flow propagates next param end-to-end: page.tsx → hidden input → actions.ts → callbackUrl
- Open Redirect protection applied at page.tsx boundary (startsWith('/') && !startsWith('//'))
- sendMagicLinkAction.test.ts passes all tests including new L-2b (next=/my encoded) and L-2c (no next)
- npm test exits 0
</success_criteria>

<output>
Create `.planning/phases/33-commitslots-upsert/33-02-SUMMARY.md` when done
</output>
