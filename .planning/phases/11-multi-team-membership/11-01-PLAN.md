---
phase: 11-multi-team-membership
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/types.ts
  - src/lib/kvMembers.ts
  - src/app/page.tsx
  - src/app/admin/AdminAddForm.tsx
  - src/app/admin/AdminMemberList.tsx
  - src/app/admin/actions.ts
  - src/app/member/[substackId]/page.tsx
autonomous: true
requirements:
  - TEAM-01
  - TEAM-02
  - TEAM-03

must_haves:
  truths:
    - "1人のメンバーが複数チームに所属できる（teamNames: string[] でKVに保存される）"
    - "KVに保存済みの旧データ（teamName: string）が読み込み時に自動変換され、チームフィルターで正しく表示される"
    - "管理画面でカンマ区切り入力によって複数チームを追加・編集できる"
    - "チームタブで絞り込んだとき、そのチームに所属するメンバー全員が表示される（複数チーム所属も含む）"
    - "メンバー詳細ページの戻りリンクが所属チーム数に応じて単数・複数を正しく表示する"
  artifacts:
    - path: "src/lib/types.ts"
      provides: "Member.teamNames: string[] 型定義"
      contains: "teamNames: string[]"
    - path: "src/lib/kvMembers.ts"
      provides: "後方互換フォールバック（旧 teamName を teamNames に変換）"
      contains: "m.teamNames ?? (m.teamName ? [m.teamName] : [])"
    - path: "src/app/page.tsx"
      provides: "flatMap によるチーム重複除去・includes によるフィルタリング"
      contains: "flatMap"
    - path: "src/app/admin/actions.ts"
      provides: "カンマ区切り文字列を string[] に変換する Server Actions"
      contains: "split(',').map"
    - path: "src/app/admin/AdminAddForm.tsx"
      provides: "teamNames input（name属性が teamNames）"
      contains: "name=\"teamNames\""
    - path: "src/app/admin/AdminMemberList.tsx"
      provides: "teamNames の表示・編集UI"
      contains: "teamNames.join"
    - path: "src/app/member/[substackId]/page.tsx"
      provides: "複数チームの戻りリンク（encodeURIComponent 使用）"
      contains: "encodeURIComponent"
  key_links:
    - from: "src/app/admin/AdminAddForm.tsx"
      to: "src/app/admin/actions.ts"
      via: "form input name=\"teamNames\""
      pattern: "name=\"teamNames\""
    - from: "src/app/admin/AdminMemberList.tsx"
      to: "src/app/admin/actions.ts"
      via: "formData.append の name が teamNames"
      pattern: "name=\"teamNames\""
    - from: "src/lib/kvMembers.ts"
      to: "src/lib/types.ts"
      via: "Member 型インポート"
      pattern: "import type \\{ Member \\}"
    - from: "src/app/page.tsx"
      to: "src/lib/kvMembers.ts"
      via: "getMembers() 呼び出し"
      pattern: "getMembers"
---

<objective>
`Member.teamName: string` を `Member.teamNames: string[]` に変更し、1人が複数チームに所属できるようにする。

Purpose: KVに保存された旧データの後方互換性を保ちながら、複数チーム所属を全レイヤー（型・KV層・フィルター・管理UI・詳細ページ）に反映する。
Output:
- src/lib/types.ts — teamNames: string[] 型定義
- src/lib/kvMembers.ts — 後方互換フォールバック付き getMembers
- src/app/page.tsx — flatMap + includes によるチームタブ・フィルター
- src/app/admin/AdminAddForm.tsx — teamNames input
- src/app/admin/AdminMemberList.tsx — teamNames 表示・編集UI
- src/app/admin/actions.ts — string[] 変換ロジック
- src/app/member/[substackId]/page.tsx — 複数チームの戻りリンク
</objective>

<execution_context>
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/kugyu10/work/keep-substack/.planning/ROADMAP.md
@/Users/kugyu10/work/keep-substack/.planning/phases/11-multi-team-membership/11-CONTEXT.md
@/Users/kugyu10/work/keep-substack/.planning/phases/11-multi-team-membership/11-RESEARCH.md

<interfaces>
<!-- 変更前の現行コード。Executor はこれを参照して差分を適用する。 -->

From src/lib/types.ts (現行):
```typescript
export type Member = {
  name: string
  substackId: string
  teamName: string      // ← 削除し teamNames: string[] に変更
  addedAt: string
}
```

From src/lib/kvMembers.ts — getMembers() (現行):
```typescript
return members.map((m) => ({
  ...m,
  teamName: m.teamName ?? m.teamId ?? '',   // ← Phase 9フォールバック
}))
```

From src/app/page.tsx (現行):
```typescript
const teams = [...new Set(allMembers.map((m) => m.teamName).filter(Boolean))]
const filteredMembers = team ? allMembers.filter((m) => m.teamName === team) : allMembers
```

From src/app/admin/AdminMemberList.tsx (現行、編集モード):
```tsx
<input defaultValue={m.teamName} name="teamName" className="..." />
// 表示モード:
<td ...>{m.teamName}</td>
// ヘッダー:
<th ...>teamName</th>
```

From src/app/admin/AdminAddForm.tsx (現行):
```tsx
<input name="teamName" placeholder="teamName (任意)" className="..." />
```

From src/app/admin/actions.ts (現行):
```typescript
// addMemberAction
const teamName = formData.get('teamName') as string
await addMember({ name, substackId, teamName: teamName ?? '' })
// updateMemberAction
const teamName = formData.get('teamName') as string
await updateMember(substackId, { name, teamName: teamName ?? '', addedAt })
```

From src/app/member/[substackId]/page.tsx (現行):
```tsx
<Link
  href={memberResult.member.teamName ? `/?team=${memberResult.member.teamName}` : '/'}
  className="text-sm text-gray-500 hover:text-gray-800 mb-4 inline-block"
>
  ← メンバー一覧
</Link>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: types.ts — teamName を teamNames: string[] に変更（D-01）</name>
  <files>src/lib/types.ts</files>
  <action>
`Member` 型の `teamName: string` を `teamNames: string[]` に変更する。

変更箇所は1行のみ:
```typescript
// 変更前
teamName: string

// 変更後
teamNames: string[]  // teamName: string から変更（Phase 11 D-01）
```

`MemberFeedResult` など他の型は変更不要（`Member` を参照しているだけ）。
  </action>
  <verify>
    <automated>cd /Users/kugyu10/work/keep-substack && grep -n "teamNames: string\[\]" src/lib/types.ts</automated>
  </verify>
  <done>src/lib/types.ts に `teamNames: string[]` が存在し、`teamName: string` が存在しない</done>
</task>

<task type="auto">
  <name>Task 2: kvMembers.ts — 後方互換フォールバック更新（D-01）</name>
  <files>src/lib/kvMembers.ts</files>
  <action>
`getMembers()` 内のフォールバック処理を `teamName` → `teamNames` 対応に書き換える。

変更前（Phase 9のフォールバック）:
```typescript
return members.map((m) => ({
  ...m,
  teamName: m.teamName ?? m.teamId ?? '',
}))
```

変更後（Phase 11のフォールバック）:
```typescript
return members.map((m) => ({
  ...m,
  teamNames: m.teamNames ?? (m.teamName ? [m.teamName] : []),
}))
```

設計意図:
- KVに `teamNames` があればそのまま使う
- KVに `teamName`（旧形式）があれば1要素の配列に変換
- どちらもなければ空配列

`addMember` と `updateMember` は `Member` 型経由で動くため、型変更の恩恵を自動的に受ける。関数本体の変更は不要。
  </action>
  <verify>
    <automated>cd /Users/kugyu10/work/keep-substack && grep -n "m.teamNames ?? (m.teamName" src/lib/kvMembers.ts</automated>
  </verify>
  <done>kvMembers.ts の getMembers() が `teamNames: m.teamNames ?? (m.teamName ? [m.teamName] : [])` を含み、旧フォールバック `teamName: m.teamName ?? m.teamId ?? ''` が存在しない</done>
</task>

<task type="auto">
  <name>Task 3: page.tsx — teams生成・フィルタリングを teamNames[] 対応に変更（D-02, D-03）</name>
  <files>src/app/page.tsx</files>
  <action>
`allMembers.map((m) => m.teamName)` を `flatMap` + `includes` に変更する。

変更前:
```typescript
const teams = [...new Set(allMembers.map((m) => m.teamName).filter(Boolean))]
const filteredMembers = team ? allMembers.filter((m) => m.teamName === team) : allMembers
```

変更後（D-02, D-03）:
```typescript
const teams = [...new Set(allMembers.flatMap((m) => m.teamNames).filter(Boolean))]
const filteredMembers = team ? allMembers.filter((m) => m.teamNames.includes(team)) : allMembers
```

- `flatMap` で複数チームを展開してから `Set` で重複除去 → 同チームが複数メンバーに設定されてもタブは1つ
- `includes` で複数チーム所属のメンバーがどのチームタブでも表示される
- その他の部分（JSX、他変数）は変更不要
  </action>
  <verify>
    <automated>cd /Users/kugyu10/work/keep-substack && grep -n "flatMap\|teamNames\.includes" src/app/page.tsx</automated>
  </verify>
  <done>page.tsx が `flatMap((m) => m.teamNames)` と `m.teamNames.includes(team)` を使っており、`m.teamName` への参照が存在しない</done>
</task>

<task type="auto">
  <name>Task 4: AdminAddForm.tsx + actions.ts — teamNames input とサーバー側変換（D-04）</name>
  <files>src/app/admin/AdminAddForm.tsx, src/app/admin/actions.ts</files>
  <action>
**AdminAddForm.tsx の変更:**

input の `name` 属性と `placeholder` を変更する。

変更前:
```tsx
<input
  name="teamName"
  placeholder="teamName (任意)"
  className="border rounded px-2 py-1 text-sm flex-1"
/>
```

変更後:
```tsx
<input
  name="teamNames"
  placeholder="チーム名（カンマ区切り・任意）"
  className="border rounded px-2 py-1 text-sm flex-1"
/>
```

**actions.ts の変更（addMemberAction と updateMemberAction 両方）:**

addMemberAction（変更前）:
```typescript
const teamName = formData.get('teamName') as string
await addMember({ name, substackId, teamName: teamName ?? '' })
```

addMemberAction（変更後、D-04）:
```typescript
const teamNamesRaw = formData.get('teamNames') as string
const teamNames = teamNamesRaw
  ? teamNamesRaw.split(',').map((s) => s.trim()).filter(Boolean)
  : []
await addMember({ name, substackId, teamNames })
```

updateMemberAction（変更前）:
```typescript
const teamName = formData.get('teamName') as string
await updateMember(substackId, { name, teamName: teamName ?? '', addedAt })
```

updateMemberAction（変更後、D-04）:
```typescript
const teamNamesRaw = formData.get('teamNames') as string
const teamNames = teamNamesRaw
  ? teamNamesRaw.split(',').map((s) => s.trim()).filter(Boolean)
  : []
await updateMember(substackId, { name, teamNames, addedAt })
```

重要: `formData.get('teamName')` の取り残しがないこと。actions.ts 内で `teamName` を参照している箇所が完全になくなるまで確認する。
  </action>
  <verify>
    <automated>cd /Users/kugyu10/work/keep-substack && grep -n "teamNames" src/app/admin/AdminAddForm.tsx src/app/admin/actions.ts</automated>
  </verify>
  <done>AdminAddForm.tsx の input name が `"teamNames"` であり、actions.ts が `formData.get('teamNames')` と `split(',')` 変換ロジックを含み、`formData.get('teamName')` への参照が存在しない</done>
</task>

<task type="auto">
  <name>Task 5: AdminMemberList.tsx — teamNames 表示・編集UI更新（D-04）</name>
  <files>src/app/admin/AdminMemberList.tsx</files>
  <action>
3箇所を変更する。

**ヘッダー（40行目付近）:**
```tsx
// 変更前
<th className="border border-gray-600 px-3 py-2">teamName</th>
// 変更後
<th className="border border-gray-600 px-3 py-2">チーム</th>
```

**編集モードの input（54行目付近）:**
```tsx
// 変更前
<input defaultValue={m.teamName} name="teamName" className="bg-gray-700 border border-gray-500 rounded px-1 w-full text-sm text-white" />
// 変更後
<input defaultValue={m.teamNames.join(', ')} name="teamNames" className="bg-gray-700 border border-gray-500 rounded px-1 w-full text-sm text-white" />
```

**表示モードの td（79行目付近）:**
```tsx
// 変更前
<td className="border border-gray-700 px-3 py-2">{m.teamName}</td>
// 変更後
<td className="border border-gray-700 px-3 py-2">{m.teamNames.join(', ')}</td>
```

重要: `name="teamName"` の取り残しが AdminMemberList.tsx 内に残らないこと。`name="teamNames"` に変更しないと、handleUpdate が formData に teamNames を送れず、Server Actions 側で空配列になる。
  </action>
  <verify>
    <automated>cd /Users/kugyu10/work/keep-substack && grep -n "teamNames\|teamName" src/app/admin/AdminMemberList.tsx</automated>
  </verify>
  <done>AdminMemberList.tsx に `m.teamNames.join(', ')` と `name="teamNames"` が存在し、`m.teamName` および `name="teamName"` への参照が存在しない</done>
</task>

<task type="auto">
  <name>Task 6: member/[substackId]/page.tsx — 複数チームの戻りリンク（D-05）</name>
  <files>src/app/member/[substackId]/page.tsx</files>
  <action>
戻りリンク部分を複数チーム対応に書き換える。

変更前（29行目付近）:
```tsx
<Link
  href={memberResult.member.teamName ? `/?team=${memberResult.member.teamName}` : '/'}
  className="text-sm text-gray-500 hover:text-gray-800 mb-4 inline-block"
>
  ← メンバー一覧
</Link>
```

変更後（D-05、encodeURIComponent 必須）:
```tsx
{memberResult.member.teamNames.length > 0 ? (
  <div className="flex gap-3 mb-4">
    {memberResult.member.teamNames.map((t) => (
      <Link
        key={t}
        href={`/?team=${encodeURIComponent(t)}`}
        className="text-sm text-gray-500 hover:text-gray-800 inline-block"
      >
        ← {t}
      </Link>
    ))}
  </div>
) : (
  <Link
    href="/"
    className="text-sm text-gray-500 hover:text-gray-800 mb-4 inline-block"
  >
    ← メンバー一覧
  </Link>
)}
```

設計意図（D-05）:
- teamNames が1件以上: 各チームへのリンクを `flex gap-3` で横並びに表示
- teamNames が空: `← メンバー一覧`（`/` に戻る）
- `encodeURIComponent` で日本語・スペースを含むチーム名に対応
- セパレーターは `|` ではなく Tailwind の `flex gap-3`（既存UIパターンと一貫）
  </action>
  <verify>
    <automated>cd /Users/kugyu10/work/keep-substack && grep -n "encodeURIComponent\|teamNames" src/app/member/\[substackId\]/page.tsx</automated>
  </verify>
  <done>member/[substackId]/page.tsx が `memberResult.member.teamNames.length` チェックと `encodeURIComponent` を使った複数リンクを含み、`memberResult.member.teamName` への参照が存在しない</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
全7ファイルへの変更が完了:
- types.ts: teamNames: string[]
- kvMembers.ts: 後方互換フォールバック
- page.tsx: flatMap + includes
- AdminAddForm.tsx: name="teamNames"
- actions.ts: split(',') 変換
- AdminMemberList.tsx: join(', ') 表示・編集
- member/[substackId]/page.tsx: 複数リンク
  </what-built>
  <how-to-verify>
1. `npm run build` でビルドエラーがないことを確認
2. `npm run dev` でローカル起動
3. http://localhost:3000/admin を開き、メンバー追加フォームの「チーム名」欄に `TeamA, TeamB` と入力して追加
4. 追加したメンバーの編集欄に `TeamA, TeamB` が表示されること
5. http://localhost:3000 を開き、TeamA タブと TeamB タブが両方表示されること
6. TeamA タブをクリックしたとき、追加したメンバーが表示されること
7. TeamB タブをクリックしたとき、同じメンバーが表示されること
8. 追加したメンバーのカードをクリックし、詳細ページに `← TeamA` と `← TeamB` の2つのリンクが横並びで表示されること
9. 各戻りリンクをクリックして対応するチームフィルタービューに遷移すること
  </how-to-verify>
  <resume-signal>"approved" と入力、または発見した問題を記述してください</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ブラウザ → Server Actions | AdminAddForm と AdminMemberList から teamNames 文字列が送られる |
| KV → getMembers() | KV上のrawデータ（旧フォーマット含む）をアプリ型に変換する |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | actions.ts — teamNames入力 | accept | teamNames はチーム名文字列のみ。SQLインジェクション等のベクタがない（KVキー汚染なし）。split+trim+filterで空文字を除去済み |
| T-11-02 | Injection | member/[substackId]/page.tsx — href生成 | mitigate | `encodeURIComponent(t)` で URLエンコードを実施。日本語・特殊文字を含むチーム名でも URL が壊れない |
| T-11-03 | Information Disclosure | kvMembers.ts — 旧 teamName フィールド残留 | accept | 旧フィールドが KV に残っても UI は teamNames のみ参照。PII含まず低リスク |
</threat_model>

<verification>
全タスク完了後、以下を確認する:

```bash
# TypeScriptコンパイルエラーがないこと
cd /Users/kugyu10/work/keep-substack && npm run build

# teamName（単数形）への参照が src/ に残っていないこと
# （コメントや旧形式の参照が取り残されていないか確認）
grep -rn "teamName[^s]" src/ | grep -v "// " | grep -v ".teamName \?" || echo "OK: 取り残しなし"
```

期待する状態:
- ビルドエラー 0 件
- `teamName:` (単数) の型参照がコード上に存在しない
- `name="teamName"` の input が存在しない
- `formData.get('teamName')` の参照が存在しない
</verification>

<success_criteria>
1. `npm run build` がエラーなく完了する（TypeScript型エラーを含む）
2. 管理画面でカンマ区切りチーム名（例: `TeamA, TeamB`）を入力して保存すると、KVに `teamNames: ["TeamA", "TeamB"]` として保存される
3. トップページのチームタブに TeamA と TeamB が独立して表示される
4. TeamA タブでそのメンバーが表示され、TeamB タブでも同じメンバーが表示される
5. メンバー詳細ページに `← TeamA` と `← TeamB` の2つの戻りリンクが表示され、それぞれ `/?team=TeamA` および `/?team=TeamB` に遷移する
6. 既存の旧データ（teamName: string 形式）を持つメンバーが、フォールバックによりチームフィルターで正しく表示される
</success_criteria>

<output>
完了後、以下のファイルを作成する:

`.planning/phases/11-multi-team-membership/11-01-SUMMARY.md`

内容:
- 実装した変更の概要
- 各ファイルの変更内容
- 後方互換フォールバックの動作確認結果
- 発生した問題と解決策（あれば）
</output>
