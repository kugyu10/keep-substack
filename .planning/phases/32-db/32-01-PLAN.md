---
phase: 32-db
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements:
  - DB-02
must_haves:
  truths:
    - "本番 DB (xolhjcngrwwwqtklmoyk) に member_commit_slots テーブルが存在する"
    - "member_commit_slots に uq_member_commit_slots_day UNIQUE 制約が存在する"
    - "/my ページでコミットスケジュールを保存するとエラーなく成功する"
    - "保存したスケジュールが /my ページをリロードしても正しく表示される"
  artifacts:
    - path: "supabase/migrations/20260602000001_add_member_commit_slots.sql"
      provides: "本番 DB への CREATE TABLE member_commit_slots + RLS ポリシー"
      contains: "CREATE TABLE IF NOT EXISTS member_commit_slots"
    - path: "supabase/migrations/20260602000002_add_member_commit_slots_unique.sql"
      provides: "本番 DB への UNIQUE 制約追加"
      contains: "ADD CONSTRAINT uq_member_commit_slots_day"
  key_links:
    - from: "src/app/my/actions.ts — updateCommitSlotsAction"
      to: "本番 DB member_commit_slots テーブル"
      via: "createSupabaseAdminClient().from('member_commit_slots').delete().insert()"
      pattern: "member_commit_slots"
---

<objective>
本番 Supabase DB (xolhjcngrwwwqtklmoyk) に未適用のマイグレーション 2 本を適用し、
コミットスケジュール保存機能を本番環境で動作させる。

Purpose: schedule-save-fails-production デバッグの根本原因（テーブル不在）を解消する。
         コード変更は不要 — DB マイグレーション適用のみ。
Output: 本番 DB に member_commit_slots テーブル + UNIQUE 制約が存在し、
        /my ページのスケジュール保存が本番で成功する。
</objective>

<execution_context>
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/debug/schedule-save-fails-production.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Pre-flight — 本番 DB の現在の状態確認</name>
  <read_first>
    supabase/migrations/20260602000001_add_member_commit_slots.sql
    supabase/migrations/20260602000002_add_member_commit_slots_unique.sql
    .planning/debug/schedule-save-fails-production.md
  </read_first>
  <what-built>
    Claude によるコード分析は完了済み（debug doc に記録）。
    本タスクでは本番 DB への接続権限を持つオペレーターが
    Supabase Dashboard の SQL Editor で現在の状態を確認する。
  </what-built>
  <how-to-verify>
    1. ブラウザで https://supabase.com/dashboard/project/xolhjcngrwwwqtklmoyk/editor を開く
       （prod プロジェクト — ref: xolhjcngrwwwqtklmoyk を確認してから開くこと）
    2. 以下の SQL を実行する:

       SELECT table_name, table_schema
       FROM information_schema.tables
       WHERE table_name = 'member_commit_slots'
         AND table_schema = 'public';

    3. 結果が 0 行 → テーブル未適用（予想通り）。Task 2 に進む。
       結果が 1 行 (public | member_commit_slots) → テーブルは既に存在する。
                   Task 2 の CREATE TABLE は IF NOT EXISTS で安全に実行できるが、
                   UNIQUE 制約が存在するか以下で確認すること:

       SELECT constraint_name
       FROM information_schema.table_constraints
       WHERE table_name = 'member_commit_slots'
         AND constraint_name = 'uq_member_commit_slots_day';

       0 行 → Task 3 の ALTER TABLE のみ適用が必要。
       1 行 → 両マイグレーションが既に適用済み。Task 2/3 をスキップして Task 4 へ。
  </how-to-verify>
  <acceptance_criteria>
    - オペレーターが本番 DB (xolhjcngrwwwqtklmoyk) の SQL Editor に接続できている
    - 上記 SELECT クエリが実行され、現在のテーブル存在状態が確認できている
    - 次に適用が必要なマイグレーション（両方 or UNIQUE のみ or なし）が判明している
  </acceptance_criteria>
  <resume-signal>確認結果を報告してください（テーブル未存在 / テーブル存在+制約なし / 両方適用済み）</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [BLOCKING] Migration 1 適用 — CREATE TABLE member_commit_slots</name>
  <read_first>
    supabase/migrations/20260602000001_add_member_commit_slots.sql
  </read_first>
  <what-built>
    本タスクは手動適用が必要な DB マイグレーションです（supabase db push は本番向きではない）。
    オペレーターが Supabase Dashboard SQL Editor で以下の SQL を実行してテーブルを作成する。
  </what-built>
  <how-to-verify>
    前提: Task 1 でテーブル未存在を確認済みであること。
    （テーブルが既に存在している場合、この Task はスキップしてよい — IF NOT EXISTS で安全だが念のため）

    1. https://supabase.com/dashboard/project/xolhjcngrwwwqtklmoyk/editor を開く
       （URL に xolhjcngrwwwqtklmoyk が含まれていることを必ず目視確認すること）
    2. supabase/migrations/20260602000001_add_member_commit_slots.sql の内容を全て貼り付けて実行する:

       BEGIN;

       CREATE TABLE IF NOT EXISTS member_commit_slots (
         id           BIGINT  GENERATED ALWAYS AS IDENTITY PRIMARY KEY,
         member_id    UUID    NOT NULL REFERENCES members(id) ON DELETE CASCADE,
         day_of_week  INT     NOT NULL CHECK (day_of_week BETWEEN 1 AND 7),
         hour         INT     NOT NULL CHECK (hour BETWEEN 0 AND 23)
       );

       ALTER TABLE member_commit_slots ENABLE ROW LEVEL SECURITY;

       DROP POLICY IF EXISTS "public select member_commit_slots" ON member_commit_slots;
       CREATE POLICY "public select member_commit_slots"
         ON member_commit_slots FOR SELECT USING (true);

       DROP POLICY IF EXISTS "member write own commit slots" ON member_commit_slots;
       CREATE POLICY "member write own commit slots"
         ON member_commit_slots
         FOR ALL
         USING (
           member_id = (SELECT id FROM members WHERE user_id = auth.uid())
         )
         WITH CHECK (
           member_id = (SELECT id FROM members WHERE user_id = auth.uid())
         );

       COMMIT;

    3. 実行後、成功メッセージ（"Success. No rows returned" または "Command completed successfully"）を確認する。
    4. 検証クエリを実行する:

       SELECT table_name FROM information_schema.tables
       WHERE table_name = 'member_commit_slots' AND table_schema = 'public';

       → 1 行返れば成功。0 行ならエラーを報告する。
  </how-to-verify>
  <acceptance_criteria>
    - SQL が prod プロジェクト (xolhjcngrwwwqtklmoyk) に対して実行されている（dev otydhiumsdsyxepnjqjp ではない）
    - SQL Editor がエラーなく完了している（BEGIN/COMMIT ブロックが成功）
    - information_schema.tables クエリが member_commit_slots を 1 行返す
    - RLS が有効（ALTER TABLE ... ENABLE ROW LEVEL SECURITY が成功）
    - "public select member_commit_slots" ポリシーが作成されている
    - "member write own commit slots" ポリシーが作成されている
  </acceptance_criteria>
  <resume-signal>Migration 1 の適用結果を報告してください（成功 / エラーメッセージ）</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: [BLOCKING] Migration 2 適用 — UNIQUE 制約追加</name>
  <read_first>
    supabase/migrations/20260602000002_add_member_commit_slots_unique.sql
  </read_first>
  <what-built>
    Task 2 に続いて、UNIQUE 制約を付与する 2 本目のマイグレーションを適用する。
    この制約は (member_id, day_of_week) の組み合わせ重複を DB レベルで防ぐ。
  </what-built>
  <how-to-verify>
    前提: Task 2 が成功済みであること（member_commit_slots テーブルが存在する）。

    1. 引き続き同じ SQL Editor セッションで実行する
       （URL に xolhjcngrwwwqtklmoyk が含まれていることを再確認）
    2. supabase/migrations/20260602000002_add_member_commit_slots_unique.sql の内容を全て貼り付けて実行する:

       BEGIN;

       ALTER TABLE member_commit_slots
         ADD CONSTRAINT uq_member_commit_slots_day
         UNIQUE (member_id, day_of_week);

       COMMIT;

    3. 実行後、成功メッセージを確認する。
    4. 検証クエリを実行する:

       SELECT constraint_name, constraint_type
       FROM information_schema.table_constraints
       WHERE table_name = 'member_commit_slots'
         AND constraint_name = 'uq_member_commit_slots_day';

       → 1 行（UNIQUE）返れば成功。
  </how-to-verify>
  <acceptance_criteria>
    - SQL が prod プロジェクト (xolhjcngrwwwqtklmoyk) に対して実行されている
    - ALTER TABLE が成功している（エラーなし）
    - information_schema.table_constraints クエリが uq_member_commit_slots_day を constraint_type = 'UNIQUE' で 1 行返す
  </acceptance_criteria>
  <resume-signal>Migration 2 の適用結果を報告してください（成功 / エラーメッセージ）</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Post-migration E2E 検証 — /my スケジュール保存の動作確認</name>
  <read_first>
    src/app/my/actions.ts (lines 132-199 — updateCommitSlotsAction)
  </read_first>
  <what-built>
    Migration 1 + 2 の適用完了後、本番環境の /my ページで実際にスケジュール保存が
    成功することをブラウザで確認する。
    コード側の変更は一切ない — DB テーブルが存在すれば actions.ts の delete/insert は正常動作する。
  </what-built>
  <how-to-verify>
    1. 本番サイト（Vercel production URL）の /my ページにアクセスする
    2. メンバーアカウントでログインしていることを確認する
    3. コミットスケジュールセクションで曜日・時刻を 1 つ以上選択する
    4. 「保存」ボタンを押す
    5. 期待結果: 「保存に失敗しました」が表示されない
                  成功を示す UI フィードバック（または無音で画面が更新される）が出る
    6. ページをリロードして保存したスケジュールが表示されていることを確認する
    7. 追加確認: Supabase SQL Editor で以下を実行しデータが存在することを確認する:

       SELECT member_id, day_of_week, hour
       FROM member_commit_slots
       ORDER BY member_id, day_of_week;

       → 保存したスロット数の行が返る
  </how-to-verify>
  <acceptance_criteria>
    - /my ページでコミットスケジュール保存操作を行っても「保存に失敗しました」が表示されない
    - ページリロード後に保存したスケジュール（曜日・時刻）が正しく表示されている
    - member_commit_slots テーブルに対応する行が SELECT クエリで確認できる
    - 同じ曜日を重複して保存しようとしても DB 側の UNIQUE 制約でエラーにならない
      （actions.ts は DELETE 後に INSERT するため重複は発生しない設計）
  </acceptance_criteria>
  <resume-signal>動作確認の結果を報告してください（成功 / 問題あり + 症状）</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| オペレーター → Supabase Dashboard SQL Editor | 管理者権限で任意の DDL を実行できる。誤った project ref を選択すると dev DB を汚染する |
| 本番 DB: member_commit_slots | 新テーブル。RLS が正しく設定されないと匿名ユーザーが write できる |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-01 | Tampering | SQL Editor — project ref 選択 | mitigate | Task 1/2/3 で URL に xolhjcngrwwwqtklmoyk が含まれることを目視確認する手順を明記。dev ref (otydhiumsdsyxepnjqjp) への誤適用を防ぐ |
| T-32-02 | Tampering | 部分マイグレーション（Migration 1 のみ適用） | mitigate | Task 2/3 を順番に分離し、各タスクが独立したチェックポイントを持つ。Task 3 では uq_member_commit_slots_day の存在を SELECT で確認してから resume |
| T-32-03 | Elevation of Privilege | member_commit_slots RLS ポリシー | mitigate | Migration 1 に "member write own commit slots" ポリシーを含む。適用後 Task 4 の E2E 検証で service_role (admin client) がバイパスできること + 匿名ユーザーが write できないことを確認 |
| T-32-04 | Denial of Service | BEGIN/COMMIT 内の DDL 失敗によるロールバック | accept | IF NOT EXISTS / DROP IF EXISTS を使用しているため冪等。失敗しても再実行で解消できる |
| T-32-SC | Tampering | npm/pip/cargo installs | accept | このフェーズはコード変更なし。パッケージインストールは一切発生しない |
</threat_model>

<verification>
## Phase-Level Verification

After all tasks complete, the following must be TRUE in the production environment:

1. Supabase Dashboard — Table Editor (prod project xolhjcngrwwwqtklmoyk):
   - member_commit_slots テーブルが存在する
   - Columns: id (bigint, PK), member_id (uuid, FK→members), day_of_week (int), hour (int)
   - RLS: enabled

2. Supabase Dashboard — Authentication → Policies:
   - "public select member_commit_slots" — SELECT, USING (true)
   - "member write own commit slots" — ALL, USING (member_id = ...)

3. Supabase Dashboard — Table Editor → Indexes/Constraints:
   - uq_member_commit_slots_day UNIQUE (member_id, day_of_week) が存在する

4. 本番 /my ページ:
   - コミットスケジュールを保存しても「保存に失敗しました」が表示されない
   - リロード後にスケジュールが正しく表示される
</verification>

<success_criteria>
- 本番 DB (xolhjcngrwwwqtklmoyk) に member_commit_slots テーブルが存在する
- uq_member_commit_slots_day UNIQUE 制約が存在する
- RLS が有効で "public select" / "member write own" ポリシーが適用されている
- /my ページでコミットスケジュールを保存すると「保存に失敗しました」が表示されない
- 保存したスケジュールがページリロード後も正しく表示される
- コード変更は一切なし（DB 適用のみで解消）
</success_criteria>

<output>
Create `.planning/phases/32-db/32-01-SUMMARY.md` when done.
Include: migrations applied (yes/no per migration), table verified (yes/no), E2E save result (success/fail), any deviations.
</output>
