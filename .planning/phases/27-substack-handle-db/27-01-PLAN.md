---
phase: 27-substack-handle-db
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/schema.sql
  - supabase/migrations/20260602000000_add_substack_handle.sql
autonomous: true
requirements:
  - PROF-01
  - PROF-02
  - PROF-03

must_haves:
  truths:
    - "members テーブルに substack_handle TEXT NULL カラムが存在する"
    - "既存の members 行は substack_handle = NULL のまま影響を受けない"
    - "schema.sql と migrations/ の両方が同一の DDL を持ち同期されている"
  artifacts:
    - path: "supabase/schema.sql"
      provides: "Fresh DB bootstrap 定義に substack_handle カラムを含む"
      contains: "ADD COLUMN IF NOT EXISTS substack_handle TEXT"
    - path: "supabase/migrations/20260602000000_add_substack_handle.sql"
      provides: "既存 DB への増分適用ファイル"
      contains: "ALTER TABLE members ADD COLUMN IF NOT EXISTS substack_handle TEXT"
  key_links:
    - from: "supabase/schema.sql"
      to: "supabase/migrations/20260602000000_add_substack_handle.sql"
      via: "identical DDL"
      pattern: "substack_handle TEXT"
---

<objective>
Add `substack_handle TEXT NULL` column to the `members` table via two coordinated files: a new incremental migration and an update to the schema.sql bootstrap source.

Purpose: Establish the DB foundation required by Plan 02's app code. Without the column, all subsequent Server Action, RSC, and component changes will fail at runtime.
Output: `supabase/migrations/20260602000000_add_substack_handle.sql` (new file) + updated `supabase/schema.sql`.
</objective>

<execution_context>
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/kugyu10/work/keep-substack/.planning/ROADMAP.md
@/Users/kugyu10/work/keep-substack/.planning/phases/27-substack-handle-db/27-CONTEXT.md

<interfaces>
<!-- Current members table definition extracted from supabase/schema.sql lines 9-18 -->
CREATE TABLE IF NOT EXISTS members (
  id             UUID        PRIMARY KEY DEFAULT gen_random_uuid(),
  name           TEXT        NOT NULL,
  publication_id TEXT        UNIQUE NOT NULL,
  image_url      TEXT,
  added_at       TIMESTAMPTZ NOT NULL DEFAULT now(),
  user_id        UUID        REFERENCES auth.users(id) UNIQUE
);

<!-- Latest migration file: supabase/migrations/20260531000000_consolidated_schema.sql -->
<!-- Format pattern: BEGIN; ... COMMIT; with IF NOT EXISTS guards -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create incremental migration + update schema.sql</name>
  <files>
    supabase/migrations/20260602000000_add_substack_handle.sql,
    supabase/schema.sql
  </files>
  <read_first>
    - supabase/schema.sql — read the current members table definition to find the correct insertion point after the existing columns (before the closing `;`)
    - supabase/migrations/20260531000000_consolidated_schema.sql — read the file format (BEGIN/COMMIT wrapper, IF NOT EXISTS pattern) to match exactly
  </read_first>
  <action>
    Create new file `supabase/migrations/20260602000000_add_substack_handle.sql` with the following structure (per D-09 and Phase 26 migration pattern):
    - Wrap in BEGIN / COMMIT transaction block
    - Single statement: ALTER TABLE members ADD COLUMN IF NOT EXISTS substack_handle TEXT;
    - Add a comment line above the ALTER: `-- Phase 27: add substack_handle for Substack profile link`

    Update `supabase/schema.sql`:
    - Inside the `CREATE TABLE IF NOT EXISTS members (...)` block, add `substack_handle TEXT` as a new column on its own line, after the existing `user_id` column and before the closing `);`
    - Keep the same indentation style (2-space) as existing columns
    - Do NOT use ADD COLUMN syntax inside CREATE TABLE — the schema.sql CREATE TABLE block defines all columns inline; the ALTER TABLE syntax is only in the separate comment block below (`-- ALTER TABLE members ADD COLUMN IF NOT EXISTS user_id...`). Add `substack_handle TEXT` as a column in the CREATE TABLE definition.

    After both files are written, verify the column appears in both files.
  </action>
  <verify>
    <automated>grep -c "substack_handle" /Users/kugyu10/work/keep-substack/supabase/schema.sql && grep -c "substack_handle" /Users/kugyu10/work/keep-substack/supabase/migrations/20260602000000_add_substack_handle.sql</automated>
  </verify>
  <acceptance_criteria>
    - supabase/schema.sql contains `substack_handle TEXT` inside the `CREATE TABLE IF NOT EXISTS members` block
    - supabase/migrations/20260602000000_add_substack_handle.sql exists and contains `ALTER TABLE members ADD COLUMN IF NOT EXISTS substack_handle TEXT`
    - Migration file is wrapped in `BEGIN;` ... `COMMIT;`
    - grep "substack_handle" supabase/schema.sql returns at least 1 match
    - grep "substack_handle" supabase/migrations/20260602000000_add_substack_handle.sql returns at least 1 match
    - No other existing lines in schema.sql are modified
  </acceptance_criteria>
  <done>
    Both files updated and committed. The migration file is idempotent (IF NOT EXISTS). schema.sql and migrations/ are in sync — both contain `substack_handle TEXT`. The column is nullable (no NOT NULL constraint, no DEFAULT) per D-09.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQL Editor / Supabase CLI → DB | Migration SQL runs with superuser privileges; malformed DDL would affect the live DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01 | Tampering | supabase/schema.sql | mitigate | `ADD COLUMN IF NOT EXISTS` is idempotent; no risk of data loss on re-run |
| T-27-02 | Tampering | migrations/20260602000000 | mitigate | BEGIN/COMMIT transaction wrapping ensures atomic application; IF NOT EXISTS prevents duplicate column error on re-run |
| T-27-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
After applying the migration to both prod and TEST Supabase projects via SQL Editor:
- `SELECT column_name FROM information_schema.columns WHERE table_name='members' AND column_name='substack_handle'` returns 1 row
- Existing members rows: `SELECT substack_handle FROM members LIMIT 5` returns all NULL values
</verification>

<success_criteria>
- supabase/schema.sql contains `substack_handle TEXT` in the members CREATE TABLE block
- supabase/migrations/20260602000000_add_substack_handle.sql exists with idempotent ALTER TABLE statement in BEGIN/COMMIT block
- Both files are consistent (same column definition)
- git diff shows only the two expected files modified/added
</success_criteria>

<output>
Create `.planning/phases/27-substack-handle-db/27-01-SUMMARY.md` when done
</output>
