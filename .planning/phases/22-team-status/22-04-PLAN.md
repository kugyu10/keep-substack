---
phase: 22-team-status
plan: "04"
type: execute
wave: 3
depends_on:
  - "22-02"
files_modified:
  - src/app/admin/teams/page.tsx
  - src/app/admin/teams/TeamStatusList.tsx
  - src/app/admin/teams/actions.ts
  - src/app/admin/page.tsx
autonomous: true
requirements:
  - TEAM-02
must_haves:
  truths:
    - "/admin/teams ページが存在し、チーム一覧をstatusドロップダウン付きテーブルで表示する"
    - "各行の「ステータスを保存」ボタンでSupabaseのteams.statusを更新できる"
    - "statusをhiddenに変更して保存する前に window.confirm() 確認ダイアログが表示される"
    - "/admin ページに「チーム設定」リンクが追加されている"
    - "管理者以外がアクセスしても 'Unauthorized' エラーで保護されている"
  artifacts:
    - path: "src/app/admin/teams/page.tsx"
      provides: "RSCページ: teamsテーブルから id,name,status を取得してTeamStatusListに渡す"
      contains: "select('id, name, status')"
    - path: "src/app/admin/teams/TeamStatusList.tsx"
      provides: "Client Component: selectドロップダウン + 保存ボタン"
      contains: "updateTeamStatusAction"
    - path: "src/app/admin/teams/actions.ts"
      provides: "Server Action: requireAdmin + supabase.update + revalidatePath"
      contains: "updateTeamStatusAction"
    - path: "src/app/admin/page.tsx"
      provides: "/admin/teams へのリンク"
      contains: "href=\"/admin/teams\""
  key_links:
    - from: "src/app/admin/teams/TeamStatusList.tsx"
      to: "src/app/admin/teams/actions.ts"
      via: "updateTeamStatusAction呼び出し"
      pattern: "updateTeamStatusAction"
    - from: "src/app/admin/page.tsx"
      to: "/admin/teams"
      via: "aタグリンク"
      pattern: "href=\"/admin/teams\""
---

<objective>
/admin/teams ページを新設する（RSCページ + Client Component + Server Action）。/admin メインページに「チーム設定」リンクを追加する。

Purpose: 管理者がchamelon以外のチームもstatusで制御できるUIを提供する。D-01〜D-03・D-04のUI要件を満たす。
Output: src/app/admin/teams/ ディレクトリ（3ファイル新規作成）+ src/app/admin/page.tsx（リンク追加）
</objective>

<execution_context>
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/kugyu10/work/keep-substack/.planning/ROADMAP.md
@/Users/kugyu10/work/keep-substack/.planning/phases/22-team-status/22-CONTEXT.md
@/Users/kugyu10/work/keep-substack/.planning/phases/22-team-status/22-RESEARCH.md
@/Users/kugyu10/work/keep-substack/.planning/phases/22-team-status/22-PATTERNS.md
@/Users/kugyu10/work/keep-substack/.planning/phases/22-team-status/22-UI-SPEC.md

<interfaces>
<!-- src/app/admin/page.tsx の現行RSCページパターン（全文）-->
```typescript
import { getMembers } from '@/lib/members'
import { createSupabaseAdminClient } from '@/lib/supabase/admin'
import AdminAddForm from './AdminAddForm'
import AdminMemberList from './AdminMemberList'
import LogoutButton from '@/components/LogoutButton'

export default async function AdminPage() {
  const supabase = createSupabaseAdminClient()
  const [members, teamsResult] = await Promise.all([
    getMembers(),
    supabase.from('teams').select('name').order('name'),
  ])
  const teams = (teamsResult.data ?? []).map((t) => t.name)

  return (
    <main className="max-w-3xl mx-auto p-6">
      <div className="flex items-center justify-between mb-6">
        <h1 className="text-2xl font-semibold">管理画面</h1>
        <LogoutButton />
      </div>
      <p className="text-sm text-gray-600 mb-4">メンバー数：{members.length}</p>
      <AdminAddForm teams={teams} />
      <AdminMemberList members={members} teams={teams} />
    </main>
  )
}
```

<!-- src/app/admin/actions.ts のServer Actionパターン（requireAdmin + try/catch + revalidatePath）-->
```typescript
'use server'

import { revalidatePath } from 'next/cache'
import { createSupabaseAdminClient } from '@/lib/supabase/admin'
import { createSupabaseServerClient } from '@/lib/supabase/server'

async function requireAdmin(): Promise<void> {
  const supabase = await createSupabaseServerClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user || user.app_metadata?.role !== 'admin') {
    throw new Error('Unauthorized')
  }
}

// パターン: try { await requireAdmin() } catch { return 'エラーメッセージ' }
// パターン: if (error) return error.message
// パターン: revalidatePath('/admin/teams')
// パターン: return null で成功を示す
```

<!-- AdminMemberList.tsx のテーブルUIスタイル（line 42-51）-->
```tsx
<div className="overflow-x-auto">
  <table className="w-full text-sm border-collapse text-[#363737]">
    <thead>
      <tr className="bg-[#fafafa] text-left">
        <th className="border border-[#ebebeb] px-3 py-2">...</th>
      </tr>
    </thead>
    <tbody>
      <tr className="bg-white hover:bg-[#fafafa]">
        <td className="border border-[#ebebeb] px-3 py-2">...</td>
      </tr>
    </tbody>
  </table>
</div>
```

<!-- UI-SPEC: 保存ボタンスタイル -->
```
text-blue-600 hover:underline text-xs
```

<!-- UI-SPEC: selectドロップダウンスタイル -->
```
w-40 border border-[#d8d8d8] rounded px-2 py-1 text-sm text-[#363737] bg-white
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: /admin/teams Server Action と RSCページ新規作成（D-01/D-02）</name>
  <files>
    src/app/admin/teams/actions.ts,
    src/app/admin/teams/page.tsx
  </files>
  <read_first>
    - src/app/admin/actions.ts（requireAdmin・revalidatePath・エラーハンドリングパターンの参照）
    - src/app/admin/page.tsx（RSCページのSupabase取得パターンの参照）
  </read_first>
  <action>
**1. src/app/admin/teams/actions.ts を新規作成する**

```typescript
'use server'

import { revalidatePath } from 'next/cache'
import { createSupabaseAdminClient } from '@/lib/supabase/admin'
import { createSupabaseServerClient } from '@/lib/supabase/server'

async function requireAdmin(): Promise<void> {
  const supabase = await createSupabaseServerClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user || user.app_metadata?.role !== 'admin') {
    throw new Error('Unauthorized')
  }
}

export async function updateTeamStatusAction(
  teamId: string,
  status: string
): Promise<string | null> {
  try { await requireAdmin() } catch { return '権限がありません' }

  const supabase = createSupabaseAdminClient()
  const { error } = await supabase
    .from('teams')
    .update({ status })
    .eq('id', teamId)
  if (error) return error.message

  revalidatePath('/admin/teams')
  return null
}
```

**2. src/app/admin/teams/page.tsx を新規作成する**

```typescript
import { createSupabaseAdminClient } from '@/lib/supabase/admin'
import TeamStatusList from './TeamStatusList'

export default async function AdminTeamsPage() {
  const supabase = createSupabaseAdminClient()
  const { data: teams } = await supabase
    .from('teams')
    .select('id, name, status')
    .order('name')

  return (
    <main className="max-w-3xl mx-auto p-6">
      <a href="/admin" className="text-sm text-blue-600 hover:underline block mb-4">
        ← 管理画面へ
      </a>
      <h1 className="text-2xl font-semibold mb-6">チーム設定</h1>
      <TeamStatusList teams={teams ?? []} />
    </main>
  )
}
```
  </action>
  <verify>
    <automated>grep -c "updateTeamStatusAction" /Users/kugyu10/work/keep-substack/src/app/admin/teams/actions.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/admin/teams/actions.ts` が存在する
    - `src/app/admin/teams/page.tsx` が存在する
    - actions.ts に `'use server'` ディレクティブが存在する
    - actions.ts に `updateTeamStatusAction` 関数が存在する
    - actions.ts に `requireAdmin()` の呼び出しが存在する（`try { await requireAdmin() } catch { return '権限がありません' }`）
    - actions.ts に `revalidatePath('/admin/teams')` が存在する
    - page.tsx に `select('id, name, status')` が存在する
    - page.tsx に `TeamStatusList` の import と使用が存在する
    - page.tsx に `← 管理画面へ` リンク（`href="/admin"`）が存在する
  </acceptance_criteria>
  <done>Server Actionとページファイルが存在し、DB取得・認証チェック・revalidateが正しく設定されている</done>
</task>

<task type="auto">
  <name>Task 2: TeamStatusList Client Component新規作成と /admin ページへのリンク追加（D-02/D-03）</name>
  <files>
    src/app/admin/teams/TeamStatusList.tsx,
    src/app/admin/page.tsx
  </files>
  <read_first>
    - src/app/admin/AdminMemberList.tsx（テーブルUIスタイルと行構造の参照）
    - src/app/admin/page.tsx（リンク追加箇所を確認する）
    - .planning/phases/22-team-status/22-UI-SPEC.md（コピーライティングとスタイルの仕様確認）
  </read_first>
  <action>
**1. src/app/admin/teams/TeamStatusList.tsx を新規作成する**

```typescript
'use client'

import { useState } from 'react'
import { updateTeamStatusAction } from './actions'

type Team = { id: string; name: string; status: string }
type Props = { teams: Team[] }

export default function TeamStatusList({ teams }: Props) {
  const [statuses, setStatuses] = useState<Record<string, string>>(
    Object.fromEntries(teams.map((t) => [t.id, t.status]))
  )
  const [saving, setSaving] = useState<Record<string, boolean>>({})
  const [errors, setErrors] = useState<Record<string, string | null>>({})

  async function handleSave(team: Team) {
    const newStatus = statuses[team.id]
    if (newStatus === 'hidden') {
      const confirmed = window.confirm(
        'このチームをhiddenにすると、メンバーがAll表示からも非表示になります。続けますか？'
      )
      if (!confirmed) return
    }

    setSaving((prev) => ({ ...prev, [team.id]: true }))
    const error = await updateTeamStatusAction(team.id, newStatus)
    setSaving((prev) => ({ ...prev, [team.id]: false }))
    setErrors((prev) => ({ ...prev, [team.id]: error }))
  }

  if (teams.length === 0) {
    return (
      <div className="text-sm text-gray-400">
        <p className="font-medium">チームがありません</p>
        <p>Supabaseのteamsテーブルにチームを追加してください</p>
      </div>
    )
  }

  return (
    <div className="overflow-x-auto">
      <table className="w-full text-sm border-collapse text-[#363737]">
        <thead>
          <tr className="bg-[#fafafa] text-left">
            <th className="border border-[#ebebeb] px-3 py-2">チーム名</th>
            <th className="border border-[#ebebeb] px-3 py-2">ステータス</th>
            <th className="border border-[#ebebeb] px-3 py-2">操作</th>
          </tr>
        </thead>
        <tbody>
          {teams.map((team) => (
            <tr key={team.id} className="bg-white hover:bg-[#fafafa]">
              <td className="border border-[#ebebeb] px-3 py-2">{team.name}</td>
              <td className="border border-[#ebebeb] px-3 py-2">
                <select
                  value={statuses[team.id]}
                  onChange={(e) =>
                    setStatuses((prev) => ({ ...prev, [team.id]: e.target.value }))
                  }
                  className="w-40 border border-[#d8d8d8] rounded px-2 py-1 text-sm text-[#363737] bg-white"
                >
                  <option value="public">public</option>
                  <option value="private">private</option>
                  <option value="hidden">hidden</option>
                </select>
              </td>
              <td className="border border-[#ebebeb] px-3 py-2">
                <button
                  onClick={() => handleSave(team)}
                  disabled={saving[team.id]}
                  className="text-blue-600 hover:underline text-xs disabled:opacity-50"
                >
                  ステータスを保存
                </button>
                {errors[team.id] && (
                  <p className="text-red-500 text-xs mt-1">{errors[team.id]}</p>
                )}
              </td>
            </tr>
          ))}
        </tbody>
      </table>
    </div>
  )
}
```

**2. src/app/admin/page.tsx にリンクを追加する（D-03）**

`<p className="text-sm text-gray-600 mb-4">メンバー数：{members.length}</p>` の直後、`<AdminAddForm ...` の前に以下を追加する:

```tsx
<div className="mb-6">
  <a href="/admin/teams" className="text-blue-600 hover:underline text-sm">
    チーム設定
  </a>
</div>
```
  </action>
  <verify>
    <automated>grep -c "updateTeamStatusAction" /Users/kugyu10/work/keep-substack/src/app/admin/teams/TeamStatusList.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/admin/teams/TeamStatusList.tsx` が存在する
    - TeamStatusList.tsx に `'use client'` ディレクティブが存在する
    - TeamStatusList.tsx に `window.confirm('このチームをhiddenにすると` が存在する（hidden時の確認ダイアログ）
    - TeamStatusList.tsx に `<option value="public">public</option>` が存在する
    - TeamStatusList.tsx に `<option value="private">private</option>` が存在する
    - TeamStatusList.tsx に `<option value="hidden">hidden</option>` が存在する
    - TeamStatusList.tsx に `ステータスを保存` が存在する（ボタンラベル）
    - TeamStatusList.tsx に `チームがありません` が存在する（空状態）
    - `src/app/admin/page.tsx` に `href="/admin/teams"` が存在する
    - `src/app/admin/page.tsx` に `チーム設定` が存在する（リンクテキスト）
    - `cd /Users/kugyu10/work/keep-substack && npx tsc --noEmit` がエラーなく通る
  </acceptance_criteria>
  <done>
    TeamStatusListがUI仕様通りに実装され、hiddenへの変更前確認ダイアログが実装され、/adminページに「チーム設定」リンクが追加され、ビルドが通る
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ブラウザ→Server Action | updateTeamStatusAction は adminロール認証を必須とする |
| middleware→/admin/teams | proxy.ts の `pathname.startsWith('/admin')` で認証済みユーザーのみアクセス可能 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-07 | Elevation of Privilege | updateTeamStatusAction | mitigate | `requireAdmin()` で `user.app_metadata?.role !== 'admin'` を検証。失敗時は '権限がありません' を返してDBへの書き込みをブロック |
| T-22-08 | Tampering | status値の入力検証 | accept | 現時点ではSupabaseへの値検証なし。status='public'/'private'/'hidden' 以外の文字列を送信しても機能に影響しないため許容。Phase 25でスキーマ制約を追加する可能性あり |
| T-22-09 | Spoofing | /admin/teams ルート保護 | accept | proxy.ts の `pathname.startsWith('/admin')` が既にカバー済み。追加のmatcher設定は不要かつ禁止（RESEARCH.md Anti-Pattern） |
</threat_model>

<verification>
```bash
# ファイル存在確認
ls -la /Users/kugyu10/work/keep-substack/src/app/admin/teams/

# Server Action確認
grep "'use server'" /Users/kugyu10/work/keep-substack/src/app/admin/teams/actions.ts
grep "requireAdmin" /Users/kugyu10/work/keep-substack/src/app/admin/teams/actions.ts
grep "revalidatePath('/admin/teams')" /Users/kugyu10/work/keep-substack/src/app/admin/teams/actions.ts

# Client Component確認
grep "'use client'" /Users/kugyu10/work/keep-substack/src/app/admin/teams/TeamStatusList.tsx
grep "window.confirm" /Users/kugyu10/work/keep-substack/src/app/admin/teams/TeamStatusList.tsx

# /admin リンク確認
grep "href=\"/admin/teams\"" /Users/kugyu10/work/keep-substack/src/app/admin/page.tsx

# TypeScriptコンパイル確認
cd /Users/kugyu10/work/keep-substack && npx tsc --noEmit
```
</verification>

<success_criteria>
- src/app/admin/teams/ に page.tsx, TeamStatusList.tsx, actions.ts の3ファイルが存在する
- actions.ts が requireAdmin() で保護され、supabase.update teams を実行し、revalidatePath('/admin/teams') を呼ぶ
- TeamStatusList.tsx が public/private/hidden の select ドロップダウンを持つ
- TeamStatusList.tsx が hidden への変更時に window.confirm() を表示する
- src/app/admin/page.tsx に /admin/teams へのリンクが存在する
- `npx tsc --noEmit` がエラーなく通る
</success_criteria>

<output>
After completion, create `.planning/phases/22-team-status/22-04-SUMMARY.md`
</output>
