---
phase: 22-team-status
plan: "02"
type: execute
wave: 2
depends_on:
  - "22-01"
files_modified:
  - src/lib/types.ts
  - src/lib/members.ts
  - src/app/admin/actions.ts
autonomous: true
requirements:
  - TEAM-04
must_haves:
  truths:
    - "Member型の teamNames: string[] が teams: {name: string, status: string}[] に置き換えられている"
    - "HIDDEN_TEAM定数が types.ts から削除されている"
    - "getMembers()が teams(name, status) をJOINして取得し Member.teams[] として返す"
    - "updateMember()が updates.teams を参照し string[]ループで処理する"
    - "TypeScriptのビルドがエラーなく通る"
    - "D-05: getMembers() の SELECT クエリを teams(name, status) にJOIN拡張する"
    - "D-06: Member 型の teamNames: string[] を teams: {name: string, status: string}[] に変更する"
    - "D-07: 呼び出し元で team名のみ必要な箇所（チェックボックスUI等）は .map(t => t.name) で対応する"
  artifacts:
    - path: "src/lib/types.ts"
      provides: "Member型定義（teams配列）"
      contains: "teams: { name: string; status: string }[]"
    - path: "src/lib/members.ts"
      provides: "getMembers()のSELECTとマッピング、updateMember()のteams参照"
      contains: "teams (name, status)"
    - path: "src/app/admin/actions.ts"
      provides: "updateMemberActionの型変換"
      contains: "updates.teams"
  key_links:
    - from: "src/lib/types.ts"
      to: "src/lib/members.ts"
      via: "Member型インポート"
      pattern: "import type \\{ Member \\}"
    - from: "src/lib/members.ts"
      to: "src/app/admin/actions.ts"
      via: "updateMember()呼び出し"
      pattern: "updateMember\\(publicationId"
---

<objective>
Member型の破壊的変更（teamNames → teams）を行い、getMembers()とupdateMember()をDBから status 情報を取得する形に拡張する。HIDDEN_TEAM定数を削除する。

Purpose: 型変更はコンパイルエラーとして全影響箇所を露出させる。Wave 2でこの変更を確定し、Wave 3のUI変更がクリーンな型を前提に動作できるようにする。
Output: 型安全な Member[] が teams: {name, status}[] で流れ、HIDDEN_TEAM定数が廃止された状態
</objective>

<execution_context>
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/kugyu10/work/keep-substack/.planning/ROADMAP.md
@/Users/kugyu10/work/keep-substack/.planning/phases/22-team-status/22-CONTEXT.md
@/Users/kugyu10/work/keep-substack/.planning/phases/22-team-status/22-RESEARCH.md
@/Users/kugyu10/work/keep-substack/.planning/phases/22-team-status/22-PATTERNS.md

<interfaces>
<!-- src/lib/types.ts 現行定義（全文）-->
```typescript
// RESEARCH.md §2「カレンダーUI向けのキーフィールド」に基づく
export type FeedItem = {
  title?: string
  link?: string
  pubDate?: string
  isoDate?: string
  thumbnail?: string
}

export type Member = {
  name: string
  publicationId: string
  teamNames: string[]  // ← これを teams: {name, status}[] に変更
  addedAt: string
}

export type MemberFeedResult = {
  member: Member
  items: FeedItem[]
  imageUrl?: string
}

export const HIDDEN_TEAM = 'chameleon'  // ← これを削除
```

<!-- src/lib/members.ts 現行の getMembers() クエリ（line 4-26）-->
```typescript
export async function getMembers(): Promise<Member[]> {
  const supabase = createSupabaseAdminClient()
  const { data, error } = await supabase
    .from('members')
    .select(`
      name,
      publication_id,
      added_at,
      member_teams (
        teams (name)       // ← teams (name, status) に変更
      )
    `)
  if (error) throw error
  if (!data) return []
  return data.map((m: any) => ({
    name: m.name,
    publicationId: m.publication_id,
    teamNames: (m.member_teams as any[])  // ← teams: [...] にマッピング変更
      .map((mt: any) => mt.teams?.name)
      .filter((n: unknown): n is string => typeof n === 'string'),
    addedAt: m.added_at,
  }))
}
```

<!-- src/lib/members.ts 現行の updateMember() teamNames参照箇所（line 101-121）-->
```typescript
if (updates.teamNames !== undefined) {
  // member_teams削除
  const { error: deleteError } = await supabase.from('member_teams').delete().eq('member_id', member.id)
  if (deleteError) throw deleteError

  for (const teamName of updates.teamNames) {  // ← updates.teams に変更
    // ...
  }
}
```

<!-- src/app/admin/actions.ts 現行の updateMemberAction（line 59-81）-->
```typescript
export async function updateMemberAction(
  publicationId: string,
  formData: FormData
): Promise<string | null> {
  try { await requireAdmin() } catch { return '権限がありません' }

  const name = formData.get('name') as string
  const teamNames = formData.getAll('teamNames') as string[]  // これを teams変換に使う
  const addedAt = formData.get('addedAt') as string

  // ...
  await updateMember(publicationId, { name, teamNames, addedAt })  // ← teams: [{name, status}] 形式に変更
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Member型変更とHIDDEN_TEAM定数削除（D-06）</name>
  <files>src/lib/types.ts</files>
  <read_first>
    - src/lib/types.ts（現行の全定義を確認してから編集する）
  </read_first>
  <action>
`src/lib/types.ts` を以下の通り変更する。

1. `Member` 型の `teamNames: string[]` を `teams: { name: string; status: string }[]` に変更する
2. `export const HIDDEN_TEAM = 'chameleon'` の行（line 27）とそのコメント行（line 26）を削除する

変更後の `Member` 型:
```typescript
export type Member = {
  name: string
  publicationId: string
  teams: { name: string; status: string }[]
  addedAt: string  // ISO 8601
}
```

`HIDDEN_TEAM` 定数とそのコメント（Phase 12に言及している行）を削除する。
`FeedItem` 型と `MemberFeedResult` 型は変更しない。
  </action>
  <verify>
    <automated>grep -c "teams: { name: string; status: string }\[\]" /Users/kugyu10/work/keep-substack/src/lib/types.ts</automated>
  </verify>
  <acceptance_criteria>
    - `teams: { name: string; status: string }[]` が存在する（grep で 1 以上）
    - `teamNames` が存在しない（`grep -c "teamNames" src/lib/types.ts` が 0）
    - `HIDDEN_TEAM` が存在しない（`grep -c "HIDDEN_TEAM" src/lib/types.ts` が 0）
    - `FeedItem` 型が存在する（削除していない）
    - `MemberFeedResult` 型が存在する（削除していない）
  </acceptance_criteria>
  <done>Member型が teams配列を持ち、HIDDEN_TEAM定数が削除されている</done>
</task>

<task type="auto">
  <name>Task 2: getMembers()のSELECT拡張とupdateMember()のteams参照修正（D-05/D-07）</name>
  <files>src/lib/members.ts, src/app/admin/actions.ts</files>
  <read_first>
    - src/lib/members.ts（現行の全関数を確認してから編集する。特にgetMembers()のクエリとupdateMember()のteamNames参照箇所）
    - src/app/admin/actions.ts（updateMemberActionのteamNames参照箇所を確認する）
  </read_first>
  <action>
**1. src/lib/members.ts の変更**

`getMembers()` のSupabaseクエリを以下のように変更する:

変更前:
```
member_teams (
  teams (name)
)
```

変更後:
```
member_teams (
  teams (name, status)
)
```

`getMembers()` のマッピングを以下のように変更する:

変更前:
```typescript
teamNames: (m.member_teams as any[])
  .map((mt: any) => mt.teams?.name)
  .filter((n: unknown): n is string => typeof n === 'string'),
```

変更後:
```typescript
teams: (m.member_teams as any[])
  .map((mt: any) => mt.teams)
  .filter((t: unknown): t is { name: string; status: string } =>
    t !== null && typeof t === 'object' && 'name' in (t as object)
  ),
```

`addMember()` 内の `for (const teamName of member.teamNames)` を `for (const teamName of member.teams.map(t => t.name))` に変更する（line 51付近）。

`updateMember()` 内の変更:
- `updates.teamNames` の参照（line 101）を `updates.teams` に変更する
- `for (const teamName of updates.teamNames)` （line 108）を `for (const teamName of updates.teams.map(t => t.name))` に変更する
- 関数シグネチャの型 `Partial<Omit<Member, 'publicationId'>>` は変更しない（Member型変更によって自動的に `teams` フィールドを持つ）

**2. src/app/admin/actions.ts の変更**

`updateMemberAction` 内の `teamNames` をFormDataから取得してMember.teams形式に変換する:

変更前:
```typescript
const teamNames = formData.getAll('teamNames') as string[]
// ...
await updateMember(publicationId, { name, teamNames, addedAt })
```

変更後:
```typescript
const teamNames = formData.getAll('teamNames') as string[]
// ...
await updateMember(publicationId, {
  name,
  teams: teamNames.map((teamName) => ({ name: teamName, status: 'public' })),
  addedAt,
})
```

注意: `addMemberAction` 内の `addMember({ name, publicationId, teamNames })` も `addMember({ name, publicationId, teams: teamNames.map(n => ({ name: n, status: 'public' })) })` に変更する（line 32付近）。
  </action>
  <verify>
    <automated>cd /Users/kugyu10/work/keep-substack && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "teams (name, status)" src/lib/members.ts` が 1（SELECTクエリ更新）
    - `grep -c "teamNames" src/lib/members.ts` が 0（全参照が teams に変更済み）
    - `grep -c "teamNames" src/app/admin/actions.ts` が 2（FormDataからの取得とmapは残る。`getAll('teamNames')`と変数宣言の2箇所）
    - `grep -c "teams: teamNames.map" src/app/admin/actions.ts` が 2（addMemberActionとupdateMemberActionの両方）
    - `npx tsc --noEmit` がエラーなく完了する（警告は許容）
  </acceptance_criteria>
  <done>
    getMembers()がteams(name, status)をJOINして返し、updateMember()がupdates.teamsを参照し、TypeScriptビルドがエラーなく通る
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Supabase→アプリ | getMembers()が取得するDB データに status が含まれる |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-03 | Information Disclosure | Member.teams[].status | accept | statusは公開データ（チームタブ制御用）。機密情報を含まない |
| T-22-04 | Tampering | updateMember() teams参照 | accept | service_role経由でのみ書き込み可能。RLSで一般ユーザーの書き込みをブロック済み |
</threat_model>

<verification>
```bash
# Member型変更確認
grep "teams: { name: string; status: string }" /Users/kugyu10/work/keep-substack/src/lib/types.ts

# HIDDEN_TEAM削除確認
grep -v "^//" /Users/kugyu10/work/keep-substack/src/lib/types.ts | grep -c "HIDDEN_TEAM"

# getMembers SELECT拡張確認
grep "teams (name, status)" /Users/kugyu10/work/keep-substack/src/lib/members.ts

# teamNames参照残存確認（0件であること）
grep -c "teamNames" /Users/kugyu10/work/keep-substack/src/lib/members.ts

# TypeScriptコンパイル確認
cd /Users/kugyu10/work/keep-substack && npx tsc --noEmit
```
</verification>

<success_criteria>
- src/lib/types.ts の Member型が `teams: { name: string; status: string }[]` を持つ
- src/lib/types.ts に HIDDEN_TEAM定数が存在しない
- src/lib/members.ts の getMembers() が `teams (name, status)` をSELECTして `teams: [{name, status}]` にマッピングする
- src/lib/members.ts に `teamNames` の参照が残っていない
- src/app/admin/actions.ts が `teams: teamNames.map(n => ({name: n, status: 'public'}))` 形式で updateMember を呼ぶ
- `npx tsc --noEmit` がエラーなく通る
</success_criteria>

<output>
After completion, create `.planning/phases/22-team-status/22-02-SUMMARY.md`
</output>
