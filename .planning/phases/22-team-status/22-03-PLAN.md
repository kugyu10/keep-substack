---
phase: 22-team-status
plan: "03"
type: execute
wave: 3
depends_on:
  - "22-02"
files_modified:
  - src/app/page.tsx
  - src/app/admin/AdminMemberList.tsx
autonomous: true
requirements:
  - TEAM-03
  - TEAM-04
must_haves:
  truths:
    - "トップページのチームタブに status=public のチームのみ表示される"
    - "Allビューでは status=hidden のチームに所属するメンバーが非表示になる"
    - "チームタブを選択した場合はstatus問わずそのチーム所属メンバーが表示される"
    - "HIDDEN_TEAM定数のインポートが page.tsx から削除されている"
    - "AdminMemberList が m.teams.some() と m.teams.map() でチーム判定・表示する"
    - "D-04: status別の表示ルール — public: チームタブあり + Allビューあり / private: チームタブなし + Allビューあり / hidden: チームタブなし + Allビューなし（完全非表示）"
    - "D-07: 呼び出し元で team名のみ必要な箇所（AdminMemberList のチェックボックス・表示等）は .map(t => t.name) で対応する"
    - "D-08: タブ一覧は member.teams.filter(t => t.status === 'public').map(t => t.name) で重複排除、Allビューは member.teams.every(t => t.status !== 'hidden') で除外判定"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "status基盤のフィルタリングロジック"
      contains: "t.status === 'public'"
    - path: "src/app/admin/AdminMemberList.tsx"
      provides: "teams配列を用いたチェックボックスとチーム名表示"
      contains: "m.teams.some"
  key_links:
    - from: "src/app/page.tsx"
      to: "Member.teams[]"
      via: "getMembers() の戻り値"
      pattern: "m\\.teams\\.filter"
    - from: "src/app/admin/AdminMemberList.tsx"
      to: "Member.teams[]"
      via: "members prop"
      pattern: "m\\.teams\\.some"
---

<objective>
トップページ（page.tsx）のフィルタリングロジックをHIDDEN_TEAM定数ベースからstatus判定ベースに移行する。AdminMemberList の teamNames参照を teams配列を使う形に修正する。

Purpose: Wave 2の型変更によりTypeScriptエラーが発生しているこれらのファイルを修正し、D-08で定義されたstatusベースの表示ルールを実装する。
Output: publicチームのみタブ表示、hiddenチームメンバーのAllビュー除外、AdminMemberListのチーム表示修正
</objective>

<execution_context>
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/kugyu10/work/keep-substack/.planning/ROADMAP.md
@/Users/kugyu10/work/keep-substack/.planning/phases/22-team-status/22-CONTEXT.md
@/Users/kugyu10/work/keep-substack/.planning/phases/22-team-status/22-PATTERNS.md
@/Users/kugyu10/work/keep-substack/.planning/phases/22-team-status/22-UI-SPEC.md

<interfaces>
<!-- src/app/page.tsx 現行のフィルタリングロジック（line 17-22）-->
```typescript
// 変更前（HIDDEN_TEAM定数ベース）
import { HIDDEN_TEAM } from '@/lib/types'  // line 3 を削除

const teams = [...new Set(allMembers.flatMap((m) => m.teamNames).filter(Boolean))].filter(
  (t) => t !== HIDDEN_TEAM
)
const filteredMembers = team
  ? allMembers.filter((m) => m.teamNames.includes(team))
  : allMembers.filter((m) => !m.teamNames.includes(HIDDEN_TEAM))
```

<!-- D-08で定義されたstatusベースの変更後ロジック -->
```typescript
// 変更後（statusベース、D-08準拠）
// HIDDEN_TEAM インポートを削除

const teams = [
  ...new Set(
    allMembers
      .flatMap((m) => m.teams.filter((t) => t.status === 'public').map((t) => t.name))
      .filter(Boolean)
  ),
]
const filteredMembers = team
  ? allMembers.filter((m) => m.teams.some((t) => t.name === team))
  : allMembers.filter((m) => m.teams.every((t) => t.status !== 'hidden'))
```

<!-- AdminMemberList.tsx の teamNames参照2箇所 -->
<!-- line 73: defaultChecked -->
```typescript
// 変更前
defaultChecked={m.teamNames.includes(team)}
// 変更後
defaultChecked={m.teams.some((t) => t.name === team)}
```

<!-- line 107: 表示用join -->
```typescript
// 変更前
{m.teamNames.join(', ')}
// 変更後
{m.teams.map((t) => t.name).join(', ')}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: page.tsx のフィルタリングロジックをstatus判定に移行（D-08）</name>
  <files>src/app/page.tsx</files>
  <read_first>
    - src/app/page.tsx（現行の全コードを確認してから編集する）
    - src/lib/types.ts（変更後のMember型を確認する）
  </read_first>
  <action>
`src/app/page.tsx` を以下の通り変更する。

**1. インポートの変更（line 3）:**
削除: `import { HIDDEN_TEAM } from '@/lib/types'`
（`getMembers`, `fetchAllFeedsCached`, `WeeklyHeatmapGrid`, `PrBanner` のインポートは変更しない）

**2. フィルタリングロジックの変更（line 17-22付近）:**

変更前:
```typescript
const teams = [...new Set(allMembers.flatMap((m) => m.teamNames).filter(Boolean))].filter(
  (t) => t !== HIDDEN_TEAM
)
const filteredMembers = team
  ? allMembers.filter((m) => m.teamNames.includes(team))
  : allMembers.filter((m) => !m.teamNames.includes(HIDDEN_TEAM))
```

変更後:
```typescript
const teams = [
  ...new Set(
    allMembers
      .flatMap((m) => m.teams.filter((t) => t.status === 'public').map((t) => t.name))
      .filter(Boolean)
  ),
]
const filteredMembers = team
  ? allMembers.filter((m) => m.teams.some((t) => t.name === team))
  : allMembers.filter((m) => m.teams.every((t) => t.status !== 'hidden'))
```

JSX部分（return以降）は変更しない。
  </action>
  <verify>
    <automated>grep -c "t.status === 'public'" /Users/kugyu10/work/keep-substack/src/app/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `HIDDEN_TEAM` が page.tsx に存在しない（`grep -c "HIDDEN_TEAM" src/app/page.tsx` が 0）
    - `t.status === 'public'` が page.tsx に存在する
    - `t.status !== 'hidden'` が page.tsx に存在する
    - `m.teams.some` が page.tsx に存在する
    - `m.teams.every` が page.tsx に存在する
    - `teamNames` が page.tsx に存在しない（`grep -c "teamNames" src/app/page.tsx` が 0）
  </acceptance_criteria>
  <done>page.tsx がHIDDEN_TEAM定数を参照せず、status判定でチームタブとAllビューをフィルタリングする</done>
</task>

<task type="auto">
  <name>Task 2: AdminMemberList の teamNames参照を teams配列に修正（D-07）</name>
  <files>src/app/admin/AdminMemberList.tsx</files>
  <read_first>
    - src/app/admin/AdminMemberList.tsx（現行の全コードを確認し、teamNames参照の全箇所を特定する）
  </read_first>
  <action>
`src/app/admin/AdminMemberList.tsx` の `teamNames` 参照を全件修正する。

**修正箇所 1（line 73付近）— チェックボックスの defaultChecked:**

変更前:
```typescript
defaultChecked={m.teamNames.includes(team)}
```

変更後:
```typescript
defaultChecked={m.teams.some((t) => t.name === team)}
```

**修正箇所 2（line 107付近）— チーム名の表示:**

変更前:
```typescript
{m.teamNames.join(', ')}
```

変更後:
```typescript
{m.teams.map((t) => t.name).join(', ')}
```

注意: 上記2箇所以外のコード（Props型定義・ハンドラ関数・JSX構造）は変更しない。`teams: string[]` の Props型定義は変更不要（admin/page.tsx が string[] を渡し続けるため）。
  </action>
  <verify>
    <automated>grep -c "teamNames" /Users/kugyu10/work/keep-substack/src/app/admin/AdminMemberList.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "teamNames" src/app/admin/AdminMemberList.tsx` が 0
    - `m.teams.some((t) => t.name === team)` が存在する
    - `m.teams.map((t) => t.name).join(', ')` が存在する
    - `teams: string[]` の Props型定義が残っている（削除していない）
    - `cd /Users/kugyu10/work/keep-substack && npx tsc --noEmit` がエラーなく通る
  </acceptance_criteria>
  <done>AdminMemberList.tsx の teamNames参照が全て teams配列を使う形に修正され、ビルドが通る</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL クエリパラメータ→フィルタリング | `?team=` クエリパラメータがフィルタリングロジックに渡される |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-05 | Information Disclosure | page.tsx team フィルタ | accept | hiddenチームはAllビューから除外される。ただし team名を直接URLに指定すれば表示される。Phase 24で /admin/teams/{teamName} の管理者専用ビューが実装されるが、現時点では team名推測によるhiddenチームアクセスは許容範囲とする（低リスク：メンバー名は公開情報） |
| T-22-06 | Tampering | AdminMemberList フォーム送信 | accept | requireAdmin() による認証チェックが Server Action側で実施済み |
</threat_model>

<verification>
```bash
# page.tsx の変更確認
grep "t.status === 'public'" /Users/kugyu10/work/keep-substack/src/app/page.tsx
grep "t.status !== 'hidden'" /Users/kugyu10/work/keep-substack/src/app/page.tsx
grep -v "^//" /Users/kugyu10/work/keep-substack/src/app/page.tsx | grep -c "HIDDEN_TEAM"

# AdminMemberList.tsx の変更確認
grep "m.teams.some" /Users/kugyu10/work/keep-substack/src/app/admin/AdminMemberList.tsx
grep "m.teams.map" /Users/kugyu10/work/keep-substack/src/app/admin/AdminMemberList.tsx
grep -c "teamNames" /Users/kugyu10/work/keep-substack/src/app/admin/AdminMemberList.tsx

# TypeScriptコンパイル確認
cd /Users/kugyu10/work/keep-substack && npx tsc --noEmit
```
</verification>

<success_criteria>
- page.tsx が `t.status === 'public'` でチームタブをフィルタリングする
- page.tsx が `m.teams.every(t => t.status !== 'hidden')` でAllビューのhiddenメンバーを除外する
- page.tsx に HIDDEN_TEAM の参照が存在しない
- AdminMemberList.tsx に teamNames の参照が存在しない
- AdminMemberList.tsx が `m.teams.some` と `m.teams.map` を使う
- `npx tsc --noEmit` がエラーなく通る
</success_criteria>

<output>
After completion, create `.planning/phases/22-team-status/22-03-SUMMARY.md`
</output>
