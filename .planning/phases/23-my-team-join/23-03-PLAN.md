---
phase: 23-my-team-join
plan: 03
type: execute
wave: 2
depends_on: ["23-01", "23-02"]
files_modified:
  - src/app/my/MyProfileForm.tsx
autonomous: true
requirements: [SELF-01, SELF-03]
must_haves:
  truths:
    - "Every status='public' team renders as an enabled checkbox named 'teams' with its team name as value; checked when the member currently belongs"
    - "A public team the member has not joined renders as an unchecked enabled checkbox (it can be joined by checking + saving)"
    - "private teams the member belongs to render as a disabled, checked, readonly row with the '管理者が設定' label and are NOT submitted to the action"
    - "private teams the member does NOT belong to do not render at all (handled upstream: never in publicTeams, never in a member row of status private the user lacks)"
    - "When no public teams exist the list shows '参加できる公開チームはありません' and no checkboxes"
    - "When the member already belongs to every public team, all public checkboxes are checked and 'すべての公開チームに参加中です' is shown"
    - "Honors locked decisions D-01 (unjoined private teams hidden), D-02 (joined private teams readonly with '管理者が設定'), D-03 (single integrated form / one 保存する button via useActionState), D-04 (batch save on button press, not immediate), D-05 and D-06 (empty-state messages), D-09 (private rows not submitted)"
  artifacts:
    - path: "src/app/my/MyProfileForm.tsx"
      provides: "Client form whose 所属チーム section is a checkbox list of public teams + readonly private rows, submitting checked names via name='teams'"
      contains: "getAll"
  key_links:
    - from: "src/app/my/MyProfileForm.tsx"
      to: "updateMyProfileAction (formData.getAll('teams'))"
      via: "checkbox name='teams' value={teamName}"
      pattern: "name=\"teams\""
    - from: "src/app/my/MyProfileForm.tsx"
      to: "page.tsx props"
      via: "currentTeams + publicTeams"
      pattern: "publicTeams"
---

<objective>
Replace the comma-separated `teamNames` text input in `src/app/my/MyProfileForm.tsx` with a checkbox list per the UI-SPEC and CONTEXT decisions:
- Public teams (from `publicTeams`) render as enabled checkboxes `name="teams" value={teamName}`, `defaultChecked` when the member currently belongs (derived from `currentTeams`). This is the field contract Plan 01's action reads via `formData.getAll('teams')`.
- Private teams the member belongs to (from `currentTeams` where `status === 'private'`) render as disabled, checked, readonly rows labelled 「管理者が設定」, and are NOT given `name="teams"` so they are never submitted (D-02, D-09 read-side guarantee).
- Empty states: no public teams → 「参加できる公開チームはありません」 (D-05); all public teams joined → 「すべての公開チームに参加中です」 (D-06).

Per D-03, name change and team join/leave stay a SINGLE integrated form: the existing `useActionState` wiring, publication-ID readonly field, name input, and the single 保存する save button all remain — the only structural change is the 所属チーム section. UI-SPEC typography/spacing/accessibility rules are applied across the form.

Purpose: Implements the UI for SELF-01 (checkbox list of public teams) and SELF-03 (private teams shown readonly when belonged-to, hidden otherwise), per CONTEXT D-01..D-06 (single integrated form / one save button via useActionState per D-03) and the full UI Design Contract.
Output: Rewritten MyProfileForm 所属チーム section + updated prop type. Consumes Plan 02's `currentTeams`/`publicTeams` props and Plan 01's `name="teams"` / `getAll` field contract.
</objective>

<execution_context>
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/23-my-team-join/23-CONTEXT.md
@.planning/phases/23-my-team-join/23-PATTERNS.md
@.planning/phases/23-my-team-join/23-UI-SPEC.md

<interfaces>
<!-- Contracts the executor needs. Extracted from codebase + Plans 01/02. No exploration required. -->

Current `src/app/my/MyProfileForm.tsx` — the FULL target of this plan.
  Keep VERBATIM (with the noted weight/aria tweaks):
    - 'use client' + `useActionState(updateMyProfileAction, null)` (lines 1-13)
    - `<form action={action} className="space-y-4">` shell (line 16)
    - error `<p>` (line 17) — ADD `role="alert"` (UI-SPEC line 203)
    - パブリケーションID readonly field (lines 19-25)
    - 名前 input (lines 27-39)
    - 保存 button (lines 56-62) — change `font-medium` → `font-semibold`, add `aria-disabled={isPending}` (UI-SPEC lines 62, 204)
  REPLACE: the 所属チーム text-input block (lines 41-54).
  CHANGE all form-label weights `font-medium` → `font-semibold` (UI-SPEC line 62).

New prop contract (from Plan 02's page.tsx):
  type Member = {
    name: string
    publicationId: string
    currentTeams: { name: string; status: string }[]   // member's joined teams (any status)
    publicTeams: { name: string }[]                      // ALL status='public' teams, name-ascending
  }
  REPLACE the current `team_names: string[]` field with these two fields.

Field contract (from Plan 01 — actions.ts reads this):
  `formData.getAll('teams')` returns the checked PUBLIC team names. So ONLY enabled public checkboxes carry `name="teams"`. Disabled private rows MUST NOT have `name="teams"` (so they are excluded from getAll → never deleted/re-inserted; D-09 is enforced server-side, this prevents the form from even submitting them).

Derivations inside the component (no extra fetch — pure props):
  - joinedNames = new Set(currentTeams.map(t => t.name))
  - public checkbox checked? → joinedNames.has(publicTeamName)
  - private readonly rows → currentTeams.filter(t => t.status === 'private')
  - "all joined" state (D-06) → publicTeams.length > 0 AND every publicTeams name ∈ joinedNames
  - "no public teams" state (D-05) → publicTeams.length === 0

Analog — empty-state guard, `src/app/admin/teams/TeamStatusList.tsx` lines 31-38.

EXACT UI-SPEC class strings + copy (must appear verbatim):
  Section label:        <label className="block text-sm font-semibold mb-2">所属チーム</label>   (UI-SPEC line 127)
  List container:       <div className="space-y-1 border rounded px-3 py-2">…</div>              (UI-SPEC line 132)
  Public checkbox row:  <label className="flex items-center gap-3 min-h-[44px] cursor-pointer">  (UI-SPEC line 107)
                        <input type="checkbox" name="teams" value={teamName} defaultChecked={…} className="w-4 h-4 accent-orange-500" />
                        <span className="text-sm text-[#363737]">{teamName}</span>               (UI-SPEC lines 108-111)
  Private readonly row: <label className="flex items-center gap-3 min-h-[44px] cursor-not-allowed opacity-60">  (UI-SPEC line 117)
                        <input type="checkbox" checked disabled aria-label={`${teamName} 管理者設定済み`} className="w-4 h-4" />  (UI-SPEC lines 118-119 + accessibility line 202)
                        <span className="text-sm text-[#363737]">{teamName}</span>
                        <span className="text-xs text-gray-500 ml-auto">管理者が設定</span>        (UI-SPEC line 121)
  Empty (no public):    <p className="text-sm text-gray-500">参加できる公開チームはありません</p>   (UI-SPEC line 122 / copy line 172)
  All joined caption:   <p className="text-xs text-gray-500 mt-1">すべての公開チームに参加中です</p> (UI-SPEC line 124 / copy line 173)
  Save button:          className="w-full bg-orange-500 text-white rounded px-4 py-2 text-sm font-semibold disabled:opacity-50"  (UI-SPEC line 130)

Note: D-04 — checkbox changes are NOT saved immediately; they batch-save on the 保存する button. Use `defaultChecked` (uncontrolled), NOT controlled `checked`, on public checkboxes so the user can toggle freely before submit.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the 所属チーム text input with a public/private checkbox list</name>
  <files>src/app/my/MyProfileForm.tsx</files>
  <read_first>
    - src/app/my/MyProfileForm.tsx (current form — useActionState lines 1-13, shell line 16, error line 17, pub-id lines 19-25, name lines 27-39, team text input lines 41-54, button lines 56-62)
    - src/app/admin/teams/TeamStatusList.tsx (empty-state guard pattern, lines 31-38)
    - .planning/phases/23-my-team-join/23-UI-SPEC.md (Component Inventory lines 96-135, Interaction Contract lines 146-162, Copywriting Contract lines 167-177, Accessibility Contract lines 200-205, Typography line 62)
    - .planning/phases/23-my-team-join/23-PATTERNS.md (§ MyProfileForm, lines 68-135 — keep-verbatim ranges + replacement blocks)
    - .planning/phases/23-my-team-join/23-CONTEXT.md (D-01 line 27, D-02 line 28, D-03 line 32, D-04 line 36, D-05 line 40, D-06 line 41; checkbox-submit note lines 105-106)
    - .planning/phases/23-my-team-join/23-01-PLAN.md (field contract: formData.getAll('teams'), objective lines 42-43)
  </read_first>
  <action>
    Modify `src/app/my/MyProfileForm.tsx`. Keep the `'use client'` directive, the `useActionState(updateMyProfileAction, null)` wiring, the `<form action={action} className="space-y-4">` shell, the パブリケーションID readonly field, and the 名前 input. Per D-03 this remains a single integrated form with one save button — do NOT split the team section into its own form/action. Apply these targeted edits:

    1. Prop type: replace the `Member` type's `team_names: string[]` field with `currentTeams: { name: string; status: string }[]` and `publicTeams: { name: string }[]` (matches Plan 02's page.tsx props).

    2. Derive (plain consts at top of component, no hooks needed): `joinedNames` = a Set of `member.currentTeams.map(t => t.name)`; `privateJoined` = `member.currentTeams.filter(t => t.status === 'private')`; `allJoined` = `member.publicTeams.length > 0 && member.publicTeams.every(t => joinedNames.has(t.name))`.

    3. Replace the 所属チーム block (current lines 41-54) with:
       - Section label `所属チーム` using `<label className="block text-sm font-semibold mb-2">`.
       - If `member.publicTeams.length === 0` AND `privateJoined.length === 0`: render the empty-state `<p className="text-sm text-gray-500">参加できる公開チームはありません</p>` and no container. (If there are private readonly rows but no public teams, still render the container with the private rows, then the empty caption below — see next bullets.)
       - Otherwise render the list container `<div className="space-y-1 border rounded px-3 py-2">` containing:
         a. One PUBLIC checkbox row per `member.publicTeams` item — `name="teams"`, `value={teamName}`, `defaultChecked={joinedNames.has(teamName)}`, `className="w-4 h-4 accent-orange-500"`, wrapped in the `min-h-[44px] cursor-pointer` label with the `text-sm text-[#363737]` name span. Use the team name as the React `key`.
         b. One PRIVATE readonly row per `privateJoined` item — `checked disabled` (NO `name` attribute, so it is excluded from `formData.getAll('teams')`), `aria-label={`${teamName} 管理者設定済み`}`, wrapped in the `min-h-[44px] cursor-not-allowed opacity-60` label, plus the trailing `<span className="text-xs text-gray-500 ml-auto">管理者が設定</span>` label.
       - If `member.publicTeams.length === 0` (only private rows exist), show `<p className="text-sm text-gray-500">参加できる公開チームはありません</p>` after the container (D-05 applies to the public join-able set).
       - If `allJoined`, show `<p className="text-xs text-gray-500 mt-1">すべての公開チームに参加中です</p>` after the container (D-06).

    4. Accessibility + typography sweep: add `role="alert"` to the error `<p>` (line 17). Add `aria-disabled={isPending}` to the submit button and change its `font-medium` → `font-semibold`. Change every remaining form-label `font-medium` → `font-semibold` (パブリケーションID label, 名前 label).

    Use `defaultChecked` (uncontrolled) on public checkboxes per D-04 — do NOT introduce `useState`/controlled `checked` for them; the batch-save model relies on the browser form state read by `getAll('teams')` on submit. Do NOT give private rows a `name` attribute. Do NOT alter `updateMyProfileAction` or page.tsx.
  </action>
  <verify>
    <automated>cd /Users/kugyu10/work/keep-substack && grep -q 'name="teams"' src/app/my/MyProfileForm.tsx && grep -q 'accent-orange-500' src/app/my/MyProfileForm.tsx && grep -q 'min-h-\[44px\]' src/app/my/MyProfileForm.tsx && grep -q '管理者が設定' src/app/my/MyProfileForm.tsx && grep -q '参加できる公開チームはありません' src/app/my/MyProfileForm.tsx && grep -q 'すべての公開チームに参加中です' src/app/my/MyProfileForm.tsx && grep -q 'role="alert"' src/app/my/MyProfileForm.tsx && grep -q 'aria-disabled={isPending}' src/app/my/MyProfileForm.tsx && grep -q 'publicTeams' src/app/my/MyProfileForm.tsx && grep -q 'currentTeams' src/app/my/MyProfileForm.tsx && ! grep -q 'team_names\|teamNames\|name="teamNames"' src/app/my/MyProfileForm.tsx && ! grep -q 'font-medium' src/app/my/MyProfileForm.tsx && echo PASS || { echo FAIL; false; }</automated>
  </verify>
  <acceptance_criteria>
    - The `Member` prop type has `currentTeams: { name: string; status: string }[]` and `publicTeams: { name: string }[]`; no `team_names` field remains.
    - Public checkboxes use `name="teams"` value={teamName}, `defaultChecked` from membership, class `w-4 h-4 accent-orange-500`, in a `min-h-[44px] cursor-pointer` label.
    - Private readonly rows are `checked disabled`, carry NO `name` attribute, have `aria-label` `${teamName} 管理者設定済み`, the `管理者が設定` trailing label, and `opacity-60 cursor-not-allowed`.
    - Section label uses `text-sm font-semibold`; the list container uses `space-y-1 border rounded px-3 py-2`.
    - Empty-state copy `参加できる公開チームはありません` and all-joined copy `すべての公開チームに参加中です` are present verbatim.
    - Error `<p>` has `role="alert"`; submit button has `aria-disabled={isPending}` and `font-semibold`; no `font-medium` remains in the file.
    - No `teamNames` / `team_names` identifier remains.
  </acceptance_criteria>
  <done>
    The 所属チーム section is a checkbox list: public teams as enabled `name="teams"` checkboxes (checked = joined), private joined teams as disabled readonly rows with no name attribute and the 管理者が設定 label, plus the D-05/D-06 empty/all-joined messages. UI-SPEC classes, copy, and accessibility attributes match verbatim. useActionState, pub-id, name input, and save button preserved.
  </done>
</task>

<task type="auto">
  <name>Task 2: Typecheck + lint the full /my flow end-to-end</name>
  <files>src/app/my/MyProfileForm.tsx</files>
  <read_first>
    - src/app/my/MyProfileForm.tsx (after Task 1)
    - src/app/my/page.tsx (Plan 02 output — the prop object passed in)
    - src/app/my/actions.ts (Plan 01 output — the action signature useActionState binds to)
  </read_first>
  <action>
    Run `npx tsc --noEmit`. Because this plan is wave 2 (Plans 01 and 02 are complete), the cross-file contract is now whole: page.tsx passes `{ name, publicationId, currentTeams, publicTeams }`, this form's prop type accepts exactly those fields, and `updateMyProfileAction` matches the `useActionState` signature. Confirm there are NO remaining type errors across `src/app/my/page.tsx`, `src/app/my/MyProfileForm.tsx`, and `src/app/my/actions.ts` — in particular the `<MyProfileForm member={...}>` prop-shape mismatch deferred from Plan 02 must now be resolved by this plan's prop-type change. If the project has an `npm run lint` or `next lint` script, run it for the my directory and confirm no new errors. Do NOT modify page.tsx or actions.ts to "fix" anything — if a mismatch surfaces, it indicates the prop type in Task 1 does not match Plan 02's prop object; correct the prop type in MyProfileForm.tsx (this file) only.
  </action>
  <verify>
    <automated>cd /Users/kugyu10/work/keep-substack && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -i "src/app/my/" || echo "no-my-errors"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` reports zero type errors for `src/app/my/page.tsx`, `src/app/my/MyProfileForm.tsx`, and `src/app/my/actions.ts` (the deferred Plan 02 prop mismatch is closed).
    - Any fix needed is made only in `MyProfileForm.tsx`'s prop type; page.tsx and actions.ts are not edited (no file-ownership conflict).
  </acceptance_criteria>
  <done>
    The full /my flow typechecks clean: page.tsx props ↔ MyProfileForm prop type ↔ updateMyProfileAction signature all agree. No errors remain in the my directory.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → browser form | User can toggle enabled public checkboxes; the form decides which team names are submitted under `name="teams"` |
| form → Server Action (Plan 01) | Only checked PUBLIC checkbox values are submitted; private readonly rows have no `name` and are never submitted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-08 | Tampering | private-team readonly rows | mitigate | Private rows are rendered `disabled` AND carry no `name` attribute, so they are excluded from `formData.getAll('teams')` and can never be submitted for join/leave (D-02, D-09). Even if a client forced their submission, Plan 01's action only honors `status='public'` names, so it is a defense-in-depth layer, not the sole gate (T-23-01). |
| T-23-09 | Spoofing / Tampering | injected non-public team name | accept | A user could hand-craft a POST with arbitrary `teams` values; this is fully neutralized server-side by Plan 01's public-set validation (T-23-01). The form adds no client-side trust assumption. |
| T-23-10 | Information Disclosure | rendered team list | mitigate | The form only renders names from `publicTeams` (public only, per Plan 02 T-23-06) and `currentTeams` the user already belongs to. No private/hidden team the user lacks is ever in props, so none can render (D-01). |
| T-23-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed; React/Tailwind already present. No supply-chain surface added. |
</threat_model>

<verification>
- `grep 'name="teams"' src/app/my/MyProfileForm.tsx` → present (field contract for Plan 01's getAll).
- `grep 'accent-orange-500' / 'min-h-[44px]'` → present (UI-SPEC public checkbox).
- `grep '管理者が設定'` → present (D-02 private label); private rows have NO `name` attribute.
- `grep '参加できる公開チームはありません' / 'すべての公開チームに参加中です'` → present (D-05/D-06).
- `grep 'role="alert"' / 'aria-disabled={isPending}'` → present (accessibility).
- `grep 'font-medium'` → absent (typography weight unified to semibold).
- `grep 'team_names\|teamNames'` → absent (old field removed).
- `npx tsc --noEmit` → zero errors across src/app/my/ (page ↔ form ↔ action contract whole).
</verification>

<success_criteria>
- 所属チーム is a checkbox list: all public teams as enabled `name="teams"` checkboxes (checked = currently joined), enabling join/leave via toggle + save (SELF-01, SELF-02 UI side).
- Private teams the member belongs to show as disabled readonly rows labelled 管理者が設定 and are never submitted (SELF-03, D-02, D-09 read-side).
- Private teams the member does not belong to never render (D-01, enforced by props).
- D-05 / D-06 empty and all-joined states display the exact UI-SPEC copy.
- Name change and team join/leave remain a single integrated form with one save button (D-03).
- UI-SPEC spacing, typography (font-semibold), accent-orange button, role="alert", aria-label on disabled checkboxes, and 44px touch targets are all applied.
- Full /my flow typechecks clean against Plans 01 and 02.
</success_criteria>

<output>
Create `.planning/phases/23-my-team-join/23-03-SUMMARY.md` when done. Record: the final checkbox-row structure (public vs private), confirmation that private rows omit `name="teams"`, the empty/all-joined state logic, and that tsc is clean across the my directory.
</output>
