---
phase: 23-my-team-join
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/my/actions.ts
  - src/app/my/__tests__/updateMyProfileAction.test.ts
autonomous: true
requirements: [SELF-02]
must_haves:
  truths:
    - "Saving with a public team checked adds that member_teams row (join)"
    - "Saving with a previously-checked public team now unchecked removes that member_teams row (leave)"
    - "A submitted team name that is not a status='public' team is ignored (no row created, no new teams row)"
    - "A private team the member already belongs to is NOT deleted by save (member_teams private rows preserved)"
    - "An unauthenticated request returns exactly 'ログインセッションが切れました。再ログインしてください' and mutates nothing"
    - "An empty/whitespace name returns exactly '名前を入力してください' and performs zero writes (no insert, no delete)"
    - "Honors locked decisions D-07 (updateMyProfileAction full replacement, public-name reconcile), D-08 (no free-creation upsert; non-public names ignored), D-09 (private member_teams rows never deleted)"
  artifacts:
    - path: "src/app/my/actions.ts"
      provides: "Rewritten updateMyProfileAction that reconciles only public-team membership via formData.getAll('teams')"
      contains: "formData.getAll('teams')"
    - path: "src/app/my/__tests__/updateMyProfileAction.test.ts"
      provides: "Unit tests proving join/leave/ignore-non-public/preserve-private/auth/name-required behaviors"
      contains: "describe"
  key_links:
    - from: "src/app/my/actions.ts"
      to: "teams (status='public')"
      via: "validation query"
      pattern: "\\.eq\\('status', 'public'\\)"
    - from: "src/app/my/actions.ts"
      to: "member_teams delete"
      via: "delete scoped to public team ids"
      pattern: "\\.in\\('team_id'"
---

<objective>
Fully replace `updateMyProfileAction` in `src/app/my/actions.ts` so members self-manage **public-team** membership only. The action accepts checked public team names via `formData.getAll('teams')`, validates them against the canonical set of `status='public'` teams, and reconciles `member_teams` with a **delete-then-insert scoped to public team ids only** — leaving private/hidden membership untouched. Free-creation of teams via upsert is removed.

This is the security boundary of Phase 23: it accepts user-submitted team names. It must NOT escalate into private/hidden teams, must NOT create arbitrary teams, and must only mutate the authenticated user's own membership.

Purpose: Implements SELF-02 (join/leave public teams) and enforces D-07, D-08, D-09.
Output: Rewritten `updateMyProfileAction` + a unit test file proving the reconcile and security behaviors. Establishes the `formData` field contract (`name="teams"`, read via `getAll`) that Plan 02's form depends on.
</objective>

<execution_context>
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/23-my-team-join/23-CONTEXT.md
@.planning/phases/23-my-team-join/23-PATTERNS.md

<interfaces>
<!-- Contracts the executor needs. Extracted from codebase. No exploration required. -->

Current `src/app/my/actions.ts` (lines 43-99) — FULL REPLACEMENT target for `updateMyProfileAction`.
Signature MUST stay identical (used by useActionState in Plan 02's form):
  export async function updateMyProfileAction(prevState: string | null, formData: FormData): Promise<string | null>

`linkMemberAction` (lines 7-41) is UNRELATED — do NOT touch it. Keep verbatim.

Supabase client factories (already imported at top of actions.ts):
  createSupabaseServerClient()  // src/lib/supabase/server.ts — anon, cookie-based; use for auth.getUser()
  createSupabaseAdminClient()   // src/lib/supabase/admin.ts — service role; use for table reads/writes

Reconcile analog (delete-then-insert) — `src/lib/members.ts` updateMember lines 103-123.
  Phase 23 NARROWS the delete to public team ids: NOT a blanket `.delete().eq('member_id', ...)`.

Tables / columns:
  teams(id uuid, name text, status text)   -- status in 'public' | 'private' | 'hidden'
  member_teams(member_id uuid, team_id uuid)
  members(id uuid, user_id uuid, name text, publication_id text)

Existing test pattern (node env, no jsdom) — `src/lib/__tests__/fetchFeed.test.ts`:
  uses `vi.mock('../path', () => ({ ... }))` to stub modules, `vi.mocked()`, describe/it/expect from 'vitest'.
  Run: `npx vitest run <file>`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite updateMyProfileAction to reconcile public-team membership only</name>
  <files>src/app/my/actions.ts</files>
  <read_first>
    - src/app/my/actions.ts (current updateMyProfileAction lines 43-99 + linkMemberAction lines 7-41 which must stay untouched)
    - src/lib/members.ts (updateMember delete-then-insert pattern, lines 103-123)
    - .planning/phases/23-my-team-join/23-PATTERNS.md (§ actions.ts — exact replacement steps 1-4, lines 159-185)
    - .planning/phases/23-my-team-join/23-CONTEXT.md (D-07, D-08, D-09 lines 44-47; copy lines for error messages)
    - .planning/phases/23-my-team-join/23-UI-SPEC.md (Copywriting Contract lines 167-177 — error strings)
  </read_first>
  <behavior>
    - join: checkedTeamNames=["Alpha"] where Alpha is status='public' and member has no Alpha row → after run, member_teams gains a row {member_id, team_id=Alpha.id}.
    - leave: member currently in public team "Beta", checkedTeamNames=[] → Beta's member_teams row is deleted.
    - ignore non-public: checkedTeamNames=["Ghost"] where Ghost is not a status='public' team (private, hidden, or nonexistent) → NO member_teams insert, NO teams upsert/insert occurs.
    - preserve private: member has a private-team row; any save (any checkedTeamNames) → that private row still exists afterward (delete was scoped via .in('team_id', publicTeamIds)).
    - auth: auth.getUser() returns no user → returns exactly 'ログインセッションが切れました。再ログインしてください', performs zero writes.
    - name still required: empty/whitespace name → returns exactly '名前を入力してください', no writes.
  </behavior>
  <action>
    Replace ONLY the body of `updateMyProfileAction` (current lines 43-99). Keep the exported signature `(prevState: string | null, formData: FormData): Promise<string | null>` unchanged.

    Steps:
    1. Parse name from `formData.get('name')` (trim); if empty return '名前を入力してください'. Parse checked names: `const checkedTeamNames = formData.getAll('teams').map(String)` — this is the field contract Plan 02 depends on (input name="teams"). Remove the old `teamNamesRaw` comma-string parse entirely (D-08).
    2. Auth: `createSupabaseServerClient()` → `auth.getUser()`; if no user return exactly 'ログインセッションが切れました。再ログインしてください' (per UI-SPEC auth-error copy, line 176). Note: 23-PATTERNS.md shared Authentication pattern now agrees with this string.
    3. Update members.name scoped to `.eq('user_id', user.id)` and `.select('id').single()` (existing pattern, keep). On error/no member: `console.error('[updateMyProfile] ...', err)` and return '保存に失敗しました。もう一度お試しください' (UI-SPEC general-error copy).
    4. Fetch the canonical public set: `admin.from('teams').select('id, name').eq('status', 'public')` — this is the validation source (D-07). Compute `publicTeamIds = (publicTeams ?? []).map(t => t.id)`.
    5. Validate submitted names (D-08): `allowed = (publicTeams ?? []).filter(t => checkedTeamNames.includes(t.name))`. Names not in the public set are silently ignored — no teams upsert/insert anywhere (the free-creation upsert from old lines 82-95 is DELETED).
    6. Delete ONLY this member's public-team rows (D-09): `admin.from('member_teams').delete().eq('member_id', member.id).in('team_id', publicTeamIds)`. Do NOT use the blanket `.delete().eq('member_id', ...)`. Guard the `.in()` when `publicTeamIds` is empty (skip delete if no public teams exist so the call is not a no-op-but-harmful broad delete).
    7. Insert checked public rows if `allowed.length > 0`: `admin.from('member_teams').insert(allowed.map(t => ({ member_id: member.id, team_id: t.id })))`.
    8. On each Supabase error: console.error with '[updateMyProfile] ...' prefix + return '保存に失敗しました。もう一度お試しください'. On success: `revalidatePath('/my')` then `return null`.

    Do NOT touch `linkMemberAction`. Do NOT add a `teams` upsert anywhere.
  </action>
  <verify>
    <automated>cd /Users/kugyu10/work/keep-substack && grep -q "getAll('teams')" src/app/my/actions.ts && grep -q "\.in('team_id'" src/app/my/actions.ts && grep -q "\.eq('status', 'public')" src/app/my/actions.ts && ! grep -q "teamNamesRaw\|onConflict: 'name'" src/app/my/actions.ts && ( ! (npx tsc --noEmit -p tsconfig.json 2>&1 | grep -i "my/actions") && echo "tsc-actions-clean=yes" || { echo "tsc-actions-has-errors"; false; } )</automated>
  </verify>
  <done>
    `updateMyProfileAction` reads `formData.getAll('teams')`, validates against `status='public'` teams, deletes member_teams scoped via `.in('team_id', publicTeamIds)`, inserts only allowed public rows, contains no `teams` upsert and no `teamNamesRaw`. `linkMemberAction` unchanged. tsc reports no errors for this file.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit tests for updateMyProfileAction reconcile + security behaviors</name>
  <files>src/app/my/__tests__/updateMyProfileAction.test.ts</files>
  <read_first>
    - src/lib/__tests__/fetchFeed.test.ts (vi.mock module-stub pattern, node env, vi.mocked usage)
    - src/app/my/actions.ts (the function under test, after Task 1)
    - .planning/phases/23-my-team-join/23-PATTERNS.md (reconcile steps to assert against, lines 159-185)
  </read_first>
  <behavior>
    - Test "join": getAll('teams')=['Alpha'], teams query returns [{id:'a',name:'Alpha',status implied public}]; assert member_teams.insert called with [{member_id, team_id:'a'}].
    - Test "leave": getAll('teams')=[]; assert member_teams.delete().in('team_id', [...publicIds]) called and insert NOT called.
    - Test "ignore non-public name": getAll('teams')=['Ghost'] not in public set; assert NO insert, NO teams.upsert/insert.
    - Test "preserve private (scoped delete)": assert the delete builder used `.in('team_id', publicTeamIds)` and NOT a bare member-only delete.
    - Test "auth fail": auth.getUser() resolves no user → returns exactly 'ログインセッションが切れました。再ログインしてください', asserts no admin writes.
    - Test "name required": name field empty/whitespace → returns exactly '名前を入力してください' and performs zero writes (no member_teams insert, no member_teams delete, no members update).
  </behavior>
  <action>
    Create `src/app/my/__tests__/updateMyProfileAction.test.ts`. Mock `@/lib/supabase/server` and `@/lib/supabase/admin` with `vi.mock`, mirroring the module-stub style in fetchFeed.test.ts. Also mock `next/cache` `revalidatePath` to a no-op `vi.fn()`.

    Build a chainable Supabase admin mock where `from()` returns an object whose `select/eq/insert/delete/in/update/single/maybeSingle/order` are `vi.fn()` returning `this` (or resolved `{ data, error }` at terminals). Capture the `insert`, `delete`, `.in`, and `update` spies so tests can assert call arguments and call counts. Make the `teams` select resolve the canonical public set for the test case.

    Use `formData.getAll('teams')` by constructing a real `FormData` and `append('teams', name)` per checked name, plus `append('name', 'Tester')`.

    Write the six tests from `<behavior>`. Use describe/it/expect/vi from 'vitest'. Each assertion must check concrete call args (e.g. `expect(insertSpy).toHaveBeenCalledWith([{ member_id: 'm1', team_id: 'a' }])`). For the scoped-delete test, assert the `.in` spy was called with `'team_id'` and an array containing the public ids. For the auth-fail test, assert the exact string `ログインセッションが切れました。再ログインしてください` is returned and no admin write spies were called. For the name-required test, append a whitespace `name` (e.g. `append('name', '   ')`), assert the exact return string `名前を入力してください`, and assert `insertSpy`, `deleteSpy`, and the members `update` spy were each NOT called (zero writes).
  </action>
  <verify>
    <automated>cd /Users/kugyu10/work/keep-substack && npx vitest run src/app/my/__tests__/updateMyProfileAction.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>
    `npx vitest run src/app/my/__tests__/updateMyProfileAction.test.ts` passes with the six tests green: join inserts, leave deletes-scoped + no insert, non-public ignored (no insert/no upsert), scoped delete uses `.in('team_id', ...)`, auth-fail returns the exact JP auth string with zero writes, name-required returns the exact '名前を入力してください' string with zero writes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser form → Server Action | User submits arbitrary `teams` name values via FormData; untrusted input crosses here |
| Server Action → Supabase (service role) | Admin client bypasses RLS; the action is the sole authorization gate for membership writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | Elevation of Privilege | updateMyProfileAction team validation | mitigate | Only names matching `teams WHERE status='public'` are honored; submitted private/hidden/nonexistent names are filtered out (D-08). Insert uses ids from the public set only. |
| T-23-02 | Tampering | member_teams delete scope | mitigate | Delete scoped with `.in('team_id', publicTeamIds)` so a save can never remove private/hidden membership rows the admin set (D-09). |
| T-23-03 | Spoofing / IDOR | member identity | mitigate | Writes scoped to the member resolved via `auth.getUser()` → `members.eq('user_id', user.id)`; no member_id comes from client input. Unauth → JP error, zero writes (T-23-A). |
| T-23-04 | Repudiation | failed writes | mitigate | All Supabase errors logged with `[updateMyProfile]` prefix before returning a user-facing JP message. |
| T-23-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan; vitest already present. No supply-chain surface added. |
</threat_model>

<verification>
- `grep "getAll('teams')" src/app/my/actions.ts` → present (field contract for Plan 02).
- `grep "\.in('team_id'" src/app/my/actions.ts` → present (scoped delete, D-09).
- `grep "onConflict: 'name'"` in actions.ts → absent (free-creation removed, D-08).
- `npx vitest run src/app/my/__tests__/updateMyProfileAction.test.ts` → all six tests pass (join, leave, ignore-non-public, scoped-delete, auth-fail, name-required).
- `npx tsc --noEmit` → no errors introduced in src/app/my/actions.ts.
</verification>

<success_criteria>
- updateMyProfileAction reconciles public-team membership only, validated against status='public' (SELF-02, D-07/D-08/D-09).
- Private/hidden membership is never deleted or created by the action.
- Unauthenticated and invalid-name requests perform zero writes.
- Unit tests prove join, leave, ignore-non-public, scoped-delete, auth-fail, and name-required behaviors.
- linkMemberAction untouched; build + typecheck clean.
</success_criteria>

<output>
Create `.planning/phases/23-my-team-join/23-01-SUMMARY.md` when done. Record: final reconcile shape, the `name="teams"` + `getAll` field contract for Plan 02, and test file location.
</output>
