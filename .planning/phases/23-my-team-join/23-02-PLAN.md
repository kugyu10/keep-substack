---
phase: 23-my-team-join
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/my/page.tsx
autonomous: true
requirements: [SELF-01, SELF-03]
must_haves:
  truths:
    - "The /my member query returns each joined team with both its name and its status"
    - "All status='public' teams are fetched (independent of membership) so the form can render the full join-able list"
    - "private/hidden teams the member does NOT belong to are never fetched into the public-teams list (no leakage)"
    - "The member query is scoped to the authenticated user (one user can never see another's membership)"
    - "An unauthenticated request to /my redirects to / before any data is read"
    - "Supplies the data that realizes decisions D-01 (unjoined private teams never enter the public list), D-05 and D-06 (empty-state / all-joined detection), and D-07 (canonical status='public' set for reconcile)"
  artifacts:
    - path: "src/app/my/page.tsx"
      provides: "RSC that fetches the member's teams with status + all public teams, and passes currentTeams + publicTeams props to MyProfileForm"
      contains: "publicTeams"
  key_links:
    - from: "src/app/my/page.tsx"
      to: "teams (status='public')"
      via: "public-teams query"
      pattern: "\\.eq\\('status', 'public'\\)"
    - from: "src/app/my/page.tsx"
      to: "MyProfileForm"
      via: "currentTeams + publicTeams props"
      pattern: "publicTeams"
    - from: "src/app/my/page.tsx"
      to: "members (this user only)"
      via: "scoped member query"
      pattern: "\\.eq\\('user_id', user\\.id\\)"
---

<objective>
Extend the `/my` RSC (`src/app/my/page.tsx`) to supply the data the new checkbox UI needs:
1. The member's JOIN query now returns each joined team's `status` (not just `name`).
2. A new query fetches ALL `status='public'` teams (the canonical join-able set), independent of membership.
3. Props passed to `MyProfileForm` change from `team_names: string[]` to `currentTeams: { name: string; status: string }[]` + `publicTeams: { name: string }[]`.

This plan is the read-side counterpart to Plan 01 (the write-side Server Action). It produces no UI changes itself — it only reshapes the data flowing into the form, which Plan 03 renders.

Purpose: Implements the data layer for SELF-01 (display all public teams as a checkbox list) and SELF-03 (distinguish private teams the member belongs to vs. hide private teams they don't), per CONTEXT D-01/D-02/D-05/D-06 and Integration Points (CONTEXT lines 90-95).
Output: Reshaped `MyPage` RSC. Establishes the `currentTeams` / `publicTeams` prop contract that Plan 03's form consumes.
</objective>

<execution_context>
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/23-my-team-join/23-CONTEXT.md
@.planning/phases/23-my-team-join/23-PATTERNS.md

<interfaces>
<!-- Contracts the executor needs. Extracted from codebase. No exploration required. -->

Current `src/app/my/page.tsx` — the FULL target of this plan.
  Auth + factory block (lines 8-13) stays VERBATIM:
    createSupabaseServerClient() → auth.getUser() → if (!user) redirect('/')
    const admin = createSupabaseAdminClient()
  Member query (lines 14-24): `.select` currently uses `member_teams ( teams (name) )` scoped `.eq('user_id', user.id).maybeSingle()`.
  Flatten (lines 26-30): currently maps to `string[]` named `teamNames`.
  Props block (lines 41-47): currently `<MyProfileForm member={{ name, publicationId, team_names: teamNames }} />`.
  The `!member` branch renders `<LinkMemberForm />` (lines 38-39) — keep VERBATIM.

Analog — public-teams query, `src/app/admin/teams/page.tsx` lines 6-9:
  `admin.from('teams').select('id, name, status').order('name')`
  This plan ADDS `.eq('status', 'public')` and selects only `id, name` (no status needed in the public list).

Analog — status-aware JOIN + flatten, `src/lib/members.ts` getMembers() lines 8-26:
  `member_teams ( teams (name, status) )`, then map `mt.teams` and filter to objects with a `name` key.

Member type (already defined) — `src/lib/types.ts` line 13:
  `teams: { name: string; status: string }[]`  (reference shape for currentTeams)

Supabase client factories (already imported at top of page.tsx):
  createSupabaseServerClient()  // src/lib/supabase/server.ts — anon, cookie-based; auth.getUser() only
  createSupabaseAdminClient()   // src/lib/supabase/admin.ts — service role; table reads

Downstream consumer — Plan 03's MyProfileForm expects props:
  currentTeams: { name: string; status: string }[]   // the member's joined teams (any status)
  publicTeams:  { name: string }[]                    // ALL status='public' teams, name-ascending
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add status to the member JOIN and add the all-public-teams query</name>
  <files>src/app/my/page.tsx</files>
  <read_first>
    - src/app/my/page.tsx (current member query lines 14-24, flatten lines 26-30, props block lines 41-47, auth block lines 8-13)
    - src/app/admin/teams/page.tsx (public-teams query analog, lines 6-9)
    - src/lib/members.ts (getMembers status JOIN + flatten, lines 8-26)
    - src/lib/types.ts (Member.teams shape, line 13)
    - .planning/phases/23-my-team-join/23-PATTERNS.md (§ page.tsx, lines 19-65 — exact extend/add/props steps)
    - .planning/phases/23-my-team-join/23-CONTEXT.md (D-01 line 27, D-02 line 28, D-07 line 45, Integration Points lines 90-95)
  </read_first>
  <action>
    Modify `src/app/my/page.tsx`. Keep the auth + factory block (lines 8-13) and the `!member` → `<LinkMemberForm />` branch VERBATIM.

    Steps:
    1. Member JOIN: change the nested select from `teams (name)` to `teams (name, status)`. Keep the scoping `.eq('user_id', user.id).maybeSingle()` exactly — this is the per-user authorization boundary (T-23-05). Do NOT widen the query to other users.
    2. Flatten: replace the `teamNames: string[]` mapping (lines 26-30) with a `currentTeams` array of `{ name: string; status: string }`. Map `mt.teams` from `member.member_teams`, and filter to objects that have a string `name` (mirror the `members.ts` getMembers filter at lines 20-26). When `member` is null, default to `[]`.
    3. ADD the public-teams query using the existing `admin` client: `admin.from('teams').select('id, name').eq('status', 'public').order('name')`. Name the result `publicTeams` (default to `[]` when null). This selects only `id, name` — do NOT select `status` here, and do NOT include private/hidden teams (T-23-06). Map to `{ name: string }[]` for the prop (drop `id`; the form keys on team name per the `name="teams"` value contract from Plan 01).
    4. Props: change the `<MyProfileForm member={...} />` block to pass `currentTeams` and `publicTeams` instead of `team_names`. Keep `name` and `publicationId` as-is. Final member prop shape: `{ name, publicationId, currentTeams, publicTeams }`.

    Do NOT touch `LinkMemberForm` rendering, the page chrome (lines 32-37), or the `LogoutButton`. The form's TS prop type is updated in Plan 03; in this plan the prop object simply carries the new fields (this file does not import the Member type).
  </action>
  <verify>
    <automated>cd /Users/kugyu10/work/keep-substack && grep -q "teams (name, status)" src/app/my/page.tsx && grep -q "\.eq('status', 'public')" src/app/my/page.tsx && grep -q "publicTeams" src/app/my/page.tsx && grep -q "currentTeams" src/app/my/page.tsx && grep -q "\.eq('user_id', user.id)" src/app/my/page.tsx && ! grep -q "team_names\|teamNames" src/app/my/page.tsx && echo PASS || { echo FAIL; false; }</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/my/page.tsx` member JOIN selects `teams (name, status)` (not `teams (name)`).
    - A second query reads `teams` filtered with `.eq('status', 'public')` and `.order('name')`, selecting only `id, name`.
    - The member query remains scoped with `.eq('user_id', user.id)` and `.maybeSingle()`.
    - `<MyProfileForm>` receives `currentTeams` (array of `{ name, status }`) and `publicTeams` (array of `{ name }`); no `team_names` / `teamNames` identifier remains anywhere in the file.
    - The `!member` branch still renders `<LinkMemberForm />`; the auth block still redirects to `/` when `!user`.
  </acceptance_criteria>
  <done>
    page.tsx fetches the member's teams with status, fetches all public teams via a `.eq('status','public')` query, and passes `currentTeams` + `publicTeams` props to MyProfileForm. No `team_names` reference remains. Per-user scoping and the unauth redirect are intact.
  </done>
</task>

<task type="auto">
  <name>Task 2: Typecheck the reshaped RSC against the new prop contract</name>
  <files>src/app/my/page.tsx</files>
  <read_first>
    - src/app/my/page.tsx (after Task 1)
    - src/app/my/MyProfileForm.tsx (current prop type — will still be `team_names` until Plan 03; this task documents the expected tsc state)
  </read_first>
  <action>
    Run a TypeScript check scoped to confirm `page.tsx` itself is internally well-typed after the reshape. Because `MyProfileForm`'s prop type is not updated until Plan 03 (wave 2), a type mismatch on the `<MyProfileForm member={...}>` call site is EXPECTED at this point and is resolved by Plan 03. This task's gate is limited to: no NEW type errors originating inside `page.tsx`'s own logic (the member flatten, the publicTeams map, the query builders). Do NOT attempt to fix the form's prop type here — that is Plan 03's responsibility and would create a file-ownership conflict.

    Capture `npx tsc --noEmit` output and confirm the only `my/page.tsx`-related error (if any) is the `MyProfileForm` prop-shape mismatch, which Plan 03 closes.
  </action>
  <verify>
    <automated>cd /Users/kugyu10/work/keep-substack && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -i "my/page.tsx" || echo "no-page-tsx-errors"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` shows no type errors inside `page.tsx`'s own data logic (flatten, publicTeams map, query chains).
    - If any `my/page.tsx` error appears, it is solely the `<MyProfileForm member={...}>` prop-shape mismatch (form prop type lags until Plan 03) — recorded in the SUMMARY as expected and closed by Plan 03.
  </acceptance_criteria>
  <done>
    tsc confirms page.tsx's own data-fetch/flatten logic is well-typed; the only acceptable outstanding error is the cross-file MyProfileForm prop mismatch, which Plan 03 resolves.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser session → /my RSC | Request carries an auth cookie; the RSC must resolve the user and read only that user's membership |
| RSC → Supabase (service role) | Admin client bypasses RLS; the `.eq('user_id', user.id)` scope is the sole authorization gate on the member read |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-05 | Information Disclosure / IDOR | member JOIN query | mitigate | Member query stays scoped with `.eq('user_id', user.id).maybeSingle()`; no member identifier comes from request params. Unauth → `redirect('/')` before any read. One user can never read another's membership. |
| T-23-06 | Information Disclosure | public-teams query | mitigate | The join-able list query filters `.eq('status', 'public')`, so private/hidden team names never enter the rendered checkbox list (D-01). Only `id, name` of public teams are exposed. |
| T-23-07 | Tampering | data reshape only | accept | This plan performs no writes; all mutation stays in Plan 01's Server Action behind its own authorization gate. No new write surface added. |
| T-23-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed; the build toolchain (tsc) is already present. No supply-chain surface added. |
</threat_model>

<verification>
- `grep "teams (name, status)" src/app/my/page.tsx` → present (status added to JOIN).
- `grep "\.eq('status', 'public')" src/app/my/page.tsx` → present (public-teams query, SELF-01).
- `grep "publicTeams" src/app/my/page.tsx` and `grep "currentTeams" src/app/my/page.tsx` → present (new prop contract).
- `grep "team_names\|teamNames" src/app/my/page.tsx` → absent (old prop removed).
- `grep "\.eq('user_id', user.id)" src/app/my/page.tsx` → present (per-user scope, T-23-05).
- `npx tsc --noEmit` → no errors inside page.tsx's own logic (form-prop mismatch deferred to Plan 03).
</verification>

<success_criteria>
- /my fetches all `status='public'` teams ordered by name and the member's joined teams with status (SELF-01, SELF-03, D-07).
- private/hidden teams the member does not belong to never enter the public-teams list (D-01, T-23-06).
- Member read stays scoped to the authenticated user; unauth redirects to / (T-23-05).
- `currentTeams` + `publicTeams` props are established for Plan 03; no `team_names` reference remains.
</success_criteria>

<output>
Create `.planning/phases/23-my-team-join/23-02-SUMMARY.md` when done. Record: the exact `currentTeams` / `publicTeams` prop shapes passed to MyProfileForm, the public-teams query filter, and note that the MyProfileForm prop-type mismatch is intentionally left for Plan 03.
</output>
