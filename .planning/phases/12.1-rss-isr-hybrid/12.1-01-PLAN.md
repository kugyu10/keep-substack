---
phase: 12.1-rss-isr-hybrid
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/fetchFeed.ts
  - src/app/page.tsx
  - src/app/member/[substackId]/page.tsx
autonomous: false
requirements:
  - ISR-01
  - ISR-02
  - ISR-03

must_haves:
  truths:
    - "トップページが5分ごとに自動再生成される（revalidate=300）"
    - "メンバー詳細ページが5分ごとに自動再生成される（revalidate=300）"
    - "fetchAllFeedsCached がライブ RSS と KV 過去記事をマージして返す"
    - "ライブ RSS 失敗時は KV のみの結果でフォールバックする"
    - "ページ表示時に KV へ書き込まない（Cron 責務を維持）"
    - "fetchAllFeedsCached のシグネチャが変わらず呼び出し元に修正がない"
  artifacts:
    - path: "src/lib/fetchFeed.ts"
      provides: "ハイブリッドフェッチ実装（fetchAllFeedsCached）"
      contains: "Promise.allSettled"
    - path: "src/app/page.tsx"
      provides: "ISR revalidate 設定"
      contains: "export const revalidate = 300"
    - path: "src/app/member/[substackId]/page.tsx"
      provides: "ISR revalidate 設定"
      contains: "export const revalidate = 300"
  key_links:
    - from: "src/lib/fetchFeed.ts fetchAllFeedsCached"
      to: "fetchWithRetry + getArticles"
      via: "Promise.allSettled 並列実行"
      pattern: "Promise\\.allSettled"
    - from: "src/app/page.tsx"
      to: "fetchAllFeedsCached"
      via: "revalidate=300 の ISR キャッシュ制御下での呼び出し"
      pattern: "export const revalidate"
---

<objective>
RSS ISR復活 + KV ハイブリッドアーキテクチャを実装する。

Purpose: 現在の KV のみ参照（最大24時間遅延）を改善し、投稿後5分以内にトップページとメンバー詳細ページへ反映させる。

Output:
- src/lib/fetchFeed.ts — fetchAllFeedsCached をハイブリッド実装に書き換え（ライブ RSS + KV マージ）
- src/app/page.tsx — export const revalidate = 300 追加
- src/app/member/[substackId]/page.tsx — export const revalidate = 300 追加
</objective>

<execution_context>
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/kugyu10/work/keep-substack/.planning/PROJECT.md
@/Users/kugyu10/work/keep-substack/.planning/ROADMAP.md
@/Users/kugyu10/work/keep-substack/.planning/STATE.md

<interfaces>
<!-- 実装に必要なインターフェースの抜粋。コードベース探索不要。 -->

From src/lib/types.ts:
```typescript
export type FeedItem = {
  title?: string
  link?: string
  pubDate?: string
  isoDate?: string
  thumbnail?: string
}

export type Member = {
  name: string
  substackId: string
  teamNames: string[]
  addedAt: string
}

export type MemberFeedResult = {
  member: Member
  items: FeedItem[]
  imageUrl?: string
}
```

From src/lib/fetchFeed.ts (変更前の現状):
```typescript
// ローカル型定義
type FeedResult = { items: FeedItem[]; imageUrl?: string }

// 既存実装（書き換え対象）
export async function fetchAllFeedsCached(members: Member[]): Promise<MemberFeedResult[]> {
  const results = await Promise.allSettled(
    members.map((m) => getArticles(m.substackId))
  )
  return members.map((member, i) => {
    const data = results[i].status === 'fulfilled' ? results[i].value : { items: [] }
    return { member, items: data.items, imageUrl: data.imageUrl }
  })
}

// 使用可能な関数（変更不要）
export async function fetchWithRetry(url: string): Promise<FeedResult>
```

From src/lib/kvArticles.ts:
```typescript
// StoredFeed は FeedResult と同じ形状
export async function getArticles(substackId: string): Promise<{ items: FeedItem[]; imageUrl?: string }>
```

From src/app/page.tsx (変更前):
```typescript
// import 群（L1-6）の後、export default async function Home（L12）の前に
// export const revalidate = 300 を追加する
export default async function Home({ searchParams }: Props) { ... }
```

From src/app/member/[substackId]/page.tsx (変更前):
```typescript
// import 群（L1-6）の後、export default async function MemberPage（L8）の前に
// export const revalidate = 300 を追加する
export default async function MemberPage({ params }: ...) { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: fetchAllFeedsCached をハイブリッドフェッチ実装に書き換える（D-02, D-03, D-04）</name>
  <files>src/lib/fetchFeed.ts</files>
  <behavior>
    - ケース1: ライブ RSS 成功 + KV 成功 → 両方をマージ（link で dedupe、ライブ RSS 優先）、isoDate 降順ソートで返す
    - ケース2: ライブ RSS 失敗 + KV 成功 → KV のみのデータを返す（フォールバック）
    - ケース3: ライブ RSS 成功 + KV 失敗 → ライブ RSS のみのデータを返す
    - ケース4: 両方失敗 → { member, items: [], imageUrl: undefined } を返す
    - ケース5: link が undefined の記事が複数あっても、それぞれ重複除去されずすべて含まれる（undefined の Set 問題を回避）
    - ケース6: imageUrl はライブ RSS 優先、なければ KV の値を使う（live.imageUrl ?? kv.imageUrl）
    - ケース7: D-03 — 関数内で saveArticles や redis.set を呼ばない（KV への書き込みなし）
    - ケース8: D-04 — シグネチャ fetchAllFeedsCached(members: Member[]): Promise&lt;MemberFeedResult[]&gt; を維持
  </behavior>
  <action>
src/lib/fetchFeed.ts の fetchAllFeedsCached 関数のみを以下の実装に書き換える。他の関数（fetchWithRetry, toFeedItems, parser 設定等）は変更しない。

実装方針（D-02 準拠）:
1. 外側の Promise.allSettled で各メンバーを並列処理する
2. 各メンバー内で Promise.allSettled を使い fetchWithRetry（ライブ RSS）と getArticles（KV）を並列実行する（内側を allSettled にすることで RSS/KV いずれかが失敗しても個別ハンドリング可能、D-02 フォールバック要件を正確に実装）
3. liveResult.status === 'fulfilled' でなければ live = { items: [], imageUrl: undefined }
4. kvResult.status === 'fulfilled' でなければ kv = { items: [] }
5. imageUrl = live.imageUrl ?? kv.imageUrl
6. link で dedupe: `if (item.link && seenLinks.has(item.link)) continue; if (item.link) seenLinks.add(item.link)` — link が undefined の記事は dedupe されずすべて merged に含める（`Set.has(undefined)` が true になる問題を回避）
7. isoDate 降順ソート（isoDate なし記事は末尾へ）
8. D-03: saveArticles / redis.set 等の KV 書き込みは一切行わない

注意: link が undefined の記事は dedupe しない。`if (item.link && seenLinks.has(item.link)) continue; if (item.link) seenLinks.add(item.link)` のパターンで、undefined link の記事はそのまま merged.push する（`Set.has(undefined)` が true になる問題を回避）。
  </action>
  <verify>
    <automated>cd /Users/kugyu10/work/keep-substack && npx tsc --noEmit 2>&1 | head -20 && echo "--- grep checks ---" && grep -c "Promise.allSettled" src/lib/fetchFeed.ts && grep -c "saveArticles\|redis\.set" src/lib/fetchFeed.ts || true</automated>
  </verify>
  <done>
- TypeScript コンパイルエラーがない
- grep -c "Promise.allSettled" src/lib/fetchFeed.ts の結果が 2 以上（外側・内側の両方）
- grep -c "saveArticles\|redis\.set" src/lib/fetchFeed.ts の結果が 0（KV 書き込みなし、D-03）
- fetchAllFeedsCached のシグネチャが (members: Member[]): Promise&lt;MemberFeedResult[]&gt; のまま（D-04）
  </done>
</task>

<task type="auto">
  <name>Task 2: page.tsx と member/[substackId]/page.tsx に export const revalidate = 300 を追加する（D-01）</name>
  <files>src/app/page.tsx, src/app/member/[substackId]/page.tsx</files>
  <action>
以下の2ファイルに対してそれぞれ `export const revalidate = 300` を追加する。

配置場所: import 群の直後、export default async function の前のトップレベル。
Next.js はルートセグメント設定を静的解析するため、必ずファイルのトップレベルに配置すること（関数内や export default の後は無効）。

src/app/page.tsx:
- 現在 L6 が `import topLogo from '@/data/top_logo.png'`、L8 が `type Props = ...`
- L6 と `type Props` 行の間に空行を挟んで `export const revalidate = 300` を追加する

src/app/member/[substackId]/page.tsx:
- 現在 L6 が `import { getMembers } from '@/lib/kvMembers'`、L8 が `export default async function MemberPage`
- L6 の後に空行を挟んで `export const revalidate = 300` を追加する
  </action>
  <verify>
    <automated>cd /Users/kugyu10/work/keep-substack && grep -v '^//' src/app/page.tsx | grep -c "export const revalidate = 300" && grep -v '^//' src/app/member/\[substackId\]/page.tsx | grep -c "export const revalidate = 300"</automated>
  </verify>
  <done>
- src/app/page.tsx に `export const revalidate = 300` が存在する（コメントアウトでない）
- src/app/member/[substackId]/page.tsx に `export const revalidate = 300` が存在する（コメントアウトでない）
- TypeScript コンパイルエラーがない（npx tsc --noEmit）
- npm run build が正常終了する
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
- fetchAllFeedsCached がライブ RSS + KV ハイブリッドフェッチを行うよう書き換えた
- page.tsx と member/[substackId]/page.tsx に revalidate=300 を追加した
  </what-built>
  <how-to-verify>

**事前確認（CLI）**

1. TypeScript チェック:
   ```
   cd /Users/kugyu10/work/keep-substack && npx tsc --noEmit
   ```
   エラーがないこと。

2. ビルド確認:
   ```
   cd /Users/kugyu10/work/keep-substack && npm run build
   ```
   正常終了すること。

3. revalidate が両ページに設定されていること:
   ```
   grep -n "export const revalidate" src/app/page.tsx src/app/member/\[substackId\]/page.tsx
   ```
   両ファイルとも `export const revalidate = 300` が表示されること。

4. HIDDEN_TEAM / HIDDEN 関連のシンボルが fetchFeed.ts に混入していないこと（D-03 境界確認）:
   ```
   grep -c "HIDDEN\|saveArticles\|redis\.set" src/lib/fetchFeed.ts
   ```
   結果が 0 であること。

5. fetchFeed.ts のハイブリッド実装確認:
   ```
   grep -n "Promise.allSettled\|fetchWithRetry\|getArticles\|seenLinks\|isoDate" src/lib/fetchFeed.ts
   ```
   - Promise.allSettled が2回（外側・内側）現れること
   - fetchWithRetry が使われていること
   - seenLinks による dedupe ロジックがあること

**ブラウザ確認**

6. ローカルサーバーを起動:
   ```
   cd /Users/kugyu10/work/keep-substack && npm run dev
   ```

7. http://localhost:3000 にアクセスし、トップページが表示されること（メンバーのヒートマップが見える）。

8. メンバー詳細ページ（例: http://localhost:3000/member/{substackId}）にアクセスし、カレンダーが表示されること。

**期待される動作:**
- ページが正常表示される
- コンソールエラーがない
- ビルドログに `export const revalidate = 300` 関連の警告がない
  </how-to-verify>
  <resume-signal>問題なければ "approved" と入力。問題があれば内容を記述してください。</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| サーバー → 外部 RSS | fetchWithRetry が外部 Substack RSS エンドポイントを呼び出す。レスポンスは信頼できない外部データ |
| サーバー → Upstash Redis | getArticles が KV からデータを読み取る。内部サービスだが認証情報は環境変数で管理 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12.1-01 | Denial of Service | fetchWithRetry | mitigate | rss-parser の timeout: 5000 設定済み（既存）。Promise.allSettled で1件失敗が全体に波及しない |
| T-12.1-02 | Information Disclosure | fetchAllFeedsCached | accept | ライブ RSS は公開フィードのみ取得。非公開情報の露出リスクなし |
| T-12.1-03 | Tampering | 外部 RSS レスポンス | accept | rss-parser による構造化パース済み。XSS リスクは既存の thumbnail 抽出ロジックで対処済み（Phase 4 確立） |
| T-12.1-04 | Elevation of Privilege | KV 書き込みなし（D-03） | accept | このフェーズでは読み取りのみ。書き込みは Cron エンドポイント（CRON_SECRET 認証済み）が担う |
</threat_model>

<verification>
フェーズ全体の完了確認:

```bash
# 1. TypeScript エラーなし
cd /Users/kugyu10/work/keep-substack && npx tsc --noEmit

# 2. ビルド成功
npm run build

# 3. revalidate が両ページに設定済み
grep -n "export const revalidate = 300" src/app/page.tsx src/app/member/\[substackId\]/page.tsx

# 4. fetchFeed.ts に KV 書き込みコードが混入していない（D-03）
grep -c "saveArticles\|redis\.set\|HIDDEN" src/lib/fetchFeed.ts
# → 0 であること

# 5. fetchAllFeedsCached がハイブリッド実装になっている
grep -n "fetchWithRetry\|getArticles\|Promise.allSettled\|seenLinks" src/lib/fetchFeed.ts
```
</verification>

<success_criteria>
以下がすべて真であること:

1. `npx tsc --noEmit` がエラーなしで終了する
2. `npm run build` が正常終了する
3. `grep -c "export const revalidate = 300" src/app/page.tsx` の結果が 1
4. `grep -c "export const revalidate = 300" src/app/member/\[substackId\]/page.tsx` の結果が 1
5. `grep -c "Promise.allSettled" src/lib/fetchFeed.ts` の結果が 2 以上（外側・内側）
6. `grep -c "saveArticles\|redis\.set" src/lib/fetchFeed.ts` の結果が 0（D-03: KV 書き込みなし）
7. `grep -c "fetchWithRetry" src/lib/fetchFeed.ts` の結果が 1 以上（ライブ RSS フェッチ使用）
8. ブラウザでトップページとメンバー詳細ページが正常表示される
</success_criteria>

<output>
完了後、以下のファイルを作成する:

`.planning/phases/12.1-rss-isr-hybrid/12.1-01-SUMMARY.md`

SUMMARY には以下を記載:
- 変更ファイル一覧と変更内容
- D-01〜D-04 の実装確認
- ハイブリッドフェッチの動作（ライブ RSS 優先 + KV フォールバック）
- ISR revalidate=300 の設定完了
- ビルド確認結果
</output>
