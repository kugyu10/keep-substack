---
phase: 29-commit-goal-view
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/types.ts
  - src/lib/members.ts
  - src/lib/commitUtils.ts
  - src/app/weekly-stamp/page.tsx
  - src/app/__tests__/page.test.tsx
  - src/app/weekly-stamp/__tests__/page.test.tsx
autonomous: true
requirements:
  - VIEW-01
  - VIEW-02
  - VIEW-03
  - VIEW-04
  - VIEW-05
  - VIEW-06

must_haves:
  truths:
    - "/weekly-stamp ルートが存在し、旧ヒートマップが引き続き表示される"
    - "CommitSlot 型が types.ts に定義され、member_commit_slots の行を型安全に表現できる"
    - "Member 型に id フィールドが追加され、getMembers() が UUID を返す"
    - "commitUtils.ts にスロットマッチングと並び順のユーティリティが存在する"
    - "既存 page.test.tsx が CommitGoalView 参照に更新済みで vitest が green"
    - "weekly-stamp/page.test.tsx が WeeklyHeatmapGrid を検証し vitest が green"
    - "member_commit_slots の anon SELECT policy が本番 DB に存在する（確認済み）"
  artifacts:
    - path: "src/app/weekly-stamp/page.tsx"
      provides: "/weekly-stamp ルート — 旧トップページのコピー"
    - path: "src/lib/types.ts"
      provides: "CommitSlot 型 + Member.id フィールド"
    - path: "src/lib/members.ts"
      provides: "getMembers() が id を返す"
    - path: "src/lib/commitUtils.ts"
      provides: "getWeekDates, matchArticleToSlot, sortMembersForCommitView"
    - path: "src/app/__tests__/page.test.tsx"
      provides: "CommitGoalView 参照に更新された既存テスト"
    - path: "src/app/weekly-stamp/__tests__/page.test.tsx"
      provides: "/weekly-stamp ページのスモークテスト"
  key_links:
    - from: "src/app/weekly-stamp/page.tsx"
      to: "src/components/WeeklyHeatmapGrid"
      via: "import"
      pattern: "WeeklyHeatmapGrid"
    - from: "src/lib/members.ts"
      to: "supabase members table"
      via: "SELECT id, name, publication_id"
      pattern: "id,"
    - from: "src/lib/commitUtils.ts"
      to: "src/lib/calendarUtils.ts"
      via: "isoToJSTDateKey import"
      pattern: "isoToJSTDateKey"
---

<objective>
Phase 29 Plan 01: 基盤レイヤー — 型・データ層・ルート移行・Wave 0 テスト

現トップページ (`src/app/page.tsx`) を `/weekly-stamp` にコピーして VIEW-01 を満たす。CommitGoalView が必要とするデータ契約（CommitSlot 型、Member.id、commitUtils ユーティリティ）を定義し、Plan 02 のコンポーネント実装を可能にする。既存 `page.test.tsx` を CommitGoalView 参照に更新し、`weekly-stamp/page.test.tsx` を新規作成して Wave 0 ギャップを解消する。

Purpose: Plan 02 が安全に page.tsx を差し替えられる状態を作る。型契約と weekly-stamp ルートを先に確定することで Plan 02 はコンポーネント実装に集中できる。

Output:
- src/app/weekly-stamp/page.tsx（旧トップの完全移行）
- src/lib/types.ts（CommitSlot 型追加、Member.id 追加）
- src/lib/members.ts（id フィールド SELECT 追加）
- src/lib/commitUtils.ts（スロットマッチング・並び順ユーティリティ）
- src/app/__tests__/page.test.tsx（CommitGoalView 参照に更新）
- src/app/weekly-stamp/__tests__/page.test.tsx（新規）
</objective>

<execution_context>
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/29-commit-goal-view/29-CONTEXT.md
@.planning/phases/29-commit-goal-view/29-RESEARCH.md
@.planning/phases/29-commit-goal-view/29-PATTERNS.md
@.planning/phases/29-commit-goal-view/29-UI-SPEC.md
@.planning/phases/29-commit-goal-view/29-VALIDATION.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/lib/types.ts (current — before modification):
```typescript
export type FeedItem = {
  title?: string
  link?: string
  pubDate?: string
  isoDate?: string
  thumbnail?: string
}

export type Member = {
  name: string
  publicationId: string
  teams: { name: string; status: string }[]
  addedAt: string
  substackHandle?: string
  // MUST ADD: id?: string  — UUID from members.id (Pitfall 1 fix)
}

export type MemberFeedResult = {
  member: Member
  items: FeedItem[]
  imageUrl?: string
}
```

From src/lib/members.ts (current SELECT — before modification):
```typescript
// Current SELECT does NOT include 'id'. Must add.
const { data, error } = await supabase
  .from('members')
  .select(`
    name,
    publication_id,
    added_at,
    substack_handle,
    member_teams ( teams (name, status) )
  `)
// Current map() does NOT include id. Must add: id: m.id
```

From src/lib/calendarUtils.ts:
```typescript
export function isoToJSTDateKey(isoDate: string): string | null
// Returns 'YYYY-MM-DD' string adjusted for JST (UTC+9). Use this for all date keys.
```

From src/lib/heatmapUtils.ts:
```typescript
// JST pattern to replicate in commitUtils.ts:
const nowJST = new Date(Date.now() + 9 * 60 * 60 * 1000)
```

From supabase/migrations/20260602000001_add_member_commit_slots.sql:
```sql
-- anon SELECT policy ALREADY EXISTS — do NOT create a duplicate migration
CREATE POLICY "public select member_commit_slots"
  ON member_commit_slots FOR SELECT USING (true);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: [BLOCKING] DB policy verification + types.ts + members.ts拡張</name>
  <files>src/lib/types.ts, src/lib/members.ts</files>
  <read_first>
    - src/lib/types.ts (現在の型定義を把握してから追加)
    - src/lib/members.ts (現在の SELECT と map() を把握してから変更)
    - supabase/migrations/20260602000001_add_member_commit_slots.sql (policy 存在確認)
    - supabase/schema.sql (member_commit_slots テーブル定義とポリシーの全文確認)
  </read_first>
  <behavior>
    - CommitSlot 型: member_id (string/UUID), day_of_week (number 1-7), hour (number 0-23)
    - Member 型に id?: string を追加（optional — 既存テスト fixture が id なしでも壊れないため）
    - getMembers() が id フィールドを返す
    - schema.sql の "public select member_commit_slots" policy が USING(true) で存在することを確認してからコードを変更
  </behavior>
  <action>
    Step 1 (BLOCKING verification): Read supabase/schema.sql and confirm that the policy "public select member_commit_slots" ON member_commit_slots FOR SELECT USING (true) exists. Also confirm in supabase/migrations/20260602000001_add_member_commit_slots.sql. If the policy is present in both files, proceed — no new migration is needed. If absent from either, flag as blocker and stop.

    Step 2 (types.ts): Add CommitSlot type after the MemberFeedResult declaration:
    - CommitSlot has fields: member_id (string), day_of_week (number), hour (number)
    - Add id?: string to the Member type (optional field, after substackHandle)

    Step 3 (members.ts): In the getMembers() function, add 'id' to the .select() template literal as the first field in the members query. In the .map() transform, add id: m.id as the first field of the returned object.

    Anti-pattern: Do NOT call getUser() or create a Supabase server client — admin client is already used in getMembers() and should remain.
    Anti-pattern: Do NOT make Member.id required — existing member() fixtures in tests omit it and must continue to work.
  </action>
  <verify>
    <automated>cd /Users/kugyu10/work/keep-substack && npx vitest run src/lib/__tests__/members.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/types.ts contains: export type CommitSlot = {
    - src/lib/types.ts contains: member_id: string
    - src/lib/types.ts contains: day_of_week: number
    - src/lib/types.ts contains: hour: number
    - src/lib/types.ts contains: id?: string (within the Member type)
    - src/lib/members.ts SELECT template literal contains: id, (on its own line or as first item)
    - src/lib/members.ts map() contains: id: m.id
    - npx vitest run src/lib/__tests__/members.test.ts exits 0 (existing member tests still pass)
  </acceptance_criteria>
  <done>CommitSlot 型が export されており、getMembers() が id を返す。既存 members.test.ts が green。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: commitUtils.ts — スロットマッチング・週日付計算・並び順</name>
  <files>src/lib/commitUtils.ts, src/lib/__tests__/commitUtils.test.ts</files>
  <read_first>
    - src/lib/heatmapUtils.ts (JST パターン、sortByWeeklyCount 構造を参照)
    - src/lib/calendarUtils.ts (isoToJSTDateKey の使い方を確認)
    - src/lib/types.ts (Task 1 完了後の CommitSlot 型と MemberFeedResult 型)
    - .planning/phases/29-commit-goal-view/29-PATTERNS.md §commitUtils.ts パターン
    - .planning/phases/29-commit-goal-view/29-RESEARCH.md §Pattern 4 および §Pattern 5
  </read_first>
  <behavior>
    - getWeekDates(mondayOffsetWeeks: number): string[] — JST での月曜起点週の日付配列 ['YYYY-MM-DD' x7] を返す。index 0 = 月曜、index 6 = 日曜。mondayOffsetWeeks=0 が現在週、=-1 が先週、=-2 が先々週
    - matchArticleToSlot(slot: CommitSlot, weekDates: string[], articleDateMap: Map<string, FeedItem[]>): FeedItem | undefined — slot.day_of_week (1=月〜7=日) を weekDates[dayOfWeek - 1] にマッピングして記事を探す。同じ日付キーに複数記事があれば最初の1件を返す
    - sortMembersForCommitView(results: MemberFeedResult[], slots: CommitSlot[]): MemberFeedResult[] — D-10 仮実装: 今週投稿数降順 → addedAt昇順（sortByWeeklyCount パターンと同様、ストリーク計算は Phase 30 に委譲）
    - テスト (src/lib/__tests__/commitUtils.test.ts): getWeekDates が正しい月曜起点7日配列を返す、matchArticleToSlot が投稿済み/未投稿を正しく判定する、sortMembersForCommitView が投稿数降順に並ぶことを確認
  </behavior>
  <action>
    Create src/lib/__tests__/commitUtils.test.ts first (RED):
    - Write failing tests for getWeekDates: verify that getWeekDates(0) returns a 7-element array where all entries are 'YYYY-MM-DD' format strings, and that getWeekDates(-1) returns dates 7 days earlier than getWeekDates(0)[0].
    - Write failing test for matchArticleToSlot: given a slot with day_of_week=1 (Monday) and a weekDates array, and an articleDateMap containing an article on that Monday's date key, it returns that FeedItem. When no article matches, returns undefined.
    - Write failing test for sortMembersForCommitView: member with 2 articles this week sorts before member with 0 articles.

    Then create src/lib/commitUtils.ts (GREEN):
    - Import isoToJSTDateKey from './calendarUtils' and type CommitSlot, FeedItem, MemberFeedResult from './types'
    - getWeekDates: compute JST now using Date.now() + 9 * 60 * 60 * 1000. Find the most recent Monday using ISO day-of-week (0=Sun → treat as 7, 1=Mon…6=Sat). Apply mondayOffsetWeeks * 7 * 24 * 60 * 60 * 1000 offset. Return 7-element string array 'YYYY-MM-DD' using UTC year/month/day on the JST-adjusted dates.
    - matchArticleToSlot: index into weekDates with (slot.day_of_week - 1). Look up articleDateMap.get(dateKey) and return items[0] if found.
    - sortMembersForCommitView: build current week's date set via getWeekDates(0), count articles per member using isoToJSTDateKey on each item's isoDate, sort descending by count then ascending by member.addedAt (per D-10 simplified).
    - NEVER use dynamic Tailwind class strings in this file — it is a pure utility module (no JSX).
  </action>
  <verify>
    <automated>cd /Users/kugyu10/work/keep-substack && npx vitest run src/lib/__tests__/commitUtils.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/commitUtils.ts exists
    - src/lib/commitUtils.ts exports: getWeekDates, matchArticleToSlot, sortMembersForCommitView
    - src/lib/__tests__/commitUtils.test.ts exists and contains describe('getWeekDates'
    - src/lib/__tests__/commitUtils.test.ts contains describe('matchArticleToSlot'
    - npx vitest run src/lib/__tests__/commitUtils.test.ts exits 0
  </acceptance_criteria>
  <done>commitUtils.ts の3関数が実装済みでテストが green。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: /weekly-stamp ルート作成 + Wave 0 テスト更新</name>
  <files>
    src/app/weekly-stamp/page.tsx,
    src/app/weekly-stamp/__tests__/page.test.tsx,
    src/app/__tests__/page.test.tsx
  </files>
  <read_first>
    - src/app/page.tsx (コピー元の完全な現在の内容)
    - src/app/__tests__/page.test.tsx (更新前の完全な現在の内容)
    - .planning/phases/29-commit-goal-view/29-PATTERNS.md §weekly-stamp/page.tsx パターン および §page.test.tsx 更新パターン
    - .planning/phases/29-commit-goal-view/29-RESEARCH.md §Pitfall 4 および §Pitfall 5
  </read_first>
  <behavior>
    weekly-stamp/page.tsx:
    - WeeklyHeatmapGrid を参照する (WeeklyHeatmapGrid テストが green のまま)
    - href が /weekly-stamp ベース (例: href="/weekly-stamp", href={`/weekly-stamp?team=...`})
    - revalidate = 300
    - All リンクと各チームタブが /weekly-stamp ベースの href を持つ

    weekly-stamp/__tests__/page.test.tsx (new):
    - WeeklyHeatmapGrid を findByType で検証
    - All リンクの href が /weekly-stamp を含む (not '/')

    src/app/__tests__/page.test.tsx (update):
    - WeeklyHeatmapGrid → CommitGoalView の mock に切り替え
    - createSupabaseAdminClient の mock を追加
    - findByType のターゲットを CommitGoalView に変更
    - existing test descriptions and structure preserved; only component references change
  </behavior>
  <action>
    Step 1 (weekly-stamp/page.tsx): Create directory src/app/weekly-stamp/ and file page.tsx. Copy the full content of src/app/page.tsx verbatim, then make exactly two href patches:
    - Change href="/" → href="/weekly-stamp" (the "All" tab link)
    - Change href={`/?team=${encodeURIComponent(t)}`} → href={`/weekly-stamp?team=${encodeURIComponent(t)}`} (individual team tab links)
    Keep all other content — imports, revalidate, WeeklyHeatmapGrid reference, PrBanner, filterring logic — identical.

    Step 2 (weekly-stamp/__tests__/page.test.tsx): Create directory src/app/weekly-stamp/__tests__/ and new test file. Copy the RSC props-inspection helpers (isElement, flattenChildren, findByType, collectAnchorLabels) verbatim from src/app/__tests__/page.test.tsx. Use the same mockGetMembers + mockFetchAllFeedsCached hoisted mock pattern. Mock @/components/WeeklyHeatmapGrid and @/components/PrBanner. Import Home from '../page' (weekly-stamp/page.tsx). Add two tests:
    (a) 'renders WeeklyHeatmapGrid with member results' — findByType(el, WeeklyHeatmapGrid) is not null.
    (b) 'All link href points to /weekly-stamp not /' — collect all anchor elements via findByType walking and verify the "All" anchor has href="/weekly-stamp" (not href="/").

    Step 3 (page.test.tsx update): Update src/app/__tests__/page.test.tsx to prepare for the new page.tsx (CommitGoalView). In the vi.mock blocks: replace the WeeklyHeatmapGrid mock with a CommitGoalView mock. Add a mock for @/lib/supabase/admin that returns createSupabaseAdminClient as a vi.fn() returning an object with from() → select() → returning { data: [], error: null }. Update the import to import CommitGoalView from '@/components/CommitGoalView'. Update all findByType(el, WeeklyHeatmapGrid) calls to findByType(el, CommitGoalView). Keep all existing test descriptions (TEAM-03, TEAM-04) but update the grid variable prop assertion: instead of `{ results: MemberFeedResult[] }`, use `{ results: MemberFeedResult[]; slots: CommitSlot[] }` from '@/lib/types'. Do NOT delete any existing test cases — only update component references.

    Anti-pattern: Do NOT add 'use client' to weekly-stamp/page.tsx — it is a Server Component.
    Anti-pattern: Do NOT change the filtering logic in weekly-stamp/page.tsx — it must remain identical to the original page.tsx filtering.
  </action>
  <verify>
    <automated>cd /Users/kugyu10/work/keep-substack && npx vitest run src/app/__tests__/page.test.tsx src/app/weekly-stamp/__tests__/page.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - src/app/weekly-stamp/page.tsx exists
    - src/app/weekly-stamp/page.tsx contains: href="/weekly-stamp"
    - src/app/weekly-stamp/page.tsx contains: href={`/weekly-stamp?team=
    - src/app/weekly-stamp/page.tsx does NOT contain: href="/"  (the root-only link)
    - src/app/weekly-stamp/page.tsx contains: WeeklyHeatmapGrid
    - src/app/weekly-stamp/__tests__/page.test.tsx exists
    - src/app/weekly-stamp/__tests__/page.test.tsx contains: findByType(el, WeeklyHeatmapGrid)
    - src/app/__tests__/page.test.tsx contains: CommitGoalView (mock reference)
    - src/app/__tests__/page.test.tsx contains: createSupabaseAdminClient (mock)
    - src/app/__tests__/page.test.tsx does NOT contain: vi.mock('@/components/WeeklyHeatmapGrid' (old mock)
    - npx vitest run src/app/__tests__/page.test.tsx exits 0
    - npx vitest run src/app/weekly-stamp/__tests__/page.test.tsx exits 0
  </acceptance_criteria>
  <done>/weekly-stamp ルートが VIEW-01 を満たす。page.test.tsx が CommitGoalView を期待して green。weekly-stamp テストが green。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server Component → Supabase | admin client bypasses RLS; only used for public data (member_commit_slots is publicly readable by design) |
| ISR cache → Browser | 5min stale data acceptable for commit schedule (low-frequency change) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-01 | Information Disclosure | member_commit_slots SELECT (all rows) | accept | Commit schedules are intentionally public per D-06. RLS policy USING(true) is by design. |
| T-29-02 | Tampering | getMembers() SELECT extended with id field | mitigate | id is UUID (non-guessable). Only used server-side for slot JOIN. Not exposed in rendered HTML. |
| T-29-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this phase. Package legitimacy audit: N/A. |
</threat_model>

<verification>
After all tasks complete, run:
```bash
cd /Users/kugyu10/work/keep-substack && npx vitest run
```
Full suite must be green (no regressions in existing tests, new tests passing).

Check weekly-stamp route exists: src/app/weekly-stamp/page.tsx contains WeeklyHeatmapGrid reference and /weekly-stamp hrefs.

Check types: src/lib/types.ts exports CommitSlot, Member has id? field.

Check commitUtils: src/lib/commitUtils.ts exports getWeekDates, matchArticleToSlot, sortMembersForCommitView.
</verification>

<success_criteria>
1. npx vitest run exits 0 (all existing + new tests green)
2. src/app/weekly-stamp/page.tsx exists with href="/weekly-stamp" and WeeklyHeatmapGrid (VIEW-01 data layer ready)
3. src/lib/types.ts exports CommitSlot type and Member.id optional field
4. src/lib/members.ts getMembers() returns id in mapped objects
5. src/lib/commitUtils.ts exports getWeekDates, matchArticleToSlot, sortMembersForCommitView
6. src/app/__tests__/page.test.tsx references CommitGoalView (not WeeklyHeatmapGrid) and green
7. src/app/weekly-stamp/__tests__/page.test.tsx exists and green
8. No TypeScript errors: npx tsc --noEmit exits 0
</success_criteria>

<output>
Create `.planning/phases/29-commit-goal-view/29-01-SUMMARY.md` when done
</output>
