---
phase: 31
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260605000000_add_substack_handle_unique.sql
  - supabase/schema.sql
autonomous: false
requirements:
  - AUTH-FIX-01

must_haves:
  truths:
    - "members.substack_handle に UNIQUE 制約が live DB に存在する"
    - "同じ substack_handle を持つ member を 2 件 INSERT しようとすると error.code === '23505' が返る"
    - "schema.sql の substack_handle 列定義に UNIQUE が含まれる"
  artifacts:
    - path: "supabase/migrations/20260605000000_add_substack_handle_unique.sql"
      provides: "substack_handle UNIQUE 制約追加 migration"
      contains: "ADD CONSTRAINT IF NOT EXISTS members_substack_handle_key UNIQUE"
    - path: "supabase/schema.sql"
      provides: "durable スキーマ定義（fresh DB 用）"
      contains: "substack_handle TEXT UNIQUE"
  key_links:
    - from: "supabase/migrations/20260605000000_add_substack_handle_unique.sql"
      to: "live DB"
      via: "supabase db push"
      pattern: "members_substack_handle_key"
---

<objective>
`members.substack_handle` カラムに UNIQUE 制約を追加する migration を作成し、live DB に適用する。

Purpose: Phase 31 D-05 の `error.code === '23505'` による unique violation ハンドリングは、DB に実際に制約が存在していなければ機能しない。この migration なしには後続の Plan 02/03 が実装しても 23505 が発生しない。

Output:
- `supabase/migrations/20260605000000_add_substack_handle_unique.sql` — 新規 migration ファイル
- `supabase/schema.sql` — substack_handle 列定義を `TEXT` から `TEXT UNIQUE` に更新
- live DB への migration 適用（`supabase db push`）
</objective>

<execution_context>
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-login-fix/31-CONTEXT.md
@.planning/phases/31-login-fix/31-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: UNIQUE 制約 migration ファイル作成 + schema.sql 更新</name>
  <files>supabase/migrations/20260605000000_add_substack_handle_unique.sql, supabase/schema.sql</files>
  <read_first>
    - supabase/migrations/20260602000000_add_substack_handle.sql — 既存 migration のファイル構造とコメントスタイル
    - supabase/schema.sql — substack_handle 列定義の現在の記述箇所を確認（TEXT のみになっているはず）
  </read_first>
  <action>
    新規ファイル `supabase/migrations/20260605000000_add_substack_handle_unique.sql` を作成する。内容は BEGIN/COMMIT で囲み、コメントに「Phase 31: add UNIQUE constraint to members.substack_handle」を記述し、ALTER 文として `ALTER TABLE members ADD CONSTRAINT IF NOT EXISTS members_substack_handle_key UNIQUE (substack_handle);` を記述する。`IF NOT EXISTS` により live DB に既に制約が存在していても安全に実行できる。

    次に `supabase/schema.sql` を編集し、members テーブルの `substack_handle TEXT` を `substack_handle TEXT UNIQUE` に変更する。既存の migration カラム追加行（`ADD COLUMN IF NOT EXISTS substack_handle TEXT`）は migrations/ にあり schema.sql には別途 durable 定義として記述されているため、schema.sql の該当列定義のみを修正する。
  </action>
  <verify>
    <automated>grep -n "members_substack_handle_key" /Users/kugyu10/work/keep-substack/supabase/migrations/20260605000000_add_substack_handle_unique.sql && grep -n "substack_handle TEXT UNIQUE" /Users/kugyu10/work/keep-substack/supabase/schema.sql</automated>
  </verify>
  <done>
    - `supabase/migrations/20260605000000_add_substack_handle_unique.sql` が存在し、`members_substack_handle_key` 制約名と `UNIQUE (substack_handle)` を含む
    - `supabase/schema.sql` の substack_handle 列が `TEXT UNIQUE` である
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [BLOCKING] supabase db push で live DB に migration 適用</name>
  <what-built>Task 1 で `supabase/migrations/20260605000000_add_substack_handle_unique.sql` を作成した。このファイルを live DB に適用する必要がある。</what-built>
  <how-to-verify>
    1. ターミナルで以下を実行し、migration を live DB に適用する:
       ```
       supabase db push
       ```
    2. プロンプトが表示された場合は `y` を入力して続行する。
    3. 成功後、Supabase ダッシュボード (Table Editor → members テーブル → 列一覧) または以下の SQL を SQL Editor で実行して制約が存在することを確認する:
       ```sql
       SELECT constraint_name FROM information_schema.table_constraints
       WHERE table_name = 'members' AND constraint_type = 'UNIQUE'
         AND constraint_name = 'members_substack_handle_key';
       ```
    4. `members_substack_handle_key` が 1 行返れば成功。
  </how-to-verify>
  <resume-signal>適用成功の場合 "applied" と入力。エラーがあった場合はエラー内容を貼り付けてください。</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration SQL → live DB | supabase db push が service_role で接続して DDL を実行する |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01 | Tampering | migration SQL | mitigate | `ADD CONSTRAINT IF NOT EXISTS` で冪等性を確保。重複実行でも既存制約を上書きしない |
| T-31-02 | Denial of Service | supabase db push | accept | migration は単一 ALTER 文。ロック時間は最小。本番での低リスク |
| T-31-SC | Tampering | npm/supabase CLI | accept | supabase CLI は公式 Supabase ツール。追加インストールなし |
</threat_model>

<verification>
```bash
# migration ファイルの存在と内容確認
grep "members_substack_handle_key" /Users/kugyu10/work/keep-substack/supabase/migrations/20260605000000_add_substack_handle_unique.sql
grep "substack_handle TEXT UNIQUE" /Users/kugyu10/work/keep-substack/supabase/schema.sql
# vitest（このプランはコードを変更しないが既存テストが壊れていないことを確認）
cd /Users/kugyu10/work/keep-substack && npx vitest run
```
</verification>

<success_criteria>
1. `supabase/migrations/20260605000000_add_substack_handle_unique.sql` が存在し `ADD CONSTRAINT IF NOT EXISTS members_substack_handle_key UNIQUE (substack_handle)` を含む
2. `supabase/schema.sql` の members テーブルに `substack_handle TEXT UNIQUE` が含まれる
3. live DB の `members` テーブルに `members_substack_handle_key` UNIQUE 制約が存在する
4. `npx vitest run` が全テスト pass（既存テストが壊れていない）
</success_criteria>

<output>
Create `.planning/phases/31-login-fix/31-01-SUMMARY.md` when done
</output>
