---
phase: 31
slug: login-fix
status: draft
shadcn_initialized: false
preset: none
created: 2026-06-05
---

# Phase 31 — UI Design Contract

> Visual and interaction contract for Phase 31: ログインフロー修正.
> Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (shadcn 未導入) |
| Preset | not applicable |
| Component library | none (Tailwind CSS v4 inline theme) |
| Icon library | none (絵文字 👑🔥 のみ) |
| Font | Lora (Google Fonts, weights 400/500/600/700) |

Source: `src/app/globals.css` — 直接確認

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline padding (`gap-1`, `mt-1`) |
| sm | 8px | Compact element spacing (`px-1`, `gap-2`) |
| md | 16px | Default element spacing (`px-3 py-2` ≒ 12/8, `space-y-4`) |
| lg | 24px | Section padding (`py-8` = 32px は例外, セクション間) |
| xl | 32px | Layout gaps |
| 2xl | 48px | Major section breaks |
| 3xl | 64px | Page-level spacing |

Exceptions:
- タッチターゲット最小高: `min-h-[44px]` (既存チェックボックス行に準拠)
- 成功メッセージ余白: `py-8` (32px) — LoginForm の送信完了表示

Source: `MyProfileForm.tsx`, `AdminMemberList.tsx`, `LoginForm.tsx` — 既存パターンから確認

---

## Typography

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 14px (`text-sm`) | 400 (regular) | 1.5 |
| Label | 14px (`text-sm`) | 600 (semibold) | 1.2 |
| Caption | 12px (`text-xs`) | 400 (regular) | 1.4 |
| Heading (送信完了) | 18px (`text-lg`) | 500 (medium) | 1.2 |

制約: フォントは Lora serif のみ。`font-semibold` (600) と plain (400) の2ウェイト。

Source: `MyProfileForm.tsx`, `LoginForm.tsx`, `AdminMemberList.tsx` — 既存 className から抽出

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `#fafafa` | ページ背景、テーブルヘッダー背景 |
| Secondary (30%) | `#ebebeb`, `#d8d8d8` | ボーダー、入力フィールド枠、テーブルセル枠 |
| Accent (10%) | `#FF6719` (Substack オレンジ) | 保存ボタン、ログインリンク送信ボタン、チェックボックス checked 状態 |
| Destructive | `#EF4444` (`text-red-500` / `text-red-600`) | エラーメッセージ、削除ボタン |

Accent reserved for:
1. プライマリ送信ボタン (`bg-orange-500`) — 「保存する」「ログインリンクを送信」
2. チェックボックスの checked 状態 (`checked:bg-orange-500 checked:border-orange-500`)

補助カラー (既存):
- 読み取り専用フィールド背景: `bg-gray-100` (= `#f3f4f6`)
- 読み取り専用テキスト: `text-gray-400` (= `#9ca3af`)
- キャプション/補足テキスト: `text-gray-500` (= `#6b7280`)
- 編集リンク: `text-blue-600` (= `#2563eb`)

Source: `globals.css`, `MyProfileForm.tsx`, `AdminMemberList.tsx` — 直接確認

---

## Component Inventory

Phase 31 で UI に触れるコンポーネントを列挙する。

### 1. LoginForm.tsx — エラーメッセージ表示 (D-01)

**変更内容:** `pid` または `handle` が空の場合に `sendMagicLinkAction` がエラー文字列を返す。既存の `{state && state !== 'SENT' && <p>}` パターンで表示される。

**表示コントラクト:**
- エラーは `<p role="alert" className="text-sm text-red-600">` で表示 (`MyProfileForm.tsx` パターンに統一)
- 現在 `LoginForm` は `role="alert"` なし → Phase 31 で追加する

### 2. MyProfileForm.tsx — substack_handle 読み取り専用制御 (D-03)

**変更内容:** `substackHandle` が設定済みなら `<input>` を `<p>` に置換し、変更不可メッセージを表示する。

**表示コントラクト (設定済みケース):**
```
<label className="block text-sm font-semibold mb-1">Substack ハンドル</label>
<p className="text-sm text-gray-400 border rounded px-3 py-2 bg-gray-100">
  {substackHandle}
</p>
<p className="text-xs text-gray-500 mt-1">Substack ハンドルは変更できません</p>
```

**表示コントラクト (未設定ケース、例外的フォールバック):**
```
<input id="substack_handle" name="substack_handle" type="text"
       placeholder="@yourhandle"
       className="w-full border rounded px-3 py-2 text-sm" />
<p className="text-xs text-gray-500 mt-1">
  例: @yourname — 入力すると個人ページからプロフィールへのリンクが作成されます
</p>
```

Pattern source: `MyProfileForm.tsx` 既存の `publicationId` 読み取り専用表示パターンを踏襲

### 3. AdminMemberList.tsx — substack_handle / publication_id フィールド追加 (D-04)

**変更内容:** 編集行 (`editingId === m.publicationId`) に `substack_handle` と `publication_id` の入力フィールドを追加する。

**表示コントラクト:**
- テーブルに `substack_handle` 列と `publication_id` 列を追加
- `publication_id` の編集入力には警告テキストを付与: `「変更すると記事データとの紐付けに影響します」`
- `substack_handle` 編集入力: `<input name="substack_handle" className="bg-white border border-[#d8d8d8] rounded px-1 w-full text-sm text-[#363737]" />`
- `publication_id` 変更時の確認ダイアログ: `window.confirm()` を使用 (既存の `deleteMemberAction` パターンと一貫性を保つ)

**確認ダイアログ文言:**
```
"publication_id を変更します。記事データとの紐付けに影響します。続けますか？"
```

### 4. エラー表示パターン (D-05)

`substack_handle` の unique 違反時エラー (`23505`):
- `/my` ページ: `<p role="alert" className="text-sm text-red-600">` で「このハンドルはすでに使用されています」
- admin 編集行: `<p className="text-red-500 text-xs mt-1">{editError}</p>` (既存 `editError` パターン)

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA — ログイン | `ログインリンクを送信` (既存、変更なし) |
| Primary CTA — プロフィール保存 | `保存する` (既存、変更なし) |
| Primary CTA — admin 保存 | `保存` (既存、変更なし) |
| Empty state — admin メンバーなし | `メンバーがいません` (既存、変更なし) |
| ログイン送信中 | `送信中...` (既存) |
| 保存中 | `保存中...` (既存) |
| エラー — 登録リンク不正 (D-01) | `登録リンクが不正です` |
| エラー — handle 重複 (D-05) | `このハンドルはすでに使用されています` |
| エラー — 汎用保存失敗 | `保存に失敗しました。もう一度お試しください` |
| 読み取り専用 — handle 設定済み | `Substack ハンドルは変更できません` |
| 読み取り専用 — handle 設定済み説明 | (キャプション不要 — 「変更できません」で十分) |
| 送信完了メッセージ heading | `メールを送信しました` (既存) |
| 送信完了メッセージ body | `受信トレイを確認してログインリンクをクリックしてください` (既存) |
| 確認ダイアログ — publication_id 変更 (D-04) | `publication_id を変更します。記事データとの紐付けに影響します。続けますか？` |
| 確認ダイアログ — メンバー削除 (既存) | `"{publicationId}" を削除しますか？` (既存、変更なし) |

Source:
- 既存コピー: `LoginForm.tsx`, `MyProfileForm.tsx`, `AdminMemberList.tsx` から直接確認
- 新規コピー (D-01, D-05, D-04): `31-CONTEXT.md` Decisions から確定

---

## Interaction States

Phase 31 で変更・追加される全インタラクション状態を列挙する。

### LoginForm

| State | Trigger | Visual |
|-------|---------|--------|
| Default | ページロード | email input 空、ボタン有効 |
| Pending | submit 後 `isPending=true` | ボタン `disabled opacity-50`、ラベル「送信中...」 |
| Error — invalid link | `pid` or `handle` 欠落 | `<p role="alert" text-red-600>` 登録リンクが不正です |
| Error — other | Supabase エラー等 | `<p role="alert" text-red-600>` {error message} |
| Success | `state === 'SENT'` | フォームを非表示、完了メッセージ表示 |

### MyProfileForm — substack_handle

| State | Trigger | Visual |
|-------|---------|--------|
| 設定済み (locked) | `substackHandle != null` | `<p>` タグ + bg-gray-100 + 「変更できません」キャプション |
| 未設定 (editable) | `substackHandle == null` | `<input>` + プレースホルダー `@yourhandle` |
| Error — 23505 | handleUpdate → 23505 | `<p role="alert" text-red-600>` このハンドルはすでに使用されています |
| Error — generic | handleUpdate → other error | `<p role="alert" text-red-600>` 保存に失敗しました |

### AdminMemberList — 編集行

| State | Trigger | Visual |
|-------|---------|--------|
| 閲覧モード | default | substack_handle, publication_id を text で表示 |
| 編集モード | 「編集」ボタンクリック | substack_handle: `<input>` 表示, publication_id: `<input>` 表示 + 警告テキスト |
| publication_id 変更確認 | `handleUpdate` で publication_id が変更された場合 | `window.confirm()` ダイアログ |
| Error — 23505 | updateMemberAction → 23505 | `editError` パターン (text-red-500 text-xs mt-1) |
| Error — generic | updateMemberAction → other | 同上 |

---

## Accessibility

| Requirement | Implementation |
|-------------|---------------|
| エラーメッセージは `role="alert"` を持つ | LoginForm の `<p>` に `role="alert"` 追加 (既存 MyProfileForm は対応済み) |
| 読み取り専用フィールドは `aria-readonly` or `<p>` 要素 | `<p>` 要素を使用 — スクリーンリーダーが通常テキストとして読む |
| ボタンの `aria-disabled` | 既存の `aria-disabled={isPending}` パターンを踏襲 |
| 確認ダイアログ | `window.confirm()` — ブラウザネイティブ (アクセシブル) |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable — shadcn 未導入 |
| Third-party | none | not applicable |

新規 npm パッケージのインストールなし。Registry vetting gate: 不要。

---

## Phase Scope Note

Phase 31 は主にバックエンドロジック修正フェーズである。フロントエンドの変更は既存コンポーネントへの **最小限の追加・変更** に限定される。

- 新規コンポーネント: **0件**
- 変更コンポーネント: `LoginForm.tsx`, `MyProfileForm.tsx`, `AdminMemberList.tsx`
- スタイルの大幅変更: **なし** — 既存の Tailwind クラスパターンをそのまま踏襲

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
