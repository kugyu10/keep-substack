---
phase: 31
plan: 03
type: execute
wave: 2
depends_on:
  - "31-01"
files_modified:
  - src/app/my/MyProfileForm.tsx
  - src/app/my/page.tsx
  - src/app/admin/AdminMemberList.tsx
  - src/app/admin/actions.ts
  - src/app/admin/__tests__/updateMemberAction.test.ts
  - src/lib/members.ts
autonomous: true
requirements:
  - AUTH-FIX-01

must_haves:
  truths:
    - "/my ページで substack_handle が設定済みの場合、<p> タグで読み取り専用表示され入力フィールドは表示されない"
    - "/my ページで substack_handle が null の場合のみ入力フィールドが表示される"
    - "admin 画面の編集行に substack_handle と publication_id の入力フィールドが表示される"
    - "admin が substack_handle を更新すると members.substack_handle が DB に保存される"
    - "admin が publication_id を変更すると articles.publication_id も連動して更新される"
    - "admin の updateMemberAction で 23505 が返ると「このハンドルはすでに使用されています」が返る"
    - "publication_id 変更時に window.confirm ダイアログが表示される"
    - "updateMemberAction.test.ts が全 pass する"
  artifacts:
    - path: "src/app/my/MyProfileForm.tsx"
      provides: "D-03: substack_handle 条件付きレンダリング（設定済み → 読み取り専用）"
      contains: "substackHandle != null"
    - path: "src/app/my/page.tsx"
      provides: "D-03: substackHandle を DB 値から直接渡す（handle URL パラメータフォールバック廃止）"
      contains: "substackHandle={substackHandle}"
    - path: "src/app/admin/AdminMemberList.tsx"
      provides: "D-04: substack_handle / publication_id 入力フィールド + confirm ダイアログ"
      contains: "new_publication_id"
    - path: "src/app/admin/actions.ts"
      provides: "D-04/D-05: updateMemberAction に substack_handle / publication_id 追加 + 23505 処理"
      contains: "このハンドルはすでに使用されています"
    - path: "src/lib/members.ts"
      provides: "D-04: updateMember に substackHandle / publicationId 対応 + articles 連動 UPDATE"
      contains: "articles"
    - path: "src/app/admin/__tests__/updateMemberAction.test.ts"
      provides: "Wave 0 テスト: admin substack_handle 更新 + 23505 処理"
      contains: "このハンドルはすでに使用されています"
  key_links:
    - from: "src/app/admin/AdminMemberList.tsx"
      to: "src/app/admin/actions.ts"
      via: "handleUpdate → updateMemberAction(publicationId, formData)"
      pattern: "new_publication_id"
    - from: "src/app/admin/actions.ts"
      to: "src/lib/members.ts"
      via: "updateMember(publicationId, { substackHandle, publicationId: new_publication_id })"
      pattern: "substackHandle.*publicationId"
    - from: "src/lib/members.ts"
      to: "articles テーブル"
      via: "admin.from('articles').update({ publication_id: newId }).eq('publication_id', oldId)"
      pattern: "articles.*publication_id"
---

<objective>
フロントエンドと admin バックエンドの 4 箇所を修正する: (1) `MyProfileForm.tsx` で `substack_handle` 設定済み判定による読み取り専用/入力切り替え (D-03)、(2) `my/page.tsx` で `substackHandle` prop を DB 値から直接渡す (D-03)、(3) `AdminMemberList.tsx` に `substack_handle` / `publication_id` 編集フィールドを追加 (D-04)、(4) `admin/actions.ts` と `lib/members.ts` で admin 編集対応 + 23505 処理 (D-04, D-05)。

Purpose: メンバーは自分の substack_handle を callback 登録後に変更不可とし、admin のみが変更できる状態にする。また admin による publication_id 変更時に articles テーブルも連動更新することでデータ整合性を保つ。

Output:
- `src/app/my/MyProfileForm.tsx` — D-03: substack_handle 条件付きレンダリング
- `src/app/my/page.tsx` — D-03: substackHandle prop 渡し方変更
- `src/app/admin/AdminMemberList.tsx` — D-04: 編集フォームに新フィールド追加
- `src/app/admin/actions.ts` — D-04/D-05: updateMemberAction 拡張
- `src/app/admin/__tests__/updateMemberAction.test.ts` — Wave 0 テスト（新規）
- `src/lib/members.ts` — D-04: updateMember に substackHandle/publicationId 対応
</objective>

<execution_context>
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-login-fix/31-CONTEXT.md
@.planning/phases/31-login-fix/31-RESEARCH.md
@.planning/phases/31-login-fix/31-PATTERNS.md
@.planning/phases/31-login-fix/31-UI-SPEC.md
@.planning/phases/31-login-fix/31-01-SUMMARY.md

<interfaces>
<!-- executor が参照する主要インタフェース -->

From src/app/my/MyProfileForm.tsx (現在の実装):
- Props: { member: Member; substackHandleDefault?: string }
- substack_handle は現在 <input> で常に編集可能（D-03 で条件分岐に変更）
- 修正後 props: { member: Member; substackHandle: string | null | undefined }

From src/app/my/page.tsx (現在の実装):
- line 50: const substackHandleDefault = (member as any)?.substack_handle ?? handle ?? undefined
- MyProfileForm に substackHandleDefault={substackHandleDefault} で渡している
- D-03 修正後: const substackHandle = (member as any)?.substack_handle ?? null
- MyProfileForm に substackHandle={substackHandle} で渡す（handle URL パラメータは不使用）

From src/app/admin/AdminMemberList.tsx (現在の実装):
- handleUpdate: tr 内の input[name] を全て FormData に自動収集する実装
- 編集行: name, addedAt の input のみ（publicationId は text 表示）
- D-04 追加: substack_handle の input (name="substack_handle"), publication_id の input (name="new_publication_id")
- handleUpdate 内に publication_id 変更時の window.confirm を追加

From src/app/admin/actions.ts (現在の実装):
- updateMemberAction(publicationId: string, formData: FormData): Promise<string | null>
- 現在: name, teamNames, addedAt のみ処理。substackHandle/publicationId は無視
- D-04 追加: substack_handle（trim + @ 自動付与）と new_publication_id を FormData から読み取り updateMember に渡す
- D-05 追加: catch ブロックで e.message.includes('23505') を検出してカスタムエラーメッセージ返す

From src/lib/members.ts — updateMember (現在の実装):
- updateMember(publicationId: string, updates: Partial<Omit<Member, 'publicationId'>>): Promise<void>
- 現在: name, addedAt, teams のみ処理
- D-04 追加: updates.substackHandle → memberUpdate.substack_handle
- D-04 追加: updates.publicationId → memberUpdate.publication_id + articles テーブル連動 UPDATE

From src/lib/types.ts — Member 型:
- id: string, name: string, publicationId: string, teams: {...}[], addedAt: string, substackHandle?: string
- updateMember の型シグネチャ: Partial<Omit<Member, 'publicationId'>> を Partial<Omit<Member, 'id'>> に変更

From src/app/auth/__tests__/callback.test.ts — モック構造（参考）:
- vi.mock('@/lib/supabase/admin', () => ({ createSupabaseAdminClient: vi.fn(() => ({ from: mockAdminFrom })) }))
- mockAdminFrom をテーブル名で分岐して各操作をモック
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 テストスキャフォールド + lib/members.ts D-04 拡張</name>
  <files>src/app/admin/__tests__/updateMemberAction.test.ts, src/lib/members.ts</files>
  <read_first>
    - src/app/auth/__tests__/callback.test.ts — vi.mock パターン（mockAdminFrom の chainable 実装）
    - src/app/my/__tests__/updateMyProfileAction.test.ts — setupAdminMock ヘルパー、FormData 作成パターン
    - src/lib/members.ts — updateMember の現在の実装全体（特に lines 82-128、memberUpdate オブジェクト構築パターン）
    - src/lib/types.ts — Member 型定義
    - src/app/admin/actions.ts — updateMemberAction の現在の実装（call chain 確認）
  </read_first>
  <behavior>
    updateMemberAction.test.ts に作成するテスト（実装前は RED）:
    - Test 1: substack_handle を含む FormData → updateMember が substackHandle: '@hoge' で呼ばれる
    - Test 2: 空の substack_handle → updateMember が substackHandle: null で呼ばれる
    - Test 3: new_publication_id を含む FormData → updateMember が publicationId: 'new-pid' で呼ばれる
    - Test 4: updateMember が 23505 を含む Error を throw → 'このハンドルはすでに使用されています' が返る
    - Test 5: requireAdmin が Unauthorized → '権限がありません' が返る（既存動作確認）
  </behavior>
  <action>
    まず `src/app/admin/__tests__/` ディレクトリを作成し、`updateMemberAction.test.ts` を新規作成する。

    モック構造: `@/lib/members` の `updateMember` を `vi.mock('@/lib/members', ...)` でモックし、`mockUpdateMember = vi.fn()` として各テストで返り値を制御する。`@/lib/supabase/server` の `createSupabaseServerClient` をモックして `auth.getUser` が admin ユーザー（`{ user: { id: 'u1', role: 'admin' } }`）を返すデフォルト設定にする。`next/cache` の `revalidatePath` を no-op でモック。

    FormData 作成ヘルパー `makeFormData(name, addedAt, substackHandle?, newPublicationId?)` を定義する。

    Test 1〜4 はこの時点で RED（action が FormData から substackHandle/publicationId を読まないため）。

    次に `src/lib/members.ts` の `updateMember` 関数を修正する:
    - 関数シグネチャを `updateMember(publicationId: string, updates: Partial<Omit<Member, 'id'>>)` に変更する（`publicationId` を更新可能にする）
    - `memberUpdate` オブジェクトの構築部分に以下を追加:
      `if (updates.substackHandle !== undefined) memberUpdate.substack_handle = updates.substackHandle`
      `if (updates.publicationId !== undefined) memberUpdate.publication_id = updates.publicationId`
    - publication_id 変更時の articles 連動 UPDATE: `if (updates.publicationId !== undefined && updates.publicationId !== publicationId)` のブロックで `admin.from('articles').update({ publication_id: updates.publicationId }).eq('publication_id', publicationId)` を実行し、articlesUpdateError があれば throw する
    - `lib/members.ts` は `createSupabaseAdminClient` を使う（変数名 `supabase` → ファイル内で既に `supabase` を使っているため、既存の変数名を維持する）
  </action>
  <verify>
    <automated>cd /Users/kugyu10/work/keep-substack && npx vitest run src/app/admin/__tests__/updateMemberAction.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>
    - `src/app/admin/__tests__/updateMemberAction.test.ts` が存在し、5 つのテストを含む
    - Test 5 は GREEN（requireAdmin は既存動作）。Test 1〜4 は RED（action 未実装のため）
    - `src/lib/members.ts` の `updateMember` が `substackHandle`, `publicationId`, articles 連動 UPDATE を含む
    - `npx vitest run` で既存テストが壊れていない（members.ts の型シグネチャ変更による影響なし）
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: D-04/D-05 実装 — admin/actions.ts 拡張 + AdminMemberList.tsx フィールド追加</name>
  <files>src/app/admin/actions.ts, src/app/admin/AdminMemberList.tsx</files>
  <read_first>
    - src/app/admin/actions.ts — updateMemberAction の現在の全実装（特に FormData 読み取りと updateMember 呼び出し部分）
    - src/app/admin/AdminMemberList.tsx — 編集行の現在の実装（teble 構造、handleUpdate、既存の confirm パターン）
    - .planning/phases/31-login-fix/31-PATTERNS.md — Pattern: AdminMemberList 編集行 D-04 追加パターン、confirm ダイアログ文言
    - .planning/phases/31-login-fix/31-UI-SPEC.md — AdminMemberList のコンポーネント仕様（テーブル列追加、警告テキスト、focal point）
  </read_first>
  <behavior>
    Test 1 (GREEN): substack_handle を含む FormData → updateMember が substackHandle: '@hoge' で呼ばれる
    Test 2 (GREEN): 空の substack_handle → updateMember が substackHandle: null で呼ばれる
    Test 3 (GREEN): new_publication_id を含む FormData → updateMember が publicationId: 'new-pid' で呼ばれる
    Test 4 (GREEN): updateMember が 23505 Error を throw → 'このハンドルはすでに使用されています'
  </behavior>
  <action>
    `src/app/admin/actions.ts` の `updateMemberAction` を修正する:
    - FormData 読み取り部分（`addedAt` の取得の後）に以下を追加:
      `const substackHandleRaw = (formData.get('substack_handle') as string | null)?.trim() ?? ''`
      `const substack_handle: string | null = substackHandleRaw === '' ? null : substackHandleRaw.startsWith('@') ? substackHandleRaw : '@' + substackHandleRaw`
      `const new_publication_id = (formData.get('new_publication_id') as string | null)?.trim() || null`
    - `updateMember` の呼び出しに `substackHandle: substack_handle` と `...(new_publication_id ? { publicationId: new_publication_id } : {})` を追加する
    - `catch (e)` ブロックを `if (e instanceof Error && (e as any).code === '23505' || e instanceof Error && e.message.includes('23505')) return 'このハンドルはすでに使用されています'` に拡張する（lib/members.ts は PostgrestError を throw するため、`(e as any).code === '23505'` が主条件）

    `src/app/admin/AdminMemberList.tsx` を修正する:
    - テーブルヘッダーに `<th className="border border-[#ebebeb] px-3 py-2">handle</th>` と `<th className="border border-[#ebebeb] px-3 py-2">publicationId (edit)</th>` 列を追加する（既存の `publicationId` ヘッダーの後ろに追加）
    - 閲覧行（non-editing）に `<td className="border border-[#ebebeb] px-3 py-2">{m.substackHandle ?? ''}</td>` と `<td className="border border-[#ebebeb] px-3 py-2">{m.publicationId}</td>` 列を追加する
    - 編集行（`editingId === m.publicationId`）に以下を追加:
      substack_handle input: `<td className="border border-[#ebebeb] px-3 py-2"><input defaultValue={m.substackHandle ?? ''} name="substack_handle" placeholder="@handle" className="bg-white border border-[#d8d8d8] rounded px-1 w-full text-sm text-[#363737]" /></td>`
      publication_id input: `<td className="border border-[#ebebeb] px-3 py-2"><input defaultValue={m.publicationId} name="new_publication_id" className="bg-white border border-[#d8d8d8] rounded px-1 w-full text-sm font-mono text-[#363737]" /><p className="text-xs text-gray-500 mt-1">変更すると記事データとの紐付けに影響します</p></td>`
    - `handleUpdate` 関数内（`const formData = new FormData()` の後、`updateMemberAction` 呼び出しの前）に以下を追加:
      `const newPublicationId = tr.querySelector<HTMLInputElement>('input[name="new_publication_id"]')?.value`
      `if (newPublicationId && newPublicationId !== publicationId) { if (!window.confirm('publication_id を変更します。記事データとの紐付けに影響します。続けますか？')) return }`
  </action>
  <verify>
    <automated>cd /Users/kugyu10/work/keep-substack && npx vitest run src/app/admin/__tests__/updateMemberAction.test.ts</automated>
  </verify>
  <done>
    - `src/app/admin/actions.ts` が `substack_handle` と `new_publication_id` を FormData から読み取り `updateMember` に渡す
    - `src/app/admin/actions.ts` の catch ブロックが `このハンドルはすでに使用されています` を返す
    - `src/app/admin/AdminMemberList.tsx` に `name="substack_handle"` と `name="new_publication_id"` の input が含まれる
    - `src/app/admin/AdminMemberList.tsx` に `publication_id を変更します` confirm ダイアログが含まれる
    - `updateMemberAction.test.ts` の全 5 テスト GREEN
    - `npx vitest run` 全体 pass
  </done>
</task>

<task type="auto">
  <name>Task 3: D-03 実装 — MyProfileForm.tsx 読み取り専用化 + my/page.tsx substackHandle prop 変更</name>
  <files>src/app/my/MyProfileForm.tsx, src/app/my/page.tsx</files>
  <read_first>
    - src/app/my/MyProfileForm.tsx — 現在の全実装（特に substack_handle の input 部分 lines 52-63 と props 型定義 line 13）
    - src/app/my/page.tsx — 現在の全実装（特に substackHandleDefault の計算 line 50 と MyProfileForm への渡し方 line 79）
    - .planning/phases/31-login-fix/31-UI-SPEC.md — Component Inventory 2: MyProfileForm の表示コントラクト（設定済みケースと未設定ケース）
    - .planning/phases/31-login-fix/31-PATTERNS.md — Pattern 3: substack_handle 読み取り専用制御（publicationId パターンを踏襲）
  </read_first>
  <action>
    `src/app/my/MyProfileForm.tsx` を修正する:
    - props 型を `{ member: Member; substackHandleDefault?: string }` から `{ member: Member; substackHandle: string | null | undefined }` に変更する（`substackHandleDefault` → `substackHandle`）
    - 関数シグネチャの分割代入も `substackHandleDefault` → `substackHandle` に変更する
    - substack_handle の表示部分（lines 52-63）を条件分岐に変更する:
      `substackHandle != null` の場合（設定済み）: UI-SPEC の表示コントラクト通りに `<label>Substack ハンドル</label>` + `<p className="text-sm text-gray-400 border rounded px-3 py-2 bg-gray-100">{substackHandle}</p>` + `<p className="text-xs text-gray-500 mt-1">Substack ハンドルは変更できません</p>` を表示する
      `substackHandle == null` の場合（未設定・フォールバックケース）: 既存の `<input id="substack_handle" name="substack_handle" type="text" placeholder="@yourhandle" className="w-full border rounded px-3 py-2 text-sm" />` + `<p className="text-xs text-gray-500 mt-1">例: @yourname — 入力すると個人ページからプロフィールへのリンクが作成されます</p>` を表示する
    - 未設定ケースの input には `defaultValue` を削除する（substackHandle が null の場合は空入力から始める）

    `src/app/my/page.tsx` を修正する:
    - line 50 の `const substackHandleDefault = (member as any)?.substack_handle ?? handle ?? undefined` を `const substackHandle = (member as any)?.substack_handle ?? null` に変更する（handle URL パラメータは廃止。D-02 で callback 時に DB に保存済みのため不要）
    - line 79 の `substackHandleDefault={substackHandleDefault}` を `substackHandle={substackHandle}` に変更する
  </action>
  <verify>
    <automated>cd /Users/kugyu10/work/keep-substack && npx vitest run && cd /Users/kugyu10/work/keep-substack && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - `src/app/my/MyProfileForm.tsx` が `substackHandle != null` による条件分岐を含む
    - `src/app/my/MyProfileForm.tsx` が設定済みケースで `Substack ハンドルは変更できません` テキストを含む
    - `src/app/my/page.tsx` が `substackHandle` を DB 値のみから取得し（handle URL パラメータを使わず）MyProfileForm に渡す
    - TypeScript コンパイルエラーなし（`npx tsc --noEmit` が clean 終了）
    - `npx vitest run` 全体 pass
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin UI → updateMemberAction | admin ユーザーのみが呼べる Server Action。requireAdmin() でガード済み |
| updateMemberAction → lib/members.ts updateMember | admin client (BYPASSRLS) で members + articles を更新 |
| browser → MyProfileForm | substack_handle が設定済みなら UI 層で input を非表示。ただし Server Action 側でも user_id スコープで UPDATE するため、クライアント側の改ざんは無効 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-08 | Elevation of Privilege | updateMemberAction | mitigate | requireAdmin() で user.role !== 'admin' を確認。throw で早期終了 → '権限がありません' を返す |
| T-31-09 | Tampering | substack_handle 読み取り専用バイパス | mitigate | UI で入力フィールドを非表示にしても、updateMyProfileAction 側は user_id スコープで UPDATE するため他メンバーの handle は変更不可。自分の handle は null ケースのみ書き込める（設定済み handle の上書き不可はサーバー側では防いでいないが、admin のみが変更権限を持つ設計で許容） |
| T-31-10 | Tampering | publication_id 変更 → articles FK | mitigate | lib/members.ts で publication_id 変更時に articles.publication_id を連動 UPDATE。confirm ダイアログで誤操作防止 |
| T-31-11 | Information Disclosure | admin 編集フォームの substack_handle 表示 | accept | admin 画面は authenticated admin ユーザーのみアクセス可能。handle 値の表示は意図的 |
| T-31-SC | Tampering | npm/pip/cargo installs | accept | 新規パッケージインストールなし。既存スタックのみ使用 |
</threat_model>

<verification>
```bash
# 全テスト pass 確認
cd /Users/kugyu10/work/keep-substack && npx vitest run

# TypeScript コンパイルエラーなし確認
cd /Users/kugyu10/work/keep-substack && npx tsc --noEmit

# 主要変更の確認
grep -n "substackHandle != null" /Users/kugyu10/work/keep-substack/src/app/my/MyProfileForm.tsx
grep -n "Substack ハンドルは変更できません" /Users/kugyu10/work/keep-substack/src/app/my/MyProfileForm.tsx
grep -n "new_publication_id" /Users/kugyu10/work/keep-substack/src/app/admin/AdminMemberList.tsx
grep -n "このハンドルはすでに使用されています" /Users/kugyu10/work/keep-substack/src/app/admin/actions.ts
grep -n "articles" /Users/kugyu10/work/keep-substack/src/lib/members.ts
```
</verification>

<success_criteria>
1. `npx vitest run` で全テスト GREEN（既存テスト含む）
2. `npx tsc --noEmit` が clean 終了（TypeScript エラーなし）
3. `MyProfileForm.tsx` が `substackHandle != null` ケースで `<p>` タグ表示（`<input>` なし）し、「Substack ハンドルは変更できません」テキストを含む
4. `my/page.tsx` が `const substackHandle = (member as any)?.substack_handle ?? null` を使い handle URL パラメータを使用しない
5. `AdminMemberList.tsx` の編集行に `name="substack_handle"` と `name="new_publication_id"` の input が存在する
6. `admin/actions.ts` の `updateMemberAction` が `substack_handle` と `new_publication_id` を FormData から読み取る
7. `lib/members.ts` の `updateMember` が `substackHandle` → `substack_handle` と `publicationId` → `publication_id` を memberUpdate に追加し、publication_id 変更時に articles テーブルを連動 UPDATE する
8. `updateMemberAction.test.ts` の全 5 テスト GREEN
</success_criteria>

<output>
Create `.planning/phases/31-login-fix/31-03-SUMMARY.md` when done
</output>
