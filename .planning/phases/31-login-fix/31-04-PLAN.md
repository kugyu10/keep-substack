---
phase: 31-login-fix
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/signin-51cf21389c56/page.tsx
  - src/app/signin-51cf21389c56/actions.ts
  - src/app/signin-51cf21389c56/LoginForm.tsx
  - src/app/signin-51cf21389c56/__tests__/sendMagicLinkAction.test.ts
  - src/app/login/page.tsx
  - src/app/login/actions.ts
  - src/app/login/LoginForm.tsx
  - src/app/login/__tests__/sendMagicLinkAction.test.ts
autonomous: true
gap_closure: true
requirements:
  - AUTH-FIX-01

must_haves:
  truths:
    - "既存メンバーが /login にメールアドレスだけ入力して Magic Link を送信できる（pid/handle 不要）"
    - "/login の sendMagicLinkAction は callbackUrl を /auth/callback のみに設定する（pid/handle パラメータなし）"
    - "新規招待ページが /signin-51cf21389c56?pid={id}&handle={handle} として引き続き機能する"
    - "/login/__tests__/sendMagicLinkAction.test.ts が pid/handle なしで SENT が返ることを検証する"
    - "/signin-51cf21389c56/__tests__/sendMagicLinkAction.test.ts が既存の pid/handle 必須テストを維持する"
    - "npx vitest run が全テスト pass する"
    - "npx tsc --noEmit が clean である"
  artifacts:
    - path: "src/app/signin-51cf21389c56/actions.ts"
      provides: "招待専用 sendMagicLinkAction（pid/handle 必須バリデーション維持）"
      contains: "登録リンクが不正です"
    - path: "src/app/login/actions.ts"
      provides: "既存メンバー再ログイン用 sendMagicLinkAction（pid/handle 不要）"
      contains: "/auth/callback"
    - path: "src/app/login/page.tsx"
      provides: "既存メンバー向けログインページ（メールアドレスのみ）"
    - path: "src/app/login/__tests__/sendMagicLinkAction.test.ts"
      provides: "/login の action テスト — pid/handle なしで SENT が返ることを確認"
  key_links:
    - from: "src/app/login/LoginForm.tsx"
      to: "src/app/login/actions.ts"
      via: "useActionState(sendMagicLinkAction)"
      pattern: "sendMagicLinkAction"
    - from: "src/app/login/actions.ts"
      to: "src/app/auth/callback/route.ts"
      via: "emailRedirectTo に /auth/callback のみ（pid/handle なし）"
      pattern: "/auth/callback"
    - from: "src/app/signin-51cf21389c56/LoginForm.tsx"
      to: "src/app/signin-51cf21389c56/actions.ts"
      via: "useActionState(sendMagicLinkAction)"
      pattern: "sendMagicLinkAction"
---

<objective>
G-01 を修正: ログインページを 2 つに分離し、既存メンバーが pid/handle なしで再ログインできるようにする。

**Gap:** D-01 の `!pid || !handle` バリデーションが既存メンバーの再ログインを阻害している。UAT テスト 3 が FAILED。

**解決方針（ユーザー決定済み — LOCKED）:**
- `src/app/login-51cf21389c56/` を `src/app/signin-51cf21389c56/` に `git mv` でリネーム（招待リンク専用）
- `src/app/login/` を新規作成（既存メンバー再ログイン用、メールのみ、pid/handle 不要）
- `/login` の callbackUrl は `/auth/callback` のみ（pid/handle パラメータなし）
- `/auth/callback/route.ts` はそのまま（pid なし → member 存在確認スキップ → /my リダイレクト）
- 招待ページ URL: `/signin-51cf21389c56?pid={id}&handle={handle}`（ハッシュ部分は変えない）

Purpose: 既存メンバーがセッション切れ後に再ログインできない問題を解消し、新規招待フローを維持する。

Output:
- `src/app/signin-51cf21389c56/` — 招待専用（pid/handle 必須）
- `src/app/login/` — 既存メンバー専用（メールのみ）
- テスト: 両ルートのテストが全 GREEN
</objective>

<execution_context>
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/31-login-fix/31-CONTEXT.md
@.planning/phases/31-login-fix/31-VERIFICATION.md
@.planning/phases/31-login-fix/31-HUMAN-UAT.md

<interfaces>
<!-- 現在の login-51cf21389c56/ ファイル構成（git mv 対象） -->

From src/app/login-51cf21389c56/actions.ts:
```typescript
'use server'
// sendMagicLinkAction(prevState: string | null, formData: FormData): Promise<string | null>
// - D-01: !pid || !handle → '登録リンクが不正です'
// - callbackUrl = `${origin}/auth/callback?pid=${...}&handle=${...}`
// - return 'SENT' on success
```

From src/app/login-51cf21389c56/LoginForm.tsx:
```typescript
// props: { pid?: string; handle?: string }
// pid/handle を hidden input で保持
// useActionState(sendMagicLinkAction, null)
```

From src/app/login-51cf21389c56/page.tsx:
```typescript
// searchParams: Promise<{ pid?: string; handle?: string }>
// セッションあり → redirect('/my')
// <LoginForm pid={pid} handle={handle} />
```

From src/app/auth/callback/route.ts (変更なし):
```typescript
// GET: searchParams から code, pid, handle を取得
// pid がなければ member 検索・INSERT をスキップ → /my にリダイレクト
// つまり /login（pid なし）で送ったリンクも /my に正しくリダイレクトされる
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: git mv でディレクトリリネーム（login-51cf21389c56 → signin-51cf21389c56）</name>
  <files>
    src/app/signin-51cf21389c56/page.tsx
    src/app/signin-51cf21389c56/actions.ts
    src/app/signin-51cf21389c56/LoginForm.tsx
    src/app/signin-51cf21389c56/__tests__/sendMagicLinkAction.test.ts
  </files>
  <read_first>
    - src/app/login-51cf21389c56/actions.ts — コピー対象（招待専用ロジック確認）
    - src/app/login-51cf21389c56/LoginForm.tsx — コピー対象（pid/handle hidden input）
    - src/app/login-51cf21389c56/page.tsx — コピー対象（searchParams pid/handle）
    - src/app/login-51cf21389c56/__tests__/sendMagicLinkAction.test.ts — テストの import パス確認
  </read_first>
  <action>
    `git mv` コマンドでディレクトリごとリネームする。コマンド:

    `git mv src/app/login-51cf21389c56 src/app/signin-51cf21389c56`

    リネーム後、signin-51cf21389c56/__tests__/sendMagicLinkAction.test.ts の import パスを更新する:
    - `from '../actions'` はそのまま（相対パスのため変更不要）
    - `vi.mock('@/lib/supabase/server', ...)` はそのまま（絶対パスのため変更不要）

    signin-51cf21389c56/actions.ts の内容はそのまま維持する（pid/handle 必須バリデーション `!pid || !handle` を保持）。

    変更後、`npx vitest run src/app/signin-51cf21389c56` を実行して既存テスト 4 件が全て PASS することを確認する。
  </action>
  <verify>
    <automated>npx vitest run src/app/signin-51cf21389c56/__tests__/sendMagicLinkAction.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/signin-51cf21389c56/` ディレクトリが存在する
    - `src/app/login-51cf21389c56/` ディレクトリが存在しない（git mv で削除済み）
    - `git status` で renamed: src/app/login-51cf21389c56/... → src/app/signin-51cf21389c56/... が表示される
    - `npx vitest run src/app/signin-51cf21389c56/__tests__/sendMagicLinkAction.test.ts` が 4 件全 PASS
    - signin-51cf21389c56/actions.ts に `登録リンクが不正です` が含まれる（D-01 バリデーション維持）
  </acceptance_criteria>
  <done>git mv が完了し、signin-51cf21389c56 テストが 4 件 PASS している</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: /login ディレクトリ新規作成（既存メンバー再ログイン用）</name>
  <files>
    src/app/login/page.tsx
    src/app/login/actions.ts
    src/app/login/LoginForm.tsx
    src/app/login/__tests__/sendMagicLinkAction.test.ts
  </files>
  <read_first>
    - src/app/signin-51cf21389c56/actions.ts — ベースとなるアクション実装パターン
    - src/app/signin-51cf21389c56/LoginForm.tsx — フォームコンポーネントパターン
    - src/app/signin-51cf21389c56/page.tsx — ページパターン
    - src/app/signin-51cf21389c56/__tests__/sendMagicLinkAction.test.ts — テストモックパターン
  </read_first>
  <behavior>
    - Test L-1: email のみ（pid/handle なし） → 'SENT' が返り、signInWithOtp が呼ばれる
    - Test L-2: callbackUrl に pid/handle パラメータが含まれない（`/auth/callback` のみ）
    - Test L-3: email が空 → 'メールアドレスを入力してください' が返り、signInWithOtp は呼ばれない
    - Test L-4: Supabase が 429 エラー → '送信制限に達しました。しばらく待ってから再試行してください'
  </behavior>
  <action>
    以下 4 ファイルを新規作成する。

    **src/app/login/__tests__/sendMagicLinkAction.test.ts:**
    - signin-51cf21389c56/__tests__/sendMagicLinkAction.test.ts のモック構造を踏襲する
    - import パス: `from '../actions'`
    - `makeFormData` は email のみ（pid/handle パラメータなし）
    - Test L-1: email あり、pid/handle なし → 'SENT' 返却、signInWithOtp 1 回呼ばれる
    - Test L-2: callbackUrl が `/auth/callback` を含み、`pid=` も `handle=` も含まない
      - `callArgs.options.emailRedirectTo` に `pid=` が含まれないことを `expect(...).not.toContain('pid=')` で確認
      - `callArgs.options.emailRedirectTo` に `handle=` が含まれないことを `expect(...).not.toContain('handle=')` で確認
    - Test L-3: email 空 → 'メールアドレスを入力してください'
    - Test L-4: Supabase が `{ error: { status: 429 } }` → '送信制限に達しました。しばらく待ってから再試行してください'

    先にテストを作成して RED を確認してから実装に進む（TDD）。

    **src/app/login/actions.ts:**
    - signin-51cf21389c56/actions.ts をベースに pid/handle 関連コードを削除する
    - `'use server'` ディレクティブを冒頭に記述する
    - `import { createSupabaseServerClient } from '@/lib/supabase/server'` と `import { headers } from 'next/headers'`
    - `sendMagicLinkAction(prevState: string | null, formData: FormData): Promise<string | null>`
    - email のみ読み取る。pid/handle の読み取り・バリデーションは一切行わない
    - callbackUrl は `${origin}/auth/callback` のみ（pid/handle クエリパラメータなし）
    - Supabase エラーハンドリング（429, 500, その他）は signin 版と同じロジックを維持する
    - 成功時は `'SENT'` を返す

    **src/app/login/LoginForm.tsx:**
    - signin-51cf21389c56/LoginForm.tsx をベースに pid/handle props と hidden input を削除する
    - `'use client'` ディレクティブを冒頭に記述する
    - props なし（`{}` または `export default function LoginForm()`）
    - hidden input（pid, handle）は含めない
    - メールアドレス入力フィールドとボタンのみ
    - UI スタイル（Tailwind クラス）は signin 版と同一にする
    - エラー表示: `<p role="alert" className="text-sm text-red-600">` パターン（UI-SPEC 準拠）
    - 送信完了: `state === 'SENT'` で完了メッセージ表示（signin 版と同じ）

    **src/app/login/page.tsx:**
    - signin-51cf21389c56/page.tsx をベースに searchParams（pid/handle）を削除する
    - セッションあり → `redirect('/my')`（signin 版と同じ）
    - `<LoginForm />` を props なしで呼び出す
    - ページ見出し: `ログイン`（signin 版と同じ）
  </action>
  <verify>
    <automated>npx vitest run src/app/login/__tests__/sendMagicLinkAction.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/login/` ディレクトリに 4 ファイル存在する（actions.ts, LoginForm.tsx, page.tsx, __tests__/sendMagicLinkAction.test.ts）
    - `npx vitest run src/app/login/__tests__/sendMagicLinkAction.test.ts` が 4 件全 PASS
    - src/app/login/actions.ts に `pid` または `handle` という識別子が含まれない（grep で確認: `grep -c "pid\|handle" src/app/login/actions.ts` が 0）
    - src/app/login/actions.ts の callbackUrl が `/auth/callback` のみ（pid/handle クエリパラメータなし）
    - src/app/login/LoginForm.tsx に `hidden` input が含まれない
  </acceptance_criteria>
  <done>/login の 4 ファイルが作成され、テスト 4 件が PASS している。pid/handle に関するコードが login/actions.ts に存在しない。</done>
</task>

<task type="auto">
  <name>Task 3: 全テスト・TypeScript コンパイルの最終確認</name>
  <files>
    (変更なし — 確認のみ)
  </files>
  <read_first>
    (なし)
  </read_first>
  <action>
    以下を順番に実行して全ての品質ゲートをパスすることを確認する。

    1. `npx vitest run` — 全テスト（116 件 + 今回追加 8 件 = 124 件以上）が pass することを確認する。
       signin-51cf21389c56 の 4 件と login の 4 件が両方 GREEN であること。
       失敗テストがある場合は前のタスクに戻って修正する。

    2. `npx tsc --noEmit` — TypeScript コンパイルエラーがないことを確認する。
       既存の `saveArticles.test.ts` の TS2493/TS2352 エラーは Phase 31 変更ファイル外なので無視してよい（VERIFICATION.md に記録済み）。
       新規作成ファイル（login/*, signin-51cf21389c56/*）に新たなエラーがないことを確認する。

    3. `ls src/app/login-51cf21389c56` が "No such file or directory" を返すことを確認する（git mv 完了の証明）。

    4. `ls src/app/signin-51cf21389c56` が 4 ファイルを返すことを確認する。

    5. `ls src/app/login` が 4 ファイルを返すことを確認する。
  </action>
  <verify>
    <automated>npx vitest run && npx tsc --noEmit 2>&1 | grep -v "saveArticles.test.ts" | grep -c "error TS" || true</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` が全テスト pass（0 failed）
    - `npx tsc --noEmit` で saveArticles.test.ts 以外の新たなエラーが 0 件
    - `src/app/login-51cf21389c56/` が存在しない
    - `src/app/signin-51cf21389c56/` が存在し、4 ファイルが含まれる
    - `src/app/login/` が存在し、4 ファイルが含まれる
  </acceptance_criteria>
  <done>全テスト GREEN、TypeScript clean（既存エラーのみ）、ディレクトリ構造が正しい。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /login action | 未認証ユーザーがメールアドレスを送信 |
| /login action → Supabase Auth | サーバー側で signInWithOtp を呼び出す（OTP コード生成） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-G01 | Spoofing | /login sendMagicLinkAction | accept | Magic Link フローは email 所有者のみが OTP を受信できる。Supabase Auth が OTP 発行・検証を管理するため追加対策不要 |
| T-31-G02 | Tampering | /login callbackUrl | mitigate | callbackUrl は `/auth/callback` のみ（pid/handle なし）。callback route の既存 open redirect 対策（`nextParam.startsWith('/')` チェック）は維持される |
| T-31-G03 | Denial of Service | /login rate limiting | accept | Supabase Auth の組み込み rate limit（status 429）をハンドリング済み。追加の rate limiting は Supabase 側に委譲 |
| T-31-SC | Tampering | npm/pip/cargo installs | accept | 新規パッケージインストールなし。既存スタックの範囲内での変更のみ |
</threat_model>

<verification>
## Phase 31 Gap Closure 検証

1. `/login` にアクセスしてメールアドレスを入力 → `SENT` が返ること（pid/handle なし）
2. `/signin-51cf21389c56?pid=test&handle=@test` が引き続き機能すること（pid/handle 必須）
3. `/login-51cf21389c56` へのアクセスが 404 になること（ディレクトリ削除済み）
4. `npx vitest run` が 0 failed で完了すること
5. `npx tsc --noEmit` が new errors 0 件であること
</verification>

<success_criteria>
1. `npx vitest run` — 全テスト pass（0 failed）
2. `npx tsc --noEmit` — 新規エラー 0 件（saveArticles.test.ts の既存エラーのみ許容）
3. `src/app/signin-51cf21389c56/` が存在し、pid/handle 必須バリデーション（`登録リンクが不正です`）が維持されている
4. `src/app/login/` が存在し、email のみで Magic Link 送信が可能（callbackUrl は `/auth/callback` のみ）
5. `src/app/login-51cf21389c56/` が存在しない（git mv 完了）
6. G-01: 既存メンバーが `/login` にメールアドレスのみ入力してログインできる（UAT テスト 3 の修正）
</success_criteria>

<output>
Create `.planning/phases/31-login-fix/31-04-SUMMARY.md` when done
</output>
