---
phase: 07-ui-polish-batch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/HeatmapTooltip.tsx
  - src/app/member/[substackId]/page.tsx
  - src/app/layout.tsx
autonomous: true
requirements:
  - TOOLTIP-01
  - TOOLTIP-02
  - NAV-01
  - NAV-02
  - FOOTER-01

must_haves:
  truths:
    - "Tooltipの記事サムネイルをクリックしても記事に遷移する（画像がリンクになっている）"
    - "Tooltipの記事間にmb-3のスペースがある（視認性向上）"
    - "メンバー詳細ページの戻りリンクのラベルが「← メンバー一覧」である"
    - "teamIdがある場合の戻りリンクURLが /?team=${teamId} になる"
    - "全ページのフッターに「コミュニティに参加する」リンクが表示される"
  artifacts:
    - path: "src/components/HeatmapTooltip.tsx"
      provides: "画像+タイトルを一体型<a>ブロックにまとめたカード型リンク、mb-3スペース"
    - path: "src/app/member/[substackId]/page.tsx"
      provides: "teamId条件分岐による戻りリンクURL、「← メンバー一覧」ラベル"
    - path: "src/app/layout.tsx"
      provides: "全ページ共通フッター（コミュニティに参加するリンク）"
  key_links:
    - from: "src/components/HeatmapTooltip.tsx"
      to: "article.link"
      via: "withUtm() applied to both <img> wrapper and title link"
      pattern: "withUtm.*article\\.link"
    - from: "src/app/member/[substackId]/page.tsx"
      to: "memberResult.member.teamId"
      via: "conditional href"
      pattern: "teamId.*team="
    - from: "src/app/layout.tsx"
      to: "https://uojun.substack.com/p/5d4"
      via: "<footer> after {children}"
      pattern: "uojun\\.substack\\.com"
---

<objective>
フロントエンド3ファイルに5件のUX改善を一括適用する。

Purpose: Tooltip記事カードのリンク使いやすさ改善、メンバー詳細ページのナビゲーション改善、全ページへのコミュニティフッター追加。バックエンド変更なし。
Output: 修正済み3ファイル（HeatmapTooltip.tsx、member/[substackId]/page.tsx、layout.tsx）
</objective>

<execution_context>
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/kugyu10/work/keep-substack/.planning/ROADMAP.md
@/Users/kugyu10/work/keep-substack/.planning/phases/07-ui-polish-batch/07-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: HeatmapTooltip — 画像+タイトルを一体型カードリンクに変更 + mb-3スペース（TOOLTIP-01, TOOLTIP-02）</name>
  <files>src/components/HeatmapTooltip.tsx</files>
  <read_first>
    - src/components/HeatmapTooltip.tsx — 現在の<li>内構造（imgとaが分離、mb-1）を把握してから変更する
  </read_first>
  <action>
`src/components/HeatmapTooltip.tsx` の `<ul>` 内のリスト項目を以下のとおり変更する。

【変更前（現在の構造）】
```tsx
<li key={i} className="mb-1 last:mb-0">
  {article.thumbnail && (
    <img
      src={article.thumbnail}
      alt=""
      className="w-full rounded mb-1 object-cover max-h-24"
    />
  )}
  <a
    href={withUtm(article.link ?? '#')}
    target="_blank"
    rel="noopener noreferrer"
    className="text-xs text-blue-600 hover:underline break-words block"
  >
    {article.title}
  </a>
</li>
```

【変更後（一体型カードリンク）】
```tsx
<li key={i} className="mb-3 last:mb-0">
  <a
    href={withUtm(article.link ?? '#')}
    target="_blank"
    rel="noopener noreferrer"
    className="block"
  >
    {article.thumbnail && (
      <img
        src={article.thumbnail}
        alt=""
        className="w-full rounded mb-1 object-cover max-h-24"
      />
    )}
    <span className="text-xs text-blue-600 hover:underline break-words block">
      {article.title}
    </span>
  </a>
</li>
```

ポイント:
- `mb-1` → `mb-3` に変更（TOOLTIP-02）
- `<img>` と タイトルを同一の `<a>` ブロック内に配置（TOOLTIP-01）
- `withUtm()` は既存のものをそのまま使用（D-03）
- タイトルの `<a>` → `<span>` に変更（親の `<a>` でリンクになるため）
  </action>
  <verify>
    <automated>grep -n "mb-3" src/components/HeatmapTooltip.tsx && grep -n "block" src/components/HeatmapTooltip.tsx | grep '<a'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'mb-3' src/components/HeatmapTooltip.tsx` が 1 以上を返す
    - `grep -c 'mb-1 last:mb-0' src/components/HeatmapTooltip.tsx` が 0 を返す（mb-1の<li>クラスが消えている）
    - `grep -c '<a' src/components/HeatmapTooltip.tsx` で <img> より前に <a が現れる（imgがa内部にある構造）
    - `grep -c 'text-blue-600' src/components/HeatmapTooltip.tsx` が 1 を返す（タイトルがspanになっている）
    - `npx tsc --noEmit` がエラーなし（型エラーがない）
  </acceptance_criteria>
  <done>サムネイル画像とタイトルが同一 &lt;a&gt; ブロック内に収まり、記事間スペースがmb-3になっている</done>
</task>

<task type="auto">
  <name>Task 2: メンバー詳細ページ — 戻りリンクのラベルとURL修正（NAV-01, NAV-02）</name>
  <files>src/app/member/[substackId]/page.tsx</files>
  <read_first>
    - src/app/member/[substackId]/page.tsx — 現在のLinkコンポーネント（href="/" ラベル「← ダッシュボードに戻る」）を確認してから変更する
  </read_first>
  <action>
`src/app/member/[substackId]/page.tsx` の `<Link>` コンポーネントを以下のとおり変更する。

【変更前】
```tsx
<Link
  href="/"
  className="text-sm text-gray-500 hover:text-gray-800 mb-4 inline-block"
>
  ← ダッシュボードに戻る
</Link>
```

【変更後】
```tsx
<Link
  href={memberResult.member.teamId ? `/?team=${memberResult.member.teamId}` : '/'}
  className="text-sm text-gray-500 hover:text-gray-800 mb-4 inline-block"
>
  ← メンバー一覧
</Link>
```

ポイント:
- ラベルを「← メンバー一覧」に変更（NAV-01、D-04）
- `memberResult.member.teamId` は既にfetchAllFeedsCachedの結果から取得済みのため追加フェッチ不要（D-06）
- teamIdが空文字・null・undefinedの場合はすべて `/` にフォールバック（D-05）
  </action>
  <verify>
    <automated>grep -n "メンバー一覧" "src/app/member/[substackId]/page.tsx" && grep -n "teamId" "src/app/member/[substackId]/page.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'メンバー一覧' src/app/member/[substackId]/page.tsx` が 1 を返す
    - `grep -c 'ダッシュボードに戻る' src/app/member/[substackId]/page.tsx` が 0 を返す
    - `grep -c 'teamId' src/app/member/[substackId]/page.tsx` が 1 以上を返す
    - `grep -c 'team=\${' src/app/member/[substackId]/page.tsx` が 1 を返す
    - `npx tsc --noEmit` がエラーなし
  </acceptance_criteria>
  <done>戻りリンクのラベルが「← メンバー一覧」になり、teamId条件によりURL /?team=${teamId} または / に切り替わる</done>
</task>

<task type="auto">
  <name>Task 3: layout.tsx — 共通フッター追加（FOOTER-01）</name>
  <files>src/app/layout.tsx</files>
  <read_first>
    - src/app/layout.tsx — 現在の<body>内構造（{children}のみ、フッターなし）を確認してから変更する
  </read_first>
  <action>
`src/app/layout.tsx` の `<body>` 内に `<footer>` を追加する。

【変更前】
```tsx
<body>{children}</body>
```

【変更後】
```tsx
<body>
  {children}
  <footer className="py-4 text-center">
    <a
      href="https://uojun.substack.com/p/5d4"
      target="_blank"
      rel="noopener noreferrer"
      className="text-xs text-gray-400 hover:text-gray-600"
    >
      コミュニティに参加する
    </a>
  </footer>
</body>
```

ポイント:
- `{children}` の後に `<footer>` を追加することで全ページ自動適用（D-07）
- スタイル: `py-4 text-center`（D-08）
- テキスト: 「コミュニティに参加する」、リンク先: `https://uojun.substack.com/p/5d4`（D-09）
- `target="_blank" rel="noopener noreferrer"` で外部リンク安全対応（D-09）
  </action>
  <verify>
    <automated>grep -n "コミュニティに参加する" src/app/layout.tsx && grep -n "uojun.substack.com" src/app/layout.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'コミュニティに参加する' src/app/layout.tsx` が 1 を返す
    - `grep -c 'uojun.substack.com/p/5d4' src/app/layout.tsx` が 1 を返す
    - `grep -c 'footer' src/app/layout.tsx` が 2 を返す（開きタグと閉じタグ）
    - `grep -c 'noopener noreferrer' src/app/layout.tsx` が 1 を返す
    - `grep -c 'py-4 text-center' src/app/layout.tsx` が 1 を返す
    - `npx tsc --noEmit` がエラーなし
  </acceptance_criteria>
  <done>全ページの下部に「コミュニティに参加する」リンクフッターが表示される</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ユーザー → 外部リンク | フッターとTooltipの外部リンクはユーザーのブラウザから外部サービスへ |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Spoofing | 外部リンク全般 | mitigate | `rel="noopener noreferrer"` を全外部リンクに付与（フッター・Tooltip既存リンク共に適用済み） |
| T-07-02 | Information Disclosure | UTMパラメータ | accept | UTMはマーケティング用途の識別子のみ。PII含まず低リスク |
</threat_model>

<verification>
全タスク完了後、以下を確認する:

1. TypeScriptビルドエラーなし: `npx tsc --noEmit`
2. 開発サーバー起動: `npm run dev` でエラーなし
3. Tooltip確認: ヒートマップのセルをホバー → サムネイル画像クリックで記事に遷移すること
4. ナビ確認: `/member/{substackId}` ページで「← メンバー一覧」が表示され、teamId有の場合 `/?team=` URLに遷移すること
5. フッター確認: トップページ・メンバー詳細ページ両方にフッターが表示されること
</verification>

<success_criteria>
- [ ] HeatmapTooltip: サムネイル画像クリックで記事リンクに遷移する（画像がaブロック内部にある）
- [ ] HeatmapTooltip: 記事間スペースがmb-3（mb-1から拡大済み）
- [ ] メンバー詳細ページ: 戻りリンクラベルが「← メンバー一覧」
- [ ] メンバー詳細ページ: teamId有の場合 `/?team=${teamId}` 、無の場合 `/` に戻る
- [ ] 全ページ: フッターに「コミュニティに参加する」→ https://uojun.substack.com/p/5d4 のリンクが表示される
- [ ] `npx tsc --noEmit` がエラーなし
</success_criteria>

<output>
完了後、`.planning/phases/07-ui-polish-batch/07-01-SUMMARY.md` を作成する。
</output>
