---
phase: 05-weekly-heatmap-tooltip
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/types.ts
  - src/lib/fetchFeed.ts
  - src/lib/heatmapUtils.ts
autonomous: true
requirements:
  - HEAT-01
  - HEAT-02
  - HEAT-03
  - TIP-01
  - TIP-02
  - TIP-03
  - UX-01

must_haves:
  truths:
    - "FeedItem型にcontentEncoded?: stringフィールドが存在する"
    - "rss-parserがcontent:encodedを取得するcustomFieldsが設定されている"
    - "getRecentDays()が今日から6日前までの7要素 YYYY-MM-DD 配列を返す"
    - "sortByWeeklyCount()が7日間投稿量降順・同数はaddedAt昇順でソートできる"
    - "buildHeatmapArticleMap()がthumbnailを含むHeatmapArticle[]のMapを返す"
    - "getIntensityClass()が投稿数0〜5以上に対して正しいTailwindクラスを返す"
    - "extractThumbnail()がcontent:encodedのHTMLから最初のimg srcを抽出する"
  artifacts:
    - path: "src/lib/types.ts"
      provides: "FeedItem型にcontentEncoded?: stringを含む"
      contains: "contentEncoded"
    - path: "src/lib/fetchFeed.ts"
      provides: "rss-parser customFields設定"
      contains: "content:encoded"
    - path: "src/lib/heatmapUtils.ts"
      provides: "ヒートマップ向けユーティリティ関数群"
      exports:
        - getRecentDays
        - sortByWeeklyCount
        - buildHeatmapArticleMap
        - getIntensityClass
        - extractThumbnail
  key_links:
    - from: "src/lib/fetchFeed.ts"
      to: "rss-parser customFields"
      via: "new Parser({ customFields: { item: [['content:encoded', 'contentEncoded']] } })"
      pattern: "content:encoded"
    - from: "src/lib/heatmapUtils.ts"
      to: "src/lib/calendarUtils.ts"
      via: "parseIsoDateを再利用"
      pattern: "parseIsoDate"
---

<objective>
Phase 5のヒートマップ実装に必要なデータ層の基盤を整備する。

Purpose: WeeklyHeatmapGrid・HeatmapTooltipコンポーネント（Plan 02で実装）が依存する型定義・ユーティリティ関数を確立する。特に rss-parser の customFields 設定でサムネイル取得を可能にし、ソート・日付生成・色マッピングのロジックをサーバーサイドユーティリティとして分離する。
Output:
- src/lib/types.ts（FeedItemにcontentEncoded追加）
- src/lib/fetchFeed.ts（customFields追加）
- src/lib/heatmapUtils.ts（新規: 5関数）
</objective>

<execution_context>
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/kugyu10/work/keep-substack/.planning/PROJECT.md
@/Users/kugyu10/work/keep-substack/.planning/ROADMAP.md
@/Users/kugyu10/work/keep-substack/.planning/STATE.md

<interfaces>
<!-- 既存コード — executorはこれを参照して実装する。再探索不要。 -->

From src/lib/types.ts (現状):
```typescript
export type FeedItem = {
  title?: string
  link?: string
  pubDate?: string
  isoDate?: string
}

export type Member = {
  name: string
  substackId: string
  teamId: string
  addedAt: string  // ISO 8601
}

export type MemberFeedResult = {
  member: Member
  items: FeedItem[]
}
```

From src/lib/fetchFeed.ts (現状):
```typescript
const parser = new Parser({ timeout: 5000 })
// ↑ customFields未設定のためcontent:encodedが取得できない
```

From src/lib/calendarUtils.ts (再利用可能):
```typescript
export function parseIsoDate(isoDate: string): { year: number; month: number; day: number } | null
// ISO 8601文字列を正規表現で前方マッチ。new Date()不使用でタイムゾーン安全。
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: FeedItem型にcontentEncodedを追加し、rss-parserにcustomFieldsを設定する</name>
  <files>src/lib/types.ts, src/lib/fetchFeed.ts</files>
  <action>
**src/lib/types.ts:**
FeedItem型に `contentEncoded?: string` フィールドを追加する。既存フィールドは変更しない。

```typescript
export type FeedItem = {
  title?: string
  link?: string
  pubDate?: string
  isoDate?: string
  contentEncoded?: string  // rss-parser customFieldsで取得。サムネイル抽出用
}
```

**src/lib/fetchFeed.ts:**
`new Parser({ timeout: 5000 })` を以下に変更する（署名・キャッシュ設定は一切変更しない）:

```typescript
const parser = new Parser({
  timeout: 5000,
  customFields: {
    item: [['content:encoded', 'contentEncoded']]
  }
})
```

`fetchAllFeedsCached`の型シグネチャ`(members: Member[]): Promise<MemberFeedResult[]>`は変更してはならない（Phase 4で確定済み）。
  </action>
  <verify>
TypeScriptのコンパイルエラーがないことを確認: `npx tsc --noEmit`
  </verify>
  <done>
- src/lib/types.ts の FeedItem に contentEncoded?: string が存在する
- src/lib/fetchFeed.ts の parser に customFields設定が存在する
- tsc --noEmit がエラーなし
  </done>
</task>

<task type="auto">
  <name>Task 2: heatmapUtils.tsを新設する（5関数）</name>
  <files>src/lib/heatmapUtils.ts</files>
  <action>
`src/lib/heatmapUtils.ts` を新規作成する。calendarUtils.tsは変更しない（DRY原則: parseIsoDateを再利用）。

以下の5関数をexportする:

**1. extractThumbnail** — content:encodedのHTMLから最初のimg srcを抽出する
```typescript
export function extractThumbnail(contentEncoded?: string): string | undefined {
  if (!contentEncoded) return undefined
  const match = contentEncoded.match(/<img[^>]+src=["']([^"']+)["']/)
  return match ? match[1] : undefined
}
```

**2. getIntensityClass** — 投稿数から6段階TailwindクラスをD-01に従って返す
```typescript
export function getIntensityClass(count: number): string {
  if (count === 0) return 'bg-gray-100 text-gray-400'
  if (count === 1) return 'bg-green-100 text-green-700'
  if (count === 2) return 'bg-green-200 text-green-800'
  if (count === 3) return 'bg-green-300 text-green-900'
  if (count === 4) return 'bg-green-500 text-white'
  return 'bg-green-700 text-white'
}
```

**3. getRecentDays** — 今日から遡って7日分の"YYYY-MM-DD"配列を返す（D-02: 今日含む）
- `new Date()`でローカル時刻を取得し、getFullYear/getMonth/getDateで年月日を個別取得してゼロパディングする（new Date(isoString)は使用しない — タイムゾーンズレ防止）
- 返す配列は [今日-6, 今日-5, ..., 今日] の昇順（左: 6日前 → 右: 今日）

```typescript
export function getRecentDays(): string[] {
  const days: string[] = []
  const now = new Date()
  for (let i = 6; i >= 0; i--) {
    const d = new Date(now)
    d.setDate(d.getDate() - i)
    const year = d.getFullYear()
    const month = String(d.getMonth() + 1).padStart(2, '0')
    const day = String(d.getDate()).padStart(2, '0')
    days.push(`${year}-${month}-${day}`)
  }
  return days
}
```

**4. buildHeatmapArticleMap** — FeedItem[]からthumbnail付きのHeatmapArticle[]をDateキーのMapに変換する
- calendarUtils.tsのparseIsoDateをimportして再利用する
- HeatmapArticle型: `{ title?: string; link?: string; thumbnail?: string }` をローカル型として定義する（exportも行う）
- contentEncodedからextractThumbnailでサムネイルURLを抽出する

```typescript
import { parseIsoDate } from './calendarUtils'
import type { FeedItem } from './types'

export type HeatmapArticle = {
  title?: string
  link?: string
  thumbnail?: string
}

export function buildHeatmapArticleMap(
  items: FeedItem[]
): Map<string, HeatmapArticle[]> {
  const map = new Map<string, HeatmapArticle[]>()
  for (const item of items) {
    if (!item.isoDate) continue
    const parsed = parseIsoDate(item.isoDate)
    if (!parsed) continue
    const key = `${parsed.year}-${String(parsed.month).padStart(2, '0')}-${String(parsed.day).padStart(2, '0')}`
    const existing = map.get(key) ?? []
    existing.push({
      title: item.title,
      link: item.link,
      thumbnail: extractThumbnail(item.contentEncoded),
    })
    map.set(key, existing)
  }
  return map
}
```

**5. sortByWeeklyCount** — MemberFeedResult[]を7日間投稿量降順・同数はaddedAt昇順でソートする（HEAT-03）
- 元配列を変更しない（スプレッドでコピー）
- dateSetにdates配列をSetとして持ち、O(1)ルックアップする

```typescript
import type { MemberFeedResult } from './types'

function countArticlesInDates(items: FeedItem[], dateSet: Set<string>): number {
  return items.filter((item) => {
    if (!item.isoDate) return false
    const parsed = parseIsoDate(item.isoDate)
    if (!parsed) return false
    const key = `${parsed.year}-${String(parsed.month).padStart(2, '0')}-${String(parsed.day).padStart(2, '0')}`
    return dateSet.has(key)
  }).length
}

export function sortByWeeklyCount(
  results: MemberFeedResult[],
  dates: string[]
): MemberFeedResult[] {
  const dateSet = new Set(dates)
  return [...results].sort((a, b) => {
    const aCount = countArticlesInDates(a.items, dateSet)
    const bCount = countArticlesInDates(b.items, dateSet)
    if (bCount !== aCount) return bCount - aCount  // 降順
    return a.member.addedAt.localeCompare(b.member.addedAt)  // addedAt昇順
  })
}
```

importはファイル先頭にまとめること。`parseIsoDate`と`FeedItem`/`MemberFeedResult`は一度だけimportする。
  </action>
  <verify>
`npx tsc --noEmit` がエラーなし。
  </verify>
  <done>
- src/lib/heatmapUtils.ts が存在し、5関数（extractThumbnail, getIntensityClass, getRecentDays, buildHeatmapArticleMap, sortByWeeklyCount）がexportされている
- HeatmapArticle型がexportされている
- tsc --noEmit がエラーなし
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RSS Feed → rss-parser | 外部SubstackサーバーのRSSコンテンツがサーバーサイドに流入 |
| content:encoded → extractThumbnail | RSS由来のHTML文字列からimg srcを抽出 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Spoofing | RSS Feed (fetchFeed.ts) | accept | SubstackのRSSは信頼できるソース。fetchAllFeedsCachedの署名変更なし。追加認証は不要。 |
| T-05-02 | Information Disclosure | extractThumbnail (heatmapUtils.ts) | accept | regexはHTMLを実行しない。img src属性の文字列マッチのみ。XSSリスクなし（スクリプト実行なし）。 |
| T-05-03 | Tampering | content:encoded → thumbnail URL | accept | img srcとして使用するのみ。JavaScriptプロトコルURLは画像として読み込まれず無害。Substackは信頼できるソース。 |
</threat_model>

<verification>
1. `npx tsc --noEmit` がゼロエラー
2. `grep -c "contentEncoded" src/lib/types.ts` が 1 以上
3. `grep -c "content:encoded" src/lib/fetchFeed.ts` が 1 以上
4. `grep -v '^//' src/lib/heatmapUtils.ts | grep -c "export function"` が 5
</verification>

<success_criteria>
- FeedItem型にcontentEncoded?: stringが追加されている
- rss-parserにcustomFields設定があり、content:encodedをcontentEncodedとして取得できる
- heatmapUtils.tsに5関数が実装・exportされている
- TypeScriptコンパイルエラーなし
- fetchAllFeedsCachedの型シグネチャが変更されていない
</success_criteria>

<output>
完了後、`.planning/phases/05-weekly-heatmap-tooltip/05-01-SUMMARY.md` を作成する。
</output>
