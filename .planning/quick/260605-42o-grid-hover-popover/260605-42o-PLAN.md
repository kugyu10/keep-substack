---
phase: quick-260605-42o
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/CommitGridCell.tsx
  - src/components/CommitGrid.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "達成セル（サムネイルあり or フォールバック画像あり）にホバーすると記事タイトルが表示される"
    - "未達成セル（点線ボーダー、記事なし）にホバーしてもポップオーバーは表示されない"
    - "CommitGrid は Server Component のままである（'use client' が追加されない）"
  artifacts:
    - path: "src/components/CommitGridCell.tsx"
      provides: "Client Component — group+hover によるタイトルポップオーバー付きセルラッパー"
      exports: ["CommitGridCell"]
    - path: "src/components/CommitGrid.tsx"
      provides: "達成セルの <a>/<div> を CommitGridCell でラップした更新済み Server Component"
  key_links:
    - from: "src/components/CommitGrid.tsx"
      to: "src/components/CommitGridCell.tsx"
      via: "import CommitGridCell — 達成セルのみラップ"
      pattern: "CommitGridCell"
---

<objective>
CommitGrid の達成セル（サムネイルまたはフォールバック画像付きセル）にホバーしたとき、記事タイトルをポップオーバーで表示する。未達成セル（点線ボーダー）にはポップオーバー不要。

Purpose: ユーザーがグリッドをホバーしてどの記事が達成に対応しているか素早く確認できるようにする。
Output: CommitGridCell Client Component + CommitGrid.tsx の更新
</objective>

<execution_context>
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/kugyu10/work/keep-substack/.planning/PROJECT.md
@/Users/kugyu10/work/keep-substack/.planning/STATE.md

<interfaces>
<!-- FeedItem (src/lib/types.ts) -->
export type FeedItem = {
  title?: string
  link?: string
  isoDate?: string
  thumbnail?: string
}

<!-- CommitGrid.tsx renderCells — achieved cell variants (3 patterns):
  Pattern A: article.thumbnail && article.link  → <a href={article.link} ...><img .../></a>
  Pattern B: article.thumbnail (no link)        → <div ...><img .../></div>
  Pattern C: article (no thumbnail) + imageUrl  → <a|div ...><div border-gray-200 bg-primary/20>✓</div></a|div>
  No-article: point-dashed border div           → no popover
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CommitGridCell — Client Component with hover popover</name>
  <files>src/components/CommitGridCell.tsx</files>
  <action>
    Create a new 'use client' Client Component `CommitGridCell` with the following props:

    ```
    type CommitGridCellProps = {
      href?: string          // article URL; if present renders <a>, else <div>
      title?: string         // popover label; required for popover to appear
      children: React.ReactNode
    }
    ```

    Implementation rules:
    - The outer element is `<a>` when `href` is present, otherwise `<div>`. Both share className `group relative h-full block`.
    - The inner `children` slot renders as-is (the `<img>` or the fallback `✓` div).
    - When `title` is truthy, render a tooltip `<span>` element that is:
      - positioned absolute, centered horizontally (`left-1/2 -translate-x-1/2`), above the cell (`bottom-full mb-1`)
      - initially hidden (`opacity-0 pointer-events-none`), shown on group hover (`group-hover:opacity-100`)
      - `z-10 whitespace-nowrap bg-gray-800 text-white text-[10px] leading-snug rounded px-2 py-1 max-w-[160px] truncate`
      - transition: `transition-opacity duration-150`
    - The `<a>` variant must also set `target="_blank" rel="noreferrer"`.
    - Do NOT add any state, useEffect, or JS event handlers — CSS hover only.
    - Keep the file lean: no additional imports beyond React.
  </action>
  <verify>
    <automated>cd /Users/kugyu10/work/keep-substack && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>CommitGridCell.tsx exists, exports CommitGridCell, compiles without TS errors, no 'useState'/'useEffect' in file.</done>
</task>

<task type="auto">
  <name>Task 2: Wire CommitGridCell into CommitGrid achieved cells</name>
  <files>src/components/CommitGrid.tsx</files>
  <action>
    Update `CommitGrid.tsx` to wrap all three achieved-cell patterns with `CommitGridCell`. Import from `./CommitGridCell`.

    The `renderCells` function currently has four rendering branches. Replace achieved patterns as follows (no-article/dashed-border branch is unchanged):

    Pattern A (article + thumbnail + link):
    - Remove the bare `<a>` wrapper.
    - Return `<CommitGridCell key={idx} href={article.link} title={article.title}>` wrapping the `<img>` element.
    - The `<img>` keeps `className="object-cover w-full h-full"`.

    Pattern B (article + thumbnail, no link):
    - Remove the bare `<div>` wrapper.
    - Return `<CommitGridCell key={idx} title={article.title}>` wrapping the `<img>`.

    Pattern C (article, no thumbnail, uses imageUrl or ✓ fallback):
    - Keep the inner cell element (`<div className="h-full ...">`) as-is.
    - Wrap it in `<CommitGridCell key={idx} href={article.link ?? undefined} title={article.title}>`.
    - Remove the outer `<a>`/`<div>` that previously handled link routing (CommitGridCell handles it now).

    The comment `// Server Component — do NOT add 'use client'` at the top must remain — do NOT add 'use client' to this file. CommitGrid stays a Server Component; only CommitGridCell is client.

    After editing, the `renderCells` function returns `CommitGridCell` elements for achieved slots and the plain `<div>` with dashed border for unachieved slots — same as before.
  </action>
  <verify>
    <automated>cd /Users/kugyu10/work/keep-substack && npx tsc --noEmit 2>&1 | head -20 && grep -c "CommitGridCell" src/components/CommitGrid.tsx</automated>
  </verify>
  <done>CommitGrid.tsx imports CommitGridCell, grep count >= 2 (import + at least one usage), tsc reports no errors, file still begins with `// Server Component`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FeedItem.title → DOM | Article title from RSS rendered as text content in tooltip span |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42o-01 | Tampering | CommitGridCell tooltip | accept | title is RSS-sourced text rendered via React (auto-escaped); no innerHTML; XSS not possible |
| T-42o-SC | Tampering | npm installs | accept | no new packages installed in this task |
</threat_model>

<verification>
1. Run `npx tsc --noEmit` — zero errors.
2. Run `npm run build` (or `next build`) — no build-time error related to client/server boundary.
3. Browser: hover an achieved cell → tooltip with article title appears above the cell.
4. Browser: hover a dashed-border unachieved cell → no tooltip appears.
5. Mobile: no layout shift; tooltip does not appear on touch (pointer-events-none on tooltip prevents interaction issues).
</verification>

<success_criteria>
- CommitGridCell.tsx created as 'use client', CSS-only hover, no JS state.
- CommitGrid.tsx remains a Server Component (no 'use client' directive).
- Hovering achieved cells shows article title in a small dark tooltip above the cell.
- Unachieved (dashed) cells are unchanged — no tooltip.
- TypeScript compiles clean.
</success_criteria>

<output>
Create `.planning/quick/260605-42o-grid-hover-popover/260605-42o-SUMMARY.md` when done.
</output>
