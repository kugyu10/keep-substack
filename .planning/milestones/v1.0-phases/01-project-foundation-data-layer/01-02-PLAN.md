---
phase: 01-project-foundation-data-layer
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/app/page.tsx
  - src/app/layout.tsx
  - .env.local
  - .env.example
autonomous: false
requirements:
  - DATA-03
  - DEP-01

must_haves:
  truths:
    - "ページにアクセスすると、各メンバーの記事タイトルと公開日が表示される"
    - "ISRによりrevalidate間隔でデータが自動更新される"
    - "Vercelにデプロイされ、公開URLでアクセスできる"
    - "REVALIDATE_SECONDS環境変数でrevalidate間隔を変更できる"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "ISR設定 + fetchAllFeedsCached呼び出し + 最小限の記事一覧表示"
    - path: ".env.example"
      provides: "必要な環境変数の一覧（コミット用）"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/lib/fetchFeed.ts"
      via: "fetchAllFeedsCached のインポートと呼び出し"
      pattern: "fetchAllFeedsCached"
    - from: "src/app/page.tsx"
      to: "src/data/members.json"
      via: "members.json を読み込んで fetchAllFeedsCached に渡す"
      pattern: "members\\.json"

user_setup:
  - service: vercel
    why: "GitHub連携でのデプロイ（D-07）"
    dashboard_config:
      - task: "Vercel管理画面で新規プロジェクトを作成し、GitHubリポジトリを連携する"
        location: "https://vercel.com/new → Import Git Repository"
      - task: "環境変数 REVALIDATE_SECONDS=300 を設定する（D-05, D-06）"
        location: "Vercel Project → Settings → Environment Variables"
---

# Plan 01-02: ISR設定 + 最小表示ページ + Vercelデプロイ

## Objective

Plan 01-01で実装したフィード取得ロジックを使って、ISR設定済みのメインページを実装する。各メンバーの記事タイトルと公開日を一覧表示する最小限のUIを作成し、GitHub連携でVercelにデプロイして公開URLからアクセスできる状態にする。

Purpose: Phase 1の完了条件（Success Criteria 1〜5）をすべて満たす。Phase 2のカレンダーUIはこのページを拡張する形で構築する。
Output: 公開URLからアクセスできるVercelデプロイ済みアプリ

## Requirements Coverage

- DATA-03: ISR（revalidate設定可能）でデータを自動更新できる
- DEP-01: Vercelにデプロイして継続的に運用できる

## Tasks

### Task 1: ISR設定済みメインページを実装する

**Goal:** `fetchAllFeedsCached` を呼び出し、各メンバーの記事タイトルと公開日を表示する最小限のページを実装する。ISRはunstable_cacheが制御するため `export const dynamic = 'force-static'` を設定する。

**Files:**
- `src/app/page.tsx` — ISR設定 + データ取得 + 最小限の表示
- `src/app/layout.tsx` — メタデータ（タイトルのみ更新）
- `.env.local` — ローカル開発用環境変数（gitignore対象）
- `.env.example` — 環境変数のサンプル（gitにコミット）

**Implementation:**

`.env.example` — コミット用のサンプルファイル:

```
# ISRのrevalidate間隔（秒）。デフォルト300秒（5分）
REVALIDATE_SECONDS=300
```

`.env.local` — ローカル開発用（D-05: デフォルト5分）:

```
REVALIDATE_SECONDS=300
```

`src/app/layout.tsx` — タイトルのみ変更、それ以外はcreate-next-appのデフォルトを維持:

```typescript
import type { Metadata } from 'next'
import './globals.css'

export const metadata: Metadata = {
  title: 'Keep Substack',
  description: 'Substackコミュニティメンバーの記事更新を確認するツール',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="ja">
      <body>{children}</body>
    </html>
  )
}
```

`src/app/page.tsx` — ISR設定 + データ取得 + 最小限の表示:

```typescript
// RESEARCH.md §3「D-06の実現方法」に従い、unstable_cacheがrevalidateを制御するため
// export const revalidate は使わず export const dynamic = 'force-static' を使う
export const dynamic = 'force-static'

import { fetchAllFeedsCached } from '@/lib/fetchFeed'
import members from '@/data/members.json'
import type { Member } from '@/lib/types'

export default async function Home() {
  // members.json の型はJSONインポートのため as でキャスト
  const results = await fetchAllFeedsCached(members as Member[])

  return (
    <main className="max-w-4xl mx-auto p-6">
      <h1 className="text-2xl font-bold mb-6">Keep Substack</h1>
      {results.map(({ member, items }) => (
        <section key={member.feedUrl} className="mb-8">
          <h2 className="text-xl font-semibold mb-2">{member.name}</h2>
          {items.length === 0 ? (
            <p className="text-gray-500">記事を取得できませんでした</p>
          ) : (
            <ul className="space-y-1">
              {items.map((item, i) => (
                <li key={i} className="flex gap-4">
                  <span className="text-gray-500 text-sm w-32 shrink-0">
                    {item.isoDate
                      ? new Date(item.isoDate).toLocaleDateString('ja-JP')
                      : item.pubDate ?? '日付不明'}
                  </span>
                  <a
                    href={item.link ?? '#'}
                    target="_blank"
                    rel="noopener noreferrer"
                    className="text-blue-600 hover:underline text-sm"
                  >
                    {item.title ?? '(タイトルなし)'}
                  </a>
                </li>
              ))}
            </ul>
          )}
        </section>
      ))}
    </main>
  )
}
```

**注意点:**
- Phase 1のUIは「各メンバーの記事タイトルと公開日が確認できる」最小限で十分。カレンダーUIはPhase 2のスコープ。
- `export const dynamic = 'force-static'` はNext.js App RouterでISR（unstable_cache）を有効にするために必要（RESEARCH.md §3参照）。
- `items.length === 0` のケースを表示することで「フィード取得失敗時は非表示」（D-01）の動作がUIで確認できる。

### Task 2: Vercelへデプロイして動作確認する（human-action + human-verify）

**Goal:** GitHubリポジトリにpushし、Vercelでデプロイして公開URLでアクセスできる状態にする（D-07）。

**Files:**
- （ファイル変更なし — デプロイ操作）

**Pre-automation（Claude実行）:**

```bash
# .gitignore に .env.local が含まれていることを確認
grep ".env.local" .gitignore

# .env.example をコミット対象に追加（サンプルファイルはコミットする）
git add .env.example
git status
```

**Human Actions Required:**

以下の手順をユーザーが実行する:

1. **GitHubにpushする**
   ```bash
   git add -A
   git commit -m "feat(phase-1): add project foundation and RSS feed logic"
   git push origin main
   ```

2. **Vercelでプロジェクトを作成する**
   - https://vercel.com/new を開く
   - 「Import Git Repository」で `keep-substack` を選択
   - 「Environment Variables」に `REVALIDATE_SECONDS=300` を追加（D-05, D-06）
   - 「Deploy」をクリック

3. **デプロイ完了後、公開URLを確認する**
   - Vercelが発行するURL（例: `https://keep-substack-xxx.vercel.app`）にアクセスする
   - 各メンバーの記事一覧が表示されることを確認する

**Verification after deploy:**

```bash
# ISRキャッシュヘッダーを確認（HIT / MISS / STALE / REVALIDATED のいずれかが返る）
curl -I https://your-app.vercel.app | grep -i x-nextjs-cache
```

## Verification

- [ ] `npm run build` がエラーなく完了する
- [ ] `npm run dev` でローカル起動し、`http://localhost:3000` に各メンバーの記事一覧が表示される
- [ ] フィード取得に失敗したメンバーは「記事を取得できませんでした」と表示され、他のメンバーの表示に影響しない
- [ ] Vercelの公開URLにアクセスして記事一覧が表示される（DEP-01）
- [ ] `curl -I {vercel-url} | grep -i x-nextjs-cache` で ISRキャッシュヘッダーが確認できる（DATA-03）
- [ ] `.env.local` が `.gitignore` に含まれ、GitHubにpushされていない
- [ ] `.env.example` がリポジトリにコミットされている

## Dependencies

- Depends on: Plan 01-01（fetchAllFeedsCached, MemberFeedResult, members.json が必要）
- Blocks: Phase 2 カレンダーUI（このページを拡張する）

<execution_context>
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/kugyu10/work/keep-substack/.planning/PROJECT.md
@/Users/kugyu10/work/keep-substack/.planning/ROADMAP.md
@/Users/kugyu10/work/keep-substack/.planning/phases/01-project-foundation-data-layer/RESEARCH.md
@/Users/kugyu10/work/keep-substack/.planning/phases/01-project-foundation-data-layer/01-CONTEXT.md
@/Users/kugyu10/work/keep-substack/.planning/phases/01-project-foundation-data-layer/01-01-SUMMARY.md

<interfaces>
<!-- Plan 01-01 が作成する型とエクスポート。Executorはこれを参照して実装すること。 -->

From src/lib/types.ts:
```typescript
export type FeedItem = {
  title?: string
  link?: string
  pubDate?: string
  isoDate?: string
}

export type Member = {
  name: string
  feedUrl: string
}

export type MemberFeedResult = {
  member: Member
  items: FeedItem[]
}
```

From src/lib/fetchFeed.ts:
```typescript
// members配列を受け取り、全メンバーのフィードを並列取得する（unstable_cacheでラップ済み）
export const fetchAllFeedsCached: (members: Member[]) => Promise<MemberFeedResult[]>
```

From src/data/members.json:
```json
[{ "name": "表示名", "feedUrl": "https://username.substack.com/feed" }]
```
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vercel環境変数→アプリ | REVALIDATE_SECONDSがunstable_cacheのrevalidate値に使われる |
| ブラウザ→Vercelページ | 公開ページのため認証なし。HTMLのみ返す |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | src/app/page.tsx | accept | 公開ページ。feedUrlはSubstack公開URL。秘密情報なし |
| T-02-02 | Tampering | REVALIDATE_SECONDS | accept | Vercel管理画面へのアクセスにはVercelアカウント認証が必要。リスク低 |
| T-02-03 | Denial of Service | Vercel ISR | accept | Vercel無料枠の帯域制限内で運用。50フィード程度は問題なし |
</threat_model>

<verification>
Plan 01-02 完了確認:
1. ローカルで `npm run dev` を起動し、`http://localhost:3000` に記事一覧が表示される
2. `npm run build` がエラーなく完了する
3. Vercelの公開URLに記事一覧が表示される
4. `curl -I {url} | grep -i x-nextjs-cache` でISRキャッシュヘッダーが確認できる
</verification>

<success_criteria>
Phase 1 Success Criteria（ROADMAP.mdより）が全て満たされた状態:
1. JSON設定ファイルにメンバーを追加するとフィード取得対象が増える（DATA-01）
2. ページにアクセスすると各メンバーの記事タイトルと公開日がサーバーサイドで取得・表示される（DATA-02）
3. 一部のフィード取得が失敗しても他のメンバーのデータは正常に表示される（DATA-04）
4. Vercelにデプロイされ、公開URLでアクセスできる（DEP-01）
5. ISRにより、revalidate間隔で自動的にデータが更新される（DATA-03）
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-foundation-data-layer/01-02-SUMMARY.md`
</output>
