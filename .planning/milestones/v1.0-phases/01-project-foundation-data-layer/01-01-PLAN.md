---
phase: 01-project-foundation-data-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - src/data/members.json
  - src/lib/types.ts
  - src/lib/fetchFeed.ts
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-04

must_haves:
  truths:
    - "src/data/members.json にメンバーを追加すると、フィード取得対象が増える"
    - "fetchAllFeedsCached を呼び出すと、全メンバーのフィードが並列取得される"
    - "特定メンバーのフィード取得が失敗しても、他のメンバーの結果は返ってくる"
  artifacts:
    - path: "src/data/members.json"
      provides: "メンバー設定（name, feedUrl）の配列"
    - path: "src/lib/types.ts"
      provides: "FeedItem, Member 型定義"
    - path: "src/lib/fetchFeed.ts"
      provides: "fetchWithRetry, fetchAllFeeds, fetchAllFeedsCached 関数"
  key_links:
    - from: "src/lib/fetchFeed.ts"
      to: "rss-parser"
      via: "parser.parseURL(feedUrl)"
      pattern: "parser\\.parseURL"
    - from: "src/lib/fetchFeed.ts"
      to: "Promise.allSettled"
      via: "並列フィード取得"
      pattern: "Promise\\.allSettled"
    - from: "src/lib/fetchFeed.ts"
      to: "unstable_cache"
      via: "環境変数 REVALIDATE_SECONDS で制御"
      pattern: "unstable_cache"
---

# Plan 01-01: Next.jsプロジェクト初期化 + メンバー設定JSON + RSS取得ロジック

## Objective

Next.js (App Router) プロジェクトを新規作成し、JSON設定ファイルによるメンバー管理と、rss-parserを使ったRSSフィード並列取得ロジックを実装する。フィード取得失敗時のリトライ（D-01）、並列取得（D-02）、タイムアウト5秒（D-03）、直近30件制限（D-04）、unstable_cacheによる環境変数revalidate管理（D-06）をすべて実装する。

Purpose: Phase 1の基盤。次のPlan 01-02がこのロジックを使ってISRページを完成させる。
Output: 動作するNext.jsプロジェクト + メンバー設定JSON + RSS取得ライブラリ関数

## Requirements Coverage

- DATA-01: JSON設定ファイルでメンバー名とSubstackフィードURLを管理できる
- DATA-02: rss-parserでサーバーサイドからRSSフィードを取得・解析できる
- DATA-04: 一部のフィード取得が失敗しても他のメンバーは正常に表示される

## Tasks

### Task 1: Next.jsプロジェクト初期化 + rss-parserインストール

**Goal:** Next.js App Router + TypeScript + Tailwind CSS のプロジェクトを現在のディレクトリに作成し、rss-parserをインストールする。

**Files:**
- `package.json` — 依存関係（next, react, rss-parser等）
- `tsconfig.json` — TypeScript設定
- `next.config.ts` — Next.js設定（デフォルト）

**Implementation:**

現在のディレクトリ (`/Users/kugyu10/work/keep-substack`) が既存のgitリポジトリであることに注意する。`create-next-app` を `.` （カレントディレクトリ）に対して実行し、既存ファイルを上書きしないよう確認しながら進める。

```bash
# カレントディレクトリにプロジェクトを初期化する
# --yes で対話プロンプトをスキップできない場合は手動で回答:
#   TypeScript: Yes
#   ESLint: Yes
#   Tailwind CSS: Yes
#   src/ directory: Yes
#   App Router: Yes
#   Turbopack (dev): No（安定性優先）
#   import alias: No（デフォルトの @ で十分）
npx create-next-app@latest . --typescript --tailwind --app --src-dir --no-turbopack

# rss-parser をインストール
npm install rss-parser

# 型定義（rss-parserに同梱されているか確認してからインストール）
# package.json の dependencies に @types/rss-parser がなければインストール
npm install --save-dev @types/rss-parser
```

**注意点:**
- `create-next-app` が既存の `.planning/` や `.claude/` ディレクトリを削除しないことを確認する。生成後に `git status` で確認すること。
- `next.config.ts` はデフォルトのままでよい（Phase 1では特別な設定不要）。

### Task 2: メンバー設定JSONとFeedItem型定義を作成する

**Goal:** メンバー管理用のJSONファイルと、フィードデータを扱う型定義ファイルを作成する。

**Files:**
- `src/data/members.json` — メンバー設定（2〜3件のサンプルデータ入り）
- `src/lib/types.ts` — FeedItem型, Member型, MemberFeedResult型の定義

**Implementation:**

`src/data/members.json` — RESEARCH.mdの推奨構造（Claude's Discretion）に従う:

```json
[
  {
    "name": "メンバーA",
    "feedUrl": "https://memberA.substack.com/feed"
  },
  {
    "name": "メンバーB",
    "feedUrl": "https://memberB.substack.com/feed"
  }
]
```

`src/lib/types.ts` — Phase 2のカレンダーUIも見据えた型を定義する:

```typescript
// RESEARCH.md §2「カレンダーUI向けのキーフィールド」に基づく
export type FeedItem = {
  title?: string
  link?: string
  pubDate?: string
  isoDate?: string // rss-parserが自動付与するISO 8601形式
}

export type Member = {
  name: string
  feedUrl: string
}

// Plan 01-02 の page.tsx が使う集約型
export type MemberFeedResult = {
  member: Member
  items: FeedItem[]
}
```

### Task 3: RSSフィード取得ロジックを実装する

**Goal:** D-01〜D-04 + D-06 を完全実装した `src/lib/fetchFeed.ts` を作成する。

**Files:**
- `src/lib/fetchFeed.ts` — fetchWithRetry, fetchAllFeeds, fetchAllFeedsCached

**Implementation:**

RESEARCH.md §1「リトライ付き並列フィード取得」および §3「D-06の実現方法」のコードパターンをそのまま採用する:

```typescript
import Parser from 'rss-parser'
import { unstable_cache } from 'next/cache'
import type { FeedItem, Member, MemberFeedResult } from './types'

// D-03: タイムアウト5秒（rss-parserの組み込みtimeoutオプション）
const parser = new Parser({ timeout: 5000 })

const RETRY_DELAY_MS = 1000 // D-01: リトライ前の待機時間
const MAX_ARTICLES = 30     // D-04: 直近30件まで

// D-01: フィード取得失敗時は1秒待ってリトライ、それでもダメなら空配列を返す
async function fetchWithRetry(feedUrl: string): Promise<FeedItem[]> {
  try {
    const feed = await parser.parseURL(feedUrl)
    return feed.items.slice(0, MAX_ARTICLES) as FeedItem[]
  } catch {
    // 1秒待ってリトライ
    await new Promise((r) => setTimeout(r, RETRY_DELAY_MS))
    try {
      const feed = await parser.parseURL(feedUrl)
      return feed.items.slice(0, MAX_ARTICLES) as FeedItem[]
    } catch {
      return [] // 非表示（D-01: ダメなら空配列）
    }
  }
}

// D-02: Promise.allSettled で全フィードを並列取得
async function fetchAllFeeds(members: Member[]): Promise<MemberFeedResult[]> {
  const results = await Promise.allSettled(
    members.map((member) => fetchWithRetry(member.feedUrl))
  )
  return members.map((member, i) => ({
    member,
    items: results[i].status === 'fulfilled' ? results[i].value : [],
  }))
}

// D-06: REVALIDATE_SECONDS 環境変数でrevalidateを管理（unstable_cacheを使用）
// export const revalidate はリテラルのみ有効なため unstable_cache でラップする
const REVALIDATE_SECONDS = parseInt(process.env.REVALIDATE_SECONDS ?? '300') // D-05: デフォルト300秒

export const fetchAllFeedsCached = unstable_cache(
  async (members: Member[]) => fetchAllFeeds(members),
  ['all-feeds'],
  { revalidate: REVALIDATE_SECONDS, tags: ['feeds'] }
)
```

**注意点:**
- `unstable_cache` の第2引数 `['all-feeds']` はキャッシュキー。固定値でよい（メンバーはJSONから読む）。
- `REVALIDATE_SECONDS` はモジュールロード時に評価される。Vercel管理画面で環境変数を変更した後は再デプロイが必要（ただし、コードは変更不要）。
- `unstable_cache` の名前は "unstable" だが、Next.js公式ドキュメントで推奨されているパターン（RESEARCH.md §3参照）。

## Verification

- [ ] `npm run build` がエラーなく完了する
- [ ] `src/data/members.json` が存在し、`name` と `feedUrl` を持つオブジェクトの配列である
- [ ] `src/lib/types.ts` が存在し、`FeedItem`, `Member`, `MemberFeedResult` をエクスポートしている
- [ ] `src/lib/fetchFeed.ts` が存在し、`fetchAllFeedsCached` をエクスポートしている
- [ ] `fetchFeed.ts` に `Promise.allSettled` が使われている（D-02）
- [ ] `fetchFeed.ts` に `timeout: 5000` が設定されている（D-03）
- [ ] `fetchFeed.ts` に `.slice(0, MAX_ARTICLES)` があり `MAX_ARTICLES = 30`（D-04）
- [ ] `fetchFeed.ts` に `RETRY_DELAY_MS = 1000` のリトライロジックがある（D-01）
- [ ] `fetchFeed.ts` に `unstable_cache` が使われ、`REVALIDATE_SECONDS` 環境変数を参照している（D-06）

## Dependencies

- Depends on: none（Phase 1の最初のプラン）
- Blocks: Plan 01-02（このプランのfetchAllFeedsCachedとMemberFeedResultを使う）

<execution_context>
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/kugyu10/work/keep-substack/.planning/PROJECT.md
@/Users/kugyu10/work/keep-substack/.planning/ROADMAP.md
@/Users/kugyu10/work/keep-substack/.planning/phases/01-project-foundation-data-layer/RESEARCH.md
@/Users/kugyu10/work/keep-substack/.planning/phases/01-project-foundation-data-layer/01-CONTEXT.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| サーバー→外部RSSフィード | rss-parserがSubstack外部URLにHTTPリクエストを送る |
| members.json→アプリ | 設定ファイルのfeedUrlが外部ネットワークリクエストに使われる |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Denial of Service | fetchWithRetry | mitigate | timeout: 5000ms（D-03）と1回リトライ制限（D-01）で応答遅延の影響を局所化。Promise.allSettledで1件の遅延が全体をブロックしない（D-02） |
| T-01-02 | Information Disclosure | members.json | accept | feedUrlはSubstack公開フィードURL。秘密情報を含まない。gitにコミットしても問題なし |
| T-01-03 | Tampering | members.json | accept | Phase 1はファイルシステム上の静的JSON。管理UIなし。変更はgit経由のみ |
| T-01-04 | Elevation of Privilege | rss-parser | accept | 外部フィードからの取得結果はサーバーサイドで処理され、ユーザー入力を受け付けない |
</threat_model>

<verification>
Plan 01-01 完了確認:
1. `npm run build` がエラーなく完了する
2. TypeScriptの型エラーがない（`npx tsc --noEmit`）
3. `src/data/members.json`、`src/lib/types.ts`、`src/lib/fetchFeed.ts` の3ファイルが存在する
4. fetchFeed.ts がキーの実装要件（D-01〜D-04, D-06）をすべて満たす
</verification>

<success_criteria>
- Next.jsプロジェクトが `npm run dev` で起動できる（デフォルトページ表示でよい）
- `fetchAllFeedsCached(members)` を呼び出すと、members.jsonのフィードが並列取得される
- フィード取得失敗時は空配列が返り、他のメンバーの結果に影響しない
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-foundation-data-layer/01-01-SUMMARY.md`
</output>
