---
phase: "03-dashboard-ux"
plan: "02"
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - src/app/member/[substackId]/page.tsx
autonomous: false
requirements:
  - DASH-02

must_haves:
  truths:
    - "/member/uojun にアクセスするとうおじゅんのフルカレンダーが表示される"
    - "/member/careerkoumei にアクセスするとキャリア孔明のフルカレンダーが表示される"
    - "「← ダッシュボードに戻る」リンクをクリックすると / に戻る"
    - "存在しない substackId（例: /member/unknown）にアクセスすると 404 になる"
    - "npm run build が成功し、ビルド出力で個人詳細ページが Static (○) として生成される"
  artifacts:
    - path: "src/app/member/[substackId]/page.tsx"
      provides: "個人詳細ページ（generateStaticParams + force-static + CalendarGrid再利用）"
      exports: ["generateStaticParams", "default"]
      contains: "generateStaticParams"
  key_links:
    - from: "src/app/member/[substackId]/page.tsx"
      to: "src/components/CalendarGrid.tsx"
      via: "CalendarGrid のインポートと articleMap props 渡し"
      pattern: "CalendarGrid"
    - from: "src/app/member/[substackId]/page.tsx"
      to: "src/lib/fetchFeed.ts"
      via: "fetchAllFeedsCached によるキャッシュ再利用"
      pattern: "fetchAllFeedsCached"
    - from: "src/app/member/[substackId]/page.tsx"
      to: "/"
      via: "next/link の href=\"/\""
      pattern: "href.*/"
---

<objective>
個人詳細ページ /member/[substackId] を静的生成で実装する。

Purpose: ダッシュボードのミニカレンダーカードをクリックした先の個人詳細ページを実装する。Phase 2 で構築した CalendarGrid（月ナビ付きフルカレンダー）を再利用し、最小限のコード追加で完成させる（per D-03）。generateStaticParams で全メンバーのパスをビルド時に静的生成し、force-static + unstable_cache の ISR パターンを維持する（per D-01）。
Output:
- src/app/member/[substackId]/page.tsx（新規）
</objective>

<execution_context>
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/kugyu10/work/keep-substack/.planning/PROJECT.md
@/Users/kugyu10/work/keep-substack/.planning/ROADMAP.md
@/Users/kugyu10/work/keep-substack/.planning/STATE.md
@/Users/kugyu10/work/keep-substack/.planning/phases/03-dashboard-ux/03-01-SUMMARY.md

<interfaces>
<!-- 再利用するコンポーネント・関数の型定義。executor はこれを参照して実装すること。 -->

src/components/CalendarGrid.tsx（Props — 個人詳細ページで再利用）:
```typescript
// 'use client' コンポーネント。useState で月ナビを管理。
type Article = { title?: string; link?: string }
type Props = {
  memberName: string
  articleMap: [string, Article[]][]  // Map はシリアライズ不可のため配列で渡す
}
export default function CalendarGrid({ memberName, articleMap }: Props)
```

src/lib/calendarUtils.ts（Plan 03-01 で追加済み）:
```typescript
export function extractSubstackId(feedUrl: string): string | null
export function buildArticleMap(items: { title?: string; link?: string; isoDate?: string }[]): ArticleMap
```

src/lib/fetchFeed.ts:
```typescript
// unstable_cache(['all-feeds'], { revalidate: REVALIDATE_SECONDS }) でラップ済み
// 複数ページで同一キャッシュエントリを共有できる
export const fetchAllFeedsCached: (members: Member[]) => Promise<MemberFeedResult[]>
```

src/lib/types.ts:
```typescript
export type Member = { name: string; feedUrl: string }
export type FeedItem = { title?: string; link?: string; isoDate?: string }
export type MemberFeedResult = { member: Member; items: FeedItem[] }
```

Next.js 16 の params 型（重要: Pitfall 1）:
```typescript
// params は Promise 型。await が必須。
// NG: const { substackId } = params  // TypeScript エラー
// OK: const { substackId } = await params
export default async function MemberPage({
  params,
}: {
  params: Promise<{ substackId: string }>
}) {
  const { substackId } = await params  // await 必須
  ...
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: src/app/member/[substackId]/page.tsx を作成する</name>
  <files>src/app/member/[substackId]/page.tsx</files>
  <action>
ディレクトリ src/app/member/[substackId]/ を作成し、page.tsx を新規作成する。

実装内容（per D-01, D-02, D-03, D-04）:
- export const dynamic = 'force-static' — ISR維持（force-static + unstable_cache パターン。Phase 1/2 確立済み）
- export const dynamicParams = false — members.json 以外の substackId は 404 にする（セキュリティ: T-03-03）
- generateStaticParams — members.json から全メンバーの substackId を抽出してパスを生成
- params は await する（Next.js 15+ の破壊的変更。Pitfall 1）
- fetchAllFeedsCached で全件取得 → extractSubstackId でフィルタ（キャッシュキー共通 ['all-feeds'] で効率的）
- memberResult が見つからない場合は notFound() を呼ぶ
- CalendarGrid を再利用（月ナビ付きフルカレンダー）
- 「← ダッシュボードに戻る」リンクを CalendarGrid の上部に表示（per D-04）

完全な実装コード:
```tsx
export const dynamic = 'force-static'
export const dynamicParams = false

import Link from 'next/link'
import { notFound } from 'next/navigation'
import { fetchAllFeedsCached } from '@/lib/fetchFeed'
import { buildArticleMap, extractSubstackId } from '@/lib/calendarUtils'
import CalendarGrid from '@/components/CalendarGrid'
import members from '@/data/members.json'
import type { Member } from '@/lib/types'

export function generateStaticParams() {
  return (members as Member[])
    .map((m) => extractSubstackId(m.feedUrl))
    .filter((id): id is string => id !== null)
    .map((substackId) => ({ substackId }))
}

export default async function MemberPage({
  params,
}: {
  params: Promise<{ substackId: string }>
}) {
  const { substackId } = await params

  const results = await fetchAllFeedsCached(members as Member[])
  const memberResult = results.find(
    (r) => extractSubstackId(r.member.feedUrl) === substackId
  )

  if (!memberResult) notFound()

  const map = buildArticleMap(memberResult.items)
  const articleMapEntries = Array.from(map.entries())

  return (
    <main className="max-w-2xl mx-auto p-6">
      <Link
        href="/"
        className="text-sm text-gray-500 hover:text-gray-800 mb-4 inline-block"
      >
        ← ダッシュボードに戻る
      </Link>
      <CalendarGrid
        memberName={memberResult.member.name}
        articleMap={articleMapEntries}
      />
    </main>
  )
}
```

注意事項:
- export const dynamic と export const dynamicParams はファイル先頭で宣言する（Next.js のルートセグメント設定）
- generateStaticParams は export function（async 不要。ローカル JSON のみ参照）
- notFound() は memberResult が undefined のときに呼ぶ。TypeScript は notFound() 後の memberResult.items アクセスをエラーにするが、notFound() は実行時に例外を投げるため到達不能になる（型ナローイング不要）
  </action>
  <verify>
    <automated>cd /Users/kugyu10/work/keep-substack && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>src/app/member/[substackId]/page.tsx が存在し、TypeScript コンパイルエラーなし。generateStaticParams と dynamicParams が含まれる。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
個人詳細ページ /member/[substackId] を静的生成で実装した。ダッシュボード（/）のミニカレンダーカードをクリックして個人詳細ページへ遷移し、フルカレンダーと戻るリンクが表示されることを確認する。
  </what-built>
  <how-to-verify>
1. 開発サーバーを起動する:
   ```bash
   cd /Users/kugyu10/work/keep-substack && npm run dev
   ```

2. ブラウザで http://localhost:3000 を開く
   - 確認: ミニカレンダーがグリッド（2件ならカード2つ）で表示される

3. いずれかのミニカレンダーカードをクリックする
   - 確認: /member/uojun または /member/careerkoumei に遷移する
   - 確認: フルサイズのカレンダーグリッド（月ナビ付き）が表示される
   - 確認: ページ上部に「← ダッシュボードに戻る」リンクがある

4. 「← ダッシュボードに戻る」をクリックする
   - 確認: / に戻りミニカレンダーグリッドが表示される

5. 存在しないパスを直接入力する: http://localhost:3000/member/nonexistent
   - 確認: 404 ページが表示される（開発モードでは Next.js のデフォルト 404）

6. （オプション）本番ビルドでの静的生成を確認する:
   ```bash
   cd /Users/kugyu10/work/keep-substack && npm run build 2>&1 | grep -E "member|○|λ"
   ```
   - 確認: /member/uojun と /member/careerkoumei が ○ (Static) として表示される
  </how-to-verify>
  <resume-signal>"approved" と入力するか、問題があれば内容を説明してください</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL path → substackId | ユーザーが任意のパスを入力できる唯一の境界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Information Disclosure | /member/[substackId] | mitigate | dynamicParams=false を設定。generateStaticParams で事前生成したパス以外は全て Next.js が 404 を返す。任意の substackId を注入されても静的ファイルが存在しないため情報漏洩リスクなし |
| T-03-04 | Denial of Service | fetchAllFeedsCached | accept | unstable_cache の ['all-feeds'] キャッシュが全ページで共有されるため、個人詳細ページへのアクセス増加によるフィード元への過剰リクエストは発生しない |
</threat_model>

<verification>
Plan 03-02 完了確認:

```bash
# 1. TypeScript コンパイルエラーなし
cd /Users/kugyu10/work/keep-substack && npx tsc --noEmit

# 2. ファイルが存在する
ls /Users/kugyu10/work/keep-substack/src/app/member/

# 3. generateStaticParams と dynamicParams が含まれる
grep -E "generateStaticParams|dynamicParams" /Users/kugyu10/work/keep-substack/src/app/member/\[substackId\]/page.tsx

# 4. 「戻るリンク」が含まれる（per D-04）
grep "ダッシュボードに戻る" /Users/kugyu10/work/keep-substack/src/app/member/\[substackId\]/page.tsx

# 5. ビルドで静的生成される（オプション）
cd /Users/kugyu10/work/keep-substack && npm run build 2>&1 | grep -E "member|Static|Dynamic"
```
</verification>

<success_criteria>
- /member/uojun と /member/careerkoumei が正しいメンバーのフルカレンダーを表示する
- 「← ダッシュボードに戻る」リンクが機能する（/ に遷移する）
- /member/nonexistent が 404 を返す
- npm run build が成功し、個人詳細ページが Static として生成される
- Phase 3 の Success Criteria がすべて満たされる:
  1. ダッシュボードで全メンバーのミニカレンダーが一覧表示される
  2. メンバーをクリックすると個人詳細ビューに切り替わり、そのメンバーのカレンダーが大きく表示される
  3. スマートフォンでもカレンダーとダッシュボードが見やすく表示される
</success_criteria>

<output>
完了後、.planning/phases/03-dashboard-ux/03-02-SUMMARY.md を作成すること。
@/Users/kugyu10/work/keep-substack/.claude/get-shit-done/templates/summary.md のテンプレートに従う。
</output>
